# CONTRACT.md — 프론트↔백엔드 계약의 이유와 원칙

> **계약의 정본은 백엔드 저장소에 있다** — [`contract/v1/deal.schema.json`](https://github.com/RYU-TOMI/galmal-backend/blob/main/contract/v1/deal.schema.json)(필드·타입·nullable·의미) ·
> [`contract/v1/vocab.json`](https://github.com/RYU-TOMI/galmal-backend/blob/main/contract/v1/vocab.json)(통제 어휘) ·
> [`collector/dests.py`](https://github.com/RYU-TOMI/galmal-backend/blob/main/collector/dests.py)(`DEST` 배정 · `REGION_NAME`).
> **이 문서는 그것들을 「왜」 그렇게 정했는지만 적는다. 목록을 옮겨 적지 않는다** — 사본으로 두었다가 레포 분리 첫날 갈렸다(`DECISIONS.md` 2026-09-17).
>
> 경위(누가 언제 제기했고 어떻게 고쳤나)는 `DECISIONS.md` 에 있다. 여기엔 **지금 참인 것**만 둔다.

## 변경 규칙

절차는 `SESSIONS.md` §계약 변경 절차가 정본이다 — **기획이 여기에 결정(이유)을 쓴다 → 백엔드가 `contract/` 를 고치고 커밋 메시지에 그 결정을 인용한다.**
결정 기록 없이 `contract/` 를 바꾸지 않는다(메시지로만 받은 결정은 인용할 곳이 없다 — 2026-09-21 실제로 그래서 멈췄다).

1. **필드 추가는 안전**(소비자가 무시하면 된다). **삭제·개명·의미 변경은 파괴적** → 양쪽 합의 후에만. 깨는 변경은 `/v2/` 를 새로 낸다.
2. 소비자는 **모든 필드에 방어적으로** 접근한다. 데이터가 없거나 0건인 날도 화면이 깨지면 안 된다.
3. 프론트의 픽스처(`fixtures/v1/`)는 **라이브에서 받아 적는다.** 손으로 쓰지 않는다.

### 🔒 설명문의 규칙 — 계약은 **자기가 보장하는 것**만 말한다 (2026-09-21)

`deal.schema.json`·`vocab.json` 의 `description` 에 **「프론트는 …한다」를 적지 않는다.** 상대의 동작을 적으면
**상대가 바뀔 때 계약이 거짓이 된다** — 실제로 `route` 설명이 아직 없는 링크의 동작을 서술하고 있었다.
적는 것은 **이 값이 무엇인가 · 언제 비는가 · 무엇을 보장하는가**뿐이다. 화면이 무엇을 하는지는 `SPEC.md` 의 몫이다.

**예외 — 「소비자가 지켜야 하는 조건」은 주어를 바꿔 남긴다.** 안 지키면 **법을 어기거나 제휴사와 틀어지는 것**(`links[].ad` 광고 고지 ·
`links[].url` `sponsored` · `links[].name` 가공 금지)은 계약의 일부다. 안 지켜도 우리만 손해인 것은 계약이 아니라 결정이다.
→ 판정표는 `DECISIONS.md` 2026-09-21.

---

## 왜 파일이 아니라 URL인가

백엔드는 **HTTP로 응답하는 것처럼** 행동한다. 오늘은 GitHub Pages가 정적 JSON을 서빙하고,
나중에 자체 서버가 생기면 **DNS만 옮긴다 — 프론트는 한 글자도 안 바뀐다.** 이게 v1 설계의 유일한 목표다.

```
base: https://api.galmal.kr/v1/

GET /v1/meta.json           생성 시각·건수·수집 상태·구독 규약
GET /v1/deals.json          발견 홈이 쓰는 전부
GET /v1/routes/index.json   노선 목록 (네비·sitemap·「다른 노선 보기」)
GET /v1/routes/{code}.json  노선 1개 통계
GET /v1/vocab.json          참조 데이터 (통제 어휘·지역 표시명)
```

**소비 시점은 빌드 타임이다.** 프론트 Actions가 받아 HTML을 만든다(브라우저가 아니다).
따라서 CORS는 v1의 관심사가 아니다 — 런타임 fetch로 바꾸는 날 다시 본다.

## 공통 규칙

**봉투**: 모든 응답은 최상위에 `schema`와 `generated`를 갖는다.

```json
{ "schema": "v1", "generated": "2026-09-08T07:12:03+09:00", "...": "..." }
```

- `generated`는 **ISO 8601 + 오프셋 필수.** 화면이 신선도를 표시하고 있어
  (`SPEC.md` §CH3 `발견가 · N일 전 가격`) 날짜 경계에서 하루가 어긋나면 조용히 틀린다.
  **표시 문자열은 백엔드가 만들지 않는다** — 정적 사이트라 완성된 문장은 다음 날 거짓말이 된다.
- 🔴 **한 발행의 스냅숏 규칙 — `generated`로 섞임을 판별한다**
  CDN 캐시는 파일마다 따로 걸린다(`max-age=600`). 그래서 소비자는 받은 응답들의 `generated`로
  **다른 발행이 섞였는지** 안다.
  | 응답 | `generated` |
  |---|---|
  | `meta` | 기준값 **G** |
  | `routes/index` · `routes/{code}` (**index에 실린 것만**) · `vocab` | **== G** |
  | `deals` — `meta.preserved == false` | **== G** |
  | `deals` — `meta.preserved == true` | **< G** (어제 파일을 일부러 보존한 날) |
  - 생산자: 발행 1회에 시각을 **한 번만** 만든다. 딜 수가 하한선에 못 미치면 `deals.json`을
    **덮어쓰지 않는다** — 「어제 데이터에 오늘 도장을 찍는 게 더 나쁘다」(2026-08-22 합의). 그날 `meta.preserved=true`.
  - 소비자: 위 표를 어기면 **배포하지 않는다.** `routes/{code}`는 **`routes/index.json`에 실린 코드만** 받는다 —
    백엔드는 발행 때 index에 없는 노선 파일을 지우지만 **CDN 캐시엔 지운 뒤에도 최대 10분 남는다.** index가 기준이다.
  - **알려진 틈**: 보존일에 CDN이 **더 오래된** `deals`를 줘도 「< G」라 통과한다. 보존일이면서 엣지가 낡아야 하는
    좁은 경우라 받아들인다. 좁히려면 `meta`에 「내보낸 `deals`의 `generated`」 필드를 더해 **정확히 일치**로 바꾸면 된다(필드 추가라 안전) — 필요해지면.
- 인코딩 UTF-8, 압축 없음(`separators=(",",":")`).
- 값이 없으면 필드를 빼지 말고 `null`. **단 "표본이 없다"를 `null`로 표현하지 않는다** — 아래.

## 🔴 창(window)과 임계(threshold)를 가른다 — v1의 핵심 규칙

| | 무엇을 정하나 | 누가 | 예 |
|---|---|---|---|
| **창** | 어느 기간·범위를 분석하나 | **백엔드** | 최근 30일 · 출발일 ≥ 오늘 |
| **임계** | 이 숫자를 보여줘도 되나 | **프론트** | 버킷 2개 미만이면 안 그린다 |

**따라서 백엔드는 얇다고 버리지 않는다.** 모든 버킷을 **건수(`n`)와 함께** 낸다. 자를지 말지는 프론트가 정한다.

**왜**
- **백엔드가 조용히 버리면 프론트는 구분할 수 없다.** 「10월이 없다」가 *자료가 없어서*인지 *얇아서 버려진 건지*.
  그래서 v1이 만든 자리는 표본 수 필드 하나가 아니라 **버킷마다 건수**다.
- **임계가 지배하는 건 차트만이 아니다 — 실제로 잘못 나간 건 문장이었다.** 버킷이 하나뿐이어도
  「9월 출발이 가장 저렴합니다」라고 쓰던 때가 있었다. **하나는 비교가 아니다.**
- **창으로만 잡히는 결함이 있다.** 이미 지난 달에 「떠나라」고 말하던 결함(2026-09-08)은 그 버킷이 **915건**이라
  어떤 임계도 통과했다. 창(`depart_date >= 오늘`)으로만 잡혔고, 창은 백엔드다 — 이 규칙의 첫 시험이었다.

> **임계는 「차트」가 아니라 「버킷 비교로 만든 모든 주장」에 건다.** 판정 함수를 하나만 두고 차트와 문장에 같이 쓴다.

**임계의 현재 값** (프론트가 구현하고, 값은 기획 결정으로 바꾼다)

| 대상 | 임계 | 근거 |
|---|---|---|
| 모든 버킷 블록 | 버킷 **2개 이상**일 때만 그린다·말한다 | 하나는 비교가 아니다 |
| `months` 막대 | `n >= 3`인 버킷만 · 월 오름차순 **앞에서 10개** | 분리 전 동작을 그대로 가져온 값. ⏳ **「앞에서 10개」가 창인지 임계인지는 미결** — 아래 `COPY.md` §6과 한 결정 |
| `weekdays` 막대 | **`n` 필터 없음** | 걸면 표가 드문 노선의 차트가 줄어든다(타이중 5칸 → 3칸) |
| `airlines` | 최저가 오름차순 **상위 8개** | 표시 결정 |
| 🔴 **팁 문장**(「○월이 가장 저렴」·「○요일이 가장 쌉니다」) | 수집 기간 **`D` ≥ 14** 이고, 가리키는 버킷이 **`n >= 3`** | **막대는 사실, 팁은 권고다** — `DECISIONS.md` 2026-09-20 (10)(11) |

> 🔴 **월 버킷의 표시 상한을 건드리는 챕터는 `COPY.md` §6을 같이 처리한다.** 월 라벨이 연도를 버리기 때문에
> (`'2027-02'` → `'2월'`) 12개를 넘기면 **막대 두 개가 같은 이름**을 단다.
> 「몇 달치를 보여줄 것인가」와 「그 달을 어떻게 부를 것인가」는 **하나의 결정**이다.

---

## 1) `GET /v1/meta.json`

```json
{
  "schema": "v1",
  "generated": "2026-09-08T07:12:03+09:00",
  "window_days": 30,
  "counts": { "deals": 119, "routes": 36, "origins": 4 },
  "preserved": false,
  "subscribe": {
    "address": "flightpromokr@gmail.com",
    "subject_subscribe": "구독신청",
    "subject_unsubscribe": "구독취소",
    "route_token": "{code}"
  }
}
```

| 키 | 타입 | 의미 |
|---|---|---|
| `window_days` | int | 통계 분석 창. 백엔드가 정한다 |
| `counts` | object | 이번 생성분의 건수 (위 숫자는 예시다) |
| `preserved` | bool | `true`면 **하한선 미달로 `deals` 갱신을 건너뛰었다** — 딜 데이터가 어제 것이다 |
| `subscribe` | object | 구독 메일 규약. 아래 참조 |

**이 응답이 감시 지점이다.** 백엔드 크론 마지막 「상태 점검」이 이 응답을 받아 `generated`가 오늘 것인지 본다 —
주소가 틀리면 404, 배포가 멈췄으면 어제 날짜다. 사이트 쪽 신선도는 사이트의 `build.json`으로 따로 본다.

### 🔴 `subscribe` — 표시가 아니라 전선(wire) 규약이다

구독 CTA는 `mailto:` 링크이고, 이 넷은 **백엔드 IMAP 파서가 실제로 읽는 값**이다. 그래서 프론트가 파생하지 않고 계약에 싣는다.

| 필드 | 무엇 |
|---|---|
| `address` | 파서가 **IMAP으로 로그인해 읽는 바로 그 메일함.** 사이트 정체성이 아니다 — 정본은 파서 모듈(`subscriptions.py`) |
| `subject_subscribe` · `subject_unsubscribe` | 제목에 이 문자열이 있어야 구독/해지로 판정한다 |
| `route_token` | 본문에 넣을 노선 코드의 **틀**(값이 아니다) |

- **주소가 갈리면 반송도 안 온다.** 사이트는 A로 보내라 하고 파서는 B를 읽는데 A도 실재하는 주소면, 사용자는 신청했다고 믿고
  우리는 신청이 없다고 믿는다. 그래서 **크론 상태 점검이 `subscribe.address`가 실제 로그인 메일함과 같은지도 본다**(시크릿이 있는 유일한 자리).
- 공개 연락처라 PII가 아니고 이미 전 페이지에 노출돼 있다.
- 문구(구독 안내·해지 안내)는 `COPY.md`가 정본이다 — **규약만** 백엔드가 낸다.

**⚠️ 틀리면 조용한 정도가 아니라 반대로 동작한다.** 파서가 노선을 못 찾으면 지금은 **「전 노선 구독」**으로 받는다.
사용자는 도쿄만 신청했는데 전 노선 메일을 받는다. **실패보다 나쁘다.**

> **실패였으면 사용자가 "메일이 안 와요"라고 알려주기라도 한다. 이건 반대로 메일이 더 온다 — 신고가 아니라 스팸 신고로 돌아온다.**
> 피해자가 신고자가 되는 실패와 피해자가 침묵하는 실패는 등급이 다르다.

> ### ⏳ 2026-09-20 결정 — 구현은 백엔드 BE6 에서 (`DECISIONS.md` 2026-09-20 (8))
> 1. **노선 패턴(정규식)을 계약에 올리지 않는다. 패턴을 없앤다.** 파서는 자기가 아는 노선 코드 집합을 본문에서
>    **글자 그대로** 찾는다. 프론트 검사는 「mailto 본문에 `route_token` 자리의 코드가 들어 있다」만 보면 되므로
>    **계약에 이미 있는 것만으로** 서고 정규식 사본이 필요 없다. 없는 노선(`ICN-XXX`)으로 생기던 「영원히 안 울리는 구독」도 사라진다.
> 2. **`ALL` 은 명시적 `전체` 키워드가 있을 때만.** 코드도 키워드도 없으면 구독을 만들지 않는다 — 조용한 과잉보다 눈에 띄는 실패.
> 3. 실패를 **보낸 사람이 알 수 있게** 안내 회신을 같이 넣는다(문구는 `COPY.md` 가 먼저).
>
> **계약 필드 변경 없음.**

## 2) `GET /v1/deals.json`

```json
{
  "schema": "v1",
  "generated": "2026-09-08T07:12:03+09:00",
  "origins": { "SEL": {"name":"서울","lat":37.55,"lon":126.99} },
  "deals": [ { "...deal..." } ]
}
```

| 키 | 의미 |
|---|---|
| `origins` | 출발 허브 맵. 키 = 허브 코드, 값 = `{name, lat, lon}`. **딜이 있는 허브만** 포함 |
| `deals` | 딜 목록. **가격 오름차순** |

데이터가 0건인 날도 이 모양은 유지된다(`"origins":{}, "deals":[]`).

**출발 허브 코드**(`origins` 키 / `deal.o`)의 값 목록은 `vocab.json`의 `hub`다.
⚠️ `SEL`은 **실제 공항 코드가 아니다**(인천+김포를 합친 집계용 가상 허브). 예약 링크에는 실제 공항이 들어간다 — 백엔드가 처리한다.

### 데이터 정책 (백엔드가 보장)
- **게이팅**: `dests.DEST` 사전에 있고 좌표가 있는 목적지만(롱테일 쓰레기 제외).
- **신선도**: 최근 수집분 + **미래 출발**만. 수집 컷은 **96시간**, 추가로 `seen`이 7일을 넘은 딜은 제외(아래 §`seen`).
- **중복 제거**: `(허브, 도시)` 단위로 **최저가 1건**만.

### deal 객체 — 필드마다의 「왜」

필드 목록은 [`deal.schema.json`](https://github.com/RYU-TOMI/galmal-backend/blob/main/contract/v1/deal.schema.json)이 정본이다. 아래는 이유다.

#### `links[]` — 비교 패널
```json
{ "name": "Aviasales", "tag": "영어·수수료", "ad": true, "url": "https://..." }
```

> **`ad`가 있는 이유** — 법적 고지(`COPY.md` 제휴 고지 절)를 붙이려면 **어느 링크가 제휴인지 소비자가 알아야 한다.**
> 이 필드 없이는 넷 다 **추측**이 된다 — 이름 하드코딩 · 순서 가정 · `tag` 문자열 파싱 · URL에 `marker=` 검색.
> **판정을 아는 건 백엔드뿐이고, 마커를 붙이는 그 자리에서 단언하는 게 맞다.**
>
> 🔑 **`ad`는 "Aviasales인가"가 아니라 "수수료를 받는가"다.**
> Trip.com이 승인되면 `tp.media` 래퍼가 씌워져 **그 순간부터 그것도 제휴 링크**다.
> Aviasales만 참으로 박았다면 승인되는 날 **고지가 조용히 빠졌을** 것이다.
> **선언(`ad`)과 실제(URL이 수수료를 추적하는가)가 갈리면 고지가 거짓말이 된다** — 백엔드 테스트가 그 일치를 검사한다.

- 순서 = 화면 노출 순서.
- **Aviasales(영어)는 `TP_MARKER` 환경변수가 있을 때만** 배열에 들어간다(수수료가 발생할 때만 노출).
- ✅ **`links[].pax_url` (2026-09-22 결정 — 필드 추가라 안전. `DECISIONS.md` 2026-09-22 (4). 백엔드 반영 3b41d52 — 기획이 커밋·인용 확인. 라이브는 2026-09-22 밤 크론 뒤)**
  그 예약처를 **인원 `{n}`명으로 여는 URL 한 벌**. `url` 은 지금처럼 **1인 완성형 그대로**(그대로 열면 되는 링크) 두고, `pax_url` 에만 치환 토큰 `{n}` 이 들어간다.
  **백엔드가 보장하는 것**: ① `pax_url` 의 `{n}` 을 `1` 로 바꾸면 **`url` 과 바이트가 같다**(생산자 테스트) — 두 필드가 갈리는 것을 막는 유일한 방법이다.
  ② `{n}` 은 인코딩 바깥에 있다(제휴 래퍼 · 구글 `q` 안이라도 소비자가 바꿔 끼우면 그대로 먹는다). ③ 인원을 못 받는 예약처는 `pax_url: null`.
  **왜 이 형태인가**: 예약처마다 인원을 받는 방식이 셋(파라미터 · 자연어 질의 · 경로 숫자)이라 「파라미터 이름」으로는 표현이 안 된다. 인원별 완성 URL 맵(2·3·4)은 가장 안전하지만
  홈에 인라인되는 `deals.json` 을 **2.3배**로 키운다 — 방문자 트래픽이다. 토큰 한 벌은 1.4배.
  **소비자 규칙**: `{n}` 하나만 치환한다. 그 외 URL 조작 금지. `pax_url` 이 `null` 이면 `url` 을 열고 「1인 기준으로 열려요」를 붙인다(`COPY.md` §3).
- 개수는 **가변**(3~5)이다. **「5개」는 지금 사실이지 계약이 아니다** — 마커가 빠지면 4로 준다. 개수를 가정하지 않는다.

#### `seen` — 가격의 나이
- **원천**: Travelpayouts 응답의 `found_at`(캐시에 그 가격이 관측된 시각). 우리가 스캔한 날이 **아니다** —
  사용자에게 의미 있는 건 가격 자체의 나이다.
- ⚠️ **타임존을 반드시 정규화한다.** 원본 `found_at`은 오프셋 없는 naive 문자열이고 **UTC다.**
  근거(백엔드 검증, 2026-08-22): 크론 실행 순간 기준으로 **UTC로 해석할 때 최소 나이가 `-0.2 ~ +3.5h`** 로 0에 붙는다.
  KST로 해석하면 **가장 신선한 가격조차 8.8h**가 되고 최대 나이가 수집 필터 자신의 컷을 넘어 **애초에 저장될 수 없는 값**이 된다.
  그대로 흘리면 **모든 딜이 9시간씩 늙어 보인다.** 백엔드가 KST로 변환해 **오프셋을 붙여**(`+09:00`) 내보낸다.
  오프셋을 붙일 수 없으면 값을 신뢰할 수 없다는 뜻이므로 `null`을 보낸다.
- **하드 컷**: `seen`이 **7일보다 오래된 딜은 제외**한다(실측 최대 5일이라 손실 0건. 3일 컷은 딜 26% 손실이라 하지 않는다 — `DECISIONS.md`).
- 백엔드는 **절대 시각만** 준다. 「3시간 전」 같은 완성된 문구를 만들지 않는다 — 나이는 **방문 시점**에 계산해야 맞다. 표기 규칙은 `SPEC.md` §CH3.

#### `tags` — 상위 6 · 하위 18

어휘의 정본은 `vocab.json`의 `tags`, 도시별 배정의 정본은 `dests.py`의 `DEST`다. 원칙은 **`TAGS.md`**.

**구조 — 이 불변식이 계약의 핵심이다.**
- **상위 6종** = **필터 칩**이 쓰는 어휘. **하위 18종** = **카드 표시**용(그 도시가 왜 좋은지 말한다).
- ⭐ **하위 태그가 붙은 목적지는 반드시 그 상위 태그도 함께 갖는다.** 그래야 `야시장`을 보고 `미식` 필터를 눌렀을 때 잡힌다.
  백엔드 계약 검증기가 `vocab.json` 기준으로 검사하며, 어기면 CI가 실패한다.
- 어휘 추가·삭제·개명은 **계약 변경**이다.
- **왜 24종인가**: 12종으로는 목적지를 구별할 수 없었다 — **몰디브와 싼야가 같은 태그, 두바이와 나고야가 같은 태그**였다.
- **시즌 표현은 어휘에 넣지 않는다** — `벚꽃`·`단풍`·`설경`은 계절을 타서 데이터가 썩는다.

#### `oa` — 실제 출발 공항 (2026-09-22 결정, 필드 추가라 안전 — `DECISIONS.md` 2026-09-22 (6))
`o` 는 허브(`SEL` = 인천+김포)라 **그 딜이 인천발인지 김포발인지 소비자는 알 수 없다.** 화면은 상세에서 실제 출발 공항을 밝히기로 했는데(§CH4)
`route` 는 노선 페이지가 있는 딜에만 붙어 78건 중 52건이 빈다 — **3분의 1에만 있으면 없느니만 못하다**(없는 쪽을 인천으로 읽는다).
→ 딜마다 **`oa`: 실제 출발 공항 코드**(`ICN`·`GMP`·`PUS`·`TAE`·`CJU`). 수집이 공항을 지정해 물었으므로 백엔드는 **안다.** `route` 가 있으면 `route` 의 앞 절반과 같아야 한다(생산자 테스트).
표시명은 `/v1/vocab.json` 에 **`airport_name`**(`region_name` 과 같은 모양)으로 — 소비자가 손 사본을 만들지 않게.

#### `d` 는 왜 도시 코드인가

**광역 수집이 도시 단위(`TYO`)로 주므로 그 딜이 나리타행인지 하네다행인지 우리는 모른다.**
그런데 `d = "NRT"`라고 적으면 **"이 딜은 나리타행"이라고 주장하는 셈**이다.

- 예약 링크도 **도시 코드로 나간다.** 그래야 공항 코드 링크로는 안 보이던 하네다 편이 나온다.
- 중복 제거가 `(허브, 도시)` 단위라 키도 도시여야 그 문장과 맞는다.
- 공유 링크도 `#SEL-TYO`다 — `#SEL-NRT`는 「나리타?」로 읽힌다.

> **출발지(`o`)는 실제 공항 기준으로 수집한다.** 우리가 `ICN`/`GMP`를 지정해 물은 값이라 **안다**.
> 실측으로도 두 공항 응답의 가격 일치가 3%뿐이다(김포→다낭이 인천의 2배). **아는 것과 모르는 것을 가른다.**

#### `when` 통제 어휘

값 문자열의 정본은 `vocab.json`의 `when`(고정 5 + 패턴 3)이다. 아래 표는 **조건(언제 그 값이 되나)의 설명**이고,
둘이 다르면 `vocab.json`이 맞다. **위에서부터 먼저 맞는 것 하나**를 쓴다 — 순서가 곧 규칙이다.

| # | 조건 | 값 | 예 |
|---|---|---|---|
| 1 | **이번 주(월~일)**의 금·토·일 | `이번 주말` | |
| 2 | **다음 주**의 금·토·일 | `다음 주말` | |
| 3 | **이번 주** 안 (나머지 요일) | `이번 주` | |
| 4 | 같은 달 | `이번 달` | 9/1 오늘, 9/16 출발 |
| 5 | 다음 달 | `다음 달` | 9/1 오늘, 10/5 출발 |
| 6 | 같은 해, 2개월 이후 | `N월` | `11월` |
| 7 | 이듬해 | `내년 N월` | `내년 1월` |
| 8 | 2년 이후 | `YYYY년 N월` | `2028년 3월` |

- 🔴 **「N일 이내」가 아니라 「달력 주」다. 주는 월요일에 시작한다.** 상대 거리로 재면서 라벨은 달력 단위를 말하면
  토요일 접속에 `+6일(금)`이 「이번 주말」이 된다 — 다음 주말인데.
- **왜 `다음 주말`을 따로 두나**: 달력으로 재면 다음 주말이 `이번 달`로 떨어져 **임박 신호를 잃는다.** 여행에서 주말은 특별한 단위다.
  (다음 주 *평일*은 `이번 달`로 둔다.)
- **1·2가 4·5보다 먼저다**: 9/30(수) 오늘, 10/3(토) 출발이면 `다음 달`이 아니라 **`이번 주말`**이다. 달을 넘어도 이번 주면 이번 주다.
- **왜 `이번 달`인가**: 오늘이 9월인데 `9월`은 아무 말도 아니다. 실측 35% — 가장 큰 덩어리였다.
- **왜 `내년 N월`인가**: 딜 출발일이 약 1년 뒤까지 뻗어 있어 `7월`이 **지난 7월로 읽힐 수 있다**(실측 10%).
- **5가 7보다 먼저다.** 오늘이 12월이고 출발이 1월이면 `내년 1월`이 아니라 **`다음 달`**이다 — 더 가깝게 느껴지는 쪽이 사실에도 맞다.
- **8은 방어용**이다. 지금 데이터엔 없지만, 없으면 미래에 조용히 `3월`로 떨어진다.

#### 아직 「완성된 문장」으로 나가는 필드 — 알고 미룬 것

`nights`(`"8박9일"`) · `when`(`"이번 주"`) · `links[].tag`(`"전체 비교"`)는 **완성된 문장**이라 원칙대로면 소비자가 만들어야 한다.
레포 분리 때는 「한 번에 하나씩 바꾼다」로 미뤘고 **아직 안 옮겼다.** `when`은 자유 문장이 아니라 통제 어휘라 급하지 않다.
**놓친 게 아니라 미룬 것이다** — 옮기려면 의미 변경이라 양쪽 합의가 필요하다.

## 3) `GET /v1/routes/index.json`

```json
{
  "schema": "v1",
  "generated": "2026-09-08T07:12:03+09:00",
  "routes": [
    { "code":"ICN-FUK", "o":"ICN", "d":"FUK",
      "o_name":"인천", "d_name":"후쿠오카", "region":"jp", "cheapest":158000 }
  ]
}
```

| 필드 | 타입 | 의미 |
|---|---|---|
| `code` | string | `{출발}-{도착}`. **URL 키**이자 `routes/{code}.json`의 이름 |
| `o` `d` | string | ⚠️ **실제 공항 코드**다(`ICN`·`GMP`·`PUS`). deal의 `o`(허브 `SEL`)·`d`(도시 코드)와 **다른 네임스페이스**다 |
| `o_name` `d_name` | string | 도시 표시명. **참조 데이터**라 백엔드가 준다(deal의 `ko`와 같은 성격) |
| `region` | string | 지역 **코드**. 표시명은 `/v1/vocab.json`의 `region_name`(정본 `dests.REGION_NAME`, 이유는 `COPY.md` §2b) |
| `cheapest` | int\|null | 최근 30일 최저가(KRW) |

- **정렬**: `config.ROUTES` 순서를 그대로 유지한다. 필요한 순서는 소비자가 다시 정렬한다.
- `"인천 → 후쿠오카"`는 백엔드가 만들지 않는다 — **화살표는 문장이다.**
- **여기 실리는 노선 = 사람이 실제로 타는 노선**이다. 노선 페이지는 제목에 공항을 달아 **제목이 곧 주장**이므로,
  직항이 없는 김포발 국제선처럼 경유편 통계로만 성립하는 코드는 넣지 않는다(`DECISIONS.md` 2026-09-20 (6)).

## 4) `GET /v1/routes/{code}.json`

노선 페이지의 원천 전부.

```json
{
  "schema": "v1",
  "generated": "2026-09-08T07:12:03+09:00",
  "code": "ICN-FUK",
  "o": "ICN", "d": "FUK",
  "o_name": "인천", "d_name": "후쿠오카", "region": "jp",
  "window_days": 30,

  "summary":  { "cheapest": 158000, "median": 214000, "n": 412 },

  "trend":    [ {"date":"2026-08-12", "price":158000} ],

  "months":   [ {"m":"2026-09", "price":158000, "n":42} ],

  "weekdays": [ {"wd":1, "price":161000, "n":58} ],

  "airlines": [ {"code":"KE", "name":"대한항공", "min":158000, "n":42} ]
}
```

### 블록별 창 (백엔드가 정한다)

| 블록 | 창 |
|---|---|
| `summary` | 최근 30일 전체 |
| `trend` | 최근 30일, **수집일마다 한 점**, 날짜 오름차순 |
| `months` | 🔴 **`depart_date >= 오늘`**, **출발월** 기준 — 수집일 창이 없다 |
| `weekdays` | **전 기간**, **출발 요일** 기준(요일은 순환한다) — 수집일 창이 없다 |
| `airlines` | 최근 30일 |

> ⚠️ **`months`·`weekdays`는 「며칠 모았나」와 무관하게 첫날부터 찬다.** 둘은 「지금 팔리는 표의 출발 월/요일별 최저가」라
> 수집 하루치로도 버킷이 선다(2026-09-21 실측: 새 7노선 전부 요일 6~7칸). 다만 하루치는 **가장 싼 100건의 단면**이라 편향이 크다.
> 그래서 「며칠 모았나」를 보는 문지기는 `trend`의 기간에서 따로 뽑는다(`SPEC.md` · `DECISIONS.md` 2026-09-20 (7)(10)).

모든 버킷은 **`n`과 함께 전부** 나간다 — 임계로 자르는 건 소비자다(§창과 임계).

### 필드 주의

- **`n`은 버킷 안의 건수다.** 배열 길이가 아니다. 같은 사실을 두 곳에 두면 갈라지므로 **배열마다 표본 수를 따로 넣지 않는다.**
- **`trend`에는 `n`이 없다.** 하루 = 한 점이고, 「이 날은 표본이 얇다」는 판단을 우리가 하지 않기 때문이다.
  ⚠️ 점 개수는 수집 일수보다 적을 수 있다(`fetched_date`가 UTC 자정 경계에서 빠지는 날이 있다) — 기간은 **첫 점~끝 점**으로 센다.
- **`wd`는 `0=일요일 … 6=토요일`**이다. 🔴 배열은 월요일부터 정렬되어 오지만 **순서를 믿지 말고 `wd`를 읽는다.**
- **`m`은 `"YYYY-MM"`**이다. `"9월"`은 백엔드가 만들지 않는다.
- `airlines[].name`은 항공사 표시명 — **참조 데이터**라 백엔드가 준다.
- `summary.cheapest`·`median`이 `null`이면 표본 0이다. 그런 노선은 `routes/index.json`에도 없다.
- `summary.median`은 **그 기간 표본의 중앙값**이다 — 하루치면 하루치의 중앙값이다. 「평소」라고 부를 수 있는지는 소비자가 기간을 보고 정한다.

### 백엔드가 만들지 않는 것 (경계)

계약은 상대의 동작을 적지 않는다(§설명문의 규칙). 다만 **경계는 적는다** — 아래는 백엔드가 **알 수 없거나 만들면 안 되는 것**이다.

- **표시 문자열** — 가격 포맷 · 월/요일 이름 · `"인천 → 후쿠오카"` · 신선도 문구 · 「언제 가면 싼가」 문장. 정본은 `COPY.md`.
- **보여줄지 말지의 판단**(임계) — §창과 임계.
- **사이트 주소를 아는 것** — `<title>` · OG · JSON-LD · sitemap. 백엔드는 자기가 어느 사이트에 실리는지 모른다.
- **구독 `mailto:`의 문구** — 규약(`meta.subscribe`)만 백엔드 것이다. 주소·제목·노선 표기는 **그대로** 써야 한다(§1).
- `dateModified`(JSON-LD)에 쓸 시각은 빌드 시각이 아니라 **`generated`** 다 — 데이터가 만들어진 시각.

## 5) `GET /v1/vocab.json` — 참조 데이터

**왜**: 어휘 목록의 정본이 백엔드 `contract/v1/`에 있는데 프론트가 그 목록을 **손으로 베껴** 칩·표시명을 만들면,
백엔드가 태그 하나를 바꾸는 날 칩은 옛 이름으로 남고 **필터가 조용히 0건**이 된다. 이 응답은 그 목록을 **URL로 건네** 사본을 없앤다.

**모양** — 정본 `vocab.json`을 **그대로** 봉투에 넣고, 지역 표시명 하나만 더한다:
```json
{
  "schema": "v1", "generated": "…",
  "tags":   { "top": ["해변", …6], "sub": {"리조트": "해변", …18} },
  "when":   { "fixed": ["이번 주말", "다음 주말", "이번 주", "이번 달", "다음 달"],
             "patterns": [{"regex": "…", "rule": "…", "example": "…"}, …3] },
  "region": ["jp", "cn", …9],
  "region_name": { "jp": "일본", … },
  "haul": ["short", "mid", "long"],
  "tier": ["major", "minor"],
  "hub":  ["SEL", "PUS", "TAE", "CJU"]
}
```
- `$comment` 키는 뺀다(파일 속 설명이다). **그 외엔 `vocab.json`과 같아야 한다** — 생산자 테스트가 대조한다.
- `region_name`의 정본은 `dests.py` `REGION_NAME`. 키 집합 == `region`(양방향 대조).
- **순서의 뜻**: `tags.top` = 필터 칩 순서 · `when.fixed` = 판정 순서이자 칩 순서. `region`의 순서는 **뜻이 없다** —
  표시 순서가 필요하면 소비자가 정한다.
- **스냅숏 규칙에 들어간다**: `generated == meta.generated`(§공통 규칙).
- **발행 전용**이다 — 이 응답으로 어휘를 **정하지** 않는다. 어휘 변경은 여전히 기획 결정 → 백엔드 `contract/` 수정.

**소비자가 지켜야 하는 조건**
- 어휘 목록을 **손으로 베끼지 않는다.** 이 응답에서 받는다.
- **어휘를 키로 쓰는 소비자 전용 매핑**(태그 → 색, `haul` → 지도 단계, 지역 표시 순서)은 남기되,
  **어휘가 매핑에서 빠지면 빌드가 실패해야 한다.** 조용한 폴백(기본 색 · `|| "far"`)을 없애는 게 목적이다.
  검사는 **어휘 ⊆ 매핑** 한 방향만 실패로 건다 — 양방향으로 걸면 어휘를 바꾸는 날 양쪽이 서로를 기다리며 멈춘다.

**범위 밖**: 도시별 배정(`DEST`)·좌표는 내보내지 않는다 — 딜에 이미 필요한 만큼 실려 있다.
