# 레포 분리 이전 계획 (M0~M6)

> **이 문서는 임시다.** 이전이 끝나면(M6) 삭제한다. 결론은 `DECISIONS.md`에,
> 최종 구조는 `PROJECT.md`에, 계약은 `CONTRACT.md`에 남는다.
>
> 소유: 기획 세션. **프론트·백엔드 세션은 자기 단계의 태스크를 여기서 읽는다.**

---

# ▶ 재개됨 (2026-09-11) — 아래는 보류 기록

**2026-09-11 사용자 지시로 재개했다** ("이제 레포 옮기기 작업 갑시다"). 재개 건강 확인:

```
① 딜 138 · 제휴 링크 138      두 숫자 일치 (딜 수는 125 → 133 → 138로 계속 바뀐다)
② docs/v1/ 39개               유지
③ 과거 출발월 버킷 0           BB29 유지
④ v1 generated 2026-09-11      크론이 보류 중에도 매일 돌았다
⑤ 크론이 site/build.py 호출    안 함 — 스위치 전 그대로
보류 중 main 커밋: 크론 2회 + 백엔드 §12.4 메모. 두 코드 경로 변경 0
```

**보류는 비용 없이 끝났다.** 「무기한 방치해도 안 깨진다」가 실제로 맞았다.

---

## ⏸ 보류 기록 (2026-09-09~11)

**사용자가 먼저 할 일이 생겨 이전을 보류했다.** 재개할 때 이 절부터 읽는다.

## 지금 어디까지 왔나

```
M0 계약 확정        ✅  CONTRACT.md §v1 (4개 엔드포인트)
정지작업 BB29·BB30  ✅  라이브 결함 수정 + 재빌드 경고, 배포 확인
M1 백엔드 v1 발행   ✅  docs/v1/ 39개. 크론이 무인으로 만든 것까지 확인
M2 프론트 화면 생성  ✅  기준선 05d0de9 대비 39/39 바이트 동일 (기획 독립 검증)
────────────────────────────────────────────── ⏸ 여기
M3 스위치·HTML 삭제  🔶  사용자 승인 대기 — 다음 행동
M4 레포 분할        ⬜  사용자 손 필요
M5 도메인 이전      ⬜  사용자 손 필요 · 다운타임 있는 유일한 단계
M6 정리            ⬜  사용자 손 필요
```

## 🟢 왜 여기가 멈추기 좋은 자리인가

**아무것도 안 깨진 채로 무기한 방치할 수 있다.** 실측(2026-09-09):

| | 상태 |
|---|---|
| 크론이 부르는 것 | `collector/build_site.py` — **옛 경로 그대로** |
| `site/build.py`를 크론이 부르나 | **아니오** (스위치는 M3 T1이다) |
| 산출물 | 옛 노선 HTML 36장 **+** 새 v1 JSON 39개가 **나란히** 나온다 |
| 사이트 | 평소와 동일. 사용자가 볼 변화 0 |

**M3가 첫 「되돌리기 어려운」 단계**이고 그걸 안 밟았다. 지금까지 한 일은 전부 **더하기**였다 —
지운 게 없다.

## ▶ 재개하면 첫 행동 (순서대로)

1. **세 세션 다 `git pull`** — 그동안 크론이 매일 커밋했다.
2. **건강 확인** — 하루면 되는 것들이 며칠 지나면 조용히 틀어져 있을 수 있다.
   ```bash
   # 제휴 링크: 두 숫자가 같아야 한다 (절대값 아님 — CLAUDE.md 참조)
   python -c "import json; d=json.load(open('docs/data/deals.json',encoding='utf-8')); print(len(d['deals']), sum(any(l.get('ad') for l in x['links']) for x in d['deals']))"
   # v1 이 계속 나오나
   git ls-tree -r --name-only origin/main docs/v1/ | wc -l     # 39
   # BB29 유지 (과거 출발월 0)
   ```
3. **`SPLIT.md` §M3부터 읽고 사용자 승인을 받는다.** M2는 끝났으므로 M3가 다음이다.
4. **기준선 `05d0de9`는 다시 안 잡아도 된다** — M2 증명이 이미 끝났다. 기준선의 일은 다 했다.

> ⚠️ **`fixtures/v1/`은 기준선에 고정돼 있고 낡는다 — 그게 정상이다.**
> 실측(2026-09-09): `fixtures` = 09-08 딜 125건 · `docs/v1` = 09-09 딜 133건.
> 크론은 `docs/v1/`만 갱신하고 `fixtures/`는 안 건드린다.
> **낡아도 배포는 안 틀린다** — 배포는 언제나 실물 API를 쓰고 픽스처는 로컬 개발용이다.
> 🔴 **재개할 때 픽스처를 함부로 다시 받아 적지 말 것.** 지금 픽스처와 기준선 산출물이
> **같은 커밋 쌍**이라 동등성을 언제든 다시 증명할 수 있다. 한쪽만 갱신하면 그 쌍이 깨진다.

## 각 세션이 멈춘 자리

| 세션 | 멈춘 곳 | 재개 시 |
|---|---|---|
| **기획** | M2 독립 검증 완료. M3 승인을 사용자에게 올린 상태 | 승인 오면 백엔드에 전달 |
| **백엔드** | M1 완료. M3 태스크 목록(T1~T6) 준비됨 | 승인 후 M3 T1 |
| **프론트** | M2 완료·push(`c777279`). **M4까지 할 일 없음** | M4에서 `site/`·`assets/`·`fixtures/` 이동 |

## 🔴 사용자만 할 수 있는 일 (재개 후)

- **M3 승인** — 챕터라 스코프 잠금이 필요하다
- **M4**: 레포 2개 생성(**둘 다 public**) · **secrets 5종** 재등록 · 변수 2종 · PAT 발급
- **M5**: DNS(`api.galmal.kr` CNAME) · Pages 커스텀 도메인 · 검색엔진 재확인
- **M6**: 구형 레포 rename 후 **private** ← ⚠️ **M5 끝나기 전에 누르면 galmal.kr이 404**

## ⏳ 시간이 지나면 나빠지는 것 / 안 나빠지는 것

**나빠지는 것 — 재개할 때 확인할 것**

| | 왜 |
|---|---|
| 🔴 **두 경로가 오래 공존한다** | `build_site.py`(HTML)와 `publish_v1.py`(JSON)가 같은 통계를 쓴다. **한쪽만 고치면 갈린다.** 지금은 `ReproducesTheCurrentScreenTest`가 잡지만, 새 기능이 한쪽에만 들어가면 M3의 diff가 커진다 → **이전 중에는 두 경로 중 어느 쪽도 건드리지 않는다** |
| 저장소 크기 | `prices.db` 하루 +0.7MB. 팩 39MB 기준 1~2년 여유라 **급하지 않다.** 감시 지점은 파일 크기가 아니라 **팩의 급증**(`VACUUM`이 델타 사슬을 끊는다) |

**안 나빠지는 것 — 안심해도 됨**

- **기준선 `05d0de9`** — SHA로 못박혀 있다. 날짜가 넘어가도 무관하다(2026-09-09에 실제로 확인)
- **계약 `CONTRACT.md` §v1** — 양쪽이 「이 계약으로 만들 수 있다」를 실물로 증명했다
- **Azure 크레딧** — ₩130,000, **2027-02 만료.** M6 후 스파이크(§5c)에 쓴다. 17개월 여유
- **BE8(노선 페이지 확대)** — 서치콘솔 데이터가 쌓일수록 유리해진다. 미루는 게 손해가 아니다

## 보류 중인 다른 것들 (M6 후)

프론트 CH6(접근성·마감) · 백엔드 BE8 · 기획 PH7(시즌 이벤트, 10월) ·
「사진을 넣을 것인가」(§5c 위) · BB31(`fmt_month` 연도, `COPY.md` §6) ·
`LIMIT 10`이 창인가 임계인가 · **서버 전환 스파이크**(§5c)

---

## 왜 하나 (2026-09-08 사용자 결정)

지금은 한 저장소 안에서 **백엔드가 HTML을 짜고 프론트가 백엔드 모듈을 import 한다.**
규모가 커져 백엔드가 자체 서버를 갖는 시점이 오면, 이 엉킴은 그때 풀기 훨씬 비싸다.
**어느 정도 완성된 지금**, 기능이 아니라 경계를 정리한다.

설계 기준은 하나로 요약된다:

> **나중에 백엔드가 자체 서버가 됐을 때, 프론트 코드가 한 글자도 안 바뀌어야 한다.**

기각한 대안과 근거는 `DECISIONS.md` 2026-09-08 항목.

---

## 1. 목표 구조

```
galmal-backend  (공개)                         galmal-frontend  (공개)
├ collector/         수집·판정·메일·알림    ├ site/
│  └ publish.py      ← 진입점              │   ├ shell.py   <head>·CSS·도메인·OG
├ data/prices.db                           │   ├ home.py    index.html
├ tests/             계약 검증             │   ├ route.py   routes/*.html (36)
└ .github/workflows/collect.yml            │   ├ seo.py     sitemap·robots
                                           │   └ build.py   ← 진입점
        ↓ 발행                             ├ assets/   discover.js|css, d3
   api.galmal.kr/v1/*.json                 ├ fixtures/ v1 응답 사본(로컬 개발용)
   (오늘은 GitHub Pages,                   └ .github/workflows/deploy.yml
    나중에 자체 서버 — DNS만 옮긴다)              ↓ 소비
                                              galmal.kr  (Pages, 도메인)

galmal-plan  (비공개)  ← promo-ticket-site를 rename
└ 기획 문서 13종 + design/ 목업.  두 레포가 `../galmal-plan/`으로 참조한다.
```

**문서는 복사하지 않는다.** 두 벌이 되면 갈라지고, 갈라지면 2026-09-01 사고
(개명한 명칭이 전달되지 않아 구명칭이 검색엔진까지 나간 것)가 재발한다.
각 레포의 `CLAUDE.md`가 `../galmal-plan/<문서>.md`를 가리키고 원본은 한 벌만 둔다.

---

## 2. 설계 원칙 — 경계 판정 기준

애매한 코드가 나오면 이 여섯으로 판정한다.

| # | 원칙 | 위반의 냄새 |
|---|---|---|
| **P1** | **백엔드는 사이트에 올라가는 것을 만들지 않는다.** `docs/`에 들어가는 산출물 전부 — HTML·CSS·sitemap·robots. | 백엔드가 `docs/`에 HTML을 쓴다 |
| **P2** | **사이트 주소의 정본은 프론트다.** `BASE_URL`·`SITE_NAME`·`OG_IMAGE`·소유확인 메타는 `site/shell.py`. 백엔드는 **하드코딩하지 않는다.** | 백엔드 코드에 `https://galmal.kr` |
| **P3** | **프론트는 DB를 모른다.** | 프론트 파일에 `sqlite3` / `conn` |
| **P4** | **데이터는 URL로 건넨다, 파일 경로가 아니라.** | 프론트가 `../data/`를 연다 |
| **P5** | **응답은 버전 경로 아래에.** 깨는 변경은 `/v2/`를 새로 내고 한동안 둘 다 낸다. | 경로에 버전이 없다 |
| **P6** | **한 응답 = 나중의 한 라우트.** 노선 36개를 한 파일에 뭉치지 않는다. | 서버 전환 때 다시 쪼개야 함 |
| **P7** | **백엔드는 사실을, 프론트는 말을 낸다.** 임계·문구·포맷은 프론트. | 백엔드가 "표본이 부족합니다" |

### P1·P2가 「HTML 금지」가 아닌 이유 — 알림 메일 (백엔드 지적, 2026-09-08)

백엔드 세션이 짚었다: `send_alerts.py`가 백엔드에 남는데 그건 **메일 HTML을 만들고**
(`deal_line()`의 `<li>`, `build_mail()`의 본문) **사이트로 링크한다**
(`send_alerts.py:32` `SITE_URL = theme.BASE_URL + "/"`). 초안의 P1·P2가 그대로면 위반이다.

**예외로 때우지 않고 원칙을 다시 그었다.** 축이 「HTML이냐 아니냐」가 아니라
**「사이트냐 아니냐」**이기 때문이다.

| | 사이트 산출물 | 채널 페이로드 |
|---|---|---|
| 무엇 | `docs/`에 올라가 galmal.kr이 서빙하는 것 | 메일 본문. 나중에 알림톡·푸시·RSS도 여기 |
| 누가 | 프론트 | 백엔드 |
| 왜 | 화면이다 | 화면이 아니다. 웹 클라이언트가 렌더하지 않는다 |

메일은 **자체 서버가 생겨도 백엔드에 남는 것**(transactional email)이라, 미래 구조와도 맞는다.
그래서 P1은 「태그 금지」가 아니라 「사이트 산출물 금지」다.

**단, 문자열의 정본은 여전히 `COPY.md`다.** 렌더하는 모듈이 백엔드라고 해서
메일 문구를 백엔드가 지어내지 않는다. 지금과 같다.

### P2 — 백엔드가 주소를 필요로 하는 한 곳

알림 메일 링크. 여기서 하드코딩하면 `theme.py:29`가 경고하는 바로 그 일이 난다:
「도메인을 바꾸는 날 알림 메일만 옛 주소를 가리킨다 — 화면은 멀쩡하고 메일만 틀리므로 아무도 모른다」.

**해법: 환경변수 `SITE_URL`.** 도메인은 코드가 아니라 **배포 설정**이다.
자체 서버가 생기면 어차피 그렇게 관리한다.

⚠️ **이때 잃는 것이 있다.** 지금은 `tests/test_site_url.py:53`이
`send_alerts.SITE_URL`과 `theme.BASE_URL`이 갈라지지 않게 잠그고 있다.
분리 후 `BASE_URL`은 프론트 레포, `SITE_URL`은 백엔드 env — **아무도 대조하지 않는다.**

**대책(R1과 합침)**: 백엔드 크론 마지막 「상태 점검」이 `$SITE_URL/v1/meta.json`을 실제로 받아
`generated`가 오늘 것인지 본다. ⚠️ **이 주소는 분리 후 404가 난다 — §5 R1c에서 두 점검으로 고쳤다.** 주소가 틀리면 404로 실패하고, PAT가 죽어 배포가 멈춰도
같은 스텝이 잡는다. **한 점검이 두 가지를 다 막는다.**

**P2의 다른 예외**: 제휴 예약 링크. `TP_MARKER` 등 시크릿이 필요해 백엔드만 만들 수 있다.
사이트 내부 링크(`/routes/…`)는 전부 프론트가 만든다.

### 지금 위반하고 있는 것 (실측, 2026-09-08)

| 위치 | 위반 | 옮길 곳 |
|---|---|---|
| `collector/build_site.py:191` `route_page()` **180줄** | P1 — sqlite를 물고 HTML을 짠다 | 프론트 `site/route.py` |
| `collector/charts.py` 전부 (SVG 11군데 · `NOT_ENOUGH` 문구) | P1 — 순수 화면 코드 | 프론트 |
| `collector/labels.py`의 **포맷만** (`fmt_date` `fmt_month` `SQL_WEEKDAY`) | P1 | 프론트 |
| `tests/test_charts.py` | 프론트 코드를 테스트한다 | 프론트 |
| `collector/build_site.py:384` `build_seo()` 20줄 | P1·P2 | 프론트 `site/seo.py` |
| `collector/theme.py` `BASE_URL` `SITE_NAME` `OG_IMAGE` `verification_meta` | P2 | 프론트 `site/shell.py` |
| `collector/discover_home.py`(프론트 소유)가 `theme`(백엔드)를 import | 경계 역행 | 둘 다 프론트로 |
| `docs/data/deals.json` — 프론트가 **파일 경로**로 읽음 | P4·P5 | `api.galmal.kr/v1/deals.json` |

`daily_min` `month_min` `weekday_min` `airline_min` `route_summary`(계 57줄)는 **통계 계산**이라
백엔드에 남는다. 다만 HTML이 아니라 **JSON으로 결과를 낸다.**

`labels.py`의 **참조 데이터**(`city` `airline_name` `region_of`)도 백엔드에 남는다.
계약이 `o_name`·`d_name`·`airlines[].name`·`region`을 주므로 프론트는 이걸 안 봐도 된다.
**한 파일이 두 성격이라 쪼갠다** — 참조는 백엔드, 포맷은 프론트(프론트 발견).

### 🔴 옮기지 말 것 — 죽은 코드 (프론트 실측, 2026-09-08)

| | 상태 |
|---|---|
| `build_site.py:129` `deal_card()` 34줄 | **아무도 안 부른다** |
| `build_site.py:100` `sparkline()` 29줄 | `deal_card()`만 부른다 → 같이 죽었다 |
| 산출물 흔적 | `docs/index.html`·`docs/routes/*.html`에 `class="card"` 0건, `class="spark"` 0건 |

발견 홈이 옛 카드 홈을 대체하며 남은 잔해다. **M3에서 지운다. 옮기지 않는다.**
초안 표가 이 63줄을 프론트로 옮기라고 했는데 취소한다.

> 살아 있었다면 문제가 됐을 것이다 — `sparkline()`은 `daily_min(..., is_direct)`로
> **딜별** 추이를 그리는데 계약의 `trend`는 **노선 단위**라 `is_direct` 축이 없다.
> 죽어 있어서 문제가 안 됐다.

### 확인된 사실 — 걱정하지 않아도 되는 것

- `send_alerts.py`는 메일 HTML을 **DB 행에서 직접** 만든다. `deals.json`을 안 쓴다.
  → 계약을 바꿔도 알림 메일은 안 깨진다. (나중에 v1 API의 첫 외부 클라이언트로 돌리는 건 별건.)
- 백엔드 테스트 11개 중 프론트 자산(`discover.js|css`)을 읽는 것은 **0개**다.
- CORS는 지금 문제가 아니다. 프론트는 **빌드 타임에** API를 가져온다(브라우저가 아니라 Actions).
  런타임 fetch로 바꾸는 날에만 확인하면 된다.

---

## 3. v1 계약 (초안 — 확정본은 `CONTRACT.md`)

```
GET /v1/meta.json           schema·generated·수집 상태
GET /v1/deals.json          홈이 쓰는 전부 (현 deals.json과 동형)
GET /v1/routes/index.json   [{code, label, o, d}] — 네비·sitemap용
GET /v1/routes/{code}.json  노선 1개 통계 — 현재 route_page()가 conn에서 뽑던 값 전부
```

모든 응답의 공통 봉투:

```json
{ "schema": "v1", "generated": "2026-09-08T07:12:03+09:00", "...": "..." }
```

`generated`에 **오프셋을 반드시 포함**한다. 화면이 신선도를 표시하고 있고
(`SPEC.md` §CH3 `발견가 · N일 전 가격`), 날짜 경계에서 하루가 어긋나면 조용히 틀린다.

### `routes/{code}.json`이 담아야 할 것 (백엔드 확인, 2026-09-08)

노선 페이지가 지금 표시하는 숫자는 5종이다 —
`route_summary` · `daily_min` · `month_min` · `weekday_min` · `airline_min`.

**여기에 「표본이 부족하다」를 표현할 자리가 있어야 한다.** 백엔드 세션 지적:
지금은 양쪽 차트에 `len(rows) < 2` 임계가 걸려 `charts.NOT_ENOUGH` 문구가 나간다.
계약에 그 자리가 없으면 **프론트가 빈 차트를 그린다.**

**P7을 적용해 이렇게 나눈다:**

| | 누가 | 무엇 |
|---|---|---|
| 표본 수 `n` | 백엔드 | 사실. 모든 차트 블록에 **항상** 넣는다 |
| 임계 `n < 2` | 프론트 | 점 하나로 선을 못 긋는다는 건 **렌더링 판단**이다 |
| 「자료가 모자랍니다」 문구 | `COPY.md` | 지금도 그렇다 |

`null`로 얼버무리지 않는다 — "값이 없다"와 "표본이 모자라다"와 "0원이다"가 구분돼야 한다.

---

## 4. 단계

각 단계는 **사이트가 살아 있는 채로** 끝난다. 도메인은 M5에서만 건드린다.

### M0 — 계약 확정 · 기획

| 태스크 | 내용 |
|---|---|
| T1 | `CONTRACT.md`에 v1 4개 엔드포인트 스키마 전문 — **완료**(2026-09-08) |
| T2 | `DECISIONS.md` 2026-09-08 (1)(2)(3) — **완료** |
| T3 | `PROJECT.md` 목표 구조 반영 |
| T4 | 양 세션에 전달 |

**DoD**: 프론트·백엔드 양쪽이 "이 계약으로 짤 수 있다"고 회신. 못 짜는 항목이 있으면 T1로 되돌아간다.

### M1 — 백엔드가 JSON을 낸다 · 백엔드 (같은 레포 안)

| 태스크 | 내용 |
|---|---|
| T1 | `docs/v1/deals.json` 발행. **기존 `docs/data/deals.json`은 그대로 둔다** (프론트가 아직 씀) |
| T2 | `docs/v1/routes/{code}.json` 36개 — `route_page()`가 conn에서 뽑던 값을 **전부** + **버킷마다** `n`. `CONTRACT.md` §v1 4)의 「백엔드가 바꿔야 할 것」 5개 포함 |
| T3 | `docs/v1/routes/index.json`, `docs/v1/meta.json` |
| T4 | `test_contract.py` 확장 — v1 4종 검증 |

**DoD**: 지금 노선 페이지에 **표시되는 모든 숫자**가 JSON 안에 있다.
빠진 값이 하나라도 있으면 M2에서 프론트가 막힌다.
**롤백**: JSON을 추가로 낼 뿐이라 사이트 영향 0. 실패해도 되돌릴 게 없다.

### M2 — 프론트가 JSON에서 화면을 만든다 · 프론트 (같은 레포 안)

| 태스크 | 내용 |
|---|---|
| T1 | `site/` 신설. `discover_home.py`→`site/home.py`, `theme.py`→`site/shell.py` (백엔드에서 인수) |
| T2 | `site/route.py` — `v1/routes/*.json`에서 노선 페이지 36장 |
| T3 | `site/seo.py` — sitemap·robots |
| T4 | `site/build.py` 진입점. `--api <URL 또는 로컬 경로>` |
| T5 | **동등성 증명** |

**DoD (T5가 이 이전 전체의 관문이다)**:
`site/build.py`가 만든 37장이 현 배포본과 **diff 0**. 차이가 있으면 한 줄씩 설명하고
"의도한 개선"인지 "깨뜨린 것"인지 판정한다.

> 🔴 **그래서 이전은 동작을 바꾸지 않는다.** 개선하고 싶은 게 있으면 **이전 전에**
> 현행 코드에서 고쳐 크론이 한 번 배포하게 하거나, **M3 이후에** 별도 챕터로 한다.
> 이전 중에 바꾸면 diff가 났을 때 **이전이 깨뜨린 건지 우리가 고친 건지 구분할 수 없다.**
> 프론트가 재현해야 할 현행 동작 3가지는 `CONTRACT.md` §「이전 중에는 동작을 바꾸지 않는다」.

> 🔴 **검수 시작 전에 기준선부터 확인한다** (프론트 제안):
> 비교 대상이 **BB29 수정 후 배포본**인가. 수정 전 화면과 비교하면 5장이 통째로 달라 보여서
> **규칙을 지켰는데도 실패로 판정하게 된다.** 걸리면 원인 찾는 데 오래 걸리는 종류다.

#### 🔴 기준선의 정의 — 커밋 SHA로 못박는다 (2026-09-08 확정)

프론트 질문: 「기준선이 `main`의 산출물인가 실제 galmal.kr인가」. **둘 다 아니다.**
더 큰 문제가 있다 — **`docs/`는 매일 새 가격으로 다시 만들어진다.**
「오늘의 `main`」과 비교하면 어제 데이터로 빌드한 순간 기준선이 이미 움직여 있다.
M2가 사흘 걸리면 기준선이 세 번 바뀐다. **움직이는 과녁에는 diff 0이 존재하지 않는다.**

**그래서 입력과 출력을 같은 커밋에 고정한다.**

```
기준선 커밋 <SHA> 하나를 고른다. 그 커밋에서:
    입력  data/prices.db          ← 이걸로 v1 JSON을 만들고
    출력  docs/index.html
          docs/routes/*.html (36) ← 이것과 diff 한다
```

같은 입력 → 같은 출력이어야 한다는 명제를 검증하는 것이지, 「오늘 화면과 같은가」가 아니다.

**기준선 커밋이 갖춰야 할 조건 셋**

| | 조건 | 왜 |
|---|---|---|
| 1 | **BB29 수정 이후** | 아니면 틀린 화면을 보존하게 된다 |
| 2 | **시크릿이 있는 환경에서 만들어진 산출물** | 아니면 제휴 링크 125건이 빠진 것을 기준선으로 삼는다(R7) |
| 3 | **`prices.db`와 `docs/`가 같은 커밋** | 서로 다른 커밋에서 가져오면 입력·출력이 어긋나 diff가 영원히 안 맞는다 |

조건 2 확인: 그 커밋에서 `grep -o '"ad":true' docs/data/deals.json | wc -l` → **125**.

**`main`의 산출물과 실제 galmal.kr의 관계**: Pages는 파일을 그대로 서빙하므로 결국 같은 바이트다.
**diff는 파일로 한다**(HTTP는 반영 지연·캐시가 낀다). 라이브는 **눈으로 보는 용도**다 —
`SPLIT.md`가 여러 번 확인한 대로 **바이트가 같아도 화면이 깨질 수 있다.** 둘 다 한다.

#### ✅ 기준선 확정: `05d0de9` (2026-09-08, 기획이 검증)

`M1 T2·T3: v1 API를 발행하고 계약 검증기를 세운다`

| 조건 | 확인 |
|---|---|
| BB29 수정 이후 | ✅ **과거 출발월 버킷 0개** (36노선 `months` 전수) |
| 시크릿 있는 환경의 산출물 | ✅ `ad:true` **125건** |
| `prices.db`와 `docs/`가 같은 커밋 | ✅ (`prices.db` 34.4MB · 노선 36장) |
| **덤: `docs/v1/` 39개가 같은 커밋에 있다** | ✅ |

**마지막 줄이 중요하다.** 픽스처와 기준선을 **같은 커밋에서** 가져오면
「같은 입력 → 같은 출력」이 구조적으로 보장된다.

```
입력   git show 05d0de9:docs/v1/…        ← fixtures/v1/ 로 받아 적는다
출력   git show 05d0de9:docs/routes/…    ← 이것과 diff
```

##### 🔴 대조는 **blob**과 한다 — 작업 트리가 아니다

```
git cat-file -p 05d0de9:docs/routes/ICN-FUK.html   ← 이것과 비교한다
```

`core.autocrlf=true`이고 `.gitattributes`가 없어서 **체크아웃된 파일은 CRLF**다
(blob 31,252 · 작업 트리 31,603 — 351줄 × 1바이트). `git archive`도 변환을 적용한다.

기획이 M2를 독립 검증할 때 **이걸 몰라서 39/39 전부 다르다고 나왔다.**
경보를 울리기 전에 자기 설정을 의심해서 잡았다. 윈도우에서 **이 함정은 항상 켜져 있다.**

> 곁가지 확인: 크론(리눅스)은 LF로 쓰고 커밋하고, 윈도우 세션은 CRLF로 쓰지만 git이
> LF로 되돌린다 — **양쪽이 같은 blob을 만든다.** M3 스위치에서 줄바꿈 대량 diff는 없다.

##### ⚠️ 기준선을 검증하다 BB31 함정에 걸렸다 — 두 번째다

`docs/routes/*.html`에서 월 숫자만 뽑아 9와 비교했더니 **`ICN-KUL 6월`·`ICN-LAX 2월`이
과거로 잡혔다.** 실제로는 `2027-06`·`2027-02`로 **미래**다. v1 JSON으로 확인하니
과거 버킷은 **0개**였다.

**프론트가 정확히 같은 자리에서 같은 오판을 했고 스스로 정정했다.** 이제 기획도 걸렸다.

> **사람 둘이 같은 라벨을 두 번 잘못 읽었다.** BB31(`fmt_month`가 연도를 버린다)이
> 「이론상 위험」이 아니라 **실제로 읽는 사람을 속이고 있다**는 증거다.
> `COPY.md` §6의 판정(「지금은 안 고친다」)은 **화면이 안 깨진다**는 뜻이지
> **안 헷갈린다**는 뜻이 아니다 — 그 구분을 §6에 적어둔다.
**+ 렌더 대조** — 단 **바이트가 다를 때만.** (프론트 정정 2026-09-08)

> **바이트가 동일하면 렌더도 동일하다.** 스크린샷은 「바이트가 다른데 화면도 달라졌나」를
> 보는 도구지, 동일함을 재확인하는 도구가 아니다. **노선 36장은 대조할 것이 없다.**
>
> **홈은 다르다.** 지도·시트를 JS가 그리므로 **HTML이 같아도 자산이 어긋나면 화면만 깨진다.**
> 홈에서는 스크린샷이 필요하다.
>
> 원래 이 항목을 넣은 이유는 여전히 유효하다 — **테스트 통과는 화면이 멀쩡하다는 증거가
> 아니다**(z-index가 「(광고)」 고지를 가린 건, 막대 1개짜리 차트, `[hidden]`이
> `display:flex`에 밀린 건 3회 — 전부 테스트를 통과했다). 다만 **바이트 동일**은
> 테스트 통과보다 강한 주장이라 이 경우엔 스크린샷이 더할 게 없다.

#### 🔴 「diff 0」은 「무변경」이 아니다 — 잠복 차이를 여기 적는다

**오늘 산출물이 같아도 동작이 다를 수 있다.** 그런 변경은 **조건과 함께 여기 적는다.**
안 적으면 나중에 갈리는 날 **회귀로 오인된다.**

| 변경 | 오늘 안 보이는 이유 | 갈리는 조건 |
|---|---|---|
| **BB28** — 임계를 차트뿐 아니라 **문장**에도 적용 | 36노선 중 버킷 2개 미만이 `months` 0 · `weekdays` 0 | **새 노선이 들어오는 날.** 그때 「언제 가면 싼가」가 사라지는 게 **정상이다** |
| ~~`dateModified` `today_utc()` → `generated`의 UTC 날짜~~ | ~~오늘 둘 다 `2026-09-08`~~ | ~~크론이 UTC 자정 근처로 옮겨갈 때~~ — 🔴 **틀렸다. 잠복 변경이 아니라 버그였다.** 아래 참조 |

**이걸 허용하는 조건 셋** — 「오늘 바이트만 같으면 뭐든 된다」가 되면 안 된다:

1. **산출물 diff-0이 성립한다** (오늘 데이터로)
2. **갈리는 조건을 여기 적는다** — 나중에 「왜 바뀌었지」가 안 되게
3. **기록된 결함을 고치거나 확정된 판정을 따르는 것**이지, **새 설계를 넣는 게 아니다**

> 🔴 **`dateModified` 정정 (2026-09-11, 프론트 발견 · 기획 재확인)**
> 구현이 `generated[:10]`이었는데 `generated`는 **KST 시각**이라 앞 10자는 **KST 날짜**다.
> 옛 경로는 `today_utc()`(UTC 날짜, BB17 규칙)다. 크론이 UTC 자정 **전**에 돌면 둘이 하루 어긋난다.
> **「자정 근처로 옮겨갈 때」가 아니라 이미 거기 있다** — 실측(`gh run list`, 최근 스케줄 실행 10회):
> `23:46`~`00:13 UTC`에 걸쳐 돌고 **10회 중 4회가 자정 전**이다. v1이 생긴 뒤 4회가 우연히 전부
> 자정 후라서 T1 첫 대조(39/39)가 통과했을 뿐이다. **자정 전에 도는 날엔 T1 대조가 노선 36장 +
> sitemap에서 실패한다.**
> **계약은 맞았다** — `CONTRACT.md:603`이 처음부터 「`generated`의 **UTC** 날짜」다. 구현이 계약과 달랐다.
> 수정본(`machine_date()` — 생성 시각을 UTC로 바꾼 뒤 날짜)이 들어가면 한 작업 안에서
> 옛 경로 `today_utc()`와 **같은 날짜**가 나오므로 **잠복 변경이 아니게 된다.**
> → 남는 잠복 변경은 **BB28 하나**, 잠복 부채는 **`inline_deals()` 하나**다.

BB28은 `BACKEND.md`에 기록된 결함이다.
**둘 다 셋을 만족한다.** 만족하지 않는 변경은 M3 이후로 미룬다.

#### 잠복 **부채** — 위와 다른 범주다 (2026-09-09)

프론트가 `home.inline_deals()`를 두고 「허용 조건 ③에 걸리나」 물었다. **안 걸린다.
범주가 다르다.**

| | 잠복 **변경** | 잠복 **부채** |
|---|---|---|
| 무엇 | 오늘 바이트는 같은데 **나중에 동작이 갈린다** | 오늘 바이트를 **같게 만들려고 넣은 임시 코드** |
| 예 | BB28 | `home.inline_deals()` |
| 필요한 것 | **갈리는 조건**을 적는다 | **없앨 조건**을 적는다 |
| 위험 | 그날 회귀로 오인된다 | 아무도 안 지워서 영구화된다 |

`inline_deals()`는 v1 봉투(`{schema, generated, …}`) 대신 현행 봉투(`{updated, …}`)를 유지한다.
`discover.js`는 `updated`를 **한 번도 안 읽는데**(실측 0회) 그래도 재현했다.
이유가 화면이 아니라 **증명**이라는 게 정확하다:

> 봉투만 바꿔도 `index.html`은 크게 diff가 나고, 그러면 「그래도 화면은 같다」를
> **사람이 눈으로 판정**해야 한다. 바이트가 같으면 판정할 게 없다.

**이건 규칙 위반이 아니라 규칙을 지키려고 치른 값이다.** 없앨 조건은 프론트가 코드에 적었다 —
M6 후 `discover.js`가 `generated`를 읽게 되면 지운다.
**롤백**: 크론은 아직 `build_site.py`를 부른다. 새 코드는 병행 존재일 뿐.

#### M1 전에 할 수 있는 것 — 프론트는 대기하지 않는다

계약(`CONTRACT.md` §v1)이 확정됐으므로 **응답을 손으로 만든 픽스처로 먼저 짤 수 있다.**
백엔드 파일을 안 건드리는 것부터 한다.

| 태스크 | M1 필요? | 비고 |
|---|---|---|
| `site/build.py` 뼈대 + `--api <URL\|경로>` | ❌ | 진입점·인자 처리 |
| `fixtures/v1/` 손으로 만들기 | ❌ | 커밋된 `deals.json`을 v1 봉투로 감싸고, `routes/{code}.json`은 계약대로 손으로 한 장 |
| `site/route.py` (T2) | ❌ | 픽스처로 짠다. M1이 오면 픽스처만 실물로 교체 |
| `site/seo.py` (T3) | ❌ | sitemap·robots는 `routes/index.json`만 있으면 된다 |
| `site/home.py`·`site/shell.py` (T1) | ⚠️ | **`theme.py`·`discover_home.py`를 백엔드 구역에서 가져온다.** M1이 `build_site.py`를 고치는 중이라 **충돌한다** → M1 완료 후 |
| T5 동등성 증명 | ✅ | 실물 JSON과 기준선이 있어야 한다 |

**픽스처를 손으로 만드는 게 계약 검증이기도 하다.** 계약만 읽고 응답을 못 만들면
계약이 부족한 것이다 — M1이 끝난 뒤가 아니라 **지금** 알아야 싸다.

##### `fixtures/v1/`은 커밋한다 — 단 「받아 적은 것」이어야 한다 (2026-09-08 확정)

프론트 질문: 커밋하면 **계약의 두 번째 사본**이 되어 갈리지 않나. 「문서는 복사하지 않는다」와
같은 종류의 위험 아닌가. **맞는 걱정이고, 답은 「커밋하되 저작을 금지」다.**

**커밋한다.** 분리 후 프론트 저장소는 **네트워크 없이도 빌드할 수 있어야** 한다.
그게 `CLAUDE.md`가 말하는 픽스처 흐름(「백엔드를 기다리지 않는다」)의 분리판이고,
이전용 임시방편이 아니라 **영구 설비**다.

> **픽스처는 「받아 적은 것」(captured)이지 「손으로 쓴 것」(authored)이 아니다.**
> 실물 응답을 그대로 저장한다. **손으로 고치지 않는다.**

- **손으로 만드는 건 딱 한 번, M1 전에만** 허용된다. 그때는 실물이 존재하지 않기 때문이다.
- **M1이 오면 실물 응답으로 덮어쓴다. 고치는 게 아니라 갈아끼운다.**
- 이후 계약이 바뀌면 픽스처를 **다시 받아 적는다.** 손으로 맞추는 순간
  「계약이 정본」이 깨지고 두 번째 출처가 된다.
- 갱신을 **한 단계로** 만들 것(`build.py --save-fixtures` 같은 것). 번거로우면 아무도 안 한다.

**갈림은 감출 수 없다** — 모든 응답에 `generated`가 있으므로 픽스처가 낡으면 눈에 보인다.
그리고 **배포는 언제나 실물 API를 쓴다.** 픽스처가 낡아도 배포는 안 틀리고 로컬만 낡는다.
이게 「문서 복사」와 다른 점이다 — 문서는 사본이 곧 진실 행세를 하지만, 픽스처는 그럴 자리가 없다.

파일 머리에 한 줄 적어둘 것:
**「계약은 `CONTRACT.md` §v1이 정본. 이 파일은 사본이다. 손으로 고치지 말 것.」**

### M3 — 스위치 · 백엔드에서 HTML 삭제 · 백엔드+프론트

| 태스크 | 담당 | 내용 |
|---|---|---|
| **T0** | 백 | 🔴 **역의존 해소** — 지금 `publish_v1` → `build_site`라 **살아남을 쪽이 사라질 쪽에 얹혀 있다**(`BACKEND.md` §11.11). 통계를 별도 모듈로 빼면 저절로 풀린다. **T1보다 먼저** 한다 (백엔드 제안, 2026-09-09) |
| T1 | 백 | 크론이 `publish.py`(JSON) → `site/build.py`(HTML) 순서로 부르게. **DoD: 스위치 직전 「오늘 데이터」로 두 경로를 blob 기준 대조해 diff 0** — M2 증명은 기준선 `05d0de9` 것이고, 스위치하는 날의 데이터로 한 번 더 본다(대조 방법은 §M2 T5 「blob과 한다」) |
| T2 | — | **하루 돌려서 정상 확인** — ✅ **2026-09-15 통과.** 아래 참조 |
| T3 | 백 | `build_site.py`의 HTML 생성부 삭제 → `publish.py`로 개명. `docs/data/deals.json` 제거 — 🔴 **단 아래 「T3의 함정」을 먼저 푼 뒤에** |
| T4 | 백 | `theme.py` 삭제. `send_alerts`가 도메인을 **env `SITE_URL`**로 받게 (P2). `test_site_url.py`는 프론트로 가고, 백엔드엔 `SITE_URL` 형식 검사만 남긴다 |
| **T5** | 백 | 🔴 **`SUBSCRIBE_ADDR`을 `theme.py` → `subscriptions.py`로 옮긴다** (BB32). `theme.py`와 같이 프론트로 딸려가면 **출처가 갈린다** — 자세한 근거는 `CONTRACT.md` §`address`의 출처 |
| **T6** | 백 | 크론 「상태 점검」에 **`subscribe.address` == `MAIL_ADDRESS` 대조** 추가. 시크릿이 있는 유일한 자리다(`test.yml`엔 없다) → R1·R1b와 **한 점검이 셋을 막는다** |

| **T7** | **기획** | ✅ **2026-09-15 완료.** T3과 같은 창에서 옛 경로를 가리키는 문서·도구를 고쳤다 — `CLAUDE.md` 5곳(:37 픽스처 경로 · :85-86 충돌 해결 절차 · :95 건강 확인 명령 · :150 빌드 진입점 · :154 로컬 확인), `design/` 목업 빌더 20개(`docs/data/deals.json` → `docs/v1/deals.json`), `CONTRACT.md` 「분리 전 현행」 절 |

#### ✅ T1은 「덮어쓰기」가 아니라 「대조 후 교체」로 했다 (백엔드 설계, 2026-09-11)

옛 경로가 아직 HTML을 쓰는 동안 새 경로로 그냥 덮으면 **새 경로가 빠뜨린 파일을 옛 파일이 가려준다.**
그러면 T2(하루 운영)는 「안 죽었다」만 확인한다. 그래서 크론이 이렇게 한다:

```
새 경로를 임시 폴더에 굽는다 → 옛 경로 산출물과 blob 기준 대조
  같다   → 새 것으로 교체해 배포
  다르다 → 옛 것 배포(사이트는 신선하게 유지) + 상태 점검이 잡을 실패로 → 메일 + 다른 파일 목록
```

**T1 DoD(오늘 데이터 대조)가 T2 동안 매일 자동으로 반복된다.** 기획 계획(「스위치 직전 한 번 대조」)보다
강하다 — 한 번이 아니라 매일이고, 틀려도 사이트가 안 깨진다. 그리고 「이전 중에는 동작을 바꾸지 않는다」를
**크론이 매일 강제**한다 — `site/` 산출물이 한 바이트만 바뀌어도 다음 날 대조가 실패한다.
→ **T2~T3 동안 프론트는 `site/`를 건드리지 않는다**(백엔드가 프론트에 통보).

T0도 끝났다 — 통계를 `collector/route_stats.py`로 뺐고, `publish_v1`은 더 이상 `build_site`를
import하지 않는다. **sqlite 전용 SQL 두 줄이 `route_stats.py`에만 있다**(§5c 그대로).

> **관찰 — 재빌드는 시각에 따라 결과가 다르다.** 오후에 재빌드하니 딜이 138 → 129가 됐다.
> `build_deals_json`이 `now_kst()` 기준 신선도 컷을 쓰기 때문이다(T0과 무관, 딜 코드 무변경).
> → **T7에서 `CLAUDE.md` 충돌 해결 절차를 고칠 때 「재빌드」보다 「원격(크론) 산출물을 취한다」를
> 기본으로 둔다.** 낮에 재빌드하면 아침 크론과 다른 딜 목록이 커밋된다.

#### ✅ M3 완료 — 2026-09-16 (기획 독립 검증)

사용자가 이전 재개를 지시해 기획이 아침에 상태를 쟀고(T4·T5·T6 미착수), 같은 날 셋 다 끝났다.
아래는 **기획이 직접 돌려 확인한 것**이다 — 담당 세션의 보고를 그대로 받아쓰지 않는다.

| 확인 | 결과 |
|---|---|
| `collector/theme.py` | 없음 ✅ |
| 백엔드 코드에 도메인 | `collector/`·`tests/` 전수 — 남은 둘은 `test_site_url.py`의 **주석과 테스트 입력값**이다. 운영 코드에 도메인 0 ✅ |
| `send_alerts` 안전장치 | `require_site_url()`이 `main():92`, SMTP 연결이 `:116`. **발송 전에 멈춘다** ✅ |
| `test_site_url.py`가 잠그는 것 | 도메인 하드코딩 금지 · env 출처 · 끝 `/` 정규화 · 공백 제거 · **빈 값이면 발송 전 정지** ✅ |
| `subscriptions.py` | `SUBSCRIBE_ADDR` 보유, `publish.py`가 거기서 읽음 ✅ |
| 크론 상태 점검 | `$API_URL/v1/meta.json` 신선도(오늘/어제) + `subscribe.address == MAIL_ADDRESS` ✅ |
| 새 구조 실전 | 2026-09-16 크론 성공. 딜 127 · 제휴 링크 **127**(둘이 같다) · 지난 달 추천 0 ✅ |

**T4의 「오늘/어제」 완화에 기획 동의.** 점검이 push 직후에 도는데 Pages 배포가 ~25초 늦어
그 순간 라이브는 대개 어제 것이다. 「오늘」로 짜면 **매일 헛실패**하고, 매일 실패하는 점검은
사람이 무시한다(BB30 늑대 소년). 하루 늦게 잡아도 R1의 목적(「몇 주를 모르는 것」 방지)은 그대로다.
시간대도 확인했다 — `publish.py:68`이 `now_kst()`, 점검의 `today`도 `TZ=Asia/Seoul`. 양쪽 KST다.

| 태스크 | 상태 | 근거 (실측) |
|---|---|---|
| T3 | ✅ | `collector/build_site.py` 없음, `publish.py` 있음, `site/` 7개 파일 |
| T4 | ✅ | `theme.py` 367줄 삭제. `send_alerts.py:39` `SITE_URL = env`. `test_route_pages.py`는 프론트로 |
| T5 | ✅ | `SUBSCRIBE_ADDR`이 `subscriptions.py`로. `publish.py`·`test_publish.py`가 `theme` import 자체를 버렸다 |
| T6 | ✅ | API 신선도 + 구독 주소 대조. **M4에서 반쪽이 되므로 T6b를 신설했다**(아래 M4) |
| T7 | ⚠️ | 문서 정정을 했으나 **`PROJECT.md`를 빠뜨렸다** → 2026-09-16 보완 (`PLAN.md` 함정 10) |

**T4의 선행 조건은 해소됐다**: `theme.py`를 지우면 `collector/discover_home.py:10`이 깨지는데
그 파일은 프론트 구역이었다. 런타임 import 0개(죽은 파일)임을 실측해 프론트에 삭제를 요청했고,
프론트가 `3e6eaac`로 지웠다. **M4 T5를 기다렸으면 T4가 하루 더 막혔다.**

**M4 T5 확인**: `site/*.py` 전수 결과 `collector/` 모듈 import **0개**. 저장소가 갈라져도 안 깨진다.

**곁가지(안 고쳤다)**: `design/*.html` 20장이 아직 `docs/data/deals.json`을 가리킨다.
T7에서 빌더(`design/*.py`)만 고치고 **산출물을 다시 굽지 않았다.** 지금 재생성하면 딜 데이터가
2026-09-02 → 오늘로 바뀌어 목업 본문의 숫자 주석(「70건 중 23곳」 등)과 갈릴 수 있으므로
**별도 태스크로 미룬다.** 목업은 배포물이 아니라 참고 자산이다.

#### T7 충돌 해결 절차 초안 — 함정 둘을 피한 형태 (2026-09-11)

**함정 ① `--theirs`는 merge와 rebase에서 뜻이 반대다** (백엔드 지적).

```
git merge origin/main     --theirs = origin/main = 크론 산출물   ✅
git pull --rebase         --theirs = 내 커밋     = 내 재빌드     ❌ 정반대
```

`collect.yml:157-160`이 이미 이 반전을 겪고 주석으로 남겼다. 지금 저장소는 `pull.rebase = false`(merge)라
맞게 돌지만, 누가 rebase로 당기면 **절차대로 했는데 에러 없이 자기 낮 재빌드를 취한다.**
→ `--theirs`를 쓰지 않고 **방향을 안 타는 `git checkout origin/main -- <경로>`**로 쓴다.

**함정 ② `docs/` 전체를 원격 것으로 덮으면 프론트 소스가 날아간다** (기획 확인).
`docs/`에는 생성물만 있는 게 아니다 — 실측(`git ls-files docs/`):

```
사람이 쓴 것   docs/assets/discover.js · discover.css · d3-*.min.js · og.png
               docs/CNAME · docs/.nojekyll · docs/data/world.geojson
```

→ **생성물만 이름으로 집는다:**

```bash
# 충돌 난 생성물은 원격(크론) 것을 취한다 — merge/rebase 방향과 무관
git checkout origin/main -- docs/index.html docs/routes docs/v1 docs/sitemap.xml docs/robots.txt data/prices.db
# (T3 전까지는 docs/data/deals.json 도 목록에 넣는다)
```

사람이 쓴 파일에서 충돌이 나면 그건 **생성물 충돌이 아니라 진짜 충돌**이다 — 원격으로 덮지 말고 그 구역 담당이 푼다.

#### ✅ T2 통과 — 4일 연속, **경계 케이스 포함** (기획 확인, 2026-09-15)

크론 4회 전부 「화면 생성(프론트 경로 · 옛 경로와 대조)」 스텝이 성공했다:

```
2026-09-12T00:10 UTC (자정 후)   대조 ✅
2026-09-12T23:56 UTC (자정 전)   대조 ✅   ← dateModified 버그가 터졌어야 할 날
2026-09-14T00:05 UTC (자정 후)   대조 ✅
2026-09-15T00:30 UTC (자정 후)   대조 ✅
```

**두 번째 줄이 T2의 값이다.** 함정 8번(「조건은 맞는데 빈도를 안 쟀다」)에서 드러난 그 경계를
**실전에서 한 번 밟고 통과**했다. 하루만 봤으면 못 봤을 것이고, 백엔드가 T2를 「매일 대조」로
짜둔 덕에 **기다리는 것만으로 경계 검증이 됐다.**

건강(2026-09-15): 딜 141 · 제휴 링크 141(일치) · `docs/v1/` 39개 · 과거 출발월 버킷 0 · 노선 HTML 36장.

#### ✅ T3 완료 — 경계가 코드에서 완성됐다 (2026-09-15, `059fc4a` + T7 `42125f2`)

```
삭제  collector/build_site.py · docs/data/deals.json
개명  collector/publish_v1.py → collector/publish.py
크론  publish.py(JSON) → site/build.py --api docs/v1 --out docs
      둘 다 success 일 때만 docs/ 를 커밋한다(collect.yml:106-107)
```

**이제 백엔드는 데이터만, 프론트는 화면만 만든다** — §1의 설계 기준이 코드에서 성립했다.

**기획 독립 검증 (2026-09-15)**: 파일 유무 · 크론 두 스텝과 커밋 게이트 확인, 그리고
**T7로 `CLAUDE.md`에 적은 명령을 실제로 돌려봤다**(함정 6번 — 문서에 넣기 전에 돌린다):

| 확인 | 결과 |
|---|---|
| 건강 확인 명령(`docs/v1/deals.json`) | `141 141` ✅ |
| `site/build.py --api docs/v1 --out <임시>` | 정상 ✅ |
| `--api fixtures/v1` (네트워크·DB 없이) | 정상 ✅ |
| **방금 만든 39장 vs 커밋된 산출물(blob)** | **동일 39 · 다름 0** ✅ |

마지막 줄이 핵심이다 — **새 경로 하나만으로 만든 화면이 지금 배포된 것과 바이트 단위로 같다.**

> ⏳ **실전 검증은 오늘 밤 크론이다.** 바뀐 게 크론의 **구조**(스텝 둘 + 커밋 게이트)라
> 로컬로는 다 못 본다. 실패해도 게이트가 `docs/`를 안 건드리고 `data/`만 커밋하므로
> **사이트는 어제 것으로 멀쩡하다.** T4는 크론 결과를 보고 착수한다(T2에서 배운 순서).

#### 🔴 T3의 함정 둘 (기획 발견, 2026-09-11 재개 점검)

**① `docs/data/deals.json`은 옛 산출물이 아니라 v1의 중간 재료다.**
`publish_v1.deals_payload()`(`:94~`)가 이 파일을 읽어 v1 봉투에 다시 담는다.
그냥 지우면 **예외가 안 난다** — `if not src.exists(): return None, 0, 0`이라
**v1 딜이 조용히 발행을 멈춘다.** 게다가 BB1(하한선 미달이면 어제 딜 유지)의
의미가 **이 파일이 남아 있다는 전제**에 얹혀 있다.
→ 「어제 것 유지」를 `docs/v1/deals.json` 자체로 옮기고, `publish_v1`이 파일을 거치지 않게
바꾼 **다음에** 지운다. 방법은 백엔드 판단이다. **순서만 지킨다.**

**② T3 뒤엔 `CLAUDE.md`의 충돌 해결 절차가 틀린 말이 된다.**
「`python collector/build_site.py`로 재빌드」라고 적혀 있는데 T3 뒤엔 그게 HTML을 안 만든다.
절차대로 하면 **HTML이 갱신 안 된 커밋**이 나간다. 그래서 문서 수정을 M6까지 못 미룬다 —
**T7로 T3과 같은 창에** 넣었다. 공용 파일이라 사용자에게 미리 알렸다(2026-09-11).

**DoD**: 백엔드 코드의 문자열 리터럴에서 HTML 태그가 0건. 사이트 정상.
**T5 후 백엔드에 남는 `theme.py` 의존은 `send_alerts`의 `BASE_URL` 하나**이고,
그건 T4가 env로 뺀다 → `theme.py`가 깨끗이 사라진다.

**T0을 할 때 딸려오는 것**: 통계를 별도 모듈로 빼면서 **sqlite 전용 SQL 두 줄이
한 파일에 모이게** 둔다(§5c 이식 표면). 어댑터를 만들라는 게 아니라 **흩어지지 않게만** 두는 것이다.
**롤백**: T3 전이면 커밋 되돌리기로 끝. T3 후 문제가 나면 재빌드.

### M4 — 레포 분할 · 사용자 + 양 세션

#### M4 T0 — 새 저장소 기본 보안 설정 (2026-09-11 사용자 지시, M3와 병행)

사용자 지시: **「레포 설정부터 하라고 해. 보안 설정 같은거」.** 빈 저장소라 M3에 영향이 없어 병행한다.
**`galmal-backend`는 백엔드가, `galmal-frontend`는 프론트가** 한다. 기준은 아래 한 벌이다 — 두 저장소가 다른 정책을 가지면 안 된다.

**사전 점검 (기획, 2026-09-11)**

- **GitHub 기본값으로 이미 켜진 것**: 비밀 스캐닝 · **푸시 차단** · `GITHUB_TOKEN` 읽기 전용 · Actions의 PR 승인 불가
- **옮길 이력은 깨끗하다** — 예전 저장소 비밀 스캐닝 경보 0 · `.env` 커밋 0 · 키/토큰 모양 문자열 0 ·
  비밀 변수 8종에 값이 대입된 줄 0(1건 걸린 건 `affiliates.py` 문서 설명 줄 `TP_TRIP_CAMPAIGN=campaign_id 값`).
  → **푸시 차단이 켜져 있어도 M4의 이력 push가 막히지 않는다.** 막혔다면 그건 비밀이 샌 것이다

**기준 (두 저장소 공통)**

| # | 설정 | 값 | 왜 |
|---|---|---|---|
| 1 | 비밀 스캐닝 + 푸시 차단 | 켬 | **이미 켜짐** — 확인만 |
| 2 | `GITHUB_TOKEN` 기본 권한 | 읽기 전용 · PR 승인 불가 | **이미 그렇다** — 워크플로가 필요한 권한만 `permissions:`로 명시한다(`collect.yml`은 `contents: write` 명시) |
| 3 | Actions 허용 범위 | **GitHub이 만든 액션만** | 공급망 공격 면을 줄인다. 현재 쓰는 건 `actions/checkout`·`actions/setup-python`뿐(실측), M4의 Pages 배포 액션도 GitHub 것이다 |
| 4 | 외부 기여자 PR의 워크플로 | **모든 외부 기여자 승인 필요** | public 저장소다 |
| 5 | `main` 브랜치 규칙 | **삭제 금지 · 강제 push 금지 — 이 둘만** | 🔴 **PR 필수·상태 체크 필수는 걸지 않는다.** 크론이 매일 `main`에 **직접** push한다(`collect.yml:158`). 막히면 **그날 수집분이 버려진다**(BB20 유형 — 2026-09-01 실제로 났다). 크론의 rebase 후 push는 강제 push가 아니라 걸리지 않는다 |
| 6 | Dependabot 경보 | 켬 | 무료, 부작용 없음. **백엔드는 보안 업데이트 PR까지** 켠다(`requirements.txt`). 프론트는 패키지 관리자가 없어(벤더링) 경보만 |
| 7 | 비공개 취약점 신고 | 켬 | public이라, 누가 취약점을 찾으면 공개 이슈 대신 비공개로 온다 |
| 8 | 위키 · 프로젝트 | 끔 | 안 쓰는 기능은 없애도 되는 면이다. 이슈는 둔다 |
| — | Pages · 커스텀 도메인 · environment | **M4 본 작업에서** | 빈 저장소에선 할 게 없다 |

**완료 판정**: 두 저장소의 설정을 `gh api`로 읽어 **위 표와 한 줄씩 대조한 결과**를 보고한다(한 저장소만 보고 끝내지 않는다).

**구현 기준 둘 (프론트가 정함, 두 저장소 공통으로 확정)**

- **5번은 branch protection이 아니라 ruleset**(`main-protection`, `refs/heads/main`)으로 건다. 빈 저장소엔
  `main` 브랜치가 아직 없어 branch protection을 걸 수 없고, ruleset은 **이름으로 걸려 브랜치가 생기는 순간부터** 적용된다.
- **우회 권한자(`bypass_actors`) 0명.** 소유자도 `main`을 지우거나 강제 push할 수 없다.
  막힌 게 아니라 **한 단계 더 거치게 한 것**이다 — 정말 강제 push가 필요한 날(예: BB3 저장소 크기 때문에
  이력을 다시 쓰는 날)엔 관리자가 ruleset을 **잠깐 끄고 → 하고 → 다시 켠다.** 실수로는 못 하고 일부러는 할 수 있다.

#### 🔴 변수 값에 `
`이 들어가 있었다 (기획 확인, 2026-09-15)

사용자가 변수 둘을 등록한 뒤 **값의 길이**를 재보니:

```
API_URL   "https://galmal.kr
"   19자   🔴 복붙하다 줄바꿈이 딸려 들어감
SITE_URL  "https://galmal.kr"        17자   ✅
```

그대로 뒀으면 점검이 `https://galmal.kr⏎/v1/meta.json`을 불러 **M4부터 매일 실패**한다.
기획이 `gh variable set`으로 고쳤고 둘 다 17자임을 재확인했다.

> **설정값은 「이름이 맞나」가 아니라 「값이 정확히 그것인가」를 본다.** 화면으로는 똑같이 보인다 —
> `gh api …/actions/variables --jq '.value|length'` 처럼 **길이나 원본 표현**으로 재야 보인다.
> 이 저장소가 반복해 만난 유형(함정 6·8·9)의 설정값 판이다.

**진행**

| 저장소 | 담당 | 상태 |
|---|---|---|
| `galmal-frontend` | 프론트 | ✅ 2026-09-11 — 8개 항목 일치 (**기획이 `gh api`로 독립 재조회**) |
| `galmal-backend` | 백엔드 | ✅ 2026-09-11 — 8개 항목 일치 (**기획 독립 재조회**) · 보안 업데이트 PR 켜짐(설계대로 백엔드만) |

**두 저장소 사이에서 갈린 것 하나 — 기획이 정했다 (2026-09-11)**

| | backend | frontend | 결정 |
|---|---|---|---|
| ruleset 대상 | `~DEFAULT_BRANCH` | `refs/heads/main` | ✅ **`refs/heads/main`** |
| ruleset 이름 | `main 보호 — 삭제·강제 push 금지` | `main-protection` | ✅ **`main-protection`** |

> ✅ **통일 완료 (2026-09-11)** — 백엔드가 PUT으로 갱신(중복 생성 없음). 기획 재조회:
> 두 저장소 모두 `main-protection · active · refs/heads/main · deletion,non_fast_forward · 우회 0 · ruleset 1개`.
> 백엔드가 **바꾼 한 줄만이 아니라 전 항목을 나란히** 대조했다 — 다른 건 기준표대로 「보안 업데이트 PR(백엔드만)」 하나뿐.
> **M4 T0 완료.**

**이유**: 지금은 둘 다 기본 브랜치가 `main`이라 효과가 같지만, 기본 브랜치가 바뀌는 날 갈린다.
우리가 실제로 의존하는 건 **이름 `main`**이다 — 크론(`collect.yml`의 `git fetch origin main`·`git rebase origin/main`)과
`CLAUDE.md`의 모든 절차가 `main`을 박아 쓴다. 누가 실수로 기본 브랜치를 바꾸면 `~DEFAULT_BRANCH`는 **보호를 다른 브랜치로
옮기는데** 크론은 계속 `main`에 push한다 — 보호가 사라진 자리에 쓰는 셈이다. **보호할 것은 이름 `main`이다.**
이름은 ASCII·짧은 쪽으로 — 로그·API 응답에서 읽기 쉽다.

크론이 이 규칙 아래서 사는지는 백엔드가 **재현으로** 확인했다 — 로컬 bare 저장소에 같은 규칙(`denyNonFastForwards`·
`denyDeletes`)을 걸고 `collect.yml:158-168`을 그대로 돌려, **거부 → fetch → rebase → push 성공**(수집분 생존) ·
대조군 강제 push·삭제는 거부됨. rebase 후 push는 fast-forward다.

**이 설정 때문에 M4에 딸려오는 것 둘 (백엔드 지적)**

- 🔴 **재업로드 예외 절차.** M4 첫 이력 push는 빈 저장소에 `main`을 **새로 만드는** 것이라 안 걸린다. 그런데
  **잘못 올려 다시 올려야 하면** 강제 push·삭제가 막혀 있다 → **사용자가 ruleset을 잠시 끄고(`enforcement: disabled`)
  → 다시 올리고 → 켠다.** 우회 주체 0은 의도다(세 세션이 `main`에 push하니 사람의 실수를 막는 게 목적).
  §6 사용자 할 일에 예외 절차로 적는다.
- **M4 T6 `repository_dispatch`는 `run:` 스텝의 `gh api`로 부른다.** `peter-evans/repository-dispatch` 같은
  마켓플레이스 액션은 3번 설정(GitHub 제작만 허용)에 막힌다.


| 태스크 | 담당 | 내용 |
|---|---|---|
| T1 | ~~사용자~~ 기획 | `galmal-backend`·`galmal-frontend` 생성 (**둘 다 public**) — ✅ **2026-09-11 완료** (기획이 `gh`로 생성, 둘 다 빈 저장소). **이름은 사용자 결정으로 `galmal-frontend`·`galmal-backend`** — 처음 `galmal-web`·`galmal-api`로 만들었다가 같은 날 바꿨다(비어 있어 비용 0, 옛 이름은 GitHub이 새 이름으로 넘겨준다). 세션 이름과 똑같이 읽히고, M6의 `galmal-plan`과 짝이 맞는다. 도메인 `api.galmal.kr`은 저장소 이름과 무관하므로 그대로다. `galmal-plan`은 **만들지 않았다** — M6에서 `promo-ticket-site`를 그 이름으로 rename 하므로 지금 만들면 이름을 막는다 |
| T2 | **사용자** | **secrets 5종**을 `galmal-backend`의 **`production` 환경**에 등록 — ✅ **2026-09-11 완료** |
| T3 | **사용자** | ✅ **2026-09-16 완료.** `DISPATCH_TOKEN`이 `galmal-backend`의 `production` 환경 시크릿으로 등록됨(기획 확인). **사용자가 만료 없음(No expiration)으로 발급했다** — 아래 박스 참조 |

#### M4 T3 — PAT 발급 절차 (사용자 몫, 2026-09-16 확인)

`github.com/settings/personal-access-tokens/new` (**fine-grained**, classic 아님)

| 칸 | 값 |
|---|---|
| Token name | `galmal-backend → frontend dispatch` |
| Resource owner | `RYU-TOMI` |
| Expiration | **No expiration** — 사용자가 실제로 이걸 골랐다(2026-09-16). 「보통 1년이 상한」이라고 적어뒀던 건 **내가 틀린 것**이다 |
| Repository access | **Only select repositories** → **`galmal-frontend` 하나만** |
| Repository permissions | **Contents: Read and write** — 이것 하나. 나머지는 전부 No access |

**왜 Contents 인가**: `POST /repos/{owner}/{repo}/dispatches`가 fine-grained 토큰에 요구하는 권한이
`contents: write`다([GitHub Docs](https://docs.github.com/en/rest/authentication/permissions-required-for-fine-grained-personal-access-tokens)).
직관과 다르다 — 「Actions」가 아니다. Actions 권한만 주면 `403 Resource not accessible`이 난다.

→ 발급된 값을 `galmal-backend` → Settings → Secrets and variables → Actions →
**`production` 환경**의 secret으로 `DISPATCH_TOKEN` 이름으로 등록.
**변수(vars)가 아니라 secret이다** — 토큰은 로그에서 가려져야 한다(`API_URL`과 반대 이유).

##### 만료 없음으로 발급됐다 — 무엇이 바뀌고 무엇이 안 바뀌나 (2026-09-16)

**바뀐 것**: R1의 원래 얼굴(**조용한 만료**)은 사실상 사라졌다. 2027-09에 죽을 일이 없다.

**안 바뀐 것**: **T6b는 그대로 필요하다.** 같은 증상(「수집은 초록불인데 프론트만 안 구워진다」)이
만료 말고도 온다 — 권한 오설정(`Actions`를 줬을 때의 403), 토큰 폐기, 저장소 rename,
Pages 배포 실패, 프론트 워크플로 자체의 실패. **T6b는 원인이 아니라 증상을 본다** —
「사이트가 API보다 뒤처졌나」. 그래서 원인 목록이 바뀌어도 계속 유효하다.

**대신 생긴 것**: 만료 없는 자격증명이 하나 상시 존재한다. 범위는 **`galmal-frontend`의
`Contents: Read and write` 하나**이므로, 새면 그 저장소에 push할 수 있고 → **galmal.kr에
임의 내용이 배포될 수 있다.** 작지 않지만 한정적이다. 지금 걸려 있는 완화:
환경 시크릿이라 로그에서 가려지고, 저장소에 ruleset이 걸려 있고, 저장소가 둘뿐이다.

→ **교환은 합리적이다.** 「조용히 죽는 것」이 「상시 존재하는 것」보다 이 프로젝트에서 더 비쌌다
(BB18이 그 증거다). 다만 **M6 이후 언젠가 재발급 주기를 정하는 것**은 남겨둔다 — 지금 할 일은 아니다.
| T3b | **사용자** | ✅ **2026-09-15 완료.** `galmal-backend`에 **변수(vars) 둘** — 🔴 **secrets가 아니다**(secrets에 넣으면 로그에서 `***`로 가려져 점검 실패 메시지를 못 읽는다). `SITE_URL` = `https://galmal.kr` · `API_URL` = `https://galmal.kr` (**M5에서 `https://api.galmal.kr`로 이것 하나만 바꾼다**). 워크플로는 변수가 없으면 기본값을 쓰므로 M3 동안은 지금 저장소에 만들 필요 없다(백엔드 확정) |
#### 결정 — **이력을 가져간다** (`git filter-repo`). 2026-09-16, 프론트 질의에 대한 답

`SPLIT.md`가 스스로 엇갈리고 있었다(프론트 지적): M4 T0은 「이력 push」를 전제하고
T4a 근거는 「첫 커밋 = 새로 시작」을 전제했다. **T4a의 근거가 오측 위에 서 있었기 때문**이다 —
blob이 이미 LF라 `.gitattributes`는 **어느 커밋에 넣어도 값이 0**이고, 따라서
「첫 커밋이어야 한다」는 제약 자체가 없다. 제약이 사라지니 선택이 자유로워졌다.

**실측 — 무엇을 버리게 되나** (`origin/main`, 총 663커밋 중 크론 76):

| 경로 | 사람 커밋 | 시작 |
|---|---|---|
| `docs/assets/` (discover.js·css) | **59** | 2026-08-01 |
| `site/` | 8 | 2026-09-08 |
| `fixtures/` | 1 | — |
| `collector/` | 81 | — |
| `tests/` | 41 | — |

**`site/`의 이력이 얕은 건 맞다**(8커밋) — 그 코드의 진짜 계보는 `collector/build_site.py`에 있고
그건 백엔드 경로다. 하지만 **`docs/assets/` 59커밋은 얕지 않다.** 지도·인터랙션 두 달치이고,
그게 프론트 저장소의 주된 산출물이다. **「대부분이 백엔드·문서 이력」은 81 대 59로 절반쯤만 맞다.**

> 🔴 **2026-09-16 저녁 — 이 결정은 뒤집혔다. 사용자가 「전체 이력 + 정리 커밋」을 골랐다.**
> 백엔드가 착수 전에 더 나은 숫자를 들고 사용자에게 올렸고, **내가 계산에 안 넣은 것이 있었다**:
> **`filter-repo`는 커밋 해시를 전부 바꾼다.** 문서가 해시를 **81곳** 참조한다
> (`FRONTEND.md` 26 · `BACKLOG.md` 26 · `BACKEND.md` 14 · `SPLIT.md` 10 · `SPEC.md` 3 · 그 외 2).
> 「이력을 살린다」면서 **이력을 가리키는 말들을 죽이는** 셈이라 목적이 절반 사라진다.
> 크기 이득도 작았다 — 팩 46MB 중 40.8MB가 `data/`고 백엔드는 어차피 가져가므로 약 4MB.
> **양쪽이 같은 해시를 공유한다는 것도 이득이다** — 문서의 해시 하나가 두 저장소 어디서나 풀린다.
> 아래 표와 기각 사유는 **그때의 판단 기록으로 남긴다**(어느 쪽도 틀린 계산은 아니었다).

~~**채택**~~(뒤집힘): `pip install git-filter-repo`(PyPI 2.47.0 확인) 후 경로 지정 복제.
```
galmal-frontend :  --path docs/assets --path site --path fixtures
galmal-backend  :  --path collector --path tests --path data --path .github
```
경로가 그대로 유지되므로 재배치가 없다. `.gitattributes`는 그 위에 **새 커밋**으로 얹는다.

**기각한 대안**
- **(가) 새로 시작** — 프론트 제안. 도구가 필요 없고 깨끗하다. **기각 이유**: `docs/assets/`
  59커밋이 공개 기록에서 사라진다. 옛 저장소가 남지만 **M6에서 private이 된다** — 보존은 되되
  **보이지는 않는다.** 그리고 「도구가 없다」는 `pip install` 하나로 사라지는 장벽이었다.
- **(나) 통째로 복제한 뒤 남의 파일을 지운다** — 도구가 필요 없고 이력이 완전하다.
  **기각 이유**: 663커밋이 양쪽에 다 들어간다. 크론 76커밋과 남의 구역 이력까지 따라와서
  **분리의 목적 자체와 반대로 간다.** 「누가 이 파일을 왜 고쳤나」를 찾기 더 어려워진다.

> **첫 커밋 메시지에 옛 저장소를 적는다** — 프론트 제안 그대로 채택.
> `site/`의 계보처럼 **경계를 넘어간 이력**은 filter-repo도 못 가져오므로, 포인터가 필요하다.

| **T4a** | 양쪽 | **새 저장소에 `.gitattributes`** — `* text=auto eol=lf` (+`*.png *.db binary`). `filter-repo` 복제 위에 새 커밋으로 얹는다(blob이 이미 LF라 어느 커밋이든 값이 0). **지금 이 저장소에는 넣지 않는다** — 워크트리 셋이 도는 중이다 |
| **T4c** | 양쪽 | **전체 이력을 그대로 push → 정리 커밋으로 남의 구역 삭제** (사용자 결정 2026-09-16). T4·T5보다 **먼저**. `filter-repo`는 **쓰지 않는다** — 해시 81곳이 죽는다 |
| T4 | 백 | `collector/` `data/` `collect.yml` 이동 + `tests/`(**`test_charts.py` 제외** — 프론트 코드를 테스트한다). Pages 켜고 `docs/v1/` 발행 |
| **T4b** | 백 | 🔴 **새 저장소의 `collect.yml`에서 `schedule:` 트리거를 뺀다** — M4 동안은 `workflow_dispatch`(손으로 돌리기)만. **구형 크론이 아직 돌고 있다.** 아래 이유. M5에서 맞바꾼다 |
| T5 | 프 | `site/` `assets/` `fixtures/` `deploy.yml` 이동 |
| **T5b** | 프 | 🔴 **`build.json` 발행** — `site/build.py`가 `{"api_generated": <meta.generated>, "built": <빌드 시각>}`을 `docs/build.json`에 쓴다. **R1c 점검의 사이트 쪽 절반이다** — 이게 없으면 M3 T6은 백엔드 자기 배포만 보게 되고 **R1(PAT 만료)을 아무도 안 본다** |

#### 🔴 크론이 두 군데서 돌면 안 된다 — 계획에 빠져 있었다 (기획 발견, 2026-09-16)

**구형 크론을 언제 끄는지가 M0~M6 어디에도 없었다.** T4가 `collect.yml`을 새 저장소로
**복사**하는 순간, 구형과 신형이 **둘 다 매일 돈다.** 구형은 M5까지 galmal.kr을 서빙해야 하므로
그냥 끌 수도 없다. 실측으로 결과를 쟀다:

| 무엇이 | 왜 두 번 되나 | 결과 |
|---|---|---|
| 🔴 **구독 알림 메일** | 중복 방지가 `alert_log` 테이블인데 그게 **`data/prices.db` 안**에 있다. DB가 저장소마다 따로다 | **구독자가 같은 특가를 두 번 받는다.** 우리는 각자 「안 보냈던 것만 보냈다」고 믿는다 |
| **Claude API(Haiku) 파싱** | `emails` 테이블도 같은 DB다 | 같은 메일을 두 번 파싱한다. **이중 과금** |
| **가격 API 호출** | — | `TP_TOKEN`으로 두 배 호출. 레이트 리밋 위험 |

**중복 방지가 저장소 로컬 상태라는 게 핵심이다.** 두 저장소는 서로의 `alert_log`를 못 본다 —
그래서 「이미 보냈나」를 각자 **자기 기준으로 맞게** 답하고, 합치면 두 번 보낸다.
**어느 쪽도 예외를 내지 않는다.** 또 그 얼굴이다.

**채택: 신형은 M4 동안 손으로만 돈다** (T4b)

```
M4   구형 schedule ✅ 돈다        신형 schedule ❌ (workflow_dispatch 만)
     → 부작용 없는 채로 신형을 실제로 돌려볼 수 있다
M5   구형 schedule ❌ 끈다  ←→  신형 schedule ✅ 켠다   (같은 작업으로)
```

**기각한 대안**
- **(가) T4에서 구형 크론을 끈다** — galmal.kr이 M5까지 딜을 갱신 못 한다. 가용성이 아니라
  **신선도**의 다운타임이지만, M5가 얼마나 걸릴지 모르는 상태에서 사이트를 먼저 멈출 이유가 없다.
- **(나) 신형에서 부작용 스텝(알림·파싱)만 끈다** — 조건문이 늘고, **M5에서 되살리는 걸 잊으면
  알림이 조용히 영영 안 나간다.** 트리거 한 줄을 빼는 쪽이 되돌리기도 눈에 보인다.

> ⚠️ **M6에서 구형을 private으로 돌려도 Actions는 계속 돈다.** 비공개 저장소도 스케줄이 실행된다.
> **끄는 건 M5에서 명시적으로** 해야 한다 — 「private으로 만들면 알아서 멈추겠지」가 아니다.

#### 왜 `.gitattributes`를 **지금** 넣지 않고 새 저장소에 넣나 (2026-09-16)

CRLF 함정이 이 이전에서 **세 번** 나왔다 — 기획의 M2 동등성 오진(작업 트리와 비교), 백엔드의
`BACKEND.md` 1418줄 헛diff, 그리고 고쳤다고 한 **뒤에 한 번 더**(bash heredoc으로 append 하면
LF가 들어가 파일이 섞인다).

> 🔴 **2026-09-16 정정 — 이 절이 한때 「문서 7개가 전부 CRLF로 커밋돼 있다」고 적고 있었다.
> 틀렸다.** 프론트가 반례를 냈고, 바이트로 다시 재니 **커밋된 blob은 전부 LF**다:
> `CLAUDE.md` 0/177 · `FRONTEND.md` 0/1015 · `BACKEND.md` 0/1563 · `SPLIT.md` 0/1149 ·
> `PLAN.md` 0/399 · `PROJECT.md` 0/190 · `CONTRACT.md` 0/611 · `site/route.py` 0/290.
>
> **오측 원인**: `git cat-file -p` 출력을 **bash 파이프로 받아** `grep -c $'$'`로 셌다.
> Git Bash(MSYS)가 그 경로에서 LF를 CRLF로 바꿔 보여준다 — **blob을 본 게 아니라
> 파이프가 만든 것을 봤다.** 함정 7번(작업 트리와 blob을 헷갈렸다)과 **같은 병**이다.
> 그때는 파일을, 이번엔 파이프를 blob으로 착각했다.
>
> → **줄바꿈은 바이트로 잰다**: `subprocess.run(["git","cat-file","-p",ref], capture_output=True).stdout`
> 를 파이썬에서 `b.count(b"
")`으로. 셸을 거치면 셸의 답을 듣는다.

**그래서 T4a의 근거가 바뀐다.** blob이 이미 LF이므로 `.gitattributes`를 지금 넣어도
**재정규화가 일어나지 않는다** — 「4,600줄이 한 번에 뒤집힌다」는 걱정은 없던 문제였다.

**그래도 T4a는 유효하다. 이유가 다를 뿐이다:**

- 지금 LF인 건 **우연에 가깝다.** `core.autocrlf=true`만 걸려 있고 규칙 파일이 없어서,
  도구가 하나 어긋나면(백엔드의 heredoc) 그날 blob이 뒤집힌다. 실제로 `94b1924`가 그랬다.
  **좋은 상태를 잠가두는 것**이 목적이지 고치는 게 아니다.
- **지금 이 저장소에 넣지 않는 이유**도 남는다 — 워크트리 셋이 동시에 돌고 있고,
  `eol=lf`는 blob은 안 건드려도 **세 작업 트리의 파일을 전부 바꾼다.**
  이전 한복판에 그럴 이유가 없다. 지금 상태는 이미 옳다.
- **새 저장소는 첫 커밋부터 규칙이 있으면 우연이 아니게 된다.** → **T4a**

`galmal-plan`(M6에서 rename)은 문서만 남으므로 그때 별도 커밋 하나로 정규화한다 — 섞일 이동이 없다.

> **그때까지의 규칙**: 이 저장소의 `.md`를 고칠 때 **bash `cat >>`·`>`·`sed -i`를 쓰지 않는다.**
> 파이썬 `io.open(..., encoding="utf-8", newline="")`로 읽고 같은 방식으로 쓴다 —
> 파일의 현재 줄바꿈이 그대로 보존돼 diff가 바뀐 줄만 나온다.
> 「도구를 고쳤다」가 「모든 경로를 고쳤다」는 아니다 — 파이썬만 고치고 bash를 안 봐서 재발했다.

> 🔴 **M4에서 조심할 것 둘** (프론트가 M2 중 발견):
> - **`site/build.py`는 `assets/`를 내보내지 않는다.** 정적 자산이라 빌드 산출물이 아니다 —
>   레포 분할 때 **따로 옮겨야** 하고, 안 옮기면 HTML은 멀쩡한데 **화면만 백지**가 된다.
> - **`docs/`를 비우고 다시 만들지 말 것.** `assets/`·`data/world.geojson`·`CNAME`이 같이 날아간다.
>   `CNAME`이 날아가면 **커스텀 도메인이 풀린다**(`test_site_url.py`가 잠그는 그 파일).
| T6 | 양쪽 | 배선: 백엔드 크론 끝 → `repository_dispatch` → 프론트 빌드·배포. 🔴 **dispatch 실패를 잡을 실패로 만든다** — 아래 |
> 🔴 **R1은 만료 말고도 온다 — 권한 오설정** (백엔드 지적, 2026-09-16)
>
> PAT에 `Contents` 대신 `Actions`를 주면 dispatch가 **403**이다. 그게 `continue-on-error`
> 아래 있으면 **수집도 발행도 초록불인데 프론트만 안 구워진다** — R1 시나리오가 *만료*가 아니라
> *첫날 오설정*으로 재현된다. 그리고 M4는 배선을 처음 켜는 날이라 **가장 그럴듯한 시점**이다.
>
> → dispatch 스텝의 실패를 **상태 점검이 잡는다.** T6b가 다음 날 잡아주긴 하지만,
> 배선을 짜는 중에 하루를 기다릴 이유가 없다. **즉시 + 다음 날**, 둘 다 둔다.
>
> 🔴 **그리고 T6b의 「못 받음」은 통과가 아니라 실패다.** `$SITE_URL/build.json`이 404여도
> 「점검을 못 했다」로 넘어가면 **아무것도 안 막는다.** M3 T6의 API 절반이 이미 그렇게 돼 있다
> (`if [ -z "$meta" ]; then fail=1`) — 사이트 절반도 같은 형태로 짠다.
> `curl ... || true` 류를 쓰지 않는다.

| **T6c** | 백 | ✅ **2026-09-16 완료.** 조건 둘 반영(구독자 **0명** 실측 · `skip_side_effects` 입력 신설) 후 수동 1회. **전 구간이 이어졌다** — 시크릿 주입 ✅ · `DISPATCH_TOKEN` dispatch ✅(403 아님) · 프론트가 깨어나 빌드 ✅ · 부작용 3스텝 skipped. **신형 발행 `15:49:59`(딜 129) == 프론트 `build.json.api_generated` `15:49:59`** · 구형 라이브는 `09:11:55`(딜 127) 그대로 → **사용자 영향 0** |
| **T6b** | 백 | 🔴 **상태 점검에 사이트 절반 추가** — `$SITE_URL/build.json`의 `api_generated` **==** `$API_URL/v1/meta.json`의 `generated`. **절대값이 아니라 두 값의 일치다.** PAT가 죽으면 사이트가 뒤처져 불일치, `SITE_URL`이 틀리면 404. `SITE_URL` 변수는 T3b에서 이미 등록돼 있다 |

> 🔴 **T5b·T6b를 M4에서 빼먹으면 R1이 조용히 무방비가 된다.**
> M3 T6(2026-09-16 완료)은 `$API_URL/v1/meta.json`만 본다. **지금은 그걸로 충분하다** —
> 한 저장소가 v1과 화면을 같은 커밋으로 배포하니 API가 신선하면 화면도 신선하다.
> **M4에서 그 등식이 깨진다.** 백엔드 Pages와 프론트 Pages가 따로 배포되므로,
> PAT가 죽어 프론트가 몇 주째 안 구워져도 **API 점검은 매일 초록불**이다. BB18 그대로다.
> R1c 대책은 §5 위험표에 적혀 있었지만 **태스크 행에는 없었다** — 위험표에만 있는 대책은
> 실행되지 않는다(`PLAN.md` 함정 10과 같은 뿌리). 2026-09-16에 행으로 못 박았다.


**DoD**: `ryu-tomi.github.io/galmal-frontend`이 현 사이트와 동일하게 뜬다.
**도메인은 아직 안 건드렸다** — 이 시점에 galmal.kr은 구형이 계속 서빙한다.

### T6c — 수동 실행 시점을 앞당긴다. 단 `mail_ingest`를 막고서 (2026-09-16)

원래 「수동 실행은 M5 ③ 직후 한 번」이었다. **백엔드가 자기 제안을 재고했고, 맞다.**
그때의 근거는 「돌리면 신형 DB만 앞서가 스왑이 복잡해진다」였는데, **M5 2b가 구형 DB로
덮어쓰므로 그 차이는 어차피 버려진다.** 근거가 사라졌다.

반면 지금 돌려야 잡히는 것 둘은 **전부 조용한 실패**다:

| 확인 | 안 하면 |
|---|---|
| `DISPATCH_TOKEN` 권한 | `403` — `continue-on-error` 아래서 조용하다. **권한 오설정은 켜는 그날** 난다 |
| `environment: production` 주입 | 시크릿이 빈 값이면 에러가 아니라 **빈손으로 초록불** |

**M5 당일에 처음 드러나면 다운타임 한복판에서 디버깅**하게 된다. 지금은 압박이 없다.

#### 🔴 다만 백엔드의 「비용 0」 근거 둘에 구멍이 있었다 (기획 실측)

**① `mail_ingest`는 메일을 소비한다. 「거의 없음」이 아니라 구조적으로 뺏는다.**

```
collector/mail_ingest.py:67   imap.select("INBOX")          ← readonly 아님
                        :73   imap.fetch(mid, "(RFC822)")   ← PEEK 아님 → \Seen 이 붙는다
                        :68   search(..., "UNSEEN")         ← 다음 사람은 그걸 못 본다
```

신형이 오늘 밤 도착한 프로모션 메일을 읽으면 **내일 구형 크론이 빈손이 된다.**
그리고 **구형이 galmal.kr을 서빙 중**이므로 **라이브 사이트가 그날 딜을 잃는다.**
신형 DB에는 들어가지만 그 DB는 M5 2b에서 **버려진다** — 양쪽 다 잃는 형태다.

(대조: `subscriptions.py:56`은 `readonly=True` + `BODY.PEEK`다. **같은 메일함을 읽는 두 코드가
서로 다르게 짜여 있다** — 그쪽이 옳다.)

**② 「`alert_log` 0행이라 구독 알림 중복 없음」은 프록시로 잰 것이다.**
구독자 목록은 `alert_log`가 아니라 **메일함**에서 온다(`subscriptions.py:50` `load_subscribers()`).
`alert_log`가 0행인 건 「아직 아무것도 안 보냈다」일 뿐, **「구독자가 없다」가 아니다.**
구독자가 있는데 양쪽 `alert_log`가 둘 다 0이면, 수동 실행과 내일 구형 크론이 **각자 처음인 줄 알고
같은 딜을 두 번 보낸다.** 재려면 `load_subscribers()`를 직접 부른다 — 그건 readonly라 안전하다.

#### 채택 — 수동 실행을 승인한다. 조건 둘

```
① load_subscribers() 를 직접 불러 구독자 수를 잰다 (readonly, 부작용 없음)
② workflow_dispatch 입력 skip_side_effects(기본 false)를 넣고,
   mail_ingest·send_alerts 스텝에 if: 를 건다. 이번 실행은 true 로 돈다
```

②는 이번만의 조치가 아니다 — **수동 실행은 언제든 부작용 없이 돌 수 있어야 한다.**
M5에서도, 나중에 배선을 고칠 때도 같은 버튼이 필요하다. 그리고 이 입력이 있으면
①의 결과와 무관하게 안전해진다.

> **일반 규칙**: 「비용 0」이라고 말할 때는 **무엇을 세어서 0인지**를 같이 적는다.
> 이번엔 「거의 없음」(양의 추측)과 「`alert_log` 0행」(프록시) 둘 다 실제 대상이 아니었다.
> `PLAN.md` 함정 9의 형태다 — 세고 말하거나 모른다고 한다.

### 🔴 R9 — M4~M5 동안 **백엔드 저장소가 둘이고 둘 다 산다** (백엔드 발견, 2026-09-16)

T4가 끝나면 `collector/`·`tests/`·`BACKEND.md`가 **양쪽에 다 있다.** 계획에
「그동안 코드를 어디에 고치나」가 **없었다.** 두 벌이 갈리면 M5 스왑에서 합쳐야 하는데,
**코드는 DB와 달리 바이트 비교도 안 된다** — 더 조용히 틀린다.

| | 구형 `promo-ticket-site` | 신형 `galmal-backend` |
|---|---|---|
| 크론 | ✅ 매일 돈다 | ❌ `schedule:` 없음 (T4b) |
| 서빙 | **galmal.kr (화면)** | v1 API |
| 코드 | 있다 | 있다 |

#### 채택 — 기본은 신형, 라이브 결함만 예외 (백엔드 제안 그대로)

```
기본   T4 완료 시점부터 백엔드 코드의 정본은 galmal-backend 다.
       구형은 **동결** — 크론 자신의 커밋(data/·docs/)만 들어간다.
예외   라이브 결함은 구형이 화면을 서빙 중이므로 **구형 먼저** 고치고,
       **곧바로** 신형에 같은 수정을 넣는다. 하나만 고치면 스왑에서 되돌아온다.
```

구형이 M5까지 할 일은 **「지금 코드로 계속 서빙」 하나**다. 새 기능이 거기 들어갈 이유가 없다.
예외가 필요한 이유는 반대다 — 라이브 결함을 신형에만 고치면 **M5까지 사용자에게는
안 고쳐진 채로 나간다**(BB29 때 「고치고 크론 한 번」으로 기준선을 만든 것과 같은 이유).

#### 🔴 M5 2a — 스왑 **전에** 두 `collector/`를 대조한다 (기획 추가)

위 예외는 **사람이 두 곳에 같은 수정을 넣는 것**에 의존한다. 이 프로젝트가 이미 한 번 실패한
형태다(`DECISIONS.md` 2026-09-08 (1) 기각안 (라): *"사람이 동기화를 책임지는 구조는
이 프로젝트에서 이미 한 번 실패했다"*). 그래서 **의존하되 검사한다**:

```bash
# 신형 저장소에서 — 전체 이력을 공유하므로 그냥 붙여서 비교된다
git remote add old https://github.com/RYU-TOMI/promo-ticket-site.git
git fetch old main
git diff old/main main -- collector/ tests/
```

**기대는 「0」이 아니다** — T6 배선이 신형에만 들어가므로 차이가 난다.
기대는 **「나오는 차이를 전부 설명할 수 있다」**이다. 설명 못 하는 줄이 하나라도 있으면
**그게 한쪽에만 들어간 수정**이다. 스왑 전에 푼다.

> 이 구간은 **짧을수록 좋다.** 갈릴 기회가 날짜 수에 비례한다 — 코드(R9)도, DB(M5 2b)도.
> **M5는 대부분 사용자 몫**(DNS)이라 계획으로 줄일 수 없다. **그래서 빨리 하는 게 대책이다.**

### 🔴 R8 — 「문서는 `galmal-plan`에 한 벌」이 **CI에서 성립하지 않는다** (백엔드 발견, 2026-09-16)

M0의 전제였다 — 기획 문서는 원본 한 벌, 양쪽이 `../galmal-plan/`을 **참조**한다.
`DECISIONS.md` 2026-09-08 (1) 기각안 (라)가 「양쪽에 복사」를 기각한 근거이기도 하다.

**로컬에선 맞고 CI에선 틀리다. 러너는 한 저장소만 체크아웃한다.**
백엔드가 남의 구역을 지우자 **테스트 11건이 깨져서** 드러났다:

| 문서 | 읽는 곳 | 무엇을 |
|---|---|---|
| `CONTRACT.md` | `tests/test_contract.py:32` | deal 객체 **필드 목록과 타입** |
| `TAGS.md` | `tests/test_dests.py:26` | IATA별 **태그 배정표** |
| `COPY.md` | `tests/test_labels.py:72` | §2b **지역 표시명** |

전수 확인: **이 셋뿐이다.** 프론트로 가는 `test_charts.py`는 문서를 읽지 않는다 →
**백엔드 저장소만의 문제**다.

#### 이 설계는 사고가 아니라 의도다 — 그래서 함부로 풀면 안 된다

두 테스트가 **왜 그렇게 짰는지를 주석에 적어뒀다**:

> `test_contract.py` — *"필드 목록을 여기 하드코딩하지 않는다(BB19). 하드코딩하면 기획이 계약에
> 필드를 추가해도 검증기가 모르고 **CI가 조용히 초록불**이 된다. 실제로 `low`·`obs_days`가
> 그렇게 통과했다."*
>
> `test_labels.py` — *"근본 원인은 **표시명 표가 어느 문서에도 없었던 것**이다. 문서에 없으니
> 코드 두 곳이 각자 이름을 지어냈다."*

**문서가 정본이고 테스트가 문서와 코드를 대조한다.** 기대값을 테스트에 적었다가
테스트가 눈멀었던 경험에서 나온 형태다 — 또 그 얼굴(「조용히 통과」)을 막는 장치다.

#### 백엔드의 후보 셋에 대한 기획 판단

- **③ 기계가 읽는 부분만 백엔드 파일로 빼고 문서가 인용** → **기각.** 이게 정확히
  BB19 이전 상태다. 값이 문서 밖으로 나가는 순간 **문서와 코드가 각자 놀 수 있게** 된다.
- **① 세 문서를 백엔드로 소유 이전** → **기각(최소 둘은).** `COPY.md`는 화면 문자열이라
  P1 기준 백엔드 것이 될 수 없다. `CONTRACT.md`는 **양쪽의 계약**이라 한쪽이 소유하면
  기획의 중재가 깨진다. `TAGS.md`만 데이터로 볼 여지가 있으나, 셋 중 하나만 옮기면 규칙이 갈린다.
- **② CI가 `galmal-plan`에서 받아온다** → **유력.** 단 M6에서 private이면 토큰이 필요하다.

#### 기획 제안 — 둘 중 하나를 사용자가 고른다 (M6 전까지)

**(가) `galmal-plan`을 public으로 둔다.** CI가 `raw.githubusercontent.com`에서 **토큰 없이** 받는다.
> 사용자가 private을 원한 이유는 **「구형이니까」**였다. 그런데 `galmal-plan`은 구형이 아니다 —
> **세 번째 현역 저장소**이고, 두 저장소의 CI가 매일 읽는 곳이 된다. **전제가 바뀌었다.**
> 덤: 이 문서들이 공개 포트폴리오가 된다.

**(나) 복사본을 두되 검사 방향을 뒤집는다.** 두 저장소에 복사본을 두고,
**`galmal-plan`의 CI가** 두 **공개** 저장소의 복사본을 받아 자기 원본과 대조해 다르면 실패.
> private이어도 된다 — 자기 CI가 공개 저장소를 읽는 건 토큰이 필요 없다.
> **원본을 가진 쪽이 검사한다**는 점에서 방향도 더 옳다. 값: 워크플로 하나.

**지금은 백엔드의 임시 조치(세 문서를 백엔드 저장소에 둠)가 맞다** — CI는 통과해야 한다.
다만 **복사본이 둘인 상태**이므로 M6 전에 반드시 닫는다. 이 항목이 그 표식이다.

### M5 — 도메인 이전 · 사용자

| 순서 | 누가 | 내용 |
|---|---|---|
| 1 | 사용자+백 | `api.galmal.kr` CNAME → `ryu-tomi.github.io` · `galmal-backend`에 커스텀 도메인 등록 → **인증서 대기** |
| 2 | 프 | 프론트 빌드가 `https://api.galmal.kr/v1/…`을 보게 전환. **1의 인증서가 나온 뒤** |
| 3 | 사용자 | 구형 레포에서 `galmal.kr` 커스텀 도메인 **해제** |
| 4 | 사용자 | `galmal-frontend`에 `galmal.kr` 등록 → **인증서 대기** (몇 분~수 시간) |
| **5** | 백 | 🔴 **크론 맞바꾸기 + DB 인계** — 아래 박스. **세 동작의 순서가 전부다.** 끝나면 신형을 손으로 1회 |
| **6** | 백 | ✅ **상태 점검이 초록불로 바뀌는지 확인** — 이게 **배선 완료 신호**다(아래) |
| 7 | 사용자 | 서치콘솔·서치어드바이저 **「소유 확인됨 · 사이트맵 오류 없음」을 며칠 뒤 눈으로 확인**. ~~사이트맵 재제출~~ **불필요** — 도메인·사이트맵 주소가 그대로다(프론트 실측: 사이트맵 200·37 URL, 네이버 메타 태그 신형에 동일, 구글 DNS TXT 그대로) |

> 🔴 **2b를 5번으로 옮겼다** (기획, 2026-09-16). 원래는 2와 3 사이였다. 이유는 **롤백**이다 —
> 3·4가 어그러졌을 때 되돌아갈 곳은 구형인데, 크론을 먼저 끄면 **되돌아갈 구형이 낡아 있다.**
> 마지막에 두면 구형이 **도메인이 넘어간 걸 확인할 때까지 계속 수집**하고,
> ②의 DB 복사가 **그때까지 모은 것을 전부** 가져간다 — 수집 공백이 0이 된다.
> `mail_ingest`가 메일을 소비하는 것(BB33)도 **한쪽만 돌 때는 문제가 아니다.**

> ✅ **6번이 왜 체크리스트 항목인가** (백엔드 요청): M4 수동 실행에서 상태 점검이
> `::error:: https://galmal.kr/build.json 을 받지 못했다`로 **빨간불**이다. 정상이다 —
> `SITE_URL`이 아직 구형을 가리키는데 `build.json`은 신형 프론트만 낸다.
> **예외를 만들지 않는다**(「못 받음은 실패」를 한 번 풀면 그 점검은 다시는 아무것도 안 막는다).
> 신형에 `schedule:`이 없어 매일 울지도 않으므로 늑대 소년이 아니다.
> → **M5에서 이게 저절로 초록불이 되는 것이 배선이 끝났다는 증거다.** 놓치지 않게 항목으로 둔다.

#### 📍 M5 당일 실측·절차 (2026-09-17)

**DNS는 가비아다.** 그리고 **galmal.kr 본체는 DNS를 안 바꾼다** — 구형·신형 둘 다 `ryu-tomi.github.io`
밑이라 apex A 레코드 4개(`185.199.108~111.153`)가 그대로 맞다. **3·4번은 GitHub 설정에서 도메인을
옮기는 것뿐**이다. 사용자가 가비아에서 하는 건 **1번의 `api` CNAME 하나**다.

| | 실측 |
|---|---|
| 구형 `promo-ticket-site` | branch 배포 `main:/docs` · `docs/CNAME` = galmal.kr |
| 신형 `galmal-backend` | branch 배포 `main:/docs` → 도메인 등록 시 GitHub가 **`docs/CNAME`을 커밋**한다. **크론이 그 파일을 지우지 않는지** 백엔드 확인 |
| 신형 `galmal-frontend` | **workflow 배포** → `CNAME` 파일 **불필요**(GitHub 문서: *"no CNAME file is created, and any existing CNAME file is ignored"*). 설정에 저장된다 |

**2번(프론트)은 변수 하나**다 — `deploy.yml:50`이 `vars.API_URL`을 읽는다. 값 `https://api.galmal.kr/v1`
(**24자**, 끝 `/`·개행 없음, 넣은 뒤 길이를 잰다). **바꾸기 전에** `https://api.galmal.kr/v1/meta.json`이
200이고 `generated`가 백엔드 Pages와 같은지 실측한다. 틀려도 안전하다 — 빌드가 404에서 **배포 전에** 멈춘다.
**2번은 3번보다 먼저** — 새 API를 보는 상태로 도메인을 옮겨야 원인이 섞이지 않는다.

**🔴 1번 실측 — 도메인을 DNS보다 먼저 붙이면 인증서 요청이 안 걸린다** (2026-09-17)

GitHub 권장 순서(레포에 먼저 등록 → DNS 나중)대로 했더니 DNS가 생긴 뒤 20분+ 동안
`https_certificate: null`이었다 — 「발급 대기」가 아니라 **요청 자체가 없는 상태**. 등록 시점의
DNS 확인이 실패했고 **자동 재시도가 없다.** 사이트는 http로 200을 내므로 **조용히 멈춘다.**

```
01:39:43Z  같은 값으로 재PUT       → 5분간 null   ✗ 값이 안 바뀌면 재확인을 안 건다
01:45:07Z  도메인 제거 (Delete CNAME 커밋)
01:45:37Z  재등록     (Create CNAME 커밋) → authorization_pending   ✓
01:46:02Z  approved  ← 재등록 뒤 25초
```

**그 전 33분은 발급이 느렸던 게 아니라 요청이 아예 없던 시간이다. 기다림은 아무것도 안 해 준다**(백엔드 실측).

→ **권장 순서를 따르면 재등록이 한 번 필요하다고 보고 절차에 넣는다.** 4번(`galmal.kr`)은 A 레코드가
이미 GitHub를 가리켜 등록 시점 확인이 통과할 가능성이 높지만, `null`이면 **즉시** 제거→재등록.

**4번 절차 (사용자 + 프론트 확인)**
```
① galmal-frontend Settings → Pages → Custom domain: galmal.kr → Save
② DNS check 통과 · 인증서 발급 대기
②-b 🔴 `https_certificate` 가 **null 이면 기다리지 말고 제거 → 재등록** (아래 1번 실측)
③ 🔴 Enforce HTTPS 가 켜져 있는지 **다시** 확인 — 도메인을 붙이면 인증서 전까지 풀릴 수 있다.
   풀려 있어도 사이트는 뜨므로 **조용히 틀리는 종류**다
④ (프론트) 배포를 **일부러 한 번 더** 돌리고 cname 이 여전히 galmal.kr 인지 읽는다
   — 「재배포 뒤 유지」는 문서에 명시가 없어서 실측한다
```

#### 🔴 M5 2b — 크론 맞바꾸기와 DB 인계 (백엔드 발견, 2026-09-16)

M4 T4가 저장소를 복제한 뒤 **구형만 매일 수집한다**(T4b로 신형 `schedule:`을 뺐으므로).
그래서 신형의 `data/prices.db`는 **복제 시점에 얼어 있다.** 그냥 스왑하면 신형이 그 상태로
재개해 **M4~M5 사이 전부가 수집 이력에서 빈다.**

**이 구멍은 화면으로 안 보인다.** 딜 수가 줄지 않는다 — `median`·`low`·`obs_days`를 재는
**비교 기준이 얇아질 뿐**이다. 그래서 **BB1(하한선)도 안 걸린다.** 또 그 얼굴이다.
그리고 관측 이력은 **누적이라 재생성이 불가능하다** — 지나간 날의 가격은 다시 못 받는다.

```
① 구형 schedule 끄기
② 구형의 data/prices.db · data/deals_latest.txt 를 신형으로 복사·커밋
③ 신형 schedule 켜기
```

**순서가 전부다.** ③을 ②보다 먼저 하면 그날 신형이 **옛 DB로 한 번 돌아 덮어쓴다.**
①을 빠뜨리면 이중 수집·이중 발송이다(구독자가 생긴 뒤라면).

#### ②가 됐는지 어떻게 아나 — **blob 해시를 대조한다**

42MB 바이너리를 눈으로 확인할 방법이 없다. 「복사했다」를 믿지 않고 **잰다**:

```bash
# 구형 저장소에서
git rev-parse HEAD:data/prices.db
# 신형 저장소에서 — 두 값이 글자 하나까지 같아야 한다
git rev-parse HEAD:data/prices.db
```

**두 저장소가 전체 이력을 공유하므로**(T4c, 사용자 결정) 같은 바이트는 **반드시 같은 SHA**다.
절대값을 기대하지 않고 **같은 것의 두 값을 비교**한다 — `PLAN.md` 함정 6 규칙 그대로다.
(참고: 2026-09-16 기준 `42117da1…`, 42,270,720바이트. **이 값을 기대값으로 쓰지 말 것** —
매일 바뀐다. 불변식은 「양쪽이 같다」이지 특정 해시가 아니다.)

> **③ 직후에 신형을 손으로 한 번 돌린다**(`workflow_dispatch`). 배선까지 다 켠 상태의
> 첫 한 바퀴라 값이 가장 크다 — 백엔드 제안. T4 시점의 수동 실행은 **일부러 안 했다**
> (신형 DB만 앞서가면 위 인계가 더 복잡해진다).

**DoD**: galmal.kr 정상 + https + 소유확인 유지. **다운타임이 있는 유일한 단계다.**

#### ✅ M5 완료 — 2026-09-17 (기획 독립 검증)

| 단계 | 결과 |
|---|---|
| 1 | `api.galmal.kr` — cert approved · enforced. 백엔드 `vars.API_URL` = `https://api.galmal.kr` (21자) |
| 2 | 프론트 `vars.API_URL` = `https://api.galmal.kr/v1` (24자) · 배포 success |
| 3·4 | 구형 cname null · `docs/CNAME` 404 / 신형 프론트 cname galmal.kr · cert approved · enforced. **재배포 후에도 유지**(프론트 실측) → Actions 배포에 CNAME 파일 대책 **불필요로 닫음** |
| 5 | 구형 `collect.yml` 워크플로 **`disabled_manually`**. **DB 인계: 구형 HEAD `905febb` == 신형 인계 커밋 `a29eaee`의 blob `905febb`** — 글자 하나까지 같다. 신형 schedule 복원(`39e133e`) · 수동 1회 success |
| 6 | ⚠️ **부분 완료 — 자동 경로가 실패했다.** 라이브는 오늘 것(generated `11:45:25+09:00` · 딜 135)이지만 **수동 재전송 덕이다.** 아래 정정 · **내일 첫 예약 실행에서 검증** |

> 🔴 **정정 (2026-09-17) — 기획이 처음 「dispatch로 프론트가 저절로 구웠다」고 적었다. 틀렸다.**
> 결과(두 값 일치)는 맞았지만 **원인을 안 재고 추정했다.** 프론트 배포 실행이 두 번이었다:
> ```
> 02:45:25Z 백엔드 발행·커밋 → 02:45:32Z 자동 dispatch → 02:45:40Z 프론트 빌드 = 어제 값(129)  ✗
>                              02:46:09Z 백엔드 Pages 배포 완료  ← 신호보다 29초 늦음
> 02:46:55Z 백엔드 수동 재전송 → 프론트 재빌드 = 오늘 값(135)  ✓  ← 기획이 본 건 이것
> ```
> **push 직후 신호를 쏘니 프론트가 매번 어제 API로 굽는다.** 그리고 T6b의 「어제까지 허용」 때문에
> **상태 점검은 초록불**이었다 — 사이트가 하루씩 영원히 뒤처져도 아무도 모르는 형태, BB18의 얼굴이다.
> 백엔드가 시각을 대보고 찾았다. 기획의 검증은 **재전송 뒤에** 쟀기 때문에 고쳐진 상태를 봤다.
>
> → **상태를 재는 것으로는 경로가 안 보인다.** 「어느 실행이 이 값을 만들었나」까지 대야 한다
> (`gh run list`의 시각 · `build.json.built`). 결론이 맞은 것과 근거가 맞은 것은 다르다 — 이번엔 기획 쪽이었다.
>
> **수정**: 백엔드 `654dc70` — 신호 전에 API가 방금 발행한 값을 **프론트가 보는 URL 그대로**(캐시 우회 없이,
> Pages `max-age=600`) 서빙하는지 확인. 최대 12분, 안 오면 신호 없이 실패. `client_payload.generated` 추가.
>
> **남은 구멍과 결정 — T6d 신설 (프론트)**: CDN 엣지가 달라 백엔드 러너는 새 값, 프론트 러너는 옛 값을 볼 수 있다.
> → **프론트 배포가 받은 `meta.generated`를 `client_payload.generated`와 대조하고, 다르면 배포하지 않고 실패**한다.
>
> **T6b 관용은 좁히지 않는다** (백엔드 질의에 대한 기획 결정). 두 점검의 **일을 가른다**:
> | 점검 | 무엇을 잡나 | 기준 |
> |---|---|---|
> | T6d (받는 쪽, 매 배포) | **정확성** — 받은 게 보낸 것과 같은가 | **정확히 일치**, 아니면 즉시 실패 |
> | T6b (보내는 쪽, 다음 날) | **생존** — 신호가 아예 안 오나(PAT·Pages 사망) | 어제까지 허용 |
> 관용을 좁히면 push 직후라는 구조 때문에 **매일 헛실패**한다(늑대 소년). 정확성은 **값을 받는 순간 아는 쪽**이 보는 게 싸고 확실하다.
> 「하루씩 영원히 뒤처짐」은 T6d가 매 배포마다 막는다.
>
> **내일 아침 확인할 것 — 초록불만으로는 부족하다**
> ```
> galmal.kr/build.json 의 api_generated == api.galmal.kr/v1/meta.json 의 generated   그리고 둘 다 9/18
> 프론트 배포 실행이 repository_dispatch 1회로 끝났는가 (수동 재전송 없이)
> ```
>
> **구형 `collect.yml`에서 `schedule:` 제거 — 허용한다** (백엔드 질의). R9 동결의 목적은 스왑 때 합칠
> 코드가 갈리는 걸 막는 것이었는데 **스왑이 끝나 합칠 일이 없다.** 지금은 워크플로 비활성으로만 꺼져 있어
> **누가 UI에서 켜는 순간 이중 발송**이 돌아온다. `workflow_dispatch`는 남긴다(되돌리기 경로).
>
> **BB34(테스트가 산출물 생성일에 묶임) — 백로그 동의, 다만 동결 사유는 M5로 끝났다.** 신형 `test.yml`이
> push마다 돌아 크론 다음 날 아침 전 push면 CI가 **헛 빨간불** — 늑대 소년이 되기 전에 **다음 챕터 첫 태스크**로 권한다.

> **오탐 하나 — 「파일에 `schedule:`이 있다」≠「돈다」** (2026-09-17). 프론트가 구형·신형 `collect.yml`에
> 둘 다 `schedule`이 있는 걸 보고 「이중 가동」 경보를 냈다. **구형은 파일은 그대로 두고 워크플로를
> `disabled_manually`로 끈 것**이었다. 파일을 읽는 것으로는 안 보인다 —
> `gh api repos/…/actions/workflows --jq '.workflows[].state'`까지 봐야 한다.
> 경보 자체는 옳은 방향이다(놓치면 알림 이중 발송). **확인 대상을 「설정 파일」이 아니라 「실행 상태」로 둔다.**

### M6 — 정리

| 태스크 | 담당 | 내용 |
|---|---|---|
| T1 | 사용자 | `promo-ticket-site` → `galmal-plan` rename 후 **private** |
| T2 | 기획 | `CLAUDE.md`를 3레포 판으로 재작성. 각 레포에 자기 `CLAUDE.md` + `../galmal-plan/` 참조 |
| T3 | 기획 | `PROJECT.md` 운영 정보 갱신, **이 문서 삭제** |

---

## 5. 위험

| # | 위험 | 대책 |
|---|---|---|
| R1 | **PAT가 조용히 만료** → 수집은 초록불인데 배포만 멈춘다. `BACKEND.md` BB18(몇 주째 죽어 있어도 아무도 몰랐던 건)의 재발 조건 | 무기한 fine-grained PAT + 백엔드 크론 끝 「상태 점검」이 `$SITE_URL/v1/meta.json`을 받아 `generated`가 오늘 것인지 본다. 아니면 잡을 실패로 |
| R1b | **도메인이 조용히 갈라진다.** 지금은 `test_site_url.py:53`이 `send_alerts.SITE_URL`과 `theme.BASE_URL`을 대조하는데, 분리 후엔 각각 다른 레포/설정이라 **아무도 안 본다** | R1과 같은 점검이 막는다 — `$SITE_URL`이 틀리면 404로 실패한다. 별도 대책 불필요 |
| **R1c** | 🔴 **R1 점검 자체가 분리 후에 조용히 깨진다** (기획 발견, 2026-09-11). 위 R1·R1b·M3 T6이 전부 `$SITE_URL/v1/meta.json`을 읽는데, **분리 후 `galmal.kr`은 `/v1/`을 서빙하지 않는다** — v1은 `api.galmal.kr`에 있다. 지금은 한 저장소라 같은 주소에서 둘 다 나와서 **테스트하면 통과하고 M5 날부터 매일 404**가 난다. 매일 실패하는 점검은 사람이 무시하게 되고(BB30 늑대 소년), 누가 주소를 API로 「고치면」 이번엔 **사이트 배포가 멈춘 걸 못 본다** | **점검을 둘로 나눈다.** ① API: `$API_URL/v1/meta.json` — `generated`가 오늘인가 · `subscribe.address == MAIL_ADDRESS`(T6). ② 사이트: 프론트 빌드가 **`build.json`에 「어느 v1으로 구웠나」(`api_generated`)를 적어 같이 배포**하고, 점검은 **사이트의 `api_generated` == API의 `generated`**를 본다. 절대값이 아니라 **두 값의 일치**다(`PLAN.md` 함정 6 규칙) — PAT가 죽으면 사이트가 뒤처져 불일치, `SITE_URL`이 틀리면 404. **`API_URL`은 M3·M4 동안 `https://galmal.kr`**(지금은 거기서 v1이 나온다), **M5에서 `https://api.galmal.kr`로 바꾼다** — 변수라서 코드는 안 바뀐다. `build.json`은 M4에서 `deploy.yml`과 같이 넣는다(M2 동등성 대상 39개에 없는 새 파일이라 기준선을 안 건드린다) |
| R2 | 백엔드는 성공, 프론트 빌드 실패 → **사이트가 어제 것** | 안전한 실패다(반쯤 쓰인 페이지가 안 나감). 다만 두 레포로 알림이 갈라지니 R1의 점검 스텝이 양쪽을 다 본다 |
| R3 | **M2 동등성 증명 실패** — JSON에 값이 빠져 있다 | M1 DoD를 엄격히. 빠지면 M1로 되돌아간다. 이게 M1/M2를 **같은 레포 안에서** 하는 이유다 |
| R4 | 도메인 이전 다운타임 · BE7 재실행 | M5를 마지막에 몰아서 한 번에. M4까지는 무중단 |
| R5 | worktree 3개 → clone 2개 + worktree 1개로 바뀐다 | M6에서 `CLAUDE.md` 재작성. 폴더는 형제로 유지해야 `../galmal-plan/` 참조가 산다 |
| R6 | `prices.db` 34.4MB | 파일만 옮긴다. DB 자체가 이력이라 git 이력은 안 가져가도 된다 |
| **R7** | 🔴 **시크릿 없이 재빌드하면 제휴 링크가 조용히 사라진다.** `affiliates.py:154`가 `if _env("TP_MARKER"):`라 마커가 없으면 **예외가 아니라 그냥 빠진다** — 사이트는 멀쩡히 뜨고 수익 링크 **125건**만 없어진다. 이전 중 M2·M3에서 재빌드를 반복하므로 **이 이전에서 가장 만나기 쉬운 함정**이다 (프론트가 실제로 겪음 → 백엔드 BB30) | ① 백엔드가 `build_site.py`에 **경고**를 넣는다(산출물 불변 → diff 0 안 건드림, 이전 전 가능) ② `CLAUDE.md`의 충돌 해결 절차를 고친다(아래) ③ 재빌드 후 `grep -c '"ad":true' docs/data/deals.json`으로 **125건을 눈으로 확인**한다 |

---

## 5b. 🔴 `CLAUDE.md` 충돌 해결 절차의 결함 (공용 파일 — 사용자 승인 필요)

`CLAUDE.md:85-86`이 이렇게 적혀 있다.

```
충돌 시: 생성물은 재빌드로 해결한다.
  git checkout --theirs data/prices.db → python collector/build_site.py → git add -A && git commit
```

**이 절차는 「재빌드하는 환경에 시크릿이 있다」를 가정한다.** 크론(Actions)은 있지만
`.env`는 gitignore라 **백엔드 worktree에만** 있다(`PROJECT.md` §작업 체제).
→ 프론트·기획 세션이 이 절차를 그대로 따르면 **제휴 링크 125건이 빠진 파일을 커밋한다.**
실제로 2026-09-08 프론트가 겪었고 커밋 직전에 잡았다.

**✅ 2026-09-08 사용자 승인 → `CLAUDE.md`에 반영 완료.** 적용된 내용:

```diff
  - 충돌 시: 생성물은 **재빌드로 해결**한다.
    `git checkout --theirs data/prices.db` → `python collector/build_site.py` → `git add -A && git commit`
+   - 🔴 **재빌드는 `.env`가 있는 환경에서만 한다.** 시크릿 없이 돌리면 `affiliates.py`가
+     제휴 링크를 **경고 없이 빼고**(`ad:true` 125건 → 0건) 사이트는 멀쩡히 뜬다.
+   - `.env`가 없으면 재빌드하지 말고 **생성물은 `--theirs`로 원격 것을 취한 뒤**
+     그 구역 담당 세션에 재빌드를 요청한다.
+   - 재빌드했으면 커밋 전에 확인: `grep -c '"ad":true' docs/data/deals.json` → **125건**
```

> 🔴 **위 diff는 2026-09-08 시점의 기록이다. 그대로 따라 하지 말 것.**
> T7(2026-09-15)에서 같은 자리를 **다시** 고쳤다. 지금의 정본은 `CLAUDE.md`이고, 세 군데가 다르다:
> ① `--theirs` → `git checkout origin/main -- <생성물 이름들>` (merge/rebase에서 뜻이 반대라서)
> ② `build_site.py` → `collector/publish.py` + `site/build.py` 두 단계
> ③ `grep -c '"ad":true'` → **쓰지 말 것.** `deals.json`은 개행이 없는 한 줄이라 **항상 `1`**이다.
> 그리고 **`125`를 기대값으로 적은 것도 틀렸다** — 딜 수는 매일 바뀐다.
> 불변식은 「딜 수 == 제휴 링크 수」다(`CLAUDE.md`의 python 한 줄).

---

## 5c. M6 다음 — 서버 전환 스파이크 (2026-09-08 사용자 확인)

**동기가 제품이 아니라 학습이다.** 사용자 확인: 서버를 띄우려는 이유는
**백엔드 경험·포트폴리오**이지 제품이 요구해서가 아니다. 사용자는 백엔드 전공이다.

제품 논리로는 지금 필요 없다 — 회원 기능이 기획도 안 됐고, 가격 소스가 캐시라
실시간이 안 되고(중앙 2일 지연), 실시간 조회 API는 MAU 5만이 조건인데 트래픽 0이고,
수집은 Actions로 무료다. **그래도 서버는 목적 자체다.**

→ 그래서 서버 얘기가 나오면 「필요한가」가 아니라 **「이전을 안 망치면서 어떻게 제대로 지을까」**로 답한다.

**자원**: Azure 무료 크레딧 **₩130,000, 2027-02 만료.**
실측 계산 — App Service + 관리형 Postgres면 월 ₩35,000~40,000이라 **3~4개월치**다.
「17개월 공짜 서버」가 아니다. 다만 만료까지 여유가 커서 **서두를 이유가 없다** —
M6 후 시작해도 크레딧을 거의 다 쓴다.

**사용자 결정: 분리를 먼저 끝낸다** ("일단 나누는 거부터 하죠"). 근거는 §1의 설계 기준
그대로다 — 분리가 **서버를 싸게 만드는 작업 자체**이기 때문이다.

```
M6 완료 후 (시간 상자)
  publish.py 를 HTTP 핸들러로 감싼다  ·  prices.db → Postgres
  api.galmal.kr 을 Azure로 돌린다 (DNS 한 번)
  측정 → 유지할지 정적으로 되돌릴지 결정 (되돌리기도 DNS 한 번)
```

**프론트는 한 글자도 안 바뀐다.** `api.galmal.kr/v1/routes/{code}.json`만 보기 때문이고,
그게 v1을 URL·버전 경로·라우트 단위로 설계한 이유 전부다.

> ⚠️ **이전 중에 이걸 앞당기지 않는다.** M2가 진행 중이고 관문이 diff-0이다.
> 목적지가 흔들리면 프론트가 무엇을 향해 만드는지가 흔들린다.
> 다만 **설계 판단이 갈릴 때는 「나중에 진짜 서버가 붙는다」를 전제로 고른다.**

#### 이식 표면 실측 — 두 줄이다 (백엔드 §11.12, 2026-09-09)

기획이 M3 참고로 짚은 셋 중 **둘은 이미 됐고 셋째는 생각보다 작다.**

| | 현 상태 |
|---|---|
| 통계가 파일 경로를 아는가 | ✅ 다섯 함수 전부 `conn`만 받는다. `DOCS` 참조 0 |
| 응답 만들기와 파일 쓰기가 갈렸는가 | ✅ `*_payload()`가 만들고 `_write()`가 쓴다 — **핸들러는 `_write`만 안 부르면 된다** |
| sqlite에 묶였는가 | ⚠️ **SQL 두 줄뿐** |

```
build_site.py:76    strftime('%Y-%m', depart_date)        →  to_char(…, 'YYYY-MM')
build_site.py:101   CAST(strftime('%w', …) AS INTEGER)    →  EXTRACT(DOW FROM …)
```

🔴 **「이식 표면이 두 줄」이 곧 「지금 추상화 계층을 세우지 말라」는 근거다**(백엔드 판단, 동의).
그날 두 줄 고치는 게 오늘 어댑터를 만드는 것보다 싸다.
**M3에서 통계를 뺄 때 그 두 줄이 한 파일에 모이게만** 두면 된다.

> 이게 「나중에 서버가 붙는다를 전제로 고른다」의 정확한 사용법이다 —
> **미리 짓는 게 아니라, 흩어지지 않게만 두는 것.**

---

## 6. 사용자만 할 수 있는 일 (세션이 대신 못 함)

- M4 T1 레포 2개 생성 (둘 다 **public** — private이면 Pages가 안 뜬다)
- M4 T2 **secrets 5종** — `TP_TOKEN` `MAIL_ADDRESS` `MAIL_APP_PASSWORD` `ANTHROPIC_API_KEY` `TP_MARKER`
  > 🔴 **「8종」이 아니다** (기획 정정, 2026-09-15). 워크플로가 **참조하는 이름**은 8개지만
  > **실제로 등록된 적이 있는 건 5개**다(예전 저장소 실측 — `gh api …/actions/secrets`).
  > `TP_TRIP_TRS`·`TP_TRIP_P`·`TP_TRIP_CAMPAIGN`은 **Trip.com 제휴 파라미터**이고 한 번도 등록된 적이 없다.
  > 없으면 `affiliates._trip_configured()`가 False라 Trip.com 링크가 **수수료 없는 일반 링크**로 나간다
  > — 사용자 화면은 같고 아무것도 안 깨진다. **M4에 필요 없다.**
  > (기획이 「참조하는 이름」을 세고 「등록된 것」으로 전달한 실수다 — 함정 9번과 같은 유형.)
- M4 T3 PAT 발급
- **예외 절차 — M4에서 이력을 잘못 올려 다시 올려야 할 때만**: 해당 저장소 Settings → Rules → `main-protection`을
  **잠깐 끄고** 다시 올린 뒤 **바로 켠다.** 우회 권한자를 일부러 0명으로 둬서 소유자도 이 절차 없이는 못 한다
- M5 전부 (DNS 레코드, Pages 커스텀 도메인, 검색엔진 재확인)
- M6 T1 rename + private 전환

**순서 경고**: 구형 레포를 private으로 돌리는 것은 **M6, 맨 마지막이다.**
M5가 끝나기 전에 누르면 galmal.kr이 404가 된다 (무료 플랜은 private 레포 Pages 불가).

---

## 7. 진행 현황

**2026-09-08 사용자 승인 3건** — BB29(라이브 결함 수정) · BB30(재빌드 경고) · `CLAUDE.md` 절차 수정.
앞의 둘은 백엔드가, 셋째는 기획이 반영했다. **BB29·BB30은 라이브 배포까지 확인됐다.**

| 단계 | 담당 | 상태 |
|---|---|---|
| **이전 전 정지작업** | 백엔드 | ✅ BB29 · BB30 — 배포 확인 완료 |
| M0 계약 확정 | 기획 | ✅ 2026-09-08 (양쪽 판정 반영해 재확정) |
| M1 백엔드 JSON 발행 | 백엔드 | ✅ **2026-09-08** — v1 39개 발행. DoD를 실측 대조로 통과(36노선 불일치 0). 산출물 diff-0 유지 |
| M2 프론트 화면 생성 | 프론트 | ✅ **2026-09-09** — 기준선 대비 **39/39 바이트 동일**(홈 포함). **기획이 독립 검증함** |
| M3 스위치·HTML 삭제 | 백 (+기획 T7) | 🔷 **진행 중** — T0·T1·T2·**T3 ✅**·**T7 ✅** (2026-09-15) · T4~T6 남음 |
| M4 레포 분할 | 사용자+양쪽 | ⬜ |
| M5 도메인 이전 | 사용자 | ⬜ |
| M6 정리 | 사용자+기획 | ⬜ |

**진행 중인 기존 작업과의 관계**: 프론트 CH4·CH5는 **완료·push됐다**(2026-09-08).

### 🔴 이전이 단일 트랙이다 — 기능 챕터는 전부 멈춘다 (2026-09-08 사용자 지시)

사용자 지시: **"일단 레포 나누는 거를 최우선으로 진행해 줘."**

**멈추는 것**: 프론트 CH6(접근성·마감) · 백엔드 BE8(노선 페이지 확대) ·
기획 PH7(시즌 이벤트) · **「사진을 넣을 것인가」**(아래).
**M6가 끝난 뒤 재개한다.** 각 세션 문서에 재개 전제와 함께 기록되어 있다.

> ⚠️ **CH4 T7(지도 미끄러짐)은 중단 대상이 아니다 — 이미 완료됐다.**
> 커밋 `2969806`, `discover.js:747 pinTarget()` · `:753 viewForPin()` · `:766 slideMs()`.
> **기획이 두 번 미완으로 적었다.** 첫 인수인계 때 「CH4 T1~T6 push 대기」로 파악한 것을
> 프론트가 정정해 회신했는데 반영하지 않았다. 재개해도 CH4에서 할 일은 없다.
> (「T5 카드 회피 로직 정리」가 필요하다면 그건 T7과 무관한 별개 태스크다.)

> **「사진을 넣을 것인가」는 기획 소관이다** (프론트 발견). 확인해 보니 **사진이 아예 없다** —
> 카드 사진 자리는 그라데이션(`c.g`)에 「사진 준비중」이 얹혀 있다(`discover.js:649`).
> 접근성 문제가 아니라 **제품 결정**이고 이미지 라이선스가 걸린다. **M6 후 기획이 판정한다.**

**왜 병렬을 접었나 — 기획의 판단 오류였다.**
M1이 승인을 기다리는 동안 프론트가 놀까 봐 CH6를 제안했다. 파일 교집합이 없어
기술적으로는 병렬이 가능했다(백엔드 확인). **그런데 그건 세션 가동률을 최적화한 것이지
사용자의 목표를 최적화한 게 아니다.** 게다가 CH6는 이전에 **해롭다** —

> 화면이 바뀌면 **M2 기준선(§M2 T5)을 다시 잡아야 한다.**
> 그 규칙을 내가 직접 써놓고도 "감수할 비용"으로 취급했다.
> 기준선을 여러 번 다시 잡을수록 **「같은 입력 → 같은 출력」 증명이 흐려진다.**

**프론트는 놀지 않는다 — 할 일이 M2다.** M1을 기다리지 않고 착수할 수 있는 M2 태스크가 있다
(아래 §M2 「M1 전에 할 수 있는 것」). 계약이 확정됐으므로 픽스처로 먼저 짤 수 있다.
