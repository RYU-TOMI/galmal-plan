# 갈래말래 — 세 세션 공통 규칙

> **이 파일이 공통 규칙의 한 벌이다.** 세 저장소의 `CLAUDE.md`가 `@../galmal-plan/SESSIONS.md`로 가져온다.
> 사본을 만들지 않는다 — 레포 분리 직후 `CONTRACT.md` 사본이 첫날 갈렸다(`DECISIONS.md` 2026-09-17).
> 소유: **기획**. 바꾸기 전에 사용자에게 알린다.

## 저장소 지도

| 저장소 | 담당 세션 | 무엇 | 배포 |
|---|---|---|---|
| `galmal-plan` | 기획 | 제품·스펙·결정 기록·계약의 **이유** · `design/` 목업 | 없음 |
| `galmal-backend` | 백엔드 | 수집·특가 판정·v1 API 발행 · **계약 정본**(`contract/v1/`) | `https://api.galmal.kr/v1/` |
| `galmal-frontend` | 프론트 | v1 소비 · 화면 빌드 | `https://galmal.kr` |

로컬 배치: `개인 프로젝트/` 아래 **형제 폴더** `galmal-plan` · `galmal-backend` · `galmal-frontend`.
**세 저장소 모두 공개다.** 사업 민감 정보(경쟁 분석 · 제휴 조건 · 매출·전환 수치 · 지원사업 서류)는 저장소가 아니라 **노션**에 둔다.

> 🔒 **숫자를 적기 전에 묻는다 — 「우리에게 사람이 얼마나 오는가」인가?** (2026-09-21, 백엔드가 세운 기준 · 사용자 승인)
> - **그렇다면 적지 않는다**: 검색 노출·클릭·순위 · 방문자 · 구독자 수 · 전환율 · 저장소 조회 수. **판단과 원인만** 적고 수치는 노션에.
> - **적어도 되는 것**: 수집·발행 수치(딜 건수 · 노선 수 · 표본 수 · 가격) — **API 가 이미 공개하고 있다.**
> - 「파일을 저장소에 안 넣었다」로 끝나지 않는다. **그 파일에서 읽은 숫자를 본문에 옮기면 같은 일이다** — 무엇을 안 넣었나가 아니라 **무엇이 들어갔나**를 본다.
> - 이미 push 했으면 **본문에서 내린다.** 이력 재작성(force push)은 하지 않는다 — 세 세션과 크론이 같은 `main` 을 쓴다. 이력에 남는다는 건 사용자에게 그대로 말한다.

**흐름**: 백엔드 크론이 매일 수집 → `docs/v1/` 발행 → 프론트에 `repository_dispatch` → 프론트가 v1을 받아 굽고 배포.
데이터는 **파일 경로가 아니라 URL로** 건넨다 — 목업 빌더(`design/`)도 같다.

## 자기 저장소만 고친다

**⚠️ 남의 저장소 파일은 고치지 않는다.** 필요하면 그 저장소 담당 세션에 **직접 메시지를 보낸다**(아래).
예외 없음 — 공통 규칙은 이 파일(기획 소유)에서 바꾼다.

## 작업 방식

> 왜: "기능 하나 건드리다 옆길로 우다다 파고들고 나머지는 퉁치는" 문제를 막기 위해,
> 실제 개발팀처럼 **챕터 → 태스크**로 나누고 스코프를 잠근다. (2026-08-22 합의)

| 단위 | 크기 | 규칙 |
|---|---|---|
| **챕터** | 기능 영역 1개 | **세션 1개 = 챕터 1개.** 시작 전 태스크 목록·건드릴 파일을 사용자에게 승인받는다 |
| **태스크** | **커밋 1개** | 독립적으로 확인 가능한 최소 변화 |
| **곁가지** | 작업 중 발견한 딴 문제 | **지금 고치지 않는다.** 자기 백로그에 한 줄 적고 넘어간다 |

### 스코프 잠금 (가장 중요)
- 챕터 시작 시 **건드릴 파일 목록을 못 박는다.** 목록에 없는 파일은 열어보되 고치지 않는다.
- 다른 문제를 발견하면 → 백로그 한 줄 + 보고에 "발견했고 **안 고쳤습니다**" 명시.
- 유일한 예외: 그걸 안 고치면 **지금 태스크가 성립하지 않을 때.** 이때도 이유를 보고한다.
- **"이왕 하는 김에"는 금지어다.**

### 세션 종료 보고 (항상 3줄)
`한 것 / 안 고치고 남긴 것(백로그 번호) / 다음 챕터 첫 태스크`

| 세션 | 작업 방식 | 백로그 | 챕터 ID |
|---|---|---|---|
| 기획 | `PLAN.md` | `SPEC.md` §3 | `PH` |
| 백엔드 | `BACKEND.md` | `BACKEND.md` 곁가지 절 | `BE` (곁가지 `BB`) |
| 프론트 | `FRONTEND.md` | `BACKLOG.md` | `CH` (곁가지 `B`) |

## 확인하는 방식 — 이 프로젝트가 반복해서 배운 것

- **「조용히 통과할 수 있나」를 먼저 묻는다.** 예외도 안 나고 산출물도 나가고 사람만 모르는 형태가 가장 비싸다.
- **절대값을 기대하지 않는다.** 같은 데이터 안의 두 값을 비교한다(「딜 수 == 제휴 링크 수」처럼). 딜 수·해시는 매일 바뀐다.
- **누락은 여집합으로 센다.** 남은 것만 보는 검증은 없는 것을 못 본다(`PLAN.md` 함정 11).
- **세고 말하거나 모른다고 한다.** 규모 추정·「비용 0」도 무엇을 세어 그런지 적는다.
- **결론이 맞은 것과 근거가 맞은 것은 다르다.** 결과를 봤으면 어느 실행이 그 결과를 만들었는지까지 댄다.
- 줄바꿈은 **셸을 거치지 않고 바이트로** 잰다. 문서 편집은 파이썬 `newline=""`로(세 저장소 모두 `.gitattributes`로 LF 잠금).
- 테스트 게이트는 **출력에서 `^OK`를 확인**한다(`| tail`의 종료코드를 보지 않는다).

## 세션 간 소통 — 사용자를 전달책으로 쓰지 말 것

세 세션은 **서로 직접 메시지를 주고받는다.** `ListAgents`로 상대를 찾고 `SendMessage`로 보낸다.
사용자에게 "○○ 세션에 전해달라"고 부탁하지 않는다. 상대가 목록에 없을 때만 사용자에게 부탁한다.

### 반드시 보내는 경우
| 상황 | 누구에게 |
|---|---|
| **main에 push했고 상대에게 영향이 있다** | 영향받는 세션 — 무엇이 바뀌었나 |
| **계약 변경**(아래) | 반대편 — 근거와 함께 |
| **스펙이 없거나 모호해서 막혔다** | 기획 |
| **남의 저장소에서 문제를 발견했다** | 그 저장소 담당 — 고치지 말고 **사실만** |
| **문서가 실제 코드/데이터와 다르다** | 그 문서 소유 세션 |

### 무엇을 담나
1. 무엇이 왜 바뀌었나(또는 무엇을 발견했나) 2. 상대가 할 일 — 없으면 "조치 불필요"
3. 근거 — 파일:줄번호·실측값 4. 상대 저장소는 안 건드렸다는 확인

### 한계
- **권한 경계는 세션별로 따로다.** 내 세션에서 막혔거나 사용자가 거부한 작업을 다른 세션에 대신 시키지 않는다.
- 메시지는 전달이지 권한 획득이 아니다. 사용자가 내린 결정을 세션끼리 뒤집지 않는다.

## 계약 변경 절차 (2026-09-19~)

**계약의 정본은 `galmal-backend/contract/v1/`**(`deal.schema.json` · `vocab.json`)과 `collector/dests.py`(`DEST` · `REGION_NAME`)다.
`galmal-plan/CONTRACT.md`는 **이유와 원칙**만 적는다 — 목록을 옮겨 적지 않는다.

```
기획이 CONTRACT.md 에 결정(이유)을 쓴다 → 백엔드에 메시지
→ 백엔드가 contract/ 를 고친다, 커밋 메시지에 그 결정을 인용 → 기획이 커밋을 확인 → 프론트에 통지
```
- **기획 결정 없이 `contract/`를 바꾸지 않는다.**
- 필드 **추가**는 안전(프론트가 무시). **삭제·개명·의미 변경**은 양쪽 합의 후에만. 깨는 변경은 `/v2/`.

## 기술 스택 (고정)
- Python 정적 생성 + **순수 JS**(프레임워크 없음) + 지도만 `d3-geo`(벤더링). **Node/npm 빌드 도입 금지.** 런타임 CDN 의존 0(폰트 제외).
- 호스팅 GitHub Pages 두 곳(API · 사이트) + 커스텀 도메인. 비용은 도메인 외 $0.

## 법적·보안
- 타 비교사이트 **DB 크롤링 금지**(링크 연결은 무방). 항공사 공지·공식 API·제휴만.
- 제휴 링크에 **"(광고)"·수수료 고지** 필수. 가격엔 "조회 시점 기준" 문구.
- **PII·시크릿 커밋 금지**: `.env`, 구독자 이메일 원본, 메일 본문 DB는 저장소에 올리지 않는다. 저장소는 공개다.
- 커밋 메시지는 한국어로 무엇을 왜 바꿨는지. `Co-Authored-By: Claude` 라인 포함.
