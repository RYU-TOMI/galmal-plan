# SPEC.md 에서 걷어낸 해소 행 (2026-09-22 문서 정리 T1, 보관)

> 현행 문서에서 경위를 뺀다는 결정(`DECISIONS.md` 2026-09-22 (5)). 아래는 지운 줄을 **원문 그대로** 옮긴 것이다 — 줄 번호는 정리 직전 `SPEC.md`(커밋 4c2de35) 기준.

- `L456` | ~~B14~~ | ~~far 뷰가 미주를 못 담음~~ | ✅ **해소** — 중심 `122°E` + 새 LOD로 전 딜 표시(위 확정) |
- `L457` | ~~B2~~ | ~~단계 전환·화면0 진입 트윈 모션~~ | ✅ **해소** — 600/400ms · `cubic-out` · 배율 로그 보간 · 트윈 중 라벨·항로 숨김(위 확정) |
- `L458` | ~~B3 / B15~~ | ~~라벨 밀집·잘림 규칙~~ | ✅ **해소** — 4방향 후보 + 가장자리 배제 + 실패 시 생략(위 확정). 실측 24/42 → 37/42 |
- `L460` | ~~B4~~ | ~~LOD 임계값~~ | ✅ **해소** — 배율 임계값 방식 자체를 폐기(위 확정) |
- `L615` | ~~B5 / F11~~ | ~~예산 상한 정책~~ | ✅ **해소** — 출발지 데이터에서 min·max 산출 + 최대치는 `제한 없음`(위 확정) |
- `L616` | ~~B6 / F12~~ | ~~필터 시 지도 과밀~~ | ✅ **해소** — 진짜 문제는 밀도가 아니라 매칭이 화면 밖에 있는 것이었다. `아주 멀리` 자동 전환 + 라벨은 매칭에게만(위 확정) |
- `L617` | ~~B7~~ | ~~접힌 도크의 활성 내역~~ | ✅ **해소** — 개수 대신 조건 문구, 최대 2개 + `외 N`(위 확정) |
- `L618` | ~~C-10~~ | ~~필터 칩 어휘가 데이터와 안 맞는다~~ | ✅ **해소 (2026-09-03)** — 분위기 칩 6종(해변·도시·미식·자연·문화·온천)으로 확정되고 프론트가 구현했다. **픽스처 121건 커버리지 121/121(100%), 어느 칩에도 안 걸리는 딜 0건**(프론트 실측). "영구히 흐린 채 남는 4건"이 사라졌다. |
- `L951` | ~~F1 / FL-1~~ | ~~딜 0건인 날 화면~~ | ✅ **해소**(위 확정) |
- `L952` | ~~F1 옛 메모~~ | ~~근거~~ | 가장 급함. `origins`가 비면 **핀도 안내도 없는 막다른 길**. 후보 문구 "오늘은 조용하네요"는 방향만 있고 미구현(`COPY.md` C-1). 재방문 유도·노선 페이지 안내를 함께 정해야 함. **백엔드도 생성 쪽 방어를 BE1에서 병행**(BB1) |
- `L954` | ~~F2 / FL-2~~ | ~~딜 희소 출발지~~ | ✅ **해소** — 10건 미만이면 안내 + 최다 허브 제안(위 확정) |
- `L955` | ~~F17~~ | ~~"특가" 도장이 근거 없이 붙는다~~ | ✅ **해소** — `≥15%`일 때만 + 문구를 `평소보다 N%↓`로(위 확정) |
- `L956` | ~~F17 옛 메모~~ | ~~근거~~ | 도장은 `특가 {discount}%↓`로 **무조건** 붙는다. **2026-09-01 확정(126건): `discount=0`이 37건(29%).** 잠정치였던 47%보다 낫지만 **여전히 카드 넷 중 하나 이상**이 `특가 0%↓`를 단다. `10%` 미만까지 넓히면 **101건(80%)**이다. 우리 스스로 정한 특가 기준은 중앙값의 65%(=35%↓)인데(`config.py`) 30%↓ 이상은 **3건뿐**이다. 게다가 `discount 0`이면서 `median ≤ price`라 **확장 상세의 근거 막대도 생략**되는 딜이 21건 — 도장은 "특가"라 하고 들어가면 근거가 없다. → 도장 노출 임계값·문구·큐레이션 배제 중 택일 |
- `L957` | ~~F18~~ | ~~훅이 근거 없는 인과를 주장~~ | ✅ **해소** — 훅 줄 자체를 제거(위 확정) |
- `L958` | ~~F18 옛 메모~~ | ~~근거~~ | 🔴 **2026-08-23 잠정 재측정(116건): B 67건(58%), 그중 `discount=0`이 37건 — 더 나빠졌다.** 이전 실측(94건): A `평소보다 N%↓` 4건 · **B `경유로 확 싸진 특가` 51건(54%)** · C 1건 · D `최근 N%↓` 29건 · E `{when} 최저가` 9건. **문제의 핵심은 0% 딜이 아니라 B 자체다** — `deals.json`에 **직항 대비 가격이 없다.** `median`도 직항/경유를 구분하지 않는다(`CONTRACT.md`). 즉 "경유해서 싸졌다"는 인과를 **뒷받침할 근거가 아예 없는데 피드의 절반이 그걸 단언한다.** 게다가 분기 순서가 B > D라 **실제 할인율이 있는 38건이 근거 있는 문구를 버리고 근거 없는 문구를 택한다.** → (1) B를 없앨지 (2) 직항 대비 가격을 계약에 추가할지 (3) 순서만 바꿀지. **대안 문구를 지어내기 전에 `COPY.md`의 "주장할 수 있는 것/없는 것" 표를 볼 것** — 소요시간도 필드가 없다. 안 쓰이는 확실한 사실은 **`직항`(41건, 44%)**이다. 삭제하면 E가 절반이 되어 피드가 단조로워지는 문제가 따라온다 |
- `L959` | ~~F19~~ | ~~"특가" 정의가 둘~~ | ✅ **해소** — 임계값 통일이 아니라 단어 분리(위 확정) |
- `L960` | ~~F19 옛 메모~~ | ~~근거~~ | 발견 홈 `whyOf()`는 `discount >= 25`, 노선 상세는 `config.py`의 중앙값 65%(=**35%↓**). 실측으로 `≥25%` 4건 · `≥30%` 3건 · `≥35%` **2건**. 같은 제품이 같은 단어를 다른 기준으로 쓴다 → **하나로 통일** |
- `L961` | ~~F16~~ | ~~hero의 fresh 게이트가 거의 항상 비어 있다~~ | ✅ **해소** — 신선도를 게이트에서 타이브레이커로 강등(위 확정) |
- `L962` | ~~F16 옛 메모~~ | ~~근거~~ | `seen` 실투입 후 실측(2026-08-22 03:06 방문 기준, 94건): **fresh 3% · ok 45% · stale 52%**. 내가 등급 경계를 정할 때 쓴 분포(fresh 36%)는 **수집 순간** 기준이었는데, 실제 방문 시점엔 이미 늙어 있다. **이 측정치는 비정상 상태가 아니다** — 크론은 매일 정상 가동 중이고(백엔드 확인), 하루 한 번 수집하는 구조에서 **다음 수집 직전에 방문했을 때의 모습**이다. 즉 분포는 하루 주기로 오르내리며 이게 **나쁜 쪽 끝**이다. → 판단 기준은 "언제 재느냐"가 아니라 **"가장 나쁜 시간대에도 hero가 뽑히는가"**. 게이트를 `ok`(<72h)로 완화할지, "가장 신선한 축" 같은 상대 기준으로 바꿀지 재검토. 지금 규칙이면 서울 hero가 fresh 1건(프랑크푸르트)으로 강제된다 |
- `L1485` | ~~F4 / FL-3~~ | ~~`links` 0건일 때 확장 상세~~ | ✅ **해소**(위 확정) |
- `L1486` | ~~옛 F4 메모~~ | ~~근거~~ | 지금은 "어디가 제일 싼지 비교해보세요" **헤더만 남는다** |
- `L1487` | ~~IA-2 / F7~~ | ~~URL 해시 상태 · 딜 딥링크~~ | ✅ **해소**(위 확정) |
- `L1488` | ~~F5 / FL-4~~ | ~~사라진 딜 딥링크 착지 화면~~ | ✅ **해소** — 출발지 적용 + 다른 허브 제안(위 확정) |
- `L1489` | ~~— 확장 카드 위치·스크롤~~ | ✅ **현행 유지 확인** — 위→아래→클램프·스크롤·모바일 시트가 이미 있다. **도크·줌 회피만 추가**(위 확정) |
- `L1490` | ~~B9~~ | ~~30일 스파크라인~~ | ⏸ **의도적 보류** — 중앙값 막대와 겹치고 시계열을 계약에 실으면 무겁다. 노선 페이지에 이미 30일 차트가 있다(위) |
- `L1491` | **BB16** | **복수 공항 도시의 예약 링크** | 광역 수집이 **도시 단위**(`TYO`)라 그 딜이 나리타인지 하네다인지 **우리는 모른다.** 링크는 대표 공항(`NRT`)으로 건다. → **지금은 조치하지 않는다.** ① 영향이 **6건/116 = 5%**(도쿄·오사카·뉴욕)뿐이고 ② 기존 고지 `실시간 최저가는 각 사이트에서 확인하세요`가 이 경우를 덮으며 ③ 5%를 위해 확장 상세에 안내를 더하면 **나머지 95%에 노이즈**다. **트리거**: 예약처가 도시 코드를 받는지 브라우저로 확인되면 **도시 코드 링크로 바꿔 근본 해결**한다(백엔드 BB16) |
- `L1492` | **B9-2** | **`low`·`obs_days` 필드 요청** | `{N}일 중 최저` 문구용. **백엔드 협의 중** — 분포를 받아 하한선(잠정 7일) 확정 |
- `L1572` | ~~B10 / F8~~ | ✅ **해소 (2026-09-04)** — **핀 탭 = 시트를 peek으로 + 그 카드를 맨 위로. 상세는 안 연다.** 카드 탭이 상세를 연다. 핀이 작고 겹치므로(B20) 되돌리기 비싼 동작을 걸지 않는다. 위 §확정 참고 |
- `L1592` | **IA-1** | **홈 → 노선 페이지 진입로** | 네비 "노선별"이 링크가 아님. 26페이지가 홈에서 도달 불가 → SEO·신뢰 근거 양쪽 손해 |
- `L1593` | **IA-3** | 빵부스러기 "특가 피드" 용어 | IA에 없는 화면 이름 |
- `L1596` | ~~**B17**~~ | **✅ 실측 완료 (2026-09-01, 프론트)** | **병목은 `path(WORLD)` 하나다 — 15~17ms로 전체의 99.7%.** 핀 투영 0.006 · 핀 SVG 0.010 · 카드 69장 0.059ms. **제가 의심한 `innerHTML` 재생성은 병목이 아니었다.**<br>🔴 **그래서 §CH1 트윈 스펙(400/600/620ms)이 현행 구조로는 성립하지 않는다** — 뷰가 바뀔 때마다 `path(WORLD)`를 다시 만들어 **프레임 예산을 정확히 100% 먹는다.**<br>✅ **해법은 검증됐다**: `geoEquirectangular`는 경위도에 선형이라 **`rotate`를 고정하면 center·scale 변경이 평면 아핀변환**이 된다 → `<g transform>` 하나로 처리(**최대 오차 `1.8e-12`px, 0.5px 초과 0/1518점**). 뷰 변경 1프레임 **15~17ms → 0.00003ms**.<br>**`rotate`는 딜이 없는 최대 경도 구간 한가운데에 고정한다** — 오늘 기준 `rotate([142.9, 0])`. **§CH1의 far 중심 계산과 같은 축이다.**<br>⚠️ 딸려오는 제약: ① 확대하면 선 굵기·라벨도 커지므로 해안선은 `vector-effect: non-scaling-stroke`, **핀·라벨은 변환 그룹 밖으로** ② seam은 **로드 시 1회만** 계산(매 렌더 바꾸면 이득이 사라진다)<br>⚠️ 브라우저 DOM 비용은 **아직 못 쟀다**(확장 미연결). 현행에 더해지는 값이라 실제는 더 나쁘고 대안은 그것도 없앤다. "스크린샷 30초 멈춤"의 직접 원인은 **여전히 미확인** |
- `L1630` | ~~B14 · B2 · B3/B15 · B4~~ | ~~CH1 전 항목~~ | S1 | ✅ **PH1에서 확정** — 단 **far 중심은 고정값→계산으로 재확정**(BB15 여파) |
- `L1631` | **BB15 여파** | **수집이 도시 코드를 못 알아봐 도쿄·오사카·파리·런던·뉴욕이 피드에 없다** | 전역 | **백엔드 BE1** — 고쳐지면 딜 좌표 분포가 바뀌어 far 뷰가 자동 재계산된다. 태그 배정 84곳 중 상당수도 그제야 화면에 뜬다 |
- `L1634` | ~~B17~~ | ✅ **실측 완료** — 병목은 `path(WORLD)` 99.7%. 렌더 구조 변경이 트윈보다 먼저다 | 전역 | **CH1** |
- `L1635` | B5 / F11 | 예산 상한 정책 | S3 | CH2 |
- `L1636` | B6 / F12 | 필터 시 지도 과밀 | S1·S3 | CH2 |
- `L1637` | B7 | 접힌 도크 활성 내역 | S3 | CH2 |
- `L1638` | **F1 / FL-1** | **딜 0건인 날 화면** | S0·S2 | **CH3 (최우선)** |
- `L1639` | F2 / FL-2 | 딜 희소 출발지 | S2 | CH3 |
- `L1640` | **F18** | **훅이 근거 없는 인과를 주장 (피드의 53%)** | S2·S4 | **CH3 (최우선)** |
- `L1641` | **F17** | **"특가" 도장이 근거 없이 붙음 (76%가 10% 미만 할인)** | S2·S4·S5 | **CH3** |
- `L1642` | **F19** | **"특가" 정의가 제품 안에 둘** (25% vs 35%) | 전역 | **CH3** |
- `L1643` | **F16** | **hero의 fresh 게이트가 거의 항상 빔** (실측 fresh 3%) | S2 | **CH3** |
- `L1644` | F4 / FL-3 | `links` 0건 확장 상세 | S5 | CH4 |
- `L1645` | IA-2 / F7 | URL 해시·딥링크 | 전역 | CH4 |
- `L1646` | F5 / FL-4 | 사라진 딜 착지 화면 | 전역 | CH4 |
- `L1647` | B10 / F8 | 모바일 탭 동작 | S4 | CH5 |
- `L1649` | ✅ **B66 해소(2026-09-20 (11))** — 팁이 가리키는 버킷에 `n ≥ 3`(월과 같은 값), **막대에는 안 건다.** 실측: 지금 36노선에선 팁을 잃는 노선 0·바뀌는 노선 0. 아래는 경위. ~~요일 팁에 n 하한이 없다.~~ 계약에서 `weekdays` 에 `n>=3` 을 안 걸기로 한 건 **차트** 이야기였는데 **팁이 같은 데이터를 쓴다** — **n=1 짜리 요일이 이길 수 있다**(새 노선뿐 아니라 **전 노선**). 방향: 팁이 가리키는 버킷에 **n 하한**(후보 `n ≥ 3`, 월 버킷이 쓰는 값). **값은 실측 뒤** — 36노선 최저 요일의 n 분포 · 하한을 걸면 팁을 잃는 노선 수 | S7 | 미정 |
- `L1651` | **🔴 B67** | **「막대가 없을 때 이유를 말한다」(§CH4, 2026-09-01 확정)가 화면에 없다.** 라이브 138건 중 **51%**(`median<=price` 65 · `null` 5)에서 근거 자리가 **말없이 사라진다.** 문구는 `COPY.md` C-16 에 있다. `0%↓`(3건)도 「비슷해요」로 보낸다 | S5 | **지금 챕터 다음 — B65보다 먼저**(2026-09-21 (2)) |
- `L1652` | **B67-전수** | **「✅ 해소」로 닫혔는데 화면에 없는 항목이 또 있나** — B65·B67 둘째다(`PLAN.md` 함정 13). 프론트에 전수 대조 요청(측정만) | 전역 | 미정 |
- `L1654` | IA-1 | 홈 → 노선 진입로 | 전역 | CH6 |
- `L1655` | IA-3 | 빵부스러기 용어 | S7 | CH6 |
- `L1573` | — | 하단 시트 높이·스냅·드래그 |
- `L1574` | — | 가로 스크롤 피드의 카드 폭·스냅·hero 위치 |
- `L1575` | — | 지도와 피드의 화면 분할 비율 |
- `L1576` | — | 브레이크포인트 860이 맞는지 (태블릿 세로 768·가로 1024) |


## T2 — 본문에서 걷어낸 경위 블록 (정리 직전 줄 번호 기준, 원문 그대로)

### L74–L75
    - ~~**조작 모델 = 버튼 3단계. 자유 줌·팬은 넣지 않는다.** (2026-08-22 확정)~~
      ~~드래그로 지도를 옮기거나 핀치로 확대하는 동작은 **없다.** 뷰는 단계 버튼이 통째로 바꾼다.~~

### L155–L155
      | ~~BB15 이전~~ | 120.5° (대서양) | 239.5° | `121.8°E` |

### L208–L213
      > ✅ **BB15가 고쳐져 실제로 이 값이 됐다**(백엔드 `db061bc`). 미리 계산해 둔 예측과 정확히 일치한다.
      > ⚠️ **정정**: 한때 "프론트는 아무것도 고칠 게 없다"고 전했는데 **틀렸다.**
      > `ecba9cc`는 **`SPEC.md`만 고쳤고 코드는 그대로**다(프론트 세션 지적).
      > 정확히는 **재작업이 없다** — 고정값 `122`를 박았다가 다시 고치는 일이 없다는 뜻이지,
      > 구현 자체는 CH1에서 해야 한다.
    

### L250–L253
          > 🔴 **여기 "충돌 검사라 매 프레임 돌리면 비싸다"고 적었던 건 틀렸다** (2026-09-01 정정).
          > 프론트 실측: **라벨·핀·카드를 다 합쳐 0.08ms**다. 진짜 병목은 **`path(WORLD)` 15~17ms**로
          > 전체의 **99.7%**다. **결론(숨긴다)은 맞았지만 근거가 틀렸다** — 근거를 안 고치면
          > 다음 사람이 그 틀린 근거로 다른 결정을 한다.

### L299–L302
         🔴 **내가 "생략률은 거의 안 오를 것"이라고 썼는데 틀렸다** (프론트 실측, 2026-09-03).
         **서울에서만 22 → 16(−27%)**, 필터를 켜면 20 → 12(−40%)까지 준다.
         부산·대구·제주는 far 배율이 작아 핀이 흩어져 **변화가 0~+1**이다 —
         **서울이 유일하게 핀이 뭉치는 허브**다.

### L565–L565
        ✅ **2026-09-03 재확정** — 9/1에 로그로 뒤집었다가 실측으로 되돌렸다. 히스토그램도 선형이다.

### L723–L754
      ✅ **2026-09-01: `15/25/35` → `15/28/42`로 옮겼다.** (전에 "유지"로 정했던 걸 뒤집었다)
    
      ~~임계값 `15/25/35`를 유지한다~~ → **뒤집혔다.** 아래 경위를 남긴다.
    
      발단: 오늘 데이터에서 **T3(5건)가 T2(3건)보다 흔했다.** 사다리가 뒤집힌 것처럼 보였다.
      "골짜기가 여러 날 재현되면 임계값을 옮긴다"를 기준으로 걸고 백엔드에 30일 이력을 요청했다.
    
      **① 골짜기는 잡음이었다 — 옮길 근거가 사라졌다.**
      30일 합산: `15-19` 139 · `20-24` 157 · `25-29` 68 · `30-34` 50 · `35-39` 30 · `40%+` 33.
      `25~34%`가 꺼지는 골짜기는 없다. 20-24를 정점으로 완만히 내려갈 뿐이다.
    
      **② 그런데 뒤집힘 자체는 구조적이었다 — 원인은 임계값이 아니라 표본 크기다.**
      하루 평균이 T1 9.9 · T2 3.9 · **T3 2.1**이라, 서너 건짜리 두 등급은 어느 쪽이 흔들려도 순서가 바뀐다.
      30일 합계로는 사다리가 멀쩡한데(296:118:63) **특정 날 화면에서는 10%(3/30일) 확률로 무너진다.**
    
      **③ 그래도 옮기지 않는 이유 — 부재가 뒤집힘보다 비싸다.**
    
      | | 뒤집힘 | T3 없는 날 | T3 하루 평균 |
      |---|---|---|---|
      | **`15/25/35` (유지)** | 3/30일 (10%) | **5일 (17%)** | 2.1건 |
      | `15/25/40` | 0일 | 10일 (33%) | 1.1건 |
      | `15/30/45` | 0일 | **18일 (60%)** | 0.5건 |
    
      - **뒤집힘은 하루 안에서 거의 안 보인다.** 사용자는 하루치 화면만 보고 등급별 건수를 세지 않는다.
        `할인율순` 정렬에서 T3가 앞에 몰려도 **위계는 오히려 단조 감소**라 어색하지 않다 —
        "오늘 35%+가 5건 있다"는 **사실이지 거짓이 아니다.** 카드 하나의 의미(`35%+` = 가장 화려)는 절대 안 깨진다.
      - **부재는 누적된다.** 재방문자에게 "화려한 도장을 본 적이 없다"로 쌓이고,
        그 등급을 위해 만든 시각 자산(그라디언트·후광·광택)이 **3일 중 1일은 안 쓰인다.**
    
      **④ 지금은 임계값을 만질 때가 아니다 — 재는 자가 흔들린다.**
      `discount`의 기준인 `median`이 **기간 창 없이 전체 이력**을 쓴다(백엔드 BB4). 이력이 길어질수록
      중앙값이 옛 가격에 끌려가 **할인율이 과대평가된다.** 지금 맞춘 임계값은 BB4 수정 후 다시 맞춰야 한다.

### L1060–L1060
      ~~확장 상세 맨 아래에~~ **「평소 시세와 비교」 구역 바로 아래**에 **`이 노선 시세 자세히 →`**를 둔다 (2026-09-21 사용자 결정 — `DECISIONS.md` 2026-09-21 (10). **반영됨**: 프론트가 라이브에서 눌러 도착지까지 확인).

### L1063–L1078
      - 🔴 **내 실측이 틀렸다** (2026-09-01 정정, 백엔드가 잡음).
        `46%`는 **목적지만 맞춘 값**이고, 실제로 페이지가 있는 건 **`(출발지, 목적지)` 쌍 기준 20%**다.
    
        ```
        목적지만 일치        58건 (45%)
        (출발지, 목적지) 일치  26건 (20%)   ← 진짜
        차이 32건            ← 잘못된 링크가 붙을 뻔했다
        ```
    
        ✅ **2026-09-01 개선**: 백엔드가 **부산 출발 노선 9개**를 추가했다(노선 26 → 35개).
        **`route` 있는 딜 26 → 32건(25%)**, 그중 **부산 9건**. 파일이 없는 `route`는 **0건**이다.
    
        **노선 페이지 26개가 전부 인천·김포 출발**이다(`ICN` 25 · `GMP` 1).
        목적지만 보고 링크를 걸면 **부산 딜을 보다가 인천 노선 분석으로 들어간다** — 다른 노선이다.
        `median` 알갱이를 맞춘 것과 같은 종류의 어긋남이다.
    

### L1206–L1210
      > ⚠️ **내가 "화면0이 제품을 설명한다"고 추천했다가 뒤집었다.**
      > **딜을 바로 보여주는 게 더 잘 설명한다** — `어디서 출발해요?`는 **아무 가치도 안 준 채
      > 질문부터** 하고, `서울 → 제주 33,004원`은 그 자체로 설명이다.
      > 첫 화면이 질문이면 **이탈 지점이 하나 는다.**
    

### L1304–L1324
      - 🔴 **눈금은 선형이다. 트랙도 히스토그램도.** (2026-09-03 재확정 — **9/1의 로그 결정을 뒤집는다**)
    
        9/1에 "로그 눈금 히스토그램"이라고 적었다. **근거가 틀렸다.** 프론트가 트랙과 히스토그램이
        갈라진다고 물어와서 실측했다 — 서울 70건을 트랙(5만~130만)에 12칸으로 나누면:
    
        | 눈금 | 빈 칸 | 중앙값 위치 |
        |---|---|---|
        | **선형** | **1 / 12** | 24% |
        | 로그 | **3 / 12** | 60% |
    
        **"고가 구간이 비어 있다"가 사실이 아니었다.** 70만~130만에 29건(41%)이 있다.
        분포는 저가로 뭉친 게 아니라 **거리대 때문에 봉우리가 둘**이다 — 단거리 20~40만(32건),
        장거리 70~100만(29건). 정작 비어 있는 건 **5만~11만**(1건)이라 **로그가 그 빈 구간을 늘린다.**
    
        - **봉우리 둘은 데이터가 아니라 상품의 성질**이다(`haul`). 오늘 표본이 우연히 그런 게 아니다.
        - 로그를 쓰면 **슬라이더가 돈으로 안 읽힌다.** 손잡이를 같은 거리 밀어도 값이 딴판으로 뛴다.
        - 2026-08-22에 선형으로 못 박은 결정(§CH2)을 뒤집을 근거가 **없어졌다.** 그대로 간다.
        - `step`은 **5만 그대로.** 로그였다면 필요했을 스냅 규칙도 필요 없다.
    
        ⚠️ **버린 대안 — 상위 10%를 잘라 트랙을 좁히기**: 재보니 중앙값이 24%→33%로만 옮고
        범위는 여전히 19배다. **꼬리를 자르는 걸로는 안 풀린다.** 애초에 풀 문제가 아니었다.

### L1562–L1569
    ## 실측치 기준 (2026-09-01, 딜 126건)
    
    > ✅ **백엔드가 BB10·BB11을 고쳐 픽스처가 정상화됐다**(`f8225c1`). 아래 수치는 **확정치**다.
    > 이전 116건 잠정치는 신선도 계산이 UTC/KST를 섞어 딜 11건을 잘라낸 상태였다.
    > (도쿄가 `287,179원`으로 잡혔던 게 실제로는 **`140,579원`**이었다.)
    >
    > 시각 계산은 이제 `collector/timeutil.py` 한 곳에만 있고, **두 모듈이 같은 함수를 쓰는지를
    > 테스트가 검사한다.** 같은 사실을 두 곳에 적었던 게 결함의 뿌리였다.

### L1571–L1587
    ## ⚠️ 이 문서의 실측치에 대하여 (2026-08-23)
    
    > **지금 커밋된 `docs/data/deals.json`은 백엔드 로컬 실행분이고, 딜 11건이 빠져 있다.**
    > 신선도 계산이 UTC와 KST를 섞어 빼는 버그(백엔드 `BB11`) 때문에 9시간만큼 더 늙게 계산돼
    > 통과했어야 할 딜이 잘렸다. **운영 크론은 UTC 러너에서 돌아 기준이 맞으므로 배포된 사이트는 영향이 없다** —
    > 우리가 보는 **픽스처만** 그렇다. (프론트 세션 전달, 백엔드 확인)
    >
    > 확인된 예: **도쿄 최저가가 실제로는 `148,796원`인데 픽스처엔 `287,179원`으로 거의 2배다.**
    >
    > | 영향 받는 실측치 | 영향 없는 실측치 |
    > |---|---|
    > | 도장 티어 건수 · F17 `discount=0` 비율 · F18 분기 배정 · 직항 건수 · 허브별 건수 | 태그 어휘 구조·고유성(C-10) · far 뷰 계산식(B14) — 좌표·어휘 기반이라 11건에 안 흔들린다 |
    >
    > **확정한 스펙은 그대로 간다.** 도장 15%·훅 폐기는 *비율의 크기*가 아니라 *주장의 근거 유무*에서
    > 나온 결정이라 11건으로 뒤집히지 않는다. 다만 **`35%+ 2건`처럼 한 자릿수 근거는 흔들릴 수 있으니**
    > 픽스처 갱신 후 다시 잰다.
    

### L1594–L1594
    | **🔴 B62** | **모바일 실기기 재검토 — 지도와 시트가 화면을 나눠 갖는 방식.** 사용자(삼성 폰) 「지도를 좀 둘러보려고 했는데 안돼 / 지도가 너무 작게 보이고 / 위에 바를 누른거 같은데 지도가 눌려서 아무 반응이 없고 / 아래 창을 위로 올리면 확장되도 좋을거 같아」, 다른 사람 「위아래 스크롤이 딱 명확하지 않음」. 실측 지도 45%(빈 지도 255px)·보이는 카드 1장. (손잡이 38×4px·잡는 자리 44px — **③ 2026-09-20 조치 완료**, 탭으로 단 전환·막대 48×5px 가시화, 프론트 2a01f49) **①둘러보기 불가(§CH1 자유 줌 기각과 충돌) ②지도가 작다 ③시트 조작 어포던스 ④시트→상세 확장** 중 **③은 분리해 프론트가 먼저 고친다**(2026-09-20 (4) 결정 — 어느 안을 택해도 살아남는 것만). 헤드리스 390px 확인은 전부 이상 없음이었다 — 측정이 구별 못 하는 문제(`PLAN.md` 함정 12) | S1·S2 모바일 | **✅ 확정(2026-09-21) — §CH5b: 피드가 기본 화면 · 지도는 버튼 뒤 전체 화면(끌기 가능) · 필터 접힘**(`DECISIONS.md` 2026-09-21 (6)(7)). **반영 전**(프론트 챕터 대기 — 함정 13: 화면에서 보기 전엔 해소가 아니다). PH5(도크→시트)·B16 과 한 챕터. 착수 조건 **실기기 관찰 3건 — ✅ 찼다(2026-09-21)**: 삼성 2(1건은 반쪽) · **iPhone 14 Pro Safari 1**(사용자 본인, ③ 손잡이 다섯 항목 「다 됨」). ⚠️ 단 **③을 고친 뒤에도 남는 답답함(①②의 순수한 몫)은 아직 안 물었다** — PH 를 열 때 그것부터 묻는다 |


## T3 — 상태 인벤토리·낡은 서술 교체 (원문 그대로)

### L11–L11
    3. **현행** = 이미 구현된 값. 바꾸라는 말이 없으면 유지한다.

### L23–L23
    | S0 | 출발지 선택 (화면0) | 홈 · 오버레이 | CH3 |

### L34–L34
    > `FLOWS.md` §4에서 이관. **프론트 QA 체크리스트의 근거**다. `❌`는 스펙 미정 → 해당 PH에서 채운다.

### L36–L65
    | 화면 | 상태 | 현재 | 스펙 |
    |---|---|---|---|
    | **S0** | 정상 (허브 4개) | ✅ | 확정 |
    | | **딜 0건 → `origins` 빈 객체** | ❌ 핀·안내 모두 없음 = 막다른 길 | ✅ 확정 §CH3 |
    | | 재진입 (출발지 pill 클릭) | ✅ | 확정 |
    | **S1** | 거리 단계 3종 | ⚠️ 라벨·투영 모두 교체 필요 | ✅ 확정 §CH1 |
    | | LOD major/minor | ⚠️ 배율 임계값(폐기 예정) | ✅ 확정 §CH1 |
    | | 라벨 밀집·겹침 | ❌ 전부 그림 | ✅ 확정 §CH1 |
    | | 단계 전환 모션 | ❌ 즉시 스냅 | ✅ 확정 §CH1 |
    | | 필터 dim | ✅ opacity .28 | 확정 |
    | | **필터 시 과밀** (전 도시 동시 표시) | ❌ 지도 붕괴 | ❌ **CH2 / B6** |
    | | 활성 핀 | ✅ | 확정 |
    | **S2** | 정상 / hero 1장 + 카드 N장 | ✅ | 확정 |
    | | 정렬 3종 | ✅ | 확정 |
    | | 필터 매칭 0건 | ✅ 문구 있음 | 확정 |
    | | **딜 0건** | ❌ | ✅ 확정 §CH3 |
    | | **딜 희소 (<10건)** | ❌ 별도 처리 없음 | ✅ 확정 §CH3 |
    | | 신선도 배지 | ❌ 미구현 (백엔드 `seen` 대기) | ✅ 확정 §CH3 |
    | **S3** | 펼침 / 접힘 | ✅ (모바일은 접힌 채 시작) | 확정 |
    | | 활성 개수 표시 | ✅ `필터 · 2` | ❌ 무엇을 켰는지 **CH2 / B7** |
    | | **예산 상한 초과 딜** | ❌ 되살릴 수 없음 | ❌ **CH2 / B5** |
    | **S4** | 컴팩트 (hover) | ✅ | 확정 |
    | | **모바일 컴팩트 단계** | ❌ 없음 (탭 = 바로 확장) | ❌ **CH5 / B10** |
    | **S5** | 확장 상세 | ✅ | 확정 |
    | | **`links` 0건** | ❌ 빈 헤더만 남음 | ❌ **CH4 / F4** |
    | | 30일 스파크라인 | 제거됨(가짜 데이터) | ✅ 재개 조건 §CH4 |
    | **S6** | JS 없음 | ✅ 최저가 30 + 노선 26 | 확정 |
    | **전역** | 새로고침·뒤로가기 | ❌ 상태 소실 | ❌ **CH4 / IA-2** |
    | | 키보드 조작 | ❌ 불가 | ❌ **CH6 / B11** |
    | | 느린 회선 (278KB) | ❌ 예산 없음 | ❌ **CH6 / B13** |

### L1093–L1093
    - ✅ **모바일(≤860px) — 지도가 배경, 카드는 3단 시트** (2026-09-01 확정)

### L1138–L1139
      > ✅ **자유 줌·팬을 안 넣기로 한 게(§CH1) 여기서 값을 한다.** 지도에 드래그 제스처가
      > 없으므로 **시트 끌기와 안 부딪힌다.** 자유 줌을 넣었다면 이 설계가 성립하지 않았다.

### L1167–L1187
      좁히면 지도가 넓어져 지구가 커지고, 넓히면 카드가 여유로워진다. 둘 다 실측으로 쟀다.
    
      | 패널 | 지도 | far 배율 | 지구 크기 | 카드 본문 | 가장 긴 줄 |
      |---|---|---|---|---|---|
      | 320 | 880 | 186 | **+7%** | 204 | **74% 채움** |
      | 360 | 840 | 178 | +2% | 244 | 61% |
      | **380** | **820** | **174** | — | **264** | **57%** |
      | 420 | 780 | 165 | −5% | 304 | 49% |
      | 460 | 740 | 157 | −10% | 344 | 44% |
    
      > ⚠️ **처음 세운 가설이 틀렸다.** "좁히면 글자가 잘린다"고 봤는데
      > **320px에서도 잘리는 카드가 0건**이다. 도시명이 길어야 `쿠알라룸푸르`(여섯 자)이고
      > 가격은 아홉 자리가 최대라 **한글 서비스는 애초에 글자가 짧다.**
      > 그래서 "잘리느냐"가 아니라 **"가장 긴 줄이 본문을 얼마나 채우나"**로 다시 쟀다.
    
      - **좁혀서 얻는 게 적다**: 60px을 좁혀도 지구는 **+7%**뿐이다. `slice` 크롭 때문에
        **무대가 넓어져도 보이는 viewBox 가로는 그만큼 안 늘어난다.**
      - **넓혀서 얻는 것도 적다**: 본문이 넉넉해져도 **채울 글자가 없다** — 빈 여백만 는다.
      - **`380px`은 이미 균형점이다.** 바꿀 근거가 데이터에 없다. **바꾸지 않는 것도 결정이다.**
      - **모바일에서는 이 저울이 통째로 달라진다** — 390px 폭에서 패널이 전폭이 되므로
        PH5 모바일 항목에서 다시 본다.
