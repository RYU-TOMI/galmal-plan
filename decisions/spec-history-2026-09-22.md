# SPEC.md 에서 걷어낸 해소 행 (2026-09-22 문서 정리 T1, 보관)

> 현행 문서에서 경위를 뺀다는 결정(`DECISIONS.md` 2026-09-22 (5)). 아래는 지운 줄을 **원문 그대로** 옮긴 것이다 — 줄 번호는 정리 직전 `SPEC.md`(커밋 4c2de35) 기준.

- `L456` | ~~B14~~ | ~~far 뷰가 미주를 못 담음~~ | ✅ **해소** — 중심 `122°E` + 새 LOD로 전 딜 표시(위 확정) |
- `L457` | ~~B2~~ | ~~단계 전환·화면0 진입 트윈 모션~~ | ✅ **해소** — 600/400ms · `cubic-out` · 배율 로그 보간 · 트윈 중 라벨·항로 숨김(위 확정) |
- `L458` | ~~B3 / B15~~ | ~~라벨 밀집·잘림 규칙~~ | ✅ **해소** — 4방향 후보 + 가장자리 배제 + 실패 시 생략(위 확정). 실측 24/42 → 37/42 |
- `L460` | ~~B4~~ | ~~LOD 임계값~~ | ✅ **해소** — 배율 임계값 방식 자체를 폐기(위 확정) |
- `L615` | ~~B5 / F11~~ | ~~예산 상한 정책~~ | ✅ **해소** — 출발지 데이터에서 min·max 산출 + 최대치는 `제한 없음`(위 확정) |
- `L616` | ~~B6 / F12~~ | ~~필터 시 지도 과밀~~ | ✅ **해소** — 진짜 문제는 밀도가 아니라 매칭이 화면 밖에 있는 것이었다. `아주 멀리` 자동 전환 + 라벨은 매칭에게만(위 확정) |
- `L617` | ~~B7~~ | ~~접힌 도크의 활성 내역~~ | ✅ **해소** — 개수 대신 조건 문구, 최대 2개 + `외 N`(위 확정) |
- `L618` | ~~C-10~~ | ~~필터 칩 어휘가 데이터와 안 맞는다~~ | ✅ **해소 (2026-09-03)** — 분위기 칩 6종(해변·도시·미식·자연·문화·온천)으로 확정되고 프론트가 구현했다. **픽스처 121건 커버리지 121/121(100%), 어느 칩에도 안 걸리는 딜 0건**(프론트 실측). "영구히 흐린 채 남는 4건"이 사라졌다. |
- `L951` | ~~F1 / FL-1~~ | ~~딜 0건인 날 화면~~ | ✅ **해소**(위 확정) |
- `L952` | ~~F1 옛 메모~~ | ~~근거~~ | 가장 급함. `origins`가 비면 **핀도 안내도 없는 막다른 길**. 후보 문구 "오늘은 조용하네요"는 방향만 있고 미구현(`COPY.md` C-1). 재방문 유도·노선 페이지 안내를 함께 정해야 함. **백엔드도 생성 쪽 방어를 BE1에서 병행**(BB1) |
- `L954` | ~~F2 / FL-2~~ | ~~딜 희소 출발지~~ | ✅ **해소** — 10건 미만이면 안내 + 최다 허브 제안(위 확정) |
- `L955` | ~~F17~~ | ~~"특가" 도장이 근거 없이 붙는다~~ | ✅ **해소** — `≥15%`일 때만 + 문구를 `평소보다 N%↓`로(위 확정) |
- `L956` | ~~F17 옛 메모~~ | ~~근거~~ | 도장은 `특가 {discount}%↓`로 **무조건** 붙는다. **2026-09-01 확정(126건): `discount=0`이 37건(29%).** 잠정치였던 47%보다 낫지만 **여전히 카드 넷 중 하나 이상**이 `특가 0%↓`를 단다. `10%` 미만까지 넓히면 **101건(80%)**이다. 우리 스스로 정한 특가 기준은 중앙값의 65%(=35%↓)인데(`config.py`) 30%↓ 이상은 **3건뿐**이다. 게다가 `discount 0`이면서 `median ≤ price`라 **확장 상세의 근거 막대도 생략**되는 딜이 21건 — 도장은 "특가"라 하고 들어가면 근거가 없다. → 도장 노출 임계값·문구·큐레이션 배제 중 택일 |
- `L957` | ~~F18~~ | ~~훅이 근거 없는 인과를 주장~~ | ✅ **해소** — 훅 줄 자체를 제거(위 확정) |
- `L958` | ~~F18 옛 메모~~ | ~~근거~~ | 🔴 **2026-08-23 잠정 재측정(116건): B 67건(58%), 그중 `discount=0`이 37건 — 더 나빠졌다.** 이전 실측(94건): A `평소보다 N%↓` 4건 · **B `경유로 확 싸진 특가` 51건(54%)** · C 1건 · D `최근 N%↓` 29건 · E `{when} 최저가` 9건. **문제의 핵심은 0% 딜이 아니라 B 자체다** — `deals.json`에 **직항 대비 가격이 없다.** `median`도 직항/경유를 구분하지 않는다(`CONTRACT.md`). 즉 "경유해서 싸졌다"는 인과를 **뒷받침할 근거가 아예 없는데 피드의 절반이 그걸 단언한다.** 게다가 분기 순서가 B > D라 **실제 할인율이 있는 38건이 근거 있는 문구를 버리고 근거 없는 문구를 택한다.** → (1) B를 없앨지 (2) 직항 대비 가격을 계약에 추가할지 (3) 순서만 바꿀지. **대안 문구를 지어내기 전에 `COPY.md`의 "주장할 수 있는 것/없는 것" 표를 볼 것** — 소요시간도 필드가 없다. 안 쓰이는 확실한 사실은 **`직항`(41건, 44%)**이다. 삭제하면 E가 절반이 되어 피드가 단조로워지는 문제가 따라온다 |
- `L959` | ~~F19~~ | ~~"특가" 정의가 둘~~ | ✅ **해소** — 임계값 통일이 아니라 단어 분리(위 확정) |
- `L960` | ~~F19 옛 메모~~ | ~~근거~~ | 발견 홈 `whyOf()`는 `discount >= 25`, 노선 상세는 `config.py`의 중앙값 65%(=**35%↓**). 실측으로 `≥25%` 4건 · `≥30%` 3건 · `≥35%` **2건**. 같은 제품이 같은 단어를 다른 기준으로 쓴다 → **하나로 통일** |
- `L961` | ~~F16~~ | ~~hero의 fresh 게이트가 거의 항상 비어 있다~~ | ✅ **해소** — 신선도를 게이트에서 타이브레이커로 강등(위 확정) |
- `L962` | ~~F16 옛 메모~~ | ~~근거~~ | `seen` 실투입 후 실측(2026-08-22 03:06 방문 기준, 94건): **fresh 3% · ok 45% · stale 52%**. 내가 등급 경계를 정할 때 쓴 분포(fresh 36%)는 **수집 순간** 기준이었는데, 실제 방문 시점엔 이미 늙어 있다. **이 측정치는 비정상 상태가 아니다** — 크론은 매일 정상 가동 중이고(백엔드 확인), 하루 한 번 수집하는 구조에서 **다음 수집 직전에 방문했을 때의 모습**이다. 즉 분포는 하루 주기로 오르내리며 이게 **나쁜 쪽 끝**이다. → 판단 기준은 "언제 재느냐"가 아니라 **"가장 나쁜 시간대에도 hero가 뽑히는가"**. 게이트를 `ok`(<72h)로 완화할지, "가장 신선한 축" 같은 상대 기준으로 바꿀지 재검토. 지금 규칙이면 서울 hero가 fresh 1건(프랑크푸르트)으로 강제된다 |
- `L1485` | ~~F4 / FL-3~~ | ~~`links` 0건일 때 확장 상세~~ | ✅ **해소**(위 확정) |
- `L1486` | ~~옛 F4 메모~~ | ~~근거~~ | 지금은 "어디가 제일 싼지 비교해보세요" **헤더만 남는다** |
- `L1487` | ~~IA-2 / F7~~ | ~~URL 해시 상태 · 딜 딥링크~~ | ✅ **해소**(위 확정) |
- `L1488` | ~~F5 / FL-4~~ | ~~사라진 딜 딥링크 착지 화면~~ | ✅ **해소** — 출발지 적용 + 다른 허브 제안(위 확정) |
- `L1489` | ~~— 확장 카드 위치·스크롤~~ | ✅ **현행 유지 확인** — 위→아래→클램프·스크롤·모바일 시트가 이미 있다. **도크·줌 회피만 추가**(위 확정) |
- `L1490` | ~~B9~~ | ~~30일 스파크라인~~ | ⏸ **의도적 보류** — 중앙값 막대와 겹치고 시계열을 계약에 실으면 무겁다. 노선 페이지에 이미 30일 차트가 있다(위) |
- `L1491` | **BB16** | **복수 공항 도시의 예약 링크** | 광역 수집이 **도시 단위**(`TYO`)라 그 딜이 나리타인지 하네다인지 **우리는 모른다.** 링크는 대표 공항(`NRT`)으로 건다. → **지금은 조치하지 않는다.** ① 영향이 **6건/116 = 5%**(도쿄·오사카·뉴욕)뿐이고 ② 기존 고지 `실시간 최저가는 각 사이트에서 확인하세요`가 이 경우를 덮으며 ③ 5%를 위해 확장 상세에 안내를 더하면 **나머지 95%에 노이즈**다. **트리거**: 예약처가 도시 코드를 받는지 브라우저로 확인되면 **도시 코드 링크로 바꿔 근본 해결**한다(백엔드 BB16) |
- `L1492` | **B9-2** | **`low`·`obs_days` 필드 요청** | `{N}일 중 최저` 문구용. **백엔드 협의 중** — 분포를 받아 하한선(잠정 7일) 확정 |
- `L1572` | ~~B10 / F8~~ | ✅ **해소 (2026-09-04)** — **핀 탭 = 시트를 peek으로 + 그 카드를 맨 위로. 상세는 안 연다.** 카드 탭이 상세를 연다. 핀이 작고 겹치므로(B20) 되돌리기 비싼 동작을 걸지 않는다. 위 §확정 참고 |
- `L1592` | **IA-1** | **홈 → 노선 페이지 진입로** | 네비 "노선별"이 링크가 아님. 26페이지가 홈에서 도달 불가 → SEO·신뢰 근거 양쪽 손해 |
- `L1593` | **IA-3** | 빵부스러기 "특가 피드" 용어 | IA에 없는 화면 이름 |
- `L1596` | ~~**B17**~~ | **✅ 실측 완료 (2026-09-01, 프론트)** | **병목은 `path(WORLD)` 하나다 — 15~17ms로 전체의 99.7%.** 핀 투영 0.006 · 핀 SVG 0.010 · 카드 69장 0.059ms. **제가 의심한 `innerHTML` 재생성은 병목이 아니었다.**<br>🔴 **그래서 §CH1 트윈 스펙(400/600/620ms)이 현행 구조로는 성립하지 않는다** — 뷰가 바뀔 때마다 `path(WORLD)`를 다시 만들어 **프레임 예산을 정확히 100% 먹는다.**<br>✅ **해법은 검증됐다**: `geoEquirectangular`는 경위도에 선형이라 **`rotate`를 고정하면 center·scale 변경이 평면 아핀변환**이 된다 → `<g transform>` 하나로 처리(**최대 오차 `1.8e-12`px, 0.5px 초과 0/1518점**). 뷰 변경 1프레임 **15~17ms → 0.00003ms**.<br>**`rotate`는 딜이 없는 최대 경도 구간 한가운데에 고정한다** — 오늘 기준 `rotate([142.9, 0])`. **§CH1의 far 중심 계산과 같은 축이다.**<br>⚠️ 딸려오는 제약: ① 확대하면 선 굵기·라벨도 커지므로 해안선은 `vector-effect: non-scaling-stroke`, **핀·라벨은 변환 그룹 밖으로** ② seam은 **로드 시 1회만** 계산(매 렌더 바꾸면 이득이 사라진다)<br>⚠️ 브라우저 DOM 비용은 **아직 못 쟀다**(확장 미연결). 현행에 더해지는 값이라 실제는 더 나쁘고 대안은 그것도 없앤다. "스크린샷 30초 멈춤"의 직접 원인은 **여전히 미확인** |
- `L1630` | ~~B14 · B2 · B3/B15 · B4~~ | ~~CH1 전 항목~~ | S1 | ✅ **PH1에서 확정** — 단 **far 중심은 고정값→계산으로 재확정**(BB15 여파) |
- `L1631` | **BB15 여파** | **수집이 도시 코드를 못 알아봐 도쿄·오사카·파리·런던·뉴욕이 피드에 없다** | 전역 | **백엔드 BE1** — 고쳐지면 딜 좌표 분포가 바뀌어 far 뷰가 자동 재계산된다. 태그 배정 84곳 중 상당수도 그제야 화면에 뜬다 |
- `L1634` | ~~B17~~ | ✅ **실측 완료** — 병목은 `path(WORLD)` 99.7%. 렌더 구조 변경이 트윈보다 먼저다 | 전역 | **CH1** |
- `L1635` | B5 / F11 | 예산 상한 정책 | S3 | CH2 |
- `L1636` | B6 / F12 | 필터 시 지도 과밀 | S1·S3 | CH2 |
- `L1637` | B7 | 접힌 도크 활성 내역 | S3 | CH2 |
- `L1638` | **F1 / FL-1** | **딜 0건인 날 화면** | S0·S2 | **CH3 (최우선)** |
- `L1639` | F2 / FL-2 | 딜 희소 출발지 | S2 | CH3 |
- `L1640` | **F18** | **훅이 근거 없는 인과를 주장 (피드의 53%)** | S2·S4 | **CH3 (최우선)** |
- `L1641` | **F17** | **"특가" 도장이 근거 없이 붙음 (76%가 10% 미만 할인)** | S2·S4·S5 | **CH3** |
- `L1642` | **F19** | **"특가" 정의가 제품 안에 둘** (25% vs 35%) | 전역 | **CH3** |
- `L1643` | **F16** | **hero의 fresh 게이트가 거의 항상 빔** (실측 fresh 3%) | S2 | **CH3** |
- `L1644` | F4 / FL-3 | `links` 0건 확장 상세 | S5 | CH4 |
- `L1645` | IA-2 / F7 | URL 해시·딥링크 | 전역 | CH4 |
- `L1646` | F5 / FL-4 | 사라진 딜 착지 화면 | 전역 | CH4 |
- `L1647` | B10 / F8 | 모바일 탭 동작 | S4 | CH5 |
- `L1649` | ✅ **B66 해소(2026-09-20 (11))** — 팁이 가리키는 버킷에 `n ≥ 3`(월과 같은 값), **막대에는 안 건다.** 실측: 지금 36노선에선 팁을 잃는 노선 0·바뀌는 노선 0. 아래는 경위. ~~요일 팁에 n 하한이 없다.~~ 계약에서 `weekdays` 에 `n>=3` 을 안 걸기로 한 건 **차트** 이야기였는데 **팁이 같은 데이터를 쓴다** — **n=1 짜리 요일이 이길 수 있다**(새 노선뿐 아니라 **전 노선**). 방향: 팁이 가리키는 버킷에 **n 하한**(후보 `n ≥ 3`, 월 버킷이 쓰는 값). **값은 실측 뒤** — 36노선 최저 요일의 n 분포 · 하한을 걸면 팁을 잃는 노선 수 | S7 | 미정 |
- `L1651` | **🔴 B67** | **「막대가 없을 때 이유를 말한다」(§CH4, 2026-09-01 확정)가 화면에 없다.** 라이브 138건 중 **51%**(`median<=price` 65 · `null` 5)에서 근거 자리가 **말없이 사라진다.** 문구는 `COPY.md` C-16 에 있다. `0%↓`(3건)도 「비슷해요」로 보낸다 | S5 | **지금 챕터 다음 — B65보다 먼저**(2026-09-21 (2)) |
- `L1652` | **B67-전수** | **「✅ 해소」로 닫혔는데 화면에 없는 항목이 또 있나** — B65·B67 둘째다(`PLAN.md` 함정 13). 프론트에 전수 대조 요청(측정만) | 전역 | 미정 |
- `L1654` | IA-1 | 홈 → 노선 진입로 | 전역 | CH6 |
- `L1655` | IA-3 | 빵부스러기 용어 | S7 | CH6 |
- `L1573` | — | 하단 시트 높이·스냅·드래그 |
- `L1574` | — | 가로 스크롤 피드의 카드 폭·스냅·hero 위치 |
- `L1575` | — | 지도와 피드의 화면 분할 비율 |
- `L1576` | — | 브레이크포인트 860이 맞는지 (태블릿 세로 768·가로 1024) |
