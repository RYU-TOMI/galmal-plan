# SPEC.md — 화면·상태 인벤토리 + 챕터별 확정 인터랙션 스펙

> **소유: 기획 세션.** 프론트 세션이 **매 챕터 시작 시 해당 절만 펼쳐 보는** 문서다.
> 시각 시스템(색·타이포·컴포넌트 모양)은 `DESIGN.md`, 문구는 `COPY.md`,
> 왜는 `DECISIONS.md`, 데이터 계약은 `CONTRACT.md`, 경로·실패는 `FLOWS.md`.

## 읽는 법 (프론트 세션용)

1. 시작할 챕터의 절(§CH1 ~ §CH6)로 바로 간다.
2. **확정** = 그대로 구현한다. 출처가 붙어 있으니 근거로 인용하면 된다.
3. **현행** = 이미 구현된 값. 바꾸라는 말이 없으면 유지한다.
4. **미결** = 아직 정해지지 않았다. **추측해서 만들지 말고 기획 세션에 요청한다**(`FRONTEND.md` §2-1).
5. 구현하다 스펙과 코드가 다르면 → `BACKLOG.md`에 적고 보고한다. 문서가 틀렸을 수도 있다.

---

## 1. 화면 인벤토리

정적 사이트라 **페이지는 2종**, 나머지는 전부 홈 안의 상태다(`IA.md`).

| ID | 화면 | 위치 | 담당 챕터 |
|---|---|---|---|
| S0 | 출발지 선택 (화면0) | 홈 · 오버레이 | CH3 |
| S1 | 지도 무대 | 홈 · 우측(모바일 상단) | CH1 |
| S2 | 카드 피드 | 홈 · 좌 340px(모바일 하단) | CH3 |
| S3 | 필터 도크 | 홈 · 지도 우측 하단 | CH2 |
| S4 | 플로팅 카드 (컴팩트) | 홈 · 항로 끝 | CH4 |
| S5 | 확장 상세 | 홈 · S4가 그 자리에서 확장 | CH4 |
| S6 | noscript 대체 | 홈 | CH6 |
| S7 | 노선 페이지 | `/routes/*.html` | 백엔드 소유 |

## 2. 상태 인벤토리

> `FLOWS.md` §4에서 이관. **프론트 QA 체크리스트의 근거**다. `❌`는 스펙 미정 → 해당 PH에서 채운다.

| 화면 | 상태 | 현재 | 스펙 |
|---|---|---|---|
| **S0** | 정상 (허브 4개) | ✅ | 확정 |
| | **딜 0건 → `origins` 빈 객체** | ❌ 핀·안내 모두 없음 = 막다른 길 | ✅ 확정 §CH3 |
| | 재진입 (출발지 pill 클릭) | ✅ | 확정 |
| **S1** | 거리 단계 3종 | ⚠️ 라벨·투영 모두 교체 필요 | ✅ 확정 §CH1 |
| | LOD major/minor | ⚠️ 배율 임계값(폐기 예정) | ✅ 확정 §CH1 |
| | 라벨 밀집·겹침 | ❌ 전부 그림 | ✅ 확정 §CH1 |
| | 단계 전환 모션 | ❌ 즉시 스냅 | ✅ 확정 §CH1 |
| | 필터 dim | ✅ opacity .28 | 확정 |
| | **필터 시 과밀** (전 도시 동시 표시) | ❌ 지도 붕괴 | ❌ **CH2 / B6** |
| | 활성 핀 | ✅ | 확정 |
| **S2** | 정상 / hero 1장 + 카드 N장 | ✅ | 확정 |
| | 정렬 3종 | ✅ | 확정 |
| | 필터 매칭 0건 | ✅ 문구 있음 | 확정 |
| | **딜 0건** | ❌ | ✅ 확정 §CH3 |
| | **딜 희소 (<10건)** | ❌ 별도 처리 없음 | ✅ 확정 §CH3 |
| | 신선도 배지 | ❌ 미구현 (백엔드 `seen` 대기) | ✅ 확정 §CH3 |
| **S3** | 펼침 / 접힘 | ✅ (모바일은 접힌 채 시작) | 확정 |
| | 활성 개수 표시 | ✅ `필터 · 2` | ❌ 무엇을 켰는지 **CH2 / B7** |
| | **예산 상한 초과 딜** | ❌ 되살릴 수 없음 | ❌ **CH2 / B5** |
| **S4** | 컴팩트 (hover) | ✅ | 확정 |
| | **모바일 컴팩트 단계** | ❌ 없음 (탭 = 바로 확장) | ❌ **CH5 / B10** |
| **S5** | 확장 상세 | ✅ | 확정 |
| | **`links` 0건** | ❌ 빈 헤더만 남음 | ❌ **CH4 / F4** |
| | 30일 스파크라인 | 제거됨(가짜 데이터) | ✅ 재개 조건 §CH4 |
| **S6** | JS 없음 | ✅ 최저가 30 + 노선 26 | 확정 |
| **전역** | 새로고침·뒤로가기 | ❌ 상태 소실 | ❌ **CH4 / IA-2** |
| | 키보드 조작 | ❌ 불가 | ❌ **CH6 / B11** |
| | 느린 회선 (278KB) | ❌ 예산 없음 | ❌ **CH6 / B13** |

---

## §CH1 — 지도 무대

### 확정
- **투영**: `d3.geoEquirectangular` + `geoPath`, Natural Earth **50m** 해안선. (`DECISIONS.md`)
  - 왜 50m: 110m에는 제주·오키나와·괌·발리 같은 **섬이 없어서 핀이 바다에 뜬다.**
- 🧑 **조작 모델 = 자유 줌·팬 + 단계 버튼은 빠른 이동** (2026-09-22 사용자 확정 — `DECISIONS.md` 2026-09-22 (1). 모바일은 §CH5b 가 하루 먼저 같은 길을 갔다)
  - **휠·핀치 = 줌, 드래그 = 팬, 더블클릭 = 한 단 확대.** 배율 하한 = `아주 멀리`(지구가 무대보다 작아지지 않는다), 상한 = `가까운 곳`의 4배.
  - **`＋/－`는 다시 줌 버튼이다.** 2026-08-22 에 「죽어 있던 버튼을 단계 스테퍼로 재활용」했는데, 이제 원래 뜻으로 돌아간다.
    단계바(`가까운 곳`·`조금 더 멀리`·`아주 멀리`)는 **그대로** — 누르면 그 뷰로 **날아가는 빠른 이동**이고, 사용자가 줌·팬을 하면 단계바의 「켜짐」은 꺼진다(어느 단계도 아니다).
  - 🔴 **필터를 켜면 「한 번」 넓힌다, 잠그지 않는다.** 필터를 켠 순간 매칭 딜이 지금 뷰 밖에 있으면 `아주 멀리`로 **한 번** 이동한다(현행 §CH2 자동 전환 그대로).
    **그 뒤의 줌·팬·단계 조작은 전부 먹는다.** 필터를 꺼도 뷰를 되돌리지 않는다 — **사용자가 만진 뷰를 우리가 옮기지 않는다.**
    (현행 구현은 필터가 켜진 동안 뷰를 `아주 멀리`에 **잠갔다** — 단계 버튼이 눌리고 불은 켜지는데 화면이 안 바뀌었다. 2026-09-22 사용자 발견, B70.)
  - **라벨·LOD**: 뷰 3개용 규칙(아래 §LOD)은 **단계 뷰에 있을 때** 그대로 쓰고, 그 사이 배율에서는 §CH5b 의 규칙 하나로 간다 —
    **싼 곳(필터 중엔 매칭)부터 자리를 잡고 겹치면 라벨을 붙이지 않는다. 핀은 전부 그린다.** 배율이 바뀔 때마다 다시 잡는다.
  - **프론트가 먼저 잴 것**: 줌·팬 중 프레임(B17 의 병목 `path(WORLD)` — transform 만 바꾸는 지금 구조면 싸다) · 라벨 재배치 비용.
- **단계 라벨은 거리 표현으로 쓴다. 지리 이름 금지.** (2026-08-22 확정)

  | 단계 | 라벨 | 여는 거리대 |
  |---|---|---|
  | 1 | **`가까운 곳`** | `haul: short` |
  | 2 | **`조금 더 멀리`** | `+ mid` |
  | 3 | **`아주 멀리`** | `+ long` |

  - **왜 지리 이름을 버렸나**: 이전 라벨 `＋유럽·미주`는 (a) 미주를 실제로 못 담았고(B14) (b) 라벨에 없는
    중동·대양주를 담고 있었다. 데이터는 매일 바뀌므로 **지리 이름은 앞으로도 계속 약속을 어긴다.**
    거리 표현은 **항상 참**이고, 목적지를 정하지 않은 사용자(destination-last)에게 더 자연스럽다.
- **`＋`/`－` 버튼은 줌이 아니라 단계 스테퍼다.** (2026-08-22 확정)
  `＋` = 한 단계 더 멀리, `－` = 한 단계 가까이. 마지막/첫 단계에서는 비활성.
  - 기호만 두지 않고 **뜻이 보이게** 한다(라벨 또는 접근 가능한 이름 — `COPY.md`).
  - 단계바와 동작이 겹치는 건 의도다. 단계바는 **"몇 단계가 있고 지금 어디냐"**를 보여주고,
    `＋/－`는 **빠르게 넘기는** 용도다. 지금처럼 아무 일도 안 하는 버튼을 두지 않는다(B1).
- **핀 인코딩: 크기 고정 + 색 진하기 = 가격**(쌀수록 진한 코랄). 크기 인코딩은 산만해서 폐기.
  색 스케일은 **현재 화면에 보이는 딜들 사이의 상대값**(거리대가 다르면 절대가격도 다르므로).
- ✅ **LOD = 숨기지 않는다. 낮춘다.** (2026-09-02 확정 — "이전 단계는 `major`만" 폐기)

  이전 규칙은 **"이번 단계가 연 거리대는 전부 + 이전 단계는 `major`만"**이었다(2026-08-22).
  **핀을 지우는 순간 F15가 흔들린다** — 피드엔 있는데 지도엔 없는 딜이 생긴다.

  🔴 **관찰(프론트 T3)**: `아주 멀리`로 갈수록 표시가 **주는** 허브가 있다 — **대구 12→9 · 제주 8→6.**
  장거리 딜이 적어 **중거리 `minor`가 빠진 만큼을 못 채우기** 때문이다.
  규칙대로지만 **"더 멀리 갔는데 점이 줄어든다"는 사용자에게 이상하다.**

  **→ 무대 안에 있으면 전부 그린다.**

  | | |
  |---|---|
  | `major` | 그대로 (11px + 후광) |
  | `minor` | **5px · 반투명 · 후광 없음** |
  | 무대 밖 | 자연히 안 보인다 — **숨기는 게 아니다** |

  - **far 뷰 밀도(66%가 16px 안에 이웃)도 같은 방식으로 풀기로 했다.**
    **한 규칙이 두 문제를 같이 푼다** — 겹침이 줄고, 단계가 **단조**가 되고,
    **F15가 모든 단계에서 완전히 성립한다.**
  - **라벨은 여전히 `major`만.** 소음의 절반은 거기서 잡힌다.
  - **카드를 누르면 그 핀이 커진다** — 조용한 점도 부를 수 있다(§CH4).

  | 단계 | 표시 |
  |---|---|
  | `가까운 곳` | short 전부 |
  | `조금 더 멀리` | **mid 전부** + short `major` |
  | `아주 멀리` | **long 전부** + short·mid `major` |

  - **왜 바꿨나**: 기존 `MINOR_SCALE=1200`은 far(230)에서 `minor`를 전부 숨겼다. 서울 장거리 딜 20건 중
    12건이 `minor`라, **"유럽·미주" 단계인데 유럽·미주가 8건만 그려졌다.** 단계 이름이 약속한 것을 LOD가 되돌린 것이다.
  - **원칙**: 사용자가 그 단계를 누른 건 **그 거리대를 보겠다는 뜻**이다. 그 거리대를 숨기지 않는다.
    이전 단계는 맥락으로 남기면 되므로 `major`로 추린다.
  - 지역별 예외 목록은 여전히 두지 않는다 — 일본이든 동남아든 같은 규칙.
  - 실측(94건 기준) 단계별 표시 수:
    서울 18 / 30 / **42** · 부산 10 / 22 / 22 · 대구 4 / 5 / 5 · 제주 4 / 6 / 6
- **`아주 멀리` 뷰 투영: 중심·배율을 모두 그날 딜 좌표에서 계산한다** (2026-08-22 확정 → **08-22 재확정**)

  ⚠️ **중심을 `122°E`로 박아 두었다가 되돌렸다.** 오늘 데이터에서 계산한 값이었는데,
  백엔드가 **도쿄·오사카·파리·런던·뉴욕이 수집에서 통째로 빠져 있다**는 걸 발견했다(BB15).
  그게 고쳐져 **뉴욕(74°W)이 들어오면 최적 중심이 `143°E`로 21° 옮겨간다.**
  → **고정값을 쓰지 않는다. 매번 계산한다.**

  **계산 방법** (딜 목록의 경도만 있으면 된다):
  1. 출발지 포함 모든 딜의 경도를 정렬하고 **가장 큰 빈 구간**(연속한 두 경도 사이 최대 간격)을 찾는다.
  2. **필요한 호 = 360° − 빈 구간**, **중심 = 빈 구간의 정반대**.
  3. 배율 = `(무대유효반폭 − 라벨여유35px) ÷ (필요한 호 ÷ 2 라디안)`.
     무대유효반폭은 `slice` 크롭과 **필터 도크 폭**을 뺀 값이다.

  | | 가장 큰 빈 구간 | 필요한 호 | 중심 |
  |---|---|---|---|
  | **지금** (2026-09-01, 서울 69건) | 73.3° (`-73.8` → `-0.5`) | **286.7°** | **`142.9°E`** |

  > ⚠️ **배율 상한을 두 번 틀리게 적었다(158 · 255). 둘 다 브라우저 폭을 전제한 값인데,
  > `discover.js:10`의 `viewBox`는 `1000 × 680` 고정이다.** 브라우저 폭은 배율이 아니라
  > `preserveAspectRatio="slice"`의 **크롭**에만 영향을 준다.

  **올바른 공식** — `d3.geoEquirectangular`, 가시 반폭(경도) `= (가시 viewBox 가로 / 2) / scale` 라디안.

  검산: 현행 `scale 230` · 가로 1000 전부 보임 → `(500/230)` rad = **124.6°**.
  프론트가 현행 코드에서 실측한 값과 정확히 일치한다.

  `slice` 크롭 — 컨테이너 종횡비가 `1000/680 = 1.47`보다
  **크면** 가로가 전부 보이고(세로가 잘림), **작으면 가로가 잘린다**(`가시 가로 = cw × 680 / ch`).

  | 지도 무대 | 종횡비 | 가시 가로 | **필요 배율(호 286.7° 담기)** |
  |---|---|---|---|
  | 와이드 `1200×680` | 1.76 | 1000 | **≤ 199** |
  | **데스크톱 `900×680`** (패널 380 기준) | 1.32 | 900 | **≤ 179** |
  | 태블릿 `760×600` | 1.27 | 861 | **≤ 172** |
  | 모바일 `390×420` | 0.93 | 631 | **≤ 126** |
  | 모바일 큰 지도 `390×600` | 0.65 | 442 | **≤ 88** |

  > **패널 폭이 지도 배율을 정한다.** 패널을 넓히면 무대가 좁아지고, 좁아진 무대에 같은 호를
  > 담으려면 배율을 더 낮춰야 한다 = **지구가 더 작게 보인다.** PH5 구조 결정과 직결된다.
  >
  > ⚠️ **"세로는 여유가 있다"고 적었던 건 반쪽만 맞았다** (2026-09-02 정정, 프론트가 잡음).
  > **딜의 위도 범위**는 다 들어온다(멜버른 67.7° vs 가시 반폭 108.8°). 그런데 제가 안 본 건
  > **지구 자체의 세로 길이**다.
  >
  > 🔴 **등거리원통도법에서 지구는 항상 가로:세로 = 2:1**이다. 배율을 정하면 세로도 정해진다.
  > ```
  > 미주까지 담으려면   호 286.7°가 가로에      →  배율 ≤ 161
  > 화면을 꽉 채우려면  지구 세로(π×배율) ≥ 무대  →  배율 ≥ 216
  > ```
  > **둘 다 만족하려면 무대 종횡비가 `1.70 : 1` 이상**이어야 한다. **딜은 다 들어오는데 지구가 화면보다 짧다.**

  | 무대 | 종횡비 | 지도가 채우는 면적 |
  |---|---|---|
  | 와이드 `1400×680` | 2.06 | **100%** |
  | 데스크톱 `900×680` | 1.32 | 77% |
  | 노트북 `1150×894`(실사용) | 1.29 | **74%** — 세로 26% 빔 |
  | 모바일 `390×420` | 0.93 | 52% |

  ✅ **지금은 그대로 둔다**(2026-09-02). 남는 부분은 **바다색이라 "빈 화면"이 아니라 극지방 바다**로 읽힌다.
  배율을 올려 채우면 **B14로 되돌아간다**(미주가 화면 밖 = F15 위반).
  - **위도 중심은 0(적도)**으로 둔다 — 지구가 화면보다 짧을 때 여백이 위아래로 **고르게** 나뉜다.
    `FAR_LAT 14`는 "세로는 다 들어온다"는 틀린 전제로 정해진 값이라 여백이 위 127·아래 48로 치우쳤었다.
  - 🔴 **§CH5 모바일 설계의 제약**: **지도 무대 종횡비 ≥ `1.70 : 1`**.
    3단 시트가 지도 위에 얹히면 실질 무대 종횡비가 달라지므로 **거기서 자연히 정리된다.**
  >
  > **이 값들을 코드에 상수로 박지 말 것.** 호와 중심은 매일 딜에서 계산해야 한다(위 계산식).


  > 🔴 **BB15 이후 더 나빠졌다 — `major`가 화면 밖에 있다.**
  > 현행 `VIEWS.far`(`lon 78`·`scale 230`) 기준 서울 딜 **6건이 화면 밖**이고,
  > 그중 **`로스앤젤레스`·`뉴욕`은 `major`**다. `major`는 정의상 **"축소해도 표시"**인데
  > **가장 축소한 뷰에서 사라진다** — LOD 규칙과 정면으로 모순된다(프론트 세션 지적).
  > 뉴욕은 피드에 ₩920,625로 떠 있으므로 `FLOWS.md` F15도 함께 깨진다.

  - **위도 중심**은 시각 균형용이라 `14°N` 고정으로 두어도 된다(세로는 어느 배율에서도 다 들어온다).
  - **왜 계산으로 바꿨나**: 고정값은 **"오늘 데이터에서는 맞는 값"**일 뿐이다.
    목적지가 하나 늘고 줄 때마다 스펙이 틀려지고, 그걸 아무도 눈치채지 못한다.

  - 위 표는 **오늘 데이터 기준 참고값**이다. 구현은 위 계산식을 쓴다.
  - 라벨 반폭 35px 여유를 포함한다.
  - `가까운 곳`·`조금 더 멀리`는 현행 유지(near `132°E/35.5°N/1500` · sea `117°E/19°N/720`).
- **출발지는 항상 한국**, 인천·김포는 "서울" 한 핀으로 통합.
- **항로**: 출발지→도시 **곡선(quadratic)**, 한 번에 하나만.
- **전환 모션 = 배율 로그 보간 + 트윈 중 라벨·항로 숨김** (2026-08-22 확정, B2)

  | 전환 | 길이 | 이징 |
  |---|---|---|
  | **출발지 변경** (헤더 드롭다운) | **600ms** | `cubic-out` |
  | 거리 단계 전환 | **400ms** | `cubic-out` |
  | 대륙 선택 확대 | **620ms** | `cubic-bezier(.22,.61,.36,1)` |
  | 항로 곡선 draw | **400ms** (기존 유지) | `cubic-out` |

  ⚠️ 첫 줄은 원래 **`화면0 → 지도`**였다. **화면0을 없앴으므로(§아래 2026-09-01 확정)
  그 자리를 「출발지 변경」이 물려받는다** — 지도가 통째로 다른 나라로 날아가는 유일한 전환이라
  길이 근거가 그대로 유효하다. (2026-09-04 정리)

  - **길이 근거**: 출발지 변경은 **뷰가 가장 크게 바뀌는** 전환이라 길게 준다.
    단계 전환은 **반복 조작**이라 짧아야 한다 — 세 단계를 오가며 비교하는 게 정상 사용이다.
  - **배율은 반드시 로그로 보간한다**: `scale = s0 × (s1/s0)^t`.
    선형 보간하면 `1500 → 190` 같은 큰 변화에서 **초반에 확 튀고 후반에 기어간다.**
    중심 경도·위도는 선형으로 보간해도 된다.
  - **트윈 중에는 라벨과 항로를 그리지 않는다.**
    - 움직이는 글자는 어차피 못 읽는다.
    - **왜 숨기나**: **움직이는 글자는 못 읽는다.** 그게 전부다.
      > 실측(2026-09-01): 라벨·핀·카드 배치는 합쳐 0.08ms. 병목은 `path(WORLD)` 15~17ms(99.7%)다 — 숨기는 이유는 비용이 아니라 가독성이다.
    - 핀은 좌표 계산만이라 싸므로 **매 프레임 갱신한다.**
  - **딜 집합 교체 시점**: 트윈 **시작할 때** 새 단계 집합으로 바꾼다.
    새로 등장하는 핀은 `opacity 0 → 1`을 트윈과 같은 길이로 준다(제자리에서 나타남).
  - **트윈이 끝난 뒤 라벨 배치를 1회 실행**한다.
  - **전환 중 다른 단계를 누르면** 진행 중인 트윈을 버리고 **현재 위치에서** 새 목표로 다시 시작한다.
    큐에 쌓아 순서대로 재생하지 않는다.
  - **`prefers-reduced-motion: reduce`면 전부 즉시 스냅**한다(트윈 없음). 결과 화면은 동일해야 한다.
- **라벨 겹침 회피 = 4방향 후보 + 실패 시 라벨만 생략** (2026-08-22 확정, B3/B15)

  1. **배치 순서**(= 우선순위): `major` 먼저, 같은 등급이면 **가격 낮은 순**. 싼 딜이 우리가 파는 것이다.
  2. **후보 4자리를 순서대로 시도**한다 — **아래 → 위 → 오른쪽 → 왼쪽**.
     (아래가 기본인 이유: 현행 동작이고, 핀 위쪽은 항로 곡선이 지나간다)
  3. 후보가 **무대를 벗어나면 건너뛴다.** ← **이것이 가장자리 클램프다.**
     왼쪽 가장자리 핀은 자연히 `오른쪽` 자리를 쓰게 되어 `칭다오`→`다오` 잘림이 사라진다.
  4. 이미 놓인 라벨의 사각형과 **겹치면 다음 후보로.**
  5. 🔴 **핀과 겹쳐도 다음 후보로.** (2026-09-03 추가 — 프론트가 실물에서 발견)

     처음 확정할 때 **라벨끼리만** 봤다. `24/42 → 37/42`라는 검증도 **라벨-라벨 쌍**만 센 것이다.
     필터를 켜서 매칭 41곳에 라벨이 다 붙자 밀집 지역에서 드러났다 —
     **`타이베이`가 핀에 덮여 `이베이`로, `다낭`이 `낭`으로** 읽힌다.

     피해야 할 사각형은 **「지금 지도 위에 떠 있는 불투명한 것」 전부**다.

     🔴 **요소 이름을 나열하지 않는다** (2026-09-04 정정 — 프론트 질문으로 드러남).
     원래 여기 **"이미 놓인 라벨 · 다른 도시의 핀 · 도크/줌 컨트롤"**이라고 셋을 콕 집어 적었는데,
     그 뒤 지도 위 흰 박스가 **넷**이 됐다(단계바·첫 안내가 늘었다). 프론트는 스펙대로 둘만
     넣었고 **좌하단 라벨이 `가까운 곳` 버튼에 잘렸다.**
     **목록을 들고 있으면 UI가 늘 때마다 갈라진다** — 이 프로젝트에서 여덟 번째 같은 유형이다.
     그래서 규칙을 **성질**로 바꾼다:

     > **판정 = 그 사각형 아래에서 글자가 읽히는가.** 불투명하면 회피 대상이고,
     > 이름이 무엇이든 새로 생기든 상관없다.

     - **현재 대상**: 이미 놓인 라벨 · 다른 도시의 핀 · 출발지 핀/라벨 ·
       필터 도크 · 줌 컨트롤 · **단계바** · **지도 위 안내**.
     - **자기 핀은 제외한다**(아래 참고).
     - ✅ **사각형은 동적이다.** 닫히거나 숨겨진 요소는 **그 순간 대상에서 빠지고 라벨을 다시 배치**한다.
       상시 존재하는 것만 영구히 자리를 먹는다.
     - ⚠️ **대가**: 사각형이 늘수록 라벨이 준다. 늘리기 전에 **줄어드는 수를 재고 보고**한다.
       완화책이 있다 — **후보를 4방향에서 8방향(대각선 추가)으로 늘리면** 사각형이 늘어도
       라벨이 덜 준다. 넣을지는 실측 후 판단한다.
     **출발지 핀과 라벨(`서울 출발`)도 포함**한다 — 별도 그룹에 있다고 빼면 안 된다.
     ⚠️ **자기 핀은 제외한다.** 기본 자리(아래)가 `y+5.1~y+21.1`이고 자기 핀이 `y±7.1`이라
     **원래 겹치게 설계돼 있다**(2단계의 `PIN_R+11` 오프셋). 안 빼면 모든 도시가 기본 자리를 못 쓴다.

     실측(2026-09-03): 자기 핀을 제외하면 서울 라벨 22 → 16, 필터 시 20 → 12. 다른 허브는 0~+1(핀이 흩어져 있다).

     **그래도 켠다.** 지표를 「놓인 라벨」이 아니라 **「읽히는 라벨」**로 세면 반대다:

     | | 놓인 라벨 | 그중 핀에 덮임 | **읽히는 라벨** |
     |---|---|---|---|
     | 전 | 22 | **15** | **7** |
     | 후 | 16 | 0 | **16** |

     - **2.3배다.** 그리고 덮인 라벨은 안 읽히는 게 아니라 **틀리게 읽힌다** —
       `타이베이`가 `이베이`로 보이면 사용자는 **없는 도시**를 본다. 빈자리보다 나쁘다.
     - ⚠️ **버린 대안 — 라벨을 핀 위에 그린다**: 라벨을 다 살릴 수 있지만 **핀 15개를 덮는다.**
       도크 자리를 정할 때 「가려지는 핀 개수」로 쟀으면서(§CH1 도크 절) 라벨엔 딴 잣대를
       쓸 수 없다. **지도는 핀을 보여주려고 있다.** 덮인 핀은 누를 수도 없다.
  5. **넷 다 실패하면 라벨만 생략한다. 핀은 반드시 남긴다.** 딜을 지우는 게 아니다.
  6. **활성(호버/선택) 핀의 라벨은 배치 결과와 무관하게 그 위에 덮어 그린다.**
     활성은 한 번에 하나이므로 다른 라벨을 흐트러뜨리지 않는다. **재배치하지 않는다**(깜빡임 방지).

  - **효과(실측, 서울)**: `아주 멀리` 아래 한 자리만 **24/42** → 4방향 **37/42**.
    `가까운 곳` 17/18 · `조금 더 멀리` 29/30.
  - 이 규칙이 없을 때 실제로 나던 증상(프론트 실측): `도하`+`두바이` → **`도하바이`**,
    `하노이`+`광저우`+`선전` 뭉침, 왼쪽 가장자리에서 `칭다오` → **`다오`**.
  - **끝내 숨는 것**: 홍콩·선전·마카오처럼 **핀 자체가 1~2px 안에 겹치는** 경우뿐이다.
    지리적 사실이라 어떤 배율·배치로도 못 푼다 → 미결 B20.
  - 라벨 폭은 **글자 수 × 폰트크기 × 0.95** 추정으로 충분하다(정밀 측정 불필요).
  - 시각 확인: `design/stages.html` — 이 규칙이 적용된 상태로 렌더된다.

### 현행 (구현됨 — 바꾸라는 말 없으면 유지)
| 항목 | 값 |
|---|---|
| 뷰포트 | `W=1000, H=680` |
| `VIEWS` | near `{lon:132, lat:35.5, scale:1500}` · sea `{lon:117, lat:19, scale:720}` · far `{lon:78, lat:30, scale:230}` |
| `MINOR_SCALE` | `1200` → **minor는 near(1500)에서만 뜬다.** sea·far에서는 안 뜸 |
| `HAUL2STAGE` | `short→near, mid→sea, long→far` |
| 핀 | `PIN_R=6`, halo `r=9.6 / opacity .16`, core `stroke #fff 2.2` |
| 핀 활성 | `stroke-width 3, r 9`, halo `.28` · dim `opacity .28` |
| 라벨 | major `13px/800`, minor `11px/700 #4a6a66`, **핀 아래** `y+PIN_R+11`, `paint-order: stroke` |
| 출발지 마커 | ring `r9` + dot `r4` + 라벨 **위** `y-13` |

> 위 값들은 한때 `DESIGN.md`와 어긋나 있었다(문서 `r 8.5 / 2.5 / .15` vs 실제 `6 / 2.2 / .16`).
> **실제 지도에서 조정된 구현값이 정본**이며, `DESIGN.md`는 2026-08-22에 여기 맞춰 정정했다.

### ⚠️ B14 — `＋유럽·미주` 단계가 미주를 담지 못한다 (CH1 최대 안건)

프론트 세션 실측(2026-08-22): far 뷰에서 **피드에는 로스앤젤레스(₩834,396)·호놀룰루(₩735,534)가 있는데
지도에 그 핀이 없다.** 서유럽도 왼쪽 가장자리에서 잘린다(`바르셀로나` → `셀로나`).

**계산으로 원인이 확정된다 — 값 튜닝으로는 고칠 수 없다.**

| 단계 | scale | 가시 경도 (viewBox 기준) |
|---|---|---|
| near | 1500 | 112.9°E ~ 151.1°E |
| sea | 720 | 77.2°E ~ 156.8°E |
| **far** | **230** | **46.6°W ~ 157.4°W** (= −46.6 ~ 202.6) |

- LA `118.4°W`, 호놀룰루 `157.9°W` → **둘 다 far 뷰 밖이다.** 라벨 겹침이 아니라 **투영 범위 문제**다.
- 게다가 지도는 `preserveAspectRatio="xMidYMid slice"`라 **좌우를 잘라낸다.**
  → 실제 가시 범위는 위 표보다 좁고 **창 비율에 따라 달라진다.** 서유럽이 가장자리에서 잘리는 이유.
- 서울(127°E)·유럽(0°)·LA(118°W)를 한 화면에 담으려면 경도 **약 270°**가 필요하다.
  등거리원통도법에서 그건 사실상 전 지구이고, 그러면 근거리 대비 한국이 지나치게 작아진다.

**→ 값 조정이 아니라 2026-08-01에 확정한 "거리 3단계" 구성(위 §확정) 자체를 다시 보는 문제다.**

| 안 | 내용 | 대가 |
|---|---|---|
| (a) far를 전 지구로 | center ≈ 16°E, scale ≈ 210, `slice` 대신 여백 허용 | 한국이 작아지고 핀이 뭉침 |
| (b) 4단계로 분리 | `가까운 곳`·`＋동남아`·`＋유럽`·`＋미주` (중심 각각 다름) | `＋`의 **누적** 의미가 깨짐 |
| (c) 단계 이름을 뷰에 맞게 축소 | 예: `＋유럽·중동` | 정직하지만 미주 딜은 지도에서 영영 못 봄 |
| (d) 미주만 별도 점프 뷰 | 단계 버튼 하나가 다른 중심으로 이동 | 연속성 상실 |

> **어떤 안을 택하든 지켜야 할 원칙**: **피드에 있는 딜은 지도에서 찾을 수 있어야 한다.**
> 목록에 있는데 지도에 없으면 발견 서비스의 기본 약속이 깨진다(`FLOWS.md` F15).
> 화면 밖 딜이 불가피하다면 **그 사실을 알려야 한다**(예: 카드 hover 시 "이 단계 밖이에요").

### 대륙 선택 — `아주 멀리` 전용 (2026-09-02 확정, 목업 `design/region.html`)

사용자 제안: **"게임에서 땅 선택하듯이 호버하면 대륙 모양으로 살짝 뜨고 그쪽이 선택되는 느낌."**

**왜 필요한가**: `아주 멀리`는 장거리 25건을 전 지구에 뿌린다. 유럽 10곳이 가로 200px 안에
뭉치고 북미 6곳은 오른쪽 끝에 붙는다. **핀을 하나씩 겨누는 게 사실상 불가능하다.**
자유 줌·팬을 안 넣기로 했으므로(위 §확정) 좁혀 보는 다른 손잡이가 필요하다.

#### 동작

| 입력 | 결과 |
|---|---|
| 대륙 위에 **호버** | 그 대륙 땅 조각이 강조색으로 뜬다(`fill #F6C3B2` · `stroke #F2603F88` 1px). 지도는 **안 움직인다** |
| 대륙 **클릭**(또는 칩 탭) | 그 대륙으로 확대 + 피드를 그 대륙 딜로 거른다 |
| 같은 대륙 **다시 클릭** | 전 지구로 되돌아간다 |
| `아주 멀리` 버튼 | 언제든 전 지구로 되돌아간다 |
| 다른 거리 단계 버튼 | 대륙 선택이 풀리고 그 단계로 간다 |

- **확대는 `<g transform>` 아핀 한 번**이다 — 등거리원통도법이 경위도에 선형이라
  center·scale 변경이 평면 아핀변환이 된다(B17 실측과 같은 원리). `path(WORLD)`를 다시 만들지 않는다.
- **핀·라벨은 변환 그룹 밖**이라 확대해도 커지지 않는다. 좌표만 같은 아핀으로 옮긴다.
- 확대 전환은 **620ms · `cubic-bezier(.22,.61,.36,1)`** — 단계 전환(600ms)과 같은 계열.
- **라벨은 대륙을 고른 뒤에만** 뜬다. 전 지구에서 25장을 다 띄우면 읽을 수 없다.
- 고르지 않은 대륙의 핀은 `opacity .12`로 남는다. **지우지 않는다** — 딜이 사라진 게 아니라 지금 안 보는 것뿐이다.

#### 고를 수 있는 대륙은 셋 — `eu` · `am` · `oc`

호버 그룹은 **딜 좌표가 아니라 대륙 자체**로 자른다(`DECISIONS.md` 2026-09-02 (13)).
딜 bbox로 자르면 **호주 본토와 캐나다가 통째로 빠진다.**

| 코드 | 라벨 | 경계(경도, 위도) | 조각 | 장거리 딜 |
|---|---|---|---|---|
| `eu` | 유럽 | −25~50 · 34~72 | 15 | 10건 |
| `am` | 미주 | −170~−55 · 8~72 | 29 | 6건 |
| `oc` | 대양주 | 110~180 · −50~−5 | 20 | 5건 |

- **`am`은 북미까지다.** 이 투영의 이음매가 `lon −37.1`(대서양)이라 남미 동해안이 잘린다.
  미주 딜 6곳은 전부 북미(위도 34~49)라 남미를 넣을 이유가 없다.
- **딜이 없는 대륙(아프리카·남미·남극)은 호버 대상이 아니다.** 회색 그대로이고 커서도 안 바뀐다.
  들어가도 보여줄 게 없는 문을 만들지 않는다.

#### 대륙에 안 담기는 4건 — `그 외` 칩

장거리 25건 중 21건이 위 셋에 들어간다. 남은 **4건**은 지리적으로 한 덩어리가 아니다:

| 곳 | region | 왜 안 담기나 |
|---|---|---|
| 두바이 | `etc` | 중동 |
| 콜롬보 | `etc` | 남아시아 |
| 호놀룰루 | `island` | 태평양 |
| 사이판 | `island` | 태평양 |

**칩 줄에 `그 외 4`를 넣는다.** 누르면 **지도는 안 움직이고** 그 4개 핀만 밝아지며 피드가 걸린다.

- 왜 넣나: 21건은 고를 수 있는데 4건만 못 고르면 **사용자가 그 4건을 막다른 길로 느낀다.**
- 왜 지도를 안 움직이나: **가리킬 한 곳이 없다.** 넷을 다 담으면 결국 전 지구다.
- 기각한 대안 — 「칩을 안 만든다」: 피드에서 스크롤로 찾으라는 뜻인데, 대륙 셋만 손잡이를 주고
  나머지는 안 주는 비대칭을 사용자가 알아챈다.
- 기각한 대안 — 「`etc`·`island`를 따로 두 칩으로」: 각 2건짜리 칩이 둘 생긴다. 너무 잘다.

#### 칩 줄은 장식이 아니라 **호버의 대체 경로**다

대륙 칩(`유럽 10` · `미주 6` · `대양주 5` · `그 외 4`)은 지도 왼쪽 위, 단계 버튼 바로 아래 줄에 둔다.

- **모바일에는 호버가 없다.** 칩 탭이 유일한 경로다(땅을 직접 탭해도 같은 동작).
- **키보드**로도 대륙을 고를 수 있어야 한다. 땅 조각은 `<path>`라 포커스 대상으로 부적절하므로
  **칩이 접근성 경로**다(§CH6 — 핀은 `aria-hidden`, 조작은 텍스트 요소로).
- 칩과 땅은 **같은 상태**를 가리킨다. 하나를 켜면 둘 다 켜진 모양이 된다.

#### 가까운 단계에서는 안 쓴다

`가까운 곳`·`조금 더 멀리`에서는 대륙 선택을 **넣지 않는다.**
그 단계는 이미 동아시아·동남아만 담고 있어 **대륙 하나가 화면을 거의 다 차지한다** — 좁힐 게 없다.
그 구간에서 좁히는 손잡이는 이미 있는 지역 칩(일본·중국·동남아·섬)이다.

---

### 미결
| # | 안건 | 메모 |
|---|---|---|
| **B20** | **핀 자체가 겹치는 경우** | 홍콩·선전·마카오가 `아주 멀리`에서 **1~2px 안에 스택**된다. 3개가 1개로 보이고 클릭하면 맨 위 것만 잡힌다. 라벨 규칙으로는 못 푸는 **별도 문제**. 다만 그 단계에서 이들은 **이전 거리대의 맥락**이고 `가까운 곳`에서는 충분히 떨어지므로 **긴급하지 않다.** 묶어서 표기할지 등은 추후 |

---

## §CH2 — 필터·정렬

### 확정
- **위치 = 지도 우측 하단 플로팅 도크**(헤더 바 아님). 기본 펼침, 헤더 눌러 접기. 줌 컨트롤은 우측 상단.
- **접었을 때는 개수가 아니라 조건을 보여준다** (2026-08-22 확정, B7)
  - `필터 · 2` ❌ → **`해변 · 50만 이하`** ⭕
  - 순서는 도크 안 순서와 같게: **날짜 → 분위기 → 예산**.
  - **최대 2개까지 쓰고 넘치면 `외 N`** — `이번 주말 · 해변 외 1`.
  - 아무것도 안 켰으면 그냥 **`필터`**.
  - 왜: 접힌 도크의 목적은 "지금 뭐가 걸려 있나"를 알려주는 것이다. **개수는 그 답이 아니다.**
- **데스크톱 도크 높이에 상한 — 넘치면 도크 안에서 스크롤** (2026-09-20 확정, B54)
  - 상한 = 무대 높이 − 거리 단계 버튼 아래끝 − 여백. **거리 단계 버튼과 도크 제목 줄은 어떤 높이에서도 가려지지 않는다.**
  - 제목 줄(`필터 ▾`)은 위에 고정, 조건 줄들만 스크롤. 모바일 펼친 도크(§CH5, `76vh` + 내부 스크롤)와 같은 방식.
  - 🔴 **잘린 줄이 있으면 잘렸다는 게 보여야 한다** — 도크 아래끝에 페이드(또는 그림자)로 「아래에 더 있음」을 알린다.
    예산 줄이 말없이 스크롤 아래로 숨으면 **필터가 둘뿐인 줄 안다.** 또 그 얼굴이다.
  - 기본 펼침은 그대로(낮은 화면에서 접힌 채 시작하는 안은 기각 — 펼치면 같은 문제가 남는다).
  - 라벨 회피는 여전히 「펼친 상태」로 재되, **상한이 적용된 높이**로 잰다 — 라벨 자리가 그만큼 돌아온다.
  - 도크 → 하단 시트 전환은 PH5에서 따로 본다. 이건 그 전까지의 최소 조치다.
- **도크·줌 컨트롤 영역은 라벨 배치에서 제외한다** (2026-08-22 확정, B16 부분)
  §CH1의 라벨 후보 4자리 중 **도크·줌 컨트롤 사각형과 겹치는 자리는 건너뛴다**(무대 밖 처리와 동일).
  도크는 **펼친 상태 기준**으로 고정 계산한다 — 접었다 펼 때마다 라벨이 재배치되면 산만하다.
- **날짜는 보편 프리셋만**: `아무때`(기본)·`이번 주`·`이번 주말`·`다음 달`·`날짜 지정`(달력).
  **시즌 한정 표현("광복절 연휴") 금지** — 지금만 유효해서 코드가 썩는다.
- **분위기 칩 = 6종, 단일 선택** (2026-08-22 확정, C-10)

  **`해변` · `도시` · `미식` · `자연` · `문화` · `온천`**

  - **필터 칩은 이 6종만.** 어휘 자체는 **24종**이고 나머지 18종은 **하위 태그**로 카드 표시에 쓴다
    (2026-08-22 확장 — `CONTRACT.md`, 배정안 `TAGS.md`).
  - **태그는 "사진이 큰 자리"에만 — 사진 위에 얹는다** ✅ 2026-09-01 확정

    | 자리 | 사진 | 태그 | 왜 |
    |---|---|---|---|
    | **작은 카드**(피드 본문) | 52~62px 썸네일 | ❌ **없음** | 사진이 태그를 담기엔 작다. 여기선 **어디·얼마·언제**만 읽으면 된다 |
    | **히어로**(오늘의 발견) | 92px 전폭 | ✅ 사진 위 2~4개 | "오늘은 여기"라는 답이라 **왜 여기인지**까지 보여준다 |
    | **확장 상세** | 100px 전폭 | ✅ 사진 위 2~4개 | 결정하는 자리. 개수가 곧 "즐길 게 얼마나 많은가"다 |

    태그가 세로를 안 먹으므로 **작은 카드가 129px → 103px**(-20%)이 되고,
    패널에 **히어로 아래 3.0장 → 3.8장**(+27%)이 들어간다. 근거는 `design/density.html`.

    > ⚠️ **잃는 것을 적어둔다.** 작은 카드에서 태그가 빠지면 "태그 개수 = 즐길 거리" 신호를
    > **스크롤 중에는 못 읽는다.** 그 신호는 이제 **히어로와 확장 상세에만** 남는다.
    > 밀도와 맞바꾼 것이고, 사용자가 알고 택했다.

    🔴 **판정 기준을 "카드 종류"가 아니라 "사진 폭"으로 바꾼다** (2026-09-03, B32 — 프론트 발견)

    위 표는 **데스크톱 62px 썸네일을 전제로** 쓰였다. 그런데 **모바일 390px에서는 피드 카드도
    사진이 전폭**이다. 카드 종류로 판정하면 사진이 전폭인데 태그가 없는 카드가 생긴다.

    **되돌아보면 원래 기준은 처음부터 사진 폭이었다.** 작은 카드에서 뺀 이유가
    "히어로가 아니어서"가 아니라 **"52~62px에 태그를 얹을 자리가 없어서"**였다.
    그리고 **태그를 사진 위에 얹기로 한 순간 세로 비용이 0**이 됐다 —
    사진이 크면 태그는 **공짜**다. 밀도와 맞바꿀 게 없다.

    | 사진 폭 | 태그 |
    |---|---|
    | **200px 미만** (데스크톱 썸네일 62px) | ❌ 없음 |
    | **200px 이상** (히어로 · 확장 상세 · **모바일 피드 카드**) | ✅ 사진 위 |

    - **200px인 이유**: 태그 하나가 약 46px이고 둘이면 간격 포함 ~100px이다.
      **사진 폭의 절반을 넘으면 사진이 아니라 태그 판이 된다.**
    - **개수는 폭에 따라 다르다** — 히어로·확장 상세는 최대 4개, **모바일 피드 카드는 최대 2개.**
      피드 카드는 스크롤로 지나가는 자리라 **읽을 수 있는 개수만** 얹는다.
    - 이 규칙이 §CH5(모바일 3단 시트)의 카드 높이 계산에 들어간다. **높이는 안 변한다** —
      태그가 사진 위라 세로를 안 먹는 게 이 규칙의 전부다.

  - **표시할 태그 고르기 = 하위 전부 + 상위 1개**(하위 없으면 상위 2개), **최대 4개.**
    **개수를 고정하지 않는다** — 태그 개수가 곧 "즐길 게 얼마나 많은가"라는 신호다
    (실측 2026-08-23: 2개 46곳 · 3개 35곳 · 4개 35곳). 하위는 반드시 상위를 동반하므로
    `야시장`을 보고 `미식` 필터를 눌러도 잡힌다 — "보이는 태그는 고를 수 있어야 한다"는 유지된다.
  - **오늘 딜에 뜬 67개 도시 중 65개가 서로 다른 태그 표시**를 갖는다(**97%**, 2026-08-23 재측정).
    겹치는 건 프놈펜/히로시마(둘 다 유적 도시)뿐이다. 이전엔 `해변` 하나만 붙은 도시가 10곳이었다.
  - **왜 그 6종인가 — 중복 측정 결과다.** 나머지 6종은 상위 태그에 거의 완전히 포섭된다:

    | 태그 | 건수 | 상위 태그와 겹침 |
    |---|---|---|
    | `휴양` | 25 | **`해변` 100%** — 정보량 0 |
    | `쇼핑` | 20 | **`도시` 95%** |
    | `유적` | 4 | **`문화` 100%** |
    | `야시장` | 2 | **`미식` 100%** |
    | `트레킹` | 2 | **`자연` 100%** |
    | `액티비티` | 5 | `해변` 60% · `휴양`/`자연` 40% |

  - **커버리지 100%**(104/104). 현행 5종은 96%였고 `델리`·`이스탄불`이 **어떤 칩에도 안 걸렸다.**
  - **`온천`을 뺐다가 넣었다**: 7건뿐이고 고유 기여도 2건이라 지웠다가, **"온천 가고 싶다"는 검색 의도가
    명확한 반면 `문화`는 모호**해서 되살렸다. 6종이면 둘 다 담을 수 있다.
  - **순서**는 감성 → 실용: `해변`이 여행 동기로 가장 강하다.
- **예산 슬라이더: 범위를 데이터에서 만든다** (2026-08-22 확정, B5/F11·C-9)

  | 항목 | 값 |
  |---|---|
  | `min` | **현재 출발지 최저가를 5만 단위로 올림** |
  | `max` | **현재 출발지 최고가를 5만 단위로 올림** |
  | `step` | 5만 (현행 유지) |
  | 초기값 | `max` (= 필터 꺼짐) |

  - **왜 고정 100만을 버리나**: 실제 최고가가 **1,918,759원**(부산→오클랜드)이라
    **상한 초과 9건을 걸러낼 수만 있고 되살릴 수 없었다.** 슬라이더를 최대로 올려도 안 돌아온다.
  - **`min`을 최저가에서 올림하는 이유**: 슬라이더를 맨 왼쪽까지 내려도 **최소 1건은 남는다.**
    조작만으로 0건을 만들 수 없게 한다.
  - **최대치 = 필터 꺼짐**(현행 `budgetOn()` 로직 유지). 이때 표기는 **`제한 없음`**.
    → 부수 효과로 `155만` 같은 **어색한 숫자가 화면에 안 나온다.**
  - **출발지를 바꾸면 슬라이더는 새 범위로 리셋**된다(= 최대치 = 필터 꺼짐). 딜이 통째로 바뀌므로 자연스럽다.
  - 참고 분포: 30만 이하 34% · 50만 이하 62% · **100만 이하 91%** · 중앙값 403,659원.
    분포가 앞쪽에 쏠려 있지만 **트랙이 실제 가격 범위를 그대로 나타내는 쪽**을 택했다(로그 눈금 기각 — §아래).
    (2026-09-03 확정 — 히스토그램도 선형.)
- **필터 = 숨김이 아니라 흐림(dim)** — 지도의 밀도·맥락을 유지하기 위해. 조건 밖은 저채도·저불투명.
- **필터를 켜면 거리 단계 무관 전 지역·전 등급에서 매칭.**
- **필터를 켜면 지도를 `아주 멀리` 뷰로 전환한다** (2026-08-22 확정, B6/F12)

  1. 전환은 **단계 전환과 같은 모션**(400ms · `cubic-out` · 배율 로그 보간 — §CH1).
  2. **단계바는 `전 지역에서 찾는 중` 한 줄로 바뀐다.** 버튼 3개를 비활성으로 두지 않고 **치운다** —
     필터 중에는 "거리 단계"라는 개념 자체가 성립하지 않는다.
  3. **필터를 끄면 버튼이 돌아오고 직전 단계로 복귀**한다(같은 모션).
  4. **핀은 전 도시를 그대로 그린다.** 조건 밖은 흐림(dim) — "숨김이 아니라 흐림" 원칙 유지.
  5. **⭐ 라벨은 매칭에게만 준다.** 조건 밖은 **흐린 점만** 남고 라벨을 받지 않는다.

  - **왜 뷰를 전환하나 — 밀도가 아니라 `F15`가 진짜 문제였다.**
    "전 지역 매칭"은 확정 스펙인데, **`가까운 곳` 단계에서 필터를 켜면 매칭 딜이 화면 밖에 있다.**
    실측(서울): `해변` 매칭 22건 중 화면 안이 **`가까운 곳` 10건 · `조금 더 멀리` 16건 · `아주 멀리` 22건.**
    피드엔 뜨는데 지도엔 없는 상태가 필터로 재현된다.
  - **왜 라벨만 걷어내나 — 밀도를 만드는 건 점이 아니라 글자다.**
    비매칭을 LOD로 추려 봐야 59개 → 44~49개로 **10~15개밖에 안 줄어든다**(실측). 효과가 없다.
    반면 라벨을 매칭에게만 주면 **매칭 라벨이 거의 다 살아남는다**:

    | 조건 | 매칭 | 라벨 생존 |
    |---|---|---|
    | `해변` | 22 | **22/22 (100%)** |
    | `자연` | 14 | **14/14 (100%)** |
    | `문화` | 16 | **16/16 (100%)** |
    | `도시` | 32 | 27/32 (84%) |
    | `미식` | 13 | 12/13 (92%) |

  - 라벨 배치 순서는 §CH1 규칙 그대로(`major` → 가격 낮은 순), **대상만 매칭으로 좁힌다.**
- **피드는 매칭 상단 정렬** — 매칭이 위로, 안 맞는 건 흐린 채 아래로.
- **필터 시 피드 헤더는 `조건에 맞는 {N}곳`** — 기존 `전 지역에서 찾는 중`은 **단계바로 옮겼다**(위).
  두 곳에 같은 문구를 두지 않는다. 어떤 조건인지는 도크가 보여준다(아래 B7).
- **정렬 토글 3종**(피드 상단, 필터와 별개): **가성비순(기본)** · 임박순 · 할인율순.
  - **"신선순"을 4번째로 추가하지 않는다.** 신선도는 사용자의 욕구가 아니라 우리 품질 문제다(§CH3).

### 현행
| 항목 | 값 |
|---|---|
| 분위기 칩 | 해변 · 도시 · 온천 · 자연 · 미식 (단일 선택) |
| 예산 슬라이더 | ⚠️ **교체 대상** — 현행 `min 100000 / max 1000000 / step 50000`. 상한이 실제 최고가(191만)보다 낮다 |
| 필터 활성 판정 | `mood \|\| dateMode \|\| budget < BUDGET_MAX` |
| 모바일 | `width ≤ 860`이면 접힌 채 시작 |

### 미결
| # | 안건 | 메모 |
|---|---|---|
| **B16** | **필터 도크가 핀을 가린다** | ⚠️ **부분 해소.** 라벨은 도크 영역을 피하도록 확정했으나, **핀 자체가 도크 뒤로 들어가는 건 남는다**(far 뷰에서 시드니·오클랜드·브리즈번 부근). 자유 줌·팬이 없어 **사용자가 피할 방법이 없다**는 게 문제의 핵심이다. 지도를 축소해 피하는 건 대가가 크다(무대 폭에서 도크 300px을 빼면 배율 상한이 195 → 141로 떨어진다). **PH5에서 도크가 하단 시트로 바뀔 때 함께 재검토** |

---

## §CH3 — 카드 피드 · 큐레이션

### 확정
- **"오늘의 발견" = "오늘 출발"이 아니라 "오늘 큐레이션한 딜"**. 출발일은 딜마다 다르다(이번 주말~수개월).
  → **카드에 날짜가 없으면 판단 불가**하므로 날짜 표기는 필수.
- **날짜 표기 = 두 단 위계** (2026-08-22 재설계 — 가독성)

  ```
  [다음 달]  3박4일          ← 스캔용: 언제쯤 · 며칠 (--ink 800)
  9/12(토)~9/15(화)          ← 확인용: 정확한 날짜 (--sub 700, 작게)
  ```

  - **왜 나눴나**: 한 줄에 몰면 `9/12(토)~9/15(화) · 3박4일`처럼 숫자·괄호·물결·가운뎃점이 뒤섞여 시끄럽다.
    스캔 단계에서 필요한 건 **"언제쯤, 며칠"**이고 정확한 날짜는 **마음이 기운 뒤에** 본다.
    줄이 하나 늘지만 **각 줄이 짧아져 더 빨리 읽힌다.**
  - **상대 라벨 배지는 코랄 유지**(2026-08-22 사용자 선택). 코랄이 날짜 배지·가격·도장 셋에 걸리지만
    **도장이 기울어져 있고 14%에만 붙어** 형태로 구분된다. 실화면에서 시끄러우면 그때 조정한다.
  - ⭐ **배지와 태그는 형태로 가른다** — 셋이 전부 알약이라 구분이 안 됐다(사용자 지적).
    **배지 = 알약 + 채움**, **태그 = 각진 사각(radius 5) + 테두리만**. 자세히는 `DESIGN.md`.
  - **기각**: 화살표형(`9/12 토 → 9/15 화`) — 괄호가 빠져 깔끔하지만 `→`가 **편도로 오해**될 수 있다.
  - 상대 라벨은 `CONTRACT.md`의 `when`, 기간은 `nights`를 그대로 쓴다.
- **hero 카드**: 배지 문구 **"진짜 갈래말래?"**(브랜드명을 인터랙션으로). 정렬을 바꿔도 hero는 고정.
- **hero 선정 = 할인율 최고 → 가격 낮은 순 → 신선한 순** (2026-08-22 수정, F16)
  - **신선도를 게이트에서 3순위 타이브레이커로 강등한다.** 원래는 `fresh`(<24h) 중에서만 뽑았다.
  - **왜 게이트를 버리나**: `seen` 실투입 후 재보니 방문 시점 기준 **`fresh`가 3%뿐**이다
    (2026-08-22 03:06 기준 fresh 3% · ok 45% · stale 52%). 이건 고장이 아니라
    **하루 한 번 수집하는 구조에서 다음 수집 직전에 방문한 정상 모습**이고, 분포는 하루 주기로 오르내린다.
    즉 **하루 중 상당 시간 hero 후보가 0~1건**이 되어 게이트가 사실상 "아무거나"를 뽑는다.
  - **판단 기준은 "가장 나쁜 시간대에도 hero가 뽑히는가"**여야 한다. 할인율 기준은 항상 뽑힌다.
  - 신선도는 **배지가 이미 정직하게 말한다**(§CH3 신선도 배지). hero 선정까지 이중으로 막을 필요가 없다.
  - 실측 hero(출발지별): 서울 `가오슝` 36% · 부산 `마쓰야마` 43% · 대구 `푸켓` 31% · **제주 `이스탄불` 11%**.
    ⚠️ 이 값은 **전체 딜 기준**으로 잰 것이다 — `아주 멀리`에 해당한다. 아래 §후보 범위 참고.

- 🔴 **hero 후보 = 지금 무대에 있는 딜** (2026-09-04 확정, B34 — 프론트 질문)

  거리 단계는 **뷰가 아니라 집합을 바꾼다**(§CH1 "여는 거리대"). 피드도 같이 걸린다.
  따라서 **hero도 그 집합 안에서 뽑는다.** 단계를 옮기면 hero가 바뀌는 게 정상이다.

  | | 집합이 바뀌나 | hero |
  |---|---|---|
  | **정렬 변경**(가성비·임박·할인율) | ❌ 같은 집합을 재배열 | **고정** |
  | **거리 단계 변경** | ✅ 집합 자체가 달라짐 | **다시 뽑는다** |
  | **필터 변경**(날짜·예산·분위기) | ✅ 집합이 달라짐 | **다시 뽑는다** |

  - **왜 전체 기준이 아닌가**: hero는 **피드의 첫 장**이다. 아래 목록에 없는 카드가
    맨 위에 있으면 목록이 아니다. 그리고 그 딜의 핀이 무대 밖이면 **F15 위반**이다 —
    "피드에 있는 딜은 지도에서 찾을 수 있어야 한다".
  - **단계가 누적이라 `아주 멀리`에서는 전체 기준과 같아진다.** 갈리는 건 좁은 단계뿐이고,
    거기서는 **좁힌 사람의 의도가 이긴다.**
  - 실측으로 갈리는 예(2026-09-04, 딜 128건): 대구 `이스탄불 23%` → **`제주 18%`**,
    제주 `하노이 37%` → **`상하이 16%`**. 서울·부산은 양쪽이 같다.
  - ⚠️ **좁힌 집합에서는 hero 할인율이 낮아진다**(제주 16%). 정상이다 —
    위에 이미 적었듯 **hero 배지는 "오늘 우리가 고른 한 장"이지 "할인 최고"라는 약속이 아니다.**
  - ⚠️ **제주처럼 최고 할인율이 도장 임계값(15%)에 못 미치면 hero에 도장이 없다. 정상이다.**
    hero 배지는 "오늘 우리가 고른 한 장"이지 "할인 최고"라는 약속이 아니다.
- **"갈래" 버튼만 둔다. "말래"는 없다.** 실수로 누르면 카드가 사라져 되돌리기 어렵다(파괴적).
  `갈래 → 자세히 보기`만. "말래"는 배지 문구로만 산다.
- **"왜 지금" 훅 줄을 없앤다** (2026-08-22 확정 — F18/F17/C-12, 2026-08-01 결정 뒤집음)

  훅의 목적은 "왜 지금 이걸 봐야 하나"인데, **우리 데이터로 그 답을 할 수 있는 건 할인율뿐이고
  그건 딜의 14%에만 있다.** 나머지 86%에 훅을 만들려다 **없는 근거를 지어냈다** —
  `경유로 확 싸진 특가`가 피드의 **54%**에 붙어 있었는데 `deals.json`에 **직항 대비 가격이 없다.**

  → 훅 줄을 **제거**하고, 그 자리에 있던 사실을 각자 제자리로 보낸다:

  | 사실 | 어디로 |
  |---|---|
  | 할인율 | **도장** (아래) — 있을 때만 |
  | 직항 / 경유 횟수 | **날짜 줄에 상시 표시** (아래) |
  | 날짜·기간 | 이미 날짜 줄에 있음 |

  **훅이라는 자리를 없애면 없는 주장을 할 자리도 없어진다.** F18은 문구를 고쳐서가 아니라
  자리를 없애서 푼다.

- **직항/경유는 카드에 상시 표시하되 자리는 `가격 줄 우측`** (2026-08-22 확정, 재배치)
  - `218,400원~` 오른쪽 끝. **날짜 줄·태그 줄에는 두지 않는다.**
  - ⚠️ **처음 스펙은 날짜 줄과 태그 줄 양쪽에 두어 중복이었다**(내 오류). 자리는 하나다.
  - **왜 가격 옆인가**: 사용자가 실제로 저울질하는 쌍이 **"이 값에 직항이냐"**다.
    그 자리가 비어 있었고, 가격 옆이라 반드시 읽힌다.
  - **왜 태그 줄이 아닌가**: 태그는 **"어떤 곳인가"**, 교통은 **"어떻게 가는가"**다.
    섞이면 배지가 태그처럼 보여 성격이 흐려진다(사용자 지적).
  - **왜 날짜 줄이 아닌가**: 여정 정보끼리 묶이는 건 맞지만 **줄이 너무 길어진다**(카드 폭 340px).
  - **지금 카드에 직항 여부가 전혀 없다.** 구매 결정에 가장 중요한 사실 중 하나인데 빠져 있었다.
  - 실측: 직항 45건(43%) · 경유 1회 50 · 2회 8 · 3회 1.
- **직항 배지는 중·장거리에만** (2026-08-22 확정)

  **자리는 하나(가격 줄 우측), 무게는 셋.**

  | 경우 | 오늘 | 표시 |
  |---|---|---|
  | **중·장거리 직항** | 25건 (20%) | **`✈ 직항` 배지** — `--coast` 채움 + 흰 글자 + 비행기 기호. 중거리 36%·장거리 14%만 직항이라 **드물다** |
  | 근거리 직항 | 27건 | **`직항` 텍스트**(`--sub`) — 근거리는 **75%가 직항이라 당연하다** |
  | 경유 | 59건 | **`경유 {N}회` 텍스트**(`--sub`). **배지 아님** — 장점이 아닌 걸 자랑 형식에 담지 않는다 |

  - **규칙 한 줄: 배지는 드문 좋은 소식에만. 모두가 가진 것은 배지가 아니다.**
  - **경유를 배지로 만들지 않는다.** 장점이 아닌 것을 자랑 형식에 담으면 시각 언어가 거짓말을 한다.
    감추지도 않는다 — 날짜 줄에 또렷이 적는다.
  - 결과: 배지 **0개 72% · 1개 24% · 2개 4%**. 배지가 붙은 카드가 실제로 눈에 띈다.

- **할인 도장 = 3단계 티어** (2026-08-22 확정 — F17/F19)

  | 티어 | 조건 | 성숙 15일 하루 평균 | 시각 (자세히는 `DESIGN.md`) |
  |---|---|---|---|
  | **T1** | `15~27%` | — | 코랄 **테두리** · 흰 배경 · -7° |
  | **T2** | `28~41%` | **7.3%** | **채움**(코랄 배경 + 흰 글자) · 더 큼 · -8° · 그림자 |
  | **T3** | `42%+` | **1.7%** | **그라디언트 + 흰 링 + 후광 + 광택 sweep** · 가장 큼 · -9° · **썸네일 위로 떠오름** |
  | 없음 | `<15%` | — | 도장 없음 |

  임계 `15/28/42` (2026-09-01 확정 — 30일 분포로 정했다. 경위와 기각한 값은 `DECISIONS.md` 2026-09-01).
  **재는 자를 고치기 전에 눈금을 옮기지 않는다.**

  > **트리거**: 백엔드 BB4(중앙값 기간 창) 정리 후 30일 분포를 다시 받는다.
  > 그때 **T3가 피드의 2%를 꾸준히 넘으면** 희소성이 무너진 것이므로 임계값을 재검토한다.

- ⏳ **확정 · 반영 전**(2026-09-22 전수 대조에서 화면에 없음이 확인됨) **신기록(`low` 대비) — 한 카드는 "왜 싼가"를 한 번만 말한다** (2026-09-01 확정 — `discover.js` 에 `low`·`obs_days` 0건. **순서 2**)

  도장과 신기록은 **다른 자를 쓴다.** 도장은 `median`(평소 시세) 대비, 신기록은 `low`(이전 관측 최저) 대비다.
  둘 다 사실이지만 **사용자는 그 차이를 알 방법이 없다.** 한 카드에 "싸다"가 둘이면 같은 말을 두 번 하는 걸로 읽힌다.

  | 자리 | 무엇을 보이나 | 왜 |
  |---|---|---|
  | **피드 카드** | **도장 우선, 없으면 신기록.** 정확히 하나 | 스캔하는 자리다. 주장이 둘이면 둘 다 흐려진다 |
  | **확장 상세** | **둘 다, 나란히** | 결정하는 자리다. 공간이 있어 **각각 무엇을 재는지** 드러낼 수 있다 |

  - **우선순위가 도장인 이유**: 도장은 **정량적이고 시각적**이라(`41%↓`) 스캔 중에 즉시 읽힌다.
    신기록은 문장이라 읽어야 한다.
  - **자격 = 조건 넷을 모두 만족.**
    ```
    low is not None  AND  obs_days >= 14  AND  price < low  AND  (1 - price/low) >= 0.05
    ```
    ✅ **하한선 14일 · 낙폭 5% 확정** — 아래 근거.

    > ⚠️ **낙폭 5%를 한 번 빠뜨렸다가 백엔드가 잡아줬다**(2026-09-01).
    > 내가 `(B)` 정의 때 직접 건 조건인데 재측정 판정식에서 누락했다.
    > **왜 필요한가**: `0.8% 더 쌈`은 기술적으로 신기록이지만 사용자에겐 **같은 가격**이다.
    > 그런 걸 "최저가예요"라고 말하면 **문구 전체의 신뢰가 깎인다.**
  - 🔴 **신기록은 절대 `%`로 쓰지 않는다.** `N일 중 최저`로만 쓴다.
    프론트가 짚은 문제다 — 같은 카드에 `41%↓`와 `16.8%↓`가 나란히 있으면
    **뒤엣것이 초라해 보이는데 실제로는 더 희귀한 사건**이다. 기준이 달라(중앙값 대비 vs 최저가 대비)
    구조적으로 신기록 쪽 숫자가 작게 나오기 때문이다.
    **숫자를 안 쓰면 이 왜곡이 아예 생기지 않는다** — `41%↓`(크기)와 `20일 중 최저`(시점)는
    다른 종류의 사실이라 나란히 놓아도 서로를 깎지 않는다.
  - **형태로 가른다**(`DESIGN.md`): 도장은 **기울어진 사각형**, 신기록은 **기울기·테두리 없는 코랄 텍스트**.
    같은 자리(도시명 줄 우측)를 쓰지만 **둘이 동시에 뜨는 일이 없으므로** 혼동하지 않는다.

  **`obs_days` 하한선 — 14일로 확정** (2026-09-01, 실측 126건)

  | `obs_days` 하한 | 자격 | 신기록(낙폭 조건 없이) | **+ 낙폭 5%** |
  |---|---|---|---|
  | 7일 | 91건 | 28건 (22.2%) | 10건 (7.9%) |
  | **14일** | **70건** | **22건 (17.5%)** | **7건 (5.6%)** ← 확정 |
  | 21일 | 22건 | 6건 (4.8%) | 1건 (0.8%) |

  최종 노출량은 **7건(5.6%)**이다. 도장(20%)보다 드물어야 맞다 —
  "이보다 싼 적이 없다"는 도장보다 강한 말이기 때문이다.

  - **7일은 약하다.** 항공권 가격은 주 단위로 움직이는데 7일은 **사이클 하나**뿐이다.
    "7일 중 최저"는 사실이지만 거의 아무 말도 아니다.
  - **21일은 지금 데이터로 너무 이르다.** 이력이 최대 29일이라 **자격 자체가 38건(30%)**으로 줄어든다.
    데이터가 어려서 생긴 제약을 규칙으로 굳히면 안 된다.
  - **14일은 주말 사이클 둘을 덮는다.** `obs_days` 중앙값이 17일이라 대부분의 노선이 자격을 얻는다.
  - 동률(`price == low`)은 **0건**이라 `<` / `≤` 차이가 없다. **`<`(엄격)으로 적는다** — 타이기록은 신기록이 아니다.
  - 🔒 **`N`은 30을 넘지 않는다.** 백엔드가 `median`에 30일 창을 걸면서(BB4) `obs_days` 상한이
    **25일**이 됐다. "N일 중 최저"의 N이 무한정 커지지 않으므로 문구 길이가 안정적이다.

  **신기록이 도장을 대신하는 9건** — 도장이 못 하는 말을 한다.

  ```
  푸켓      339,655원  할인  2%  26일 중 최저   (이전 341,097원)
  타이베이   193,917원  할인  9%  25일 중 최저   (이전 201,804원)
  후쿠오카   132,370원  할인  8%  24일 중 최저   (이전 139,851원)
  다낭      180,547원  할인  9%  21일 중 최저   (이전 189,581원)
  ```

  > **이게 신기록을 남기는 이유다.** 할인율만 보면 평범한 카드인데 **"이보다 싼 적이 없다"**는
  > 전혀 다른 정보다. 도장 없이 신기록만 있는 9건이 없었다면 신기록은 도장의 중복이었을 것이다.

  - **문구는 티어와 무관하게 같다**: `평소보다 {N}%↓`. 숫자와 시각이 계단을 말한다.
  - ⏳ **조건부 강화 (데이터 대기 — 생각보다 가깝다)**
    `역대급` 같은 최상급은 지금 쓸 수 없다(역대 데이터가 계약에 없다). 대신 **검증 가능한 강한 문구**를 준비한다:

    > **`{N}일 중 최저가예요`**

    `역대급`보다 **약해 보이지만 실제로는 더 강하다** — 확인 가능한 사실이라서다.
    그리고 **서비스가 오래될수록 문구가 저절로 세진다**(12일 → 30일 → 90일 중 최저).
    - **필요 필드**: 관측 기간 최저가 + **관측 일수**(며칠을 봤는지 말할 수 있어야 한다).
    - **데이터는 이미 쌓이는 중이다**: `broad_offers` **23일치**(2026-07-29~08-21),
      (허브,도시) 조합당 관측 일수 **중앙값 12일 · 최대 21일**.
    - **관측 일수가 짧으면 쓰지 않는다.** 하한선은 필드를 받은 뒤 실측으로 정한다(잠정 7일).
    - **PH4 재검토 결과**: `B9`(스파크라인)는 **보류**하고 **요약값 두 필드만** 요청한다(§CH4).
      시계열을 계약에 실으면 무거운데, 문구에는 요약값이면 충분하다.
  - **T3는 매일 나오지 않는다.** 오늘 서울 36%·부산 43%는 있지만 **제주는 최고가 11%라 도장이 하나도 없다.**
    **그게 희소성이 작동한다는 뜻**이다 — 매일 뜨는 배지는 배지가 아니다.
  - `특가`라는 단어를 **쓰지 않는다**(F19 — 아래).
  - **임계값 미만이면 도장이 없다.** 카드 우상단이 비는 건 정상이다 —
    날짜 줄에 직항/경유가 추가되므로 카드의 정보량은 오히려 늘어난다.
  - 실측 분포: `≥10%` 24% · **`≥15%` 14%** · `≥20%` 11% · `≥25%` 6% · `≥35%` 2%.
    → 15%면 **일곱 장 중 한 장**에만 붙어 도장이 실제로 눈에 띈다. 지금은 **100%에 붙어 무의미**했다.

- **발견 홈에서 "특가"라는 단어를 쓰지 않는다** (2026-08-22 확정 — F19)
  - 제품 안에 "특가"의 정의가 둘이었다: 발견 홈 `discount ≥ 25`, 노선 페이지·알림 `중앙값의 65%`(=`35%↓`).
  - **임계값을 맞추는 대신 단어를 나눴다.** 둘은 재는 대상이 다르다 —
    노선 쪽은 **그 노선 시세의 급락(사건)**, 발견 홈은 **오늘 최저가와 평소 시세의 편차**다.
    같은 이름을 붙인 게 잘못이지 같은 임계값을 써야 하는 게 아니다.
  - 발견 홈은 **`평소보다 N%↓`라는 서술만** 쓴다. "특가"는 노선 페이지·알림 메일에만 남는다.
- **양방향 연동**: 카드 hover → 항로 + 핀/플로팅 카드 켜짐. 핀 hover → 카드 하이라이트(스크롤). 활성은 항상 1개.
- **커버리지는 넓게, 표시는 데이터 게이팅**: 목적지 사전(`dests.py`)은 지방 소도시까지 넓게 유지하되,
  **그날 유효한 최근 최저가가 있는 도시만 핀·카드로 낸다**(`CONTRACT.md` 데이터 정책).
  얇은/계절 노선은 특가가 뜬 날만 등장하므로 **자동으로 "갈 만한 곳만" 남는다.** 이게 롱테일을 유지하면서
  화면이 지저분해지지 않는 유일한 방법이다.

### 신선도 배지 (2026-08-22 확정)
`seen`(소스가 그 가격을 마지막으로 관측한 시각, `CONTRACT.md`) 기준 **3등급**. **방문 시점에 브라우저가 계산**한다.

| 등급 | 나이 | 문구 | 시각 |
|---|---|---|---|
| `fresh` | < 24h | <3h면 **`방금 가격`**, 아니면 **`N시간 전 가격`** | 코랄 점 + `--ink` 700 |
| `ok` | 24~72h | **`어제 가격`** / **`N일 전 가격`** | 점 없음, `--sub` 500 |
| `stale` | > 72h | **`N일 전 가격`** | 점 없음, `--sub` 500, opacity .7 |
| — | `seen` 없음 | `스캔 시점 기준` | `--sub` 500 |

🔴 **`확인` → `가격`으로 바꿨다** (2026-09-04, 백엔드 실측 뒤)

원래 `N일 전 확인`이었다. **문제는 주어다** — "확인"은 동작이라 **누가 확인했나**를 부르고,
읽는 사람은 그걸 **우리**로 읽는다. `5일 전 확인`이 화면의 72%에 뜨면
**"이 사이트는 5일에 한 번 일한다"**가 된다.

**사실은 우리 게으름이 아니다.** 나이는 두 성분이다(백엔드 분해, 2026-09-04):

| | 크기 | 우리가 줄일 수 있나 |
|---|---|---|
| ① **소스 캐시** — 받는 순간 이미 오래된 값 | 중앙 약 45~51h | ❌ **못 줄인다.** 캐시는 남들이 검색해야 갱신된다 |
| ② **피드 창**(`STALE_DAYS=3`) | 최대 72h | ⚠️ 줄일 수 있지만 **1일로 조이면 딜의 2/3가 사라진다** |
| ③ 구조적 상한 | **168h = 정확히 7일** | |

**절반은 소스, 절반은 우리 선택**이고 둘 다 이유가 있다. `N일 전 가격`은 **가격의 나이**를
말할 뿐 **누가 게을렀나를 말하지 않는다.** 사실은 그대로고 누명만 빠진다.

⚠️ **`SEEN_MAX_DAYS=7`은 죽은 가드다.** 상한이 정확히 7일이라 잘라낼 것이 원리상 없다
(생성 시점 최대 6.82일 vs 컷 7일 — **0.18일 차이로 안 걸린다**). **"안전장치가 있다"고 믿으면 안 된다.**

- **부착 위치 = 새 요소가 아니라 기존 가격 라벨의 치환**: `발견가 · 스캔 시점` → `발견가 · 어제 확인`.
  신선도는 딜이 아니라 **가격의 속성**이고, 카드는 이미 포화 상태다.
- **신호등(빨강·노랑) 금지.** 등급 차이는 **명도·굵기·불투명도로만** 낸다.
- 정렬 토글·필터·핀 인코딩에는 **반영하지 않는다.** 동점 타이브레이커로만 쓴다.
- 시각 확인: `design/freshness.html`

### 현행
| 항목 | 값 |
|---|---|
| 피드 폭 | 좌 340px (모바일 하단) |
| 헤더 | `오늘의 발견` + `{출발지} 출발 · N곳` / 필터 시 `전 지역에서 찾는 중` |
| 정렬 기본 | `value`(가성비순) |
| 빈 상태 | 필터 0건만 구현: "이 조건엔 딜이 없어요 — 조건을 바꿔보세요" |
| 사진 | 태그별 그라디언트 + "사진 준비중" (실사진 미도입) |

### 빈 상태 · 희소 상태 (2026-08-22 확정 — F1/F2)

**딜 0건(`origins`가 빈 객체)** — 화면0에 핀도 안내도 없는 **막다른 길**이었다.
- 한국 지도 자리를 **안내로 교체**한다: `오늘은 조용하네요` + `수집한 특가가 없어요. 내일 아침에 다시 채워집니다.`
  ("내일 아침"은 사실이다 — 크론이 매일 07:10 KST에 돈다)
- **노선별 페이지 링크를 함께 준다.** 그날 발견 홈이 줄 게 없어도 **26개 노선 시세는 살아 있다.**
  (덤으로 `IA.md` IA-1의 부분 해소 — 홈에서 노선으로 가는 첫 링크가 생긴다)
- **생성 쪽 방어는 백엔드가 맡는다**(`BE1`/`BB1` — 수집 실패 시 이전 산출물 유지).
  실측상 **4일 연속 수집 실패**라야 이 화면이 뜬다. 이건 **마지막 방어선**이다.

**딜 희소(10건 미만)** — 대구 9건 · 제주 7건 (서울 65 · 부산 35). *2026-08-23 재측정, 판정선 10건 유효.*
- **"적은 게 비정상"으로 읽히지 않게** 사실만 적고, **사용자가 할 수 있는 행동**을 알려준다.
- 숫자는 **그날 데이터에서 계산**한다. 최다 허브를 고정으로 박지 않는다.

🔴 **안내는 두 단계다 — 출발지를 바꾸라고 하기 전에 거리를 넓히라고 한다** (2026-09-04 개정, B37)

원래 안내가 `{출발지} 출발은 오늘 {N}곳이에요` 하나뿐이었는데, **헤더의 `{출발지} 출발 · N곳`과
숫자가 달라 모순으로 읽혔다.** 제주에서 헤더는 `3곳`(지금 무대), 안내는 `9곳`(출발지 전체)이었다.
**둘 다 맞는 숫자인데 나란히 있으면 하나는 틀린 것처럼 보인다.**

원인은 B34와 같다 — **거리 단계가 집합을 바꾸는데** 안내만 전체 기준을 쓰고 있었다.

| | 조건 | 문구 |
|---|---|---|
| **1순위** | 무대 안 `N` < 출발지 전체 `M` | **`더 멀리까지 보면 {M}곳이에요.`** |
| **2순위** | 전체 `M`도 10건 미만 | `{출발지} 출발은 오늘 {M}곳이에요.` + `{최다 허브}에서 출발하면 {K}곳까지 늘어나요.` |

- **1순위가 먼저인 이유**: 제주 3곳(가까운 곳) / 전체 9곳이면 **사용자가 할 수 있는 진짜 행동은
  거리를 넓히는 것**이다. 출발지를 바꾸라는 건 **여행 계획 자체를 바꾸라는 말**이라 훨씬 무겁다.
  **가벼운 해결책을 먼저 제안한다.**
- **모순이 사라진다**: 1순위 문구는 `{M}`을 **"더 멀리까지 보면"이라는 범위와 함께** 말하므로
  헤더의 `N`과 다른 게 당연해진다. 숫자 둘이 나란히 있어도 각자 무엇을 세는지 읽힌다.
- ⚠️ **판정선(10건)은 출발지 전체 `M` 기준 그대로**다. 무대 기준으로 재면
  `가까운 곳`에서 서울조차 희소로 잡힌다.
- **헤더 `{출발지} 출발 · N곳`의 `N`은 지금 피드에 있는 수**다(무대·필터 반영). 피드와 헤더가
  다른 수를 말하면 그게 진짜 모순이다.

### 미결
| # | 안건 | 메모 |
|---|---|---|
| **EV-1** | **시즌 이벤트 (벚꽃·단풍·눈축제)** | 사용자 제안. **태그가 아니라 이벤트로** — 팝업·카드 스타일·별도 페이지 중 택. ⚠️ **"지금 벚꽃이 예쁘다"는 개화 데이터가 없어 못 하는 주장**이지만, **도시별 공식 축제 일정(삿포로 눈축제 2월 초·도쿄 벚꽃축제 등)은 사실**이라 그걸 근거로 삼는다(사용자 지적). 우리가 아는 `dep`(출발일)로 "그 시기 출발 딜"을 묶는 형태. **지금 만들면 10~11월까지 아무도 못 봐 코드가 썩는다** → **10월 초 단풍 시즌 직전 착수** | **PH7 (후순위)** |
| — | 피드 카드 최대 개수·무한스크롤 여부 | 현행 전량 렌더 |

---

## §CH4 — 확장 상세 · 전환

### 확정
- **전환은 외부 직행이 아니라 사이트 안에서 여는 상세.** **예약 CTA만** 외부 제휴로 나간다.
  - 왜: 우리 자산은 신뢰다. 확인 없이 외부로 던지면 "가격 다르다"는 배신감이 그대로 이탈이 된다.

- ✅ **여는 방식 = 지도 위에 띄우되 지도가 그 핀으로 미끄러진다** (2026-09-01 확정)

  > ⚠️ 이 절에 "카드가 **그 자리에서** 확장된다"고 적혀 있었으나 **바뀌었다.**
  > 원래 의도는 "외부로 안 나간다"였고 그 원칙은 그대로다. **여는 위치만 바뀐다.**

  **동작 순서** — 목업 `design/open.html`(실제로 눌러볼 수 있다)

  | 순서 | 무엇이 | 시간 | 곡선 |
  |---|---|---|---|
  | 1 | **지도가 그 핀으로 미끄러진다** | 620ms | `cubic-bezier(.22,.61,.36,1)` |
  | 1 | 핀이 커지고 이름표가 뜬다 | 300ms | 이동과 **동시** |
  | 1 | 서울에서 **항로가 그려진다** | 500ms | `stroke-dashoffset` |
  | 2 | **카드가 핀에서 피어난다** | **+300ms 뒤** 340ms | `cubic-bezier(.2,.9,.3,1.2)` |

  - **왜 300ms를 기다리나**: 지도가 아직 움직이는 중에 카드가 뜨면 **카드가 미끄러지는 것처럼**
    보인다. **시선이 목적지에 도착한 뒤** 열어야 "여기다"가 된다. 전체 체감 **0.9초**.
  - 🔴 **`prefers-reduced-motion`이면 전부 `0ms`** — 이동도 등장도 즉시.
    큰 화면 이동이라 어지럼증을 유발할 수 있다. **선택이 아니라 필수다.**
  - **배율은 안 건드린다.** 거리 단계가 뷰를 정하는 모델이라(§CH1) 확대까지 하면 그 모델이 깨진다.
    **이동만** 한다.
  - **핀을 무대 아래쪽 72%·가로 46%에 놓는다**(2026-09-02, 예약처 5곳 반영). 카드가 핀 **위로** 피어나므로
    가운데에 두면 카드가 무대 밖으로 넘친다.
  - **이동량이 클 때**(다낭 → 뉴욕) 620ms가 짧을 수 있다 → 거리에 따라 **420~760ms** 가변 제안.
    짧은 이동이 느리면 굼떠 보인다.
  - **이동 중 다른 카드를 누르면 진행 중인 이동을 가로챈다.** 큐에 쌓아 순서대로 가면 안 된다.
  - 목업은 `translate` 선형 근사다. **실제는 `d3.geoEquirectangular`의 `rotate`/`center` 보간**이
    된다 — 느낌은 같고 구현은 프론트가 정한다.

  **열고 닫기**

  | 동작 | 결과 |
  |---|---|
  | 카드를 누른다 | 위 순서대로 · `pushState` `#{허브}-{목적지}` |
  | **핀을 누른다** | 같은 동작 + **패널이 그 카드로 스크롤**해 강조 — **양방향 대칭** |
  | 다른 카드/핀 | 먼저 것이 닫히고 새 곳으로 미끄러진다 **(동시에 하나만)** |
  | `×` · 같은 것 다시 · 빈 지도 | 카드만 닫힌다. **지도는 그 자리에 둔다** — 되돌리면 방금 온 길이 무의미해지고 멀미가 난다 |
  | 브라우저 뒤로가기 | **사이트를 안 떠나고** 상세만 닫힌다 · `#{허브}` |
  | 정렬·필터 변경 | 닫는다. **지도는 그대로** |
  | 거리 단계 변경 | 닫는다. 뷰가 통째로 바뀌므로 **이동 상태도 초기화** |
  | 딥링크 진입 | 단계를 딜의 `haul`에 맞추고 **이동 없이** 그 위치에서 시작 — 첫 화면부터 움직이면 어지럽다 |
  | 그 딜이 오늘 없다 | 출발지만 적용 + 안내(**F5**). 다른 허브에 같은 목적지가 있으면 제안 |
  | **모바일**(≤860px) | 이동 없이 **하단 시트**. 화면이 좁아 카드가 지도를 다 덮는다 — 현행 분기 유지 |

- ✅ **상세 안의 사진에는 태그를 사진 위에 얹는다** (2026-09-01 확정, §CH3의 통일 규칙)
- **비교 패널은 중립**: 예약처 버튼을 **전부 동등한 흰 버튼**으로. 특정 사이트만 코랄 강조 금지.
  `tag` 필드(`전체 비교` 등)는 **표시하지 않는다**(데이터는 유지).
  - 왜: "중립 비교"라면서 하나만 강조하면 강조 자체가 중립성을 깨뜨린다.
- ✅ **`links`는 5개 고정이다** (2026-09-02 백엔드 실측 확인)

  ```
  스카이스캐너 → 네이버 항공권 → 구글 항공권 → Trip.com → Aviasales
  ```

  - **117건 전부 5개**, 편차 없음. 순서도 고정이고 **Aviasales가 항상 마지막**이다
    (한국어 4곳이 앞, 영어가 뒤라 순서 자체가 자연스럽다).
  - **마커는 Aviasales 링크에만 붙는다.** 나머지 4곳은 순수 검색 링크다.
  - ⚠️ **마커가 빠지면 4개로 준다.** **배열 길이를 하드코딩하지 말 것** — 5개는 지금 사실이지 계약이 아니다.

- 🔴 **상세 카드 높이 — 5개가 되면서 규칙을 고친다** (2026-09-02)

  링크가 3 → 5개가 되며 **상세가 약 `432px`**가 됐다(+52px).
  기존 규칙(**핀을 무대 아래쪽 62%**)이면 무대 640에서 카드 위쪽이 **`-35px`, 즉 넘친다.**

  | | 규칙 |
  |---|---|
  | **핀 위치** | 무대 높이 × **`0.72`** (기존 0.62) |
  | **카드 최대 높이** | **핀 y − 16px** |
  | 넘치면 | **카드 안에서 스크롤** (`overflow-y:auto`) |

  - **왜 스크롤이 필요한가**: 광고 배너(115px)를 넣으면 무대가 565가 되고,
    그때는 핀 72%(=407)에서도 **432가 안 들어간다.** **핀 위치만으로는 못 푼다.**
  - 🔴 **링크를 접거나 3개만 보이는 건 안 된다** — **비교 패널의 중립성**을 깬다(2026-08-22 확정).
    **예약처는 전부 동등하게 보인다.**
- **가격 표기 정책**:
  - 정확한 숫자를 쓴다. **근사 표기(`14만원대~`) 금지** — "신뢰성 있는 어그로"가 목표다.
  - `발견가 · {신선도}` 라벨 + `~` + "실시간 최저가는 각 사이트에서 확인" 고지 필수.
  - 🔴 **확장 상세에는 「왜 며칠 전 값인가」를 한 줄 넣는다** (2026-09-04).
    화면의 72%가 `N일 전 가격`이라 **설명이 없으면 사용자가 우리 탓으로 읽는다.**
    **설명은 한 번, 라벨은 짧게** — 카드는 스캔하는 자리고 상세는 결정하는 자리다.
    문구는 `COPY.md` §신선도.
  - **평소 시세(중앙값) vs 발견가** 막대는 **실데이터**(`median`)로만.

- ⏳ **확정 · 반영 전**(2026-09-22 전수 대조에서 화면에 없음이 확인됨) **키보드·스크린리더** (2026-09-01 확정, B11 — aria-label 고정 문구 · 핀 `aria-hidden` 0/23 · `tabindex` 0. **PH5b+5c 뒤에** — 지도 조작 모델이 바뀌면 키보드 모델도 바뀐다)

  > **핀은 카드의 시각적 표현이지 별도 조작 대상이 아니다.**
  > 지도 SVG는 `role="img"` + `aria-label="서울 출발 69곳"`, **개별 핀은 `aria-hidden`**.
  > 키보드·스크린리더 사용자에게 **피드가 곧 지도**다 — 같은 것을 두 번 훑게 하지 않는다.

  | 자리 | 조작 |
  |---|---|
  | Tab 순서 | 헤더(로고 → 출발지) → **검색** → 날짜 → 예산 → **단계바** → 피드 카드 |
  | 카드 | `<button>`. `Enter`·`Space`로 상세 |
  | 상세 | 열면 **포커스가 안으로**, `Esc`로 닫고 **누른 카드로 포커스 복귀** |
  | 단계바 | `←` `→`로 단계 이동 (radiogroup) |
  | 검색 | `↓` `↑` 이동 · `Enter` 선택 · `Esc` 닫기 · `Backspace`는 빈 칸에서 마지막 토큰 삭제 |
  | 시트(모바일) | `Esc`로 한 단 내림 |

  - **읽히는 문구를 따로 준다** — `41%↓`는 스크린리더가 "사십일 퍼센트 아래 화살표"로 읽는다.
    | 화면 | `aria-label` |
    |---|---|
    | `41%↓` | `평소보다 41% 싸요` |
    | `20일 최저` | `20일 중 가장 싼 가격이에요` |
    | `✈ 직항` | `직항편이에요` |
  - **`prefers-reduced-motion`이면 모든 전환이 `0ms`**(§CH4) — 접근성 요구이지 선택이 아니다.

- ✅ **홈 → 노선 페이지 진입로** (2026-09-01 확정, IA-1)

  **「평소 시세와 비교」 구역 바로 아래**에 **`이 노선 시세 자세히 →`**를 둔다 (2026-09-21 사용자 결정 — `DECISIONS.md` 2026-09-21 (10). **반영됨**: 프론트가 라이브에서 눌러 도착지까지 확인).
  🔒 **지킬 것은 위치가 아니라 「고지 사슬을 가르지 않는다」**다 — `(광고)` 설명 → 예약처 목록 → 가격 고지 사이에 이 링크가 끼면 안 된다(B61).

  - 판정 근거(2026-09-01 실측): 목적지만 맞추면 45%가 링크를 얻지만 그중 절반은 **다른 출발지의 노선 페이지**로 간다 —
    부산 딜을 보다가 인천 노선 분석으로 들어가는 것은 **다른 노선**이다. `median` 알갱이를 맞춘 것과 같은 종류의 어긋남.

  - ✅ **판정은 `(출발지, 목적지)` 쌍으로 한다. 없으면 링크를 안 보인다.**
  - 🔴 **프론트는 이 판정을 못 한다** — 허브 `SEL`은 가상이라 `ICN`인지 `GMP`인지
    `deals.json`에 없다. → **`route` 필드를 계약에 추가한다**(있으면 `"ICN-FUK"`, 없으면 `null`).
    백엔드가 실제 공항 코드를 알고 있으므로 **아는 쪽이 판정해서 넣는다.**
    프론트가 `ICN`으로 추측하면 **"모르는 걸 안다고 하는" 그 문제를 다시 만드는 것**이다.
  - **왜 필요한가**: 홈에서 노선 페이지로 가는 링크가 **`<noscript>` 안에만 있어**
    정상 방문자는 도달할 수 없었다(`IA.md` 구멍 A). SEO 내부 링크도, 신뢰 근거(30일 시세)도
    만들어 두고 진입로를 막아 놓은 상태였다.
  - **위치가 상세인 이유**: 피드 카드에 두면 **밀도를 깎고**(129→103px로 줄인 걸 되돌린다),
    무엇보다 **결정하는 자리는 상세**다.

- ✅ **`특가 피드` → `발견`** (2026-09-01 확정, IA-3)

  노선 페이지 빵부스러기가 `특가 피드 › 동남아 › 서울 → 다낭`인데
  **"특가 피드"라는 화면이 IA에 없다.** 홈의 이름은 **발견**이다.

- ⏳ **확정 · 반영 전**(2026-09-22 전수 대조에서 화면에 없음이 확인됨) **갱신 시각을 화면에 노출한다** (2026-09-01 확정, C-15 — `updated` 가 아직 `<noscript>` 안에만. **순서 1** — 보존일에 어제 데이터가 오늘로 읽히는 조용한 실패가 그대로 있다)

  피드 헤드 `서울 출발 · 69곳` 뒤에 **`· 12:26 기준`**을 붙인다.

  🔒 **확정 문자열 (2026-09-22, `COPY.md` §2d-2)** — 한 줄에 붙이고 별도 줄을 만들지 않는다(첫 방문 안내 띠와 자리가 겹친다).
  | `generated` 의 KST 날짜 | 헤드 뒤 |
  |---|---|
  | 오늘 | `· {HH:MM} 기준` |
  | 어제 | `· {M/D(요일)} {HH:MM} 기준 · 어제 자료예요` |
  | 그제 이상 | `· {M/D(요일)} {HH:MM} 기준 · {N}일 전 자료예요` |
  - 「어제」/「{N}일 전」의 경계는 §2d 신선도 문구와 같다. `어제`로 고정하면 그제인데 어제라고 말하게 된다(프론트 지적).
  - 낡았을 때 날짜를 앞에 붙이는 이유: 「어제 자료」만으로는 **어느 어제인지** 확인할 수 없다. 두 단 위계(스캔용 상대 + 확인용 절대) 그대로.
  - 판정은 **KST 날짜**로(프론트 결정) — 해외 방문자의 브라우저 날짜로 비교하면 멀쩡한 데이터가 「어제」가 된다.
  - 필터가 켜져 헤드가 `조건에 맞는 N곳`일 때도 **똑같이** 붙는다(프론트 결정 — 데이터의 나이는 필터와 무관한 페이지 전체의 사실).
  - 시각 처리: 낡음 문구는 **굵게, `--ink`**. `--accent`는 쓰지 않는다 — 코랄은 이 화면에서 「싸다」의 색이다(`DESIGN.md`). 경고색도 만들지 않는다(신선도 배지가 경고하지 않고 사실만 적는 것과 같은 이유).

  - **왜**: 백엔드가 F1 방어를 넣어 수집이 실패하면 **어제 산출물을 그대로 서빙**한다.
    가용성은 옳지만 **실패가 조용해졌다.** `updated`는 `<noscript>` 안에만 있어 안 보인다.
  - **`updated`가 오늘(KST)이 아니면** 더 눈에 띄게 — `어제 자료예요` 같은 중립적 사실.
    **주장이 아니라 사실이다.**
  - 딜별 `seen` 배지는 개별 나이를 말하지만 **페이지 전체가 늙었다는 신호는 따로 필요하다.**

- ⏳ **확정 · 반영 전**(2026-09-22 전수 대조에서 화면에 없음이 확인됨) **`서울`은 인천+김포다 — 상세에서 공항을 밝힌다** (2026-09-01 확정 — 상세 본문에 없음. **순서 3**, 인원 선택과 같은 자리라 같이 해도 된다)

  헤더 알약은 `서울 출발`이라 말하는데 `SEL`은 **가상 허브**다.
  **확장 상세 가격 줄 아래에 실제 출발 공항**(`인천 출발`)을 적는다.
  김포 딜을 보고 인천으로 갈까 헷갈릴 자리다.

- 📌 **되풀이되는 패턴 — 같은 사실이 두 곳에 있으면 갈라진다**

  이 프로젝트에서 **다섯 번** 같은 원인으로 사고가 났다. **매번 증상이 달라서 늦게 알아챈다.**

  | # | 어디 | 증상 |
  |---|---|---|
  | 1 | `found_at` 시각 계산 | 두 모듈이 각자 구현 → 9시간 어긋나 딜 11건이 잘림 |
  | 2 | `median` ↔ `low` 알갱이 | 공항 단위 vs 도시 단위 → 사과와 오렌지를 비교 |
  | 3 | 도장 임계값 | 목업 빌더 4개가 각자 `TIERS`를 들고 있어 한 곳만 고치면 조용히 틀어짐 |
  | 4 | 도시 한글명 | `labels.CITY`와 `dests` 두 곳 → 페이지 제목이 `PUS → 도쿄` |
  | 5 | **날짜 라벨/필터** | 백엔드가 `when`을, 프론트가 `dateWindow()`를 각자 계산 → **라벨과 필터가 서로 다른 답** |

  **해법도 매번 같았다 — 아는 곳을 하나로 만든다.**
  `timeutil.py` · `design/_fmt.py` · `labels.city()` 폴백 · **필터를 `when` 값으로 거르기**.

  > **새 규칙**: 두 곳에서 같은 것을 계산하게 되면 **그 자체가 결함**이다.
  > "지금은 값이 같으니 괜찮다"는 이유가 안 된다 — **갈라지는 건 시간 문제**다.

- ⏸ **성능 — 지금은 안 고친다** (2026-09-01 판단, B13)

  실측: `index.html` **304KB**(`deals.json` 133KB + `world.geojson` 인라인).

  - **목표는 세워둔다**: 초기 HTML **250KB 이하**.
  - **다만 `file://`로 열려야 한다는 제약이 이긴다.** 데이터를 `fetch`로 빼면 용량은 줄지만
    로컬 확인이 막히고 **그게 개발 속도를 좌우한다.**
  - 🔴 **무엇보다 병목이 측정된 적이 없다**(B17). **재기 전에 고치지 않는다** —
    도장 임계값을 median 고치기 전에 안 옮긴 것과 같은 원칙이다.

- ✅ **모바일(≤860px) — 지도가 배경, 카드는 3단 시트** (2026-09-01 확정)

  데스크톱은 **좌우**로 나눴지만 모바일은 **위아래**라 저울이 통째로 다르다.
  패널 폭에서 얻은 결론이 여기선 안 통한다.

  > **현행의 진짜 문제는 비중이 아니라 방향이다.** 지도 56%는 나쁘지 않은데
  > 카드가 **가로로** 흘러 390px에 **1.8장**밖에 안 보인다. 발견 서비스에서
  > **비교가 안 되는** 게 치명적이고, 세로로 읽는 화면에서 **손가락 방향이 바뀐다.**

  **지도가 화면 전체를 채우고 카드 시트가 위로 올라온다. 높이는 세 단.**

  | 단 | 높이 | 카드 | 언제 |
  |---|---|---|---|
  | **peek** | 160px | 1.0장 | 지도를 볼 때 · 핀을 누를 때 |
  | **half** | 340px | **2.6장** | **기본값** — 지도와 목록을 같이 |
  | **full** | 화면 대부분 | 5.2장 | 훑고 비교할 때 |

  - **비중은 우리가 정할 문제가 아니다.** 지도를 볼 때와 훑을 때 필요한 비중이 다르다.
    **고정하면 둘 중 하나는 늘 답답하다.**
  - **상세와 같은 은유다.** 카드를 누르면 **같은 시트 안에서 상세로 밀려 들어가고**
    뒤로가기로 돌아온다 — **시트 두 장이 겹치지 않는다.**

  🔴 **핀 탭은 상세를 열지 않는다** (2026-09-04 확정, B10)

  | 동작 | 결과 |
  |---|---|
  | **핀 탭** | 시트가 **peek으로** 내려가고 **그 카드가 시트 맨 위로**. 핀은 강조. **상세는 안 열린다** |
  | **카드 탭** | 같은 시트 안에서 **상세로 밀려 들어간다** |
  | 지도 빈 곳 탭 | 강조 해제. 시트 단은 그대로 |

  - **왜 핀이 상세를 안 여나 — 핀은 작고 겹친다.** B20에서 홍콩·선전·마카오가
    **1~2px 안에 스택**되는 걸 확인했다. **작고 오조작이 쉬운 대상에 되돌리기 비싼 동작을 걸지 않는다.**
    잘못 눌러서 전체 화면이 덮이면 되돌리는 데 두 번이 든다.
  - **탭이 hover와 click을 둘 다 대신해야 하는 문제를 시트가 나눠 푼다.**
    데스크톱은 hover=미리보기 / click=상세인데 모바일엔 hover가 없다.
    **핀 탭이 hover 자리, 카드 탭이 click 자리**다. 컴팩트 단계가 시트로 부활하는 셈이다.
  - **peek으로 내려가는 이유**: 핀을 누를 수 있었다는 건 **지도가 보였다는 뜻**이다.
    **지도를 조작한 사람에게 지도를 더 준다.** 이미 peek이면 그대로 둔다.
  - §CH4의 「카드를 누르면 지도가 그 핀으로 미끄러진다」와 **정확히 역방향**이다 —
    지도→피드, 피드→지도. **양쪽이 서로를 가리킨다**는 게 이 제품의 구조다.
  - **단계바는 시트 바로 위에 띄운다** — 지도 조작이라 지도 가까이 두되 **엄지가 닿는 아래쪽**이다.
    **검색 바는 위에** 둔다(자주 안 쓰고, 누르면 전체 화면 검색으로 열려 위치가 덜 중요하다).
  - 🔴 **드래그가 세 군데서 부딪힌다** — 시트 높이 조절 · 시트 안 스크롤 · **예산 슬라이더**.
    슬라이더를 잡는 동안 시트 드래그를 막는 것과 같은 처리가 필요하다.

  > ✅ **자유 줌·팬을 안 넣기로 한 게(§CH1) 여기서 값을 한다.** 지도에 드래그 제스처가
  > 없으므로 **시트 끌기와 안 부딪힌다.** 자유 줌을 넣었다면 이 설계가 성립하지 않았다.

  **기각**: 탭 전환(카드는 가장 많이 보이나 **지도와 목록이 서로를 못 본다**) ·
  지도 40% 고정(현행보다 낫지만 비중이 고정) · 현행 가로 스크롤.

- ✅ **화면0을 없앤다 — 서울 기본 + 헤더에서 변경** (2026-09-01 확정)

  현행은 전체 화면 인트로다(`discover.js:262~284`). **없앤다.**


  | | 규칙 |
  |---|---|
  | **기본 출발지** | **서울(`SEL`)** — 딜의 **55%**(69/126)이고 인천이 관문이다 |
  | **바꾸기** | 헤더 알약 → 드롭다운. **건수를 붙인다**(`제주 8곳`), 희소 허브는 미리 티를 낸다 |
  | **알약 모양** | **코랄 테두리**(회색이면 장식으로 읽힌다). 이 제품에서 **두 번째로 중요한 조작**이다 |
  | **첫 방문** | 헤더 아래 **한 줄 띠** — `지금 서울 출발로 보고 있어요 · 바꾸기`. **막지 않는다**, 닫으면 끝 |
  | **기억** | 고른 출발지를 `localStorage`에 남긴다. 안내 띠도 첫 방문에만 |
  | **딥링크 우선** | `#PUS-DAD`면 저장값도 기본값도 무시하고 **부산**(§CH4) |

  - **유일한 위험은 "서울 전용 서비스"로 읽히는 것**이다. 서울이 아닌 **45%**가 바꾸는 버튼을
    못 찾으면 그대로 나간다. 그래서 알약을 코랄로 올리고 첫 방문 안내를 붙인다.
  - **드롭다운을 미리 펼쳐 두지 않는다** — 그건 **작아진 화면0**이고 딜을 가린다.
  - ⚠️ **`서울`은 인천+김포 가상 허브다**(`SEL`). 알약은 "서울"이라 말하는데 실제로는 두 공항이라
    김포 딜을 보고 인천으로 갈까 헷갈릴 자리다 → **확장 상세에서 공항을 밝힌다.**
  - `localStorage`는 **필터를 URL에 안 넣기로 한 것과 다른 문제**다 — 그건 *공유받은 사람에게
    남의 필터를 강요하지 않으려는* 결정이었고, 출발지 기억은 **내 기기에서 나만 겪는 편의**다.

- 🗂 **패널 폭 `340px`** (2026-09-22 문서를 화면에 맞춤 — 2026-09-01 에 「380 유지」라 적었으나 라이브는 **340**이었고 3주간 아무도 문제 삼지 않았다. 지도가 40px 넓은 쪽이 이 제품엔 이득이고, PH5c(자유 줌) 뒤엔 패널 폭이 지도 배율을 잠그지 않는다. `DECISIONS.md` 2026-09-22 (3))

  좁히면 지도가 넓어져 지구가 커지고, 넓히면 카드가 여유로워진다. 둘 다 실측으로 쟀다.

  | 패널 | 지도 | far 배율 | 지구 크기 | 카드 본문 | 가장 긴 줄 |
  |---|---|---|---|---|---|
  | 320 | 880 | 186 | **+7%** | 204 | **74% 채움** |
  | 360 | 840 | 178 | +2% | 244 | 61% |
  | **380** | **820** | **174** | — | **264** | **57%** |
  | 420 | 780 | 165 | −5% | 304 | 49% |
  | 460 | 740 | 157 | −10% | 344 | 44% |

  > ⚠️ **처음 세운 가설이 틀렸다.** "좁히면 글자가 잘린다"고 봤는데
  > **320px에서도 잘리는 카드가 0건**이다. 도시명이 길어야 `쿠알라룸푸르`(여섯 자)이고
  > 가격은 아홉 자리가 최대라 **한글 서비스는 애초에 글자가 짧다.**
  > 그래서 "잘리느냐"가 아니라 **"가장 긴 줄이 본문을 얼마나 채우나"**로 다시 쟀다.

  - **좁혀서 얻는 게 적다**: 60px을 좁혀도 지구는 **+7%**뿐이다. `slice` 크롭 때문에
    **무대가 넓어져도 보이는 viewBox 가로는 그만큼 안 늘어난다.**
  - **넓혀서 얻는 것도 적다**: 본문이 넉넉해져도 **채울 글자가 없다** — 빈 여백만 는다.
  - **`380px`은 이미 균형점이다.** 바꿀 근거가 데이터에 없다. **바꾸지 않는 것도 결정이다.**
  - **모바일에서는 이 저울이 통째로 달라진다** — 390px 폭에서 패널이 전폭이 되므로
    PH5 모바일 항목에서 다시 본다.

- ✅ **날짜·예산 필터** (2026-09-01 확정) — 목업 `design/filters.html` · `design/budget.html`

  검색 바 오른쪽 **알약 둘**이 각각 팝오버를 연다. 검색창에 안 넣는 이유는 **값이 범위**라
  타이핑으로 고르기 나쁘기 때문이다.

  **날짜 — 두 축**

  | 축 | 값 |
  |---|---|
  | **언제** | `아무때` · `이번 주말` · **`다음 주말`** · `이번 주` · `이번 달` · `다음 달` · `그 이후` · `날짜 지정` |
  | **며칠** | `상관없어` · `1~3박` · `4~6박` · `7~13박` · `2주 이상` |

  🔴 **필터는 날짜를 다시 계산하지 않는다. `when` 값으로 거른다.** (2026-09-01 확정)

  | 칩 | 판정 |
  |---|---|
  | `이번 주말` · `다음 주말` · `이번 주` · `이번 달` · `다음 달` | **`when`이 그 문자열과 같은 딜** |
  | `그 이후` | **`when`이 위 다섯 중 아무것도 아닌 딜** (`N월`·`내년 N월`·`YYYY년 N월`) |
  | `날짜 지정` | `dep`으로 범위 비교 — **여기서만 날짜를 본다** |
  | `아무때` | 전부 |

  > **왜 이렇게 하나 — 버그의 종류 자체를 없앤다.**
  > 프론트가 "이번 주"를 따로 계산하면 **백엔드의 `when` 계산과 갈라진다.**
  > 실제로 갈라졌다 — 백엔드는 **라벨**이 틀렸고(`이번 주말`이 다음 주말에 붙음),
  > 프론트는 **필터**가 틀렸다. **원인이 같은데 두 곳에서 따로 터졌다.**
  >
  > ```
  > 프론트 실측 · 일요일에 「이번 주」를 누르면
  >   정상 0건인데  →  18건이 나온다 (전부 다음 주 딜)
  > 토요일에 「이번 주말」을 누르면
  >   오늘·내일 출발 10건이 사라지고  →  다음 주말 4건만 남는다
  > ```
  >
  > **`when`으로 거르면 배지와 필터가 구조적으로 어긋날 수 없다.**
  > 같은 사실을 두 곳에 적으면 하나는 틀린다 — 백엔드가 `timeutil.py`로 얻은 교훈과 같다.

  - **일요일에 `이번 주`가 `0곳`이 되는 건 정상이다.** 이번 달력 주의 평일이 다 지났기 때문이다.
    **버그가 아니다** — 우리는 이미 **칩에 건수를 보이므로**(`이번 주 0곳`) 화면이 스스로 설명한다.
    🔴 **`0곳` 칩은 흐리게 하고 못 누르게 한다.**
  - 오늘 실측: `이번 주말 8` · `다음 주말 0`(백엔드 구현 전) · `이번 주 12` ·
    `이번 달 44` · `다음 달 32` · `그 이후 32`.

  - **칩을 `when` 어휘와 맞춘다.** 화면에 뜨는 라벨과 고르는 칩이 같은 말이면 배울 게 없다.
    현행 칩엔 `이번 달`도 `그 이후`도 없어 **77건(61%)을 좁힐 수가 없었다.**
  - 🔴 **`며칠`은 새 축이다.** `nights` 필드가 이미 있는데 안 쓰고 있었다.
    실측 **1~3박 48 · 4~6박 27 · 7~13박 35 · 2주 이상 15**로 고르게 퍼져 있다.
    **주말 2박3일과 일주일 휴가는 전혀 다른 여행**인데 지금은 못 가른다.
  - **`날짜 지정`을 누르면 달력이 열린다.** 달력을 기본으로 두지 않는 이유 —
    달력은 **목적지가 정해진 사람**의 도구다. 우리 사용자는 반대로 **날짜가 먼저 있고 목적지를 찾는다.**

  **예산 — 히스토그램 슬라이더 + 바로가기 칩 (데스크톱·모바일 같은 구조)**

  - 🔴 **눈금은 선형이다. 트랙도 히스토그램도.** (2026-09-03 확정) `step` 5만.
    로그 눈금은 실측으로 기각 — 분포가 저가로 뭉친 게 아니라 **거리대(`haul`) 때문에 봉우리가 둘**(단거리 20~40만 · 장거리 70~100만)이고,
    로그는 정작 비어 있는 5만~11만 구간을 늘리며 **슬라이더가 돈으로 안 읽힌다.** 꼬리(상위 10%)를 잘라도 범위는 19배라 안 풀린다.

  - **칩은 슬라이더의 바로가기**다 — `30만` · `50만` · `100만` · `상관없어`.
    누르면 슬라이더가 그 값으로 간다. **경쟁 관계가 아니라 같은 값을 두 방법으로 고르는 것**이다.
    - 곳 수는 **출발지마다 다르고 매일 바뀐다.** 문서에 숫자를 박지 않는다 —
      9/1에 적어둔 `47/77/111`은 이미 딜 126건 시절 값이라 틀렸다.
    - 🔴 **출발지 최고가보다 큰 칩은 숨긴다.** 제주는 최고가가 71만이라 `100만` 칩이
      `상관없어`와 **같은 뜻**이 된다. 뜻이 겹치는 칩은 고를 이유가 없다.
      **「0곳이면 흐리게+비활성」과는 다른 처리다** — 그건 *없다*는 정보를 주지만,
      이건 *중복*이라 정보가 없다. 없는 정보를 회색으로 남겨두면 자리만 먹는다.
    - **`상관없어` 앞에 8~10px 간격을 둔다.** `100만`보다 비싼 다음 칸이 아니라 **축 밖**이다.
    - 칩 순서는 `30만 · 50만 · 100만 · 상관없어`로 **끝에 `상관없어`**다. 날짜 줄이 `아무때`를
      앞에 두는 것과 다른데, **칩 줄 아래에 축이 있느냐**의 차이다. 예산 칩은 슬라이더 바로가기라
      **왼쪽→오른쪽이 싼 값→비싼 값→무제한**으로 읽혀야 한다. 날짜 줄엔 그 아래 축이 없다.

  - **히스토그램 칸은 목표 14개에 맞춰 묶는다** (2026-09-03 확정, 프론트 실측)

    `step`(5만)마다 막대를 세우면 **딜이 적은 허브에서 잡음**이 된다 — 대구는 24칸 중 16칸이 빈다.
    막대 한 칸은 **슬라이더 칸의 배수**여야 손잡이가 멈추는 자리와 막대 경계가 어긋나지 않는다.

    | 출발지 | 막대 | 칸 폭 | 빈 칸 |
    |---|---|---|---|
    | 서울 69건 | 13 | 10만 | 2 |
    | 부산 31건 | 12 | 10만 | 3 |
    | 대구 11건 | 12 | 10만 | 5 |
    | 제주 10건 | 10 | 5만 | 5 |

    - **막대 합계 = 딜 수**가 반드시 맞아야 한다. 트랙 최저는 5만 올림값이라 그보다 싼 딜이
      왼쪽 밖에 생기는데, **첫 칸에 넣는다.** 합이 안 맞으면 사용자는 세어보지 않아도 틀린 걸 느낀다.
    - 칸 폭이 출발지마다 달라도 된다. **출발지를 바꾸면 트랙 자체가 바뀌므로 두 화면은 비교 대상이 아니다.**
    - 손잡이 왼쪽 막대는 강조색, 오른쪽은 회색 — "지금 자르면 뭐가 남나"가 보인다.
    - 손잡이는 반지름만큼 안쪽에서 멈추므로 **막대띠를 좌우로 9px 물린다.**
  - 🔴 **모바일 손잡이는 `44px`.** 데스크톱 `18px`은 터치 최소(iOS 44pt · Material 48dp)의
    **2.4배 모자란다.** 다만 **히스토그램은 읽는 것이라 터치 크기와 무관**하므로 모바일에서도 그대로 둔다.
  - ⚠️ **시트 끌기와 슬라이더 드래그가 부딪힌다.** 모바일에서 슬라이더를 잡는 동안
    **시트 드래그를 막아야** 한다.
  - **현행 슬라이더는 범위가 `10만~100만`이라 18건(14%)이 밖에 있었다** — 끝까지 밀어도 안 나온다.

  > **기기마다 다른 UI를 주지 않는다.** 폰으로 보고 데스크톱에서 예약하는 사용자가
  > "내 폰엔 왜 없지"를 겪지 않게 한다.
  - ✅ **막대가 없을 때 이유를 말한다** (2026-09-01 확정, 프론트 지적)

    막대가 생략되는 경우가 **둘인데 이유가 다르다.** 지금은 화면에서 구분이 안 돼
    사용자가 "왜 근거가 없지?"라고 생각할 자리다.

    | 경우 | 뜻 | 문구 |
    |---|---|---|
    | `median` = `null` | **비교할 게 없다** — 이 노선을 아직 충분히 안 봤다 | `아직 이 노선의 평소 시세를 모아두지 못했어요` |
    | `median` ≤ `price` | **비교했는데 안 쌌다** | `지금은 평소 시세와 비슷해요` |

    - **둘을 한 문구로 뭉치지 않는다.** "모른다"와 "알아봤는데 아니다"는 전혀 다른 말이고,
      전자를 후자처럼 쓰면 **없는 근거를 있는 척**하는 게 된다.
    - 후자를 `평소보다 비싸요`로 쓰지 않는다 — `price`는 그날 최저가, `median`은 거친 기준선이라
      **몇 %p 차이를 "비싸다"고 단정할 만큼 정밀하지 않다.**
- **30일 스파크라인은 계속 보류한다** (2026-08-22 재검토, B9)
  - 가짜 데이터로 그렸다가 제거했고(2026-08-06), 히스토리가 쌓이면 재개하기로 했었다.
    `broad_offers`에 **24일치**가 쌓였지만 **지금도 만들지 않는다.**
  - **왜 아직인가**:
    1. 확장 상세엔 이미 **"평소 시세(중앙값) vs 발견가" 막대**가 있고 **실데이터**다. 스파크라인은 그 위에 겹친다.
    2. 스파크라인은 **일별 시계열을 계약에 실어야 한다** — 딜 99건 × 24일이면 `deals.json`이 눈에 띄게 무거워진다
       (`index.html`이 이미 278KB, B13).
    3. **노선 상세 페이지에 이미 30일 차트가 있다.** 깊은 분석은 그쪽 일이다.
  - **대신 같은 데이터로 훨씬 가벼운 걸 먼저 한다** → 아래 `{N}일 중 최저`.
- **`{N}일 중 최저가예요` — 필드 2개만 요청한다** (2026-08-22, 백엔드 협의 중)
  - `역대급` 같은 최상급은 **역대 데이터가 없어 못 쓴다**. 대신 **확인 가능해서 더 강한** 문구를 쓴다.
    그리고 **서비스가 오래될수록 N이 자라 문구가 저절로 세진다.**
  - 필요한 것은 시계열이 아니라 **요약값 둘**이다:

    | 필드 | 타입 | 의미 |
    |---|---|---|
    | `low` | number\|null | **오늘을 제외한 이전 기간 최저가** |
    | `obs_days` | number | 관측 일수 (`broad_offers` 기준) |

  - **표시 조건 (2026-08-22 확정, 백엔드 실측 반영)**
    ```
    obs_days >= 7  AND  price < low  AND  (1 - price/low) >= 0.05
    ```
    → 오늘 기준 **10건(10%)**. 셋 다 만족할 때만 쓰고, 아니면 **아무 말도 하지 않는다.**
  - ⚠️ **처음 정의(오늘 포함 최저)는 버렸다.** 그러면 **41%에 붙는다**(백엔드 실측) —
    `deals.json`의 `price`가 **이미 최근 3~4일 중 최저가**라, 최저를 뽑아놓고 "최저냐"를 묻는
    **동어반복** 구조였다. 도장이 100%에 붙어 무의미했던 문제를 그대로 반복할 뻔했다.
  - **왜 5% 낙폭까지 요구하나**: 신기록만 보면 34%가 걸리는데 그중엔
    `서울→호치민 180,358원 / 이전 최저 181,838원(0.8% 차이)` 같은 게 섞인다.
    **기술적으로는 신기록이지만 사용자에겐 같은 가격**이고, 그런 게 문구의 신뢰를 깎는다.
  - **위치는 확장 상세** — "평소 시세 vs 발견가" 막대 아래 한 줄.
    카드에 넣지 않는다. 카드는 이미 도장·직항 배지로 포화이고,
    이건 **"가격이 진짜인가"를 확인하는 정보**라 상세가 제자리다.
  - **`low`·`discount`는 다른 축이다**: 도장은 **평소 시세(중앙값) 대비**, 이건 **관측 기간 최저 갱신**.
    둘 다 붙을 수 있고 그건 정상이다.
  - ⚠️ **`offers`(26노선 깊은 수집)와 `broad_offers`(전 목적지 최저가)는 성격이 다르다.**
    예전의 "발견 딜의 82%는 히스토리 없음"은 `offers` 기준이었다 —
    **이 문구는 `broad_offers`로 계산해야 한다**(백엔드 지적, 2026-08-22).
- **광고 고지 필수**: `(광고)` + 수수료 고지. 외부 링크는 `target="_blank" rel="noopener sponsored"`.

- **URL 해시로 상태를 남긴다** (2026-08-22 확정 — IA-2/F7)

  | 화면 | URL | 히스토리 |
  |---|---|---|
  | 화면0 (출발지 선택) | `/` (해시 없음) | — |
  | 출발지 선택됨 | **`#{허브}`** — 예 `#SEL` | `pushState` |
  | 딜 상세 열림 | **`#{허브}-{목적지}`** — 예 `#SEL-DAD` | `pushState` |

  - **키가 유일한 걸 확인했다**: `(허브, 목적지)` 조합이 딜 하나에 1:1로 대응한다(99/99, 중복 0).
    백엔드가 `(허브, 도시)` 단위로 최저가 1건만 내보내기 때문이다(`CONTRACT.md`).
  - **뒤로가기가 자연스럽게 동작한다**: 상세에서 뒤로 → 지도, 지도에서 뒤로 → 화면0.
    상세를 닫는 동작(X·바깥 클릭)은 **`history.back()`**으로 처리해 URL과 화면을 어긋나지 않게 한다.
  - **URL에 넣지 않는 것**: 필터 · 정렬 · 거리 단계.
    공유받은 사람에게 남의 필터를 강요하게 되고, 휘발성 상태다(2026-08-22 확정, `IA.md`).
  - ⚠️ **딥링크로 들어오면 거리 단계를 그 딜에 맞춰 자동 전환한다.**
    `#SEL-LAX`(장거리)로 들어왔는데 `가까운 곳` 단계면 **핀이 화면 밖이라 안 보인다** — F15 재발이다.
    단계는 URL에 없지만 **상태는 딜의 `haul`을 따라간다.**
  - **해시가 형식에 안 맞으면**(`#XXX` 등) 무시하고 화면0을 띄운다. 오류 문구를 내지 않는다.

- **공유 버튼을 확장 상세에 둔다** (2026-08-22 확정)
  - 딥링크를 만들어 놓고 **공유 수단이 없으면 반쪽**이다. 특히 모바일에서 주소창 복사는 어렵다.
  - `navigator.share`가 있으면 그것을, 없으면 **클립보드 복사 + 짧은 피드백**(`COPY.md`).
  - 백로그의 "커뮤니티 시딩(뽐뿌 등)"이 이걸로 비로소 가능해진다.

- **딥링크한 딜이 오늘 없을 때** (2026-08-22 확정 — F5/FL-4)
  - **딜은 매일 바뀐다.** 어제 공유한 `#SEL-DAD`가 오늘은 없을 수 있다.
  - **출발지는 적용하고**(허브는 유효하므로) 피드를 정상으로 보여준 뒤, 상단에 안내를 얹는다.
  - ⭐ **같은 목적지가 다른 허브에 있으면 그걸 제안한다.**
    실측 — **목적지 59곳 중 31곳(53%)이 여러 허브에서 간다.** 절반은 건질 수 있다.
    → `서울 출발 다낭은 오늘 없지만, 부산 출발이 있어요` + 그 딜로 가는 링크.
  - 다른 허브에도 없으면 담백하게: `찾으시던 특가는 오늘 없어요`.
    **도시명을 쓰지 않는다** — 딜이 없으면 `ko`(한글명)를 알 수 없고, IATA 코드를 화면에 내보낼 순 없다.

- **`links`가 비면 예약처 섹션을 통째로 생략한다** (2026-08-22 확정 — F4/FL-3)
  - 지금은 `compareHTML`이 `""`를 반환해도 **`어디가 제일 싼지 비교해보세요` 헤더만 남는다.**
  - 섹션(헤더 포함)을 숨기고 한 줄만 남긴다: `예약처 링크를 준비하지 못했어요`.
  - **현재 실측은 99건 전부 `links` 4개**다. 즉 이건 **장애 시 방어**이지 일상 경로가 아니다.

- **확장 카드 위치는 현행 로직을 유지한다** (2026-08-22 확인)
  - 이미 잘 되어 있다: 핀 **위 → 안 들어가면 아래 → 그래도 넘치면 무대 안에 고정**,
    카드가 무대보다 높으면 `max-height` + 세로 스크롤, 좌우 120px 클램프, 모바일은 하단 시트.
  - **추가할 것 하나**: **필터 도크·줌 컨트롤 영역을 피한다.**
    지금은 확장 카드가 도크 뒤로 들어갈 수 있다(B16과 같은 뿌리).

### 미결
없음 — 열린 안건은 §3 통합 목록에만 둔다.

---

## §CH4 보강 — 인원: 「1인 기준」을 말하고, 인원은 상세에서 골라 예약처로 넘긴다 (2026-09-22 확정)

> 사용자: 「몇 명이서 가는지가 안 정해져 있다. 고지해 둘까, 처음에 정하게 할까」 → **고지 + 상세에서 선택.** 처음에 묻지 않는다. `DECISIONS.md` 2026-09-22 (2).

- **사실**: 가격 데이터는 **성인 1명 왕복**이고, 예약처 링크도 인원 `1`로 나간다. 화면엔 「1인」이 **한 글자도 없었다**(`COPY.md` 0건).
- **카드·hero 가격 단위를 `1인 왕복`으로.** 「왕복」처럼 **단위**로 붙는다 — 고지가 아니라 표기라 매 카드에 있어도 소음이 아니다.
- **확장 상세에 인원 선택**(`몇 명이 가요?` 1~4명, 기본 1). 고르면 **예약처로 나가는 링크의 인원 값만 바뀐다.** 화면의 가격·도장·비교 막대는 **그대로 1인**이다.
  선택은 세션 안에서 기억한다(다른 카드를 열어도 유지). 저장하지 않는다.
- **고지 한 줄**은 가격 고지 **바로 위**(`COPY.md` §3). 고지 사슬(`(광고)` 설명 → 예약처 목록 → 가격 고지)을 **가르지 않는다** — 그 앞에 붙는다.
- **인원을 링크로 못 받는 예약처**는 이름 옆에 `1인 기준으로 열려요`. 어느 예약처가 받는지는 **백엔드가 확인해 계약(`links[]`)에 싣는다** — 프론트가 이름으로 추측하지 않는다(`ad` 와 같은 이유).
- **인원별 총액을 계산해 보여주지 않는다.** 모르는 것을 아는 척하지 않는다.
- 노선 페이지: hero 단위 `1인 왕복` + 푸터 고지 한 문장. 알림 메일 딜 한 줄도 `1인 왕복`.

## §CH5b — 모바일: **피드가 기본 화면, 지도는 버튼 뒤 전체 화면** (2026-09-21 확정)

> **이 절이 모바일의 현행 스펙이다.** 아래 §CH5 와 §CH2 의 「모바일 하단 시트」 서술은 **이 절로 대체된다**(지우지 않고 남겨 둔다 — 경위).
> 왜·무엇을 버렸나는 `DECISIONS.md` 2026-09-21 (6)(7). 목업은 `design/mobile_feed.html` — **폰에서 직접 여는 목업**이고 사용자가 iPhone 14 Pro 로 보고 골랐다.
> 표기: 🧑 = 사용자가 고른 것 · 🗂 = 기획이 채운 것(사용자에게 묻지 않았다 — 어긋나면 되돌린다).

**적용 범위**: `≤ 860px`(현행 브레이크포인트 그대로). **데스크톱은 하나도 바꾸지 않는다** — 거긴 지도가 크다.

### 1. 기본 화면 = 피드 🧑
- 지도는 기본 화면에 **없다.** 화면 전체를 카드가 쓴다.
- 🔴 **페이지가 통째로 스크롤된다.** 안쪽에 따로 스크롤되는 상자를 두지 않는다 — B62 「스크롤이 어디로 가는지 모르겠다」의 답이다.
  위에 붙어 다니는 것은 머리(로고·출발지·`필터`)와 **거리 칩 한 줄**뿐이다.
- 카드·확장 상세의 내용은 §CH3·§CH4 그대로다(도장·신기록·신선도·비교 막대·이유 문구·고지 순서·`이 노선 시세 자세히 →`). **카드 탭 = 그 자리에서 펼침.**
- **사라지는 것**: 모바일 하단 시트(접힘·반·전체 세 단)와 그 손잡이, 지도 위 단계바, 「핀 탭 → 시트 peek」(B10).

### 2. 필터 — 접힌 채 시작 🧑
- **거리 칩 한 줄만 늘 보인다**: `가까운 곳` · `조금 더 멀리` · `아주 멀리`(문구는 기존 거리 단계 그대로). 하나만 고르고, 다시 누르면 풀린다.
  지도에 있던 단계가 **필터로 내려온 것**이다 — 피드에서는 「어디를 보나」가 곧 「무엇을 거르나」다.
- 분위기 · 언제 · 예산은 **`필터` 버튼 뒤**에 있고 **접힌 채 시작**한다. 켜진 것이 있으면 버튼이 `필터 2`처럼 **개수**를 말한다(거리 칩은 세지 않는다 — 늘 보이니까).
- 0건이면 `조건에 맞는 곳이 아직 없어요` + `필터 지우기`(§CH2 빈 상태 그대로).

### 3. 「지도로 보기」 → 전체 화면 지도 🧑
- 피드 위에 떠 있는 버튼 **`지도로 보기`**(화면 아래 가운데). 누르면 지도가 **화면 전체**를 쓴다. **`← 목록`**으로 돌아온다.
- 🧑 **지도를 끌고 벌릴 수 있다.** 2026-08-22 「자유 줌 기각」을 **모바일 전체 화면에 한해** 뒤집는다(`DECISIONS.md` 2026-09-21 (7)).
- 🗂 **단계 버튼 셋은 남긴다** — 끌기와 나란히, **빠른 이동**으로. 지도를 열 때는 피드의 거리 칩에 맞는 단계에서 연다(칩이 없으면 `조금 더 멀리`).
- 🗂 **지도는 피드의 필터를 그대로 따른다.** 지도 화면 안에는 필터 UI 를 두지 않는다(첫 판). 대신 **`지도에 {n}곳 · 전체 {N}곳`**으로 지금 보이는 수를 말한다.
- 🗂 **핀**은 전부 그린다. 색은 기존대로 **보이는 딜 사이의 상대 가격**(싼 곳이 진하다). 싼 핀이 위에 온다.
- 🗂 **라벨(`도시 N만`)은 싼 곳부터 자리를 잡고, 겹치면 붙이지 않는다.** 핀은 생략하지 않는다. 배율이 바뀔 때마다 다시 잡는다.
  데스크톱의 라벨·LOD 규칙(§CH1)을 **가져오지 않는다** — 그 규칙은 뷰 3개에서만 성립하게 만든 것이다.
- 🗂 **핀 탭 = 아래에 카드 한 장**(피드 카드와 같은 내용 + `자세히 보기`). 지도의 빈 곳을 누르면 닫힌다.
  **`자세히 보기`** = 지도를 닫고 **피드에서 그 카드를 펼친 채** 화면 위쪽에 놓는다. 상세를 지도 위에 열지 않는다 — 상세는 길고, 결정하는 자리는 피드 하나로 둔다.

### 4. 공유 링크의 착지 🗂
`#SEL-TYO` 로 들어오면 **피드에서 그 카드를 펼친 채** 보여준다. 지도는 열지 않는다. (데스크톱의 §CH4·CH7 동작은 그대로.)

### 5. 착수 전에 프론트가 재 줄 것
- **끌기의 성능.** B17 실측에서 병목은 `path(WORLD)` 99.7% 였다. 끄는 동안 매 프레임 세계를 다시 그리면 저사양 폰에서 끊긴다.
  목업은 canvas 로 그렸고 **iPhone 14 Pro 에서는 사용자가 「좋다」고 했다 — 그 기기 하나의 관측이다.** 어떻게 그릴지는 프론트가 정하되, **중저가 안드로이드에서 끌 때 끊기지 않는지**를 먼저 본다.
- 끊긴다면 돌아올 자리: **단계 버튼만**(끌기 없이 전체 화면). 그것만으로도 「지도가 작다」는 풀린다.

---

## §CH5 — 모바일 (⚠️ 2026-09-21 §CH5b 로 대체 — 경위로 남긴다)

> **거의 전부 미결이다.** 2026-08-01 확정 때 "구현하며 확정"으로 미뤄 둔 유일한 영역이고,
> hi-fi 시안도 없다. **PH5에서 와이어프레임을 먼저 그린다.**

### 확정 (원칙만)
- **터치 우선. 호버에 의존하는 기능을 두지 않는다.**
- 피드 = 하단 가로 스크롤 · 필터 도크 = 하단 시트 · 플로팅 카드 = 하단 시트 (**추천안, 미확정**)
- 거리 확장은 단계 버튼(자유 줌 아님) — 모바일에서 특히 유리해서 택한 방식이다.

### 현행
| 항목 | 값 |
|---|---|
| 브레이크포인트 | `≤ 860px` (JS·CSS 공통) |
| 필터 도크 | 접힌 채 시작 |
| 플로팅 카드 | 하단 시트(CSS), JS는 위치 계산 생략 |
| **컴팩트 단계** | **없음** — hover가 `mouseenter`라 터치에서 안 뜨고, 탭하면 곧장 확장 |

### 미결
없음 — 열린 안건은 §3 통합 목록에만 둔다.

---

## §CH6 — 접근성 · 마감

### 확정 (원칙)
- 색각이상 안전: 코랄 단일 시그니처 + 청록 중립 유지. **정보를 색으로만 전달하지 않는다.**
- JS 없는 환경 `<noscript>` 대체 유지.
- **사진 정책**: 목적지당 1장 큐레이션, **호버 시 lazy-load**, 작은 webp, 라이선스(Unsplash/Wikimedia) 표기.
  정적 사이트 용량을 고려해 $0을 유지한다.

### 미결
| # | 안건 | 메모 |
|---|---|---|
| **B11 / F9** | **키보드 조작** | 핀(`<g>`)·카드(`<div>`)에 `click`만 있고 `tabindex`·`role`·키 핸들러·포커스 스타일 없음 = **조작 불가** |
| **B12** | 사진 실제 도입 | 현행 전부 그라디언트 |
| **B13 / F14** | **성능 예산** | `index.html` 278KB(인라인). 예산 수치를 정해야 판단 가능 |

---

## 실측치 기준

이 문서의 수치는 **2026-09-01 픽스처(딜 126건, 정상화된 것)** 기준이다. 딜 수·비율은 매일 바뀌므로 **절대값이 아니라 근거의 모양**으로 읽는다.

## 3. 미결 통합 목록

| # | 안건 | 화면 | PH/CH |
|---|---|---|---|
| **B20** | 핀 자체가 1~2px 안에 스택 (홍콩·선전·마카오) | S1 | 추후 |
| B16 | 필터 도크가 핀을 가림 | S1·S3 | CH2 |
| **B62 → §CH5b·§CH1** | 모바일 재검토 — 스펙은 확정(피드 기본·전체 화면 지도·자유 줌), **반영 전** | S1·S2 | PH5b+5c → 프론트 |
| **BE8-1** | **노선 페이지의 유지 하한** — 넣을 때 하한(`D` ≥ 14, sitemap)은 정해졌고(§노선 페이지), **표본이 있다가 마른 노선**을 어떻게 할지는 미결 — 표본이 있다가 마른 경우다. 현행 36개 중 4개가 하루 10건 미만(`ICN-GUM` 2 · `ICN-LHR` 7 · `PUS-TPE` 4 · `ICN-RMQ` 1, 2026-09-19 하루치). 30일 창으로 쌓여 당장 빈 페이지는 아니지만, **표본이 얇아진 페이지가 시세를 계속 주장한다.** 정할 것: 유지 하한 · 하한 아래일 때 화면이 뭐라고 말하나(지우지는 않는다 — 색인 자산) | S7 | PH(미정) |
| B11 / F9 | 키보드 조작 | 전역 | CH6 |
| B12 | 사진 도입 | S2·S4 | CH6 |
| B13 / F14 | 성능 예산 | 전역 | CH6 |
| **B53 후속** | **사이트 전체 다크 모드** — 홈(지도 바다·땅·핀·항로·카드 사진 그라디언트) + 노선 페이지를 **한 번에** 켠다. 그 전까진 전부 라이트(`DESIGN.md` §컬러, 2026-09-20) | 전역 | 기획이 다크 매핑 먼저(PH) → CH |
