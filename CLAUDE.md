# galmal-plan — 기획 세션

@SESSIONS.md

> 위 한 줄이 **세 세션 공통 규칙**을 가져온다(`SESSIONS.md`). 이 파일에는 **이 저장소만의 규칙**을 둔다.

## 이 저장소

갈래말래의 **제품·스펙·결정 기록·계약의 이유**와 `design/` 목업. **제품 코드와 운영 데이터는 없다**(M6, 2026-09-19).
공개 저장소다 — 사업 민감 정보는 노션에.

| 문서 | 내용 |
|---|---|
| `PROJECT.md` | 상태·로드맵·운영 정보 (**전체 지도**) |
| `PRODUCT.md` | 제품 본질(대상·목적·포지셔닝) |
| **`SPEC.md`** | 화면·상태 인벤토리 + 챕터별 확정 인터랙션 스펙 — 프론트가 챕터마다 펼쳐 본다. 미결은 §3 |
| `DESIGN.md` | 디자인 시스템(**시각 언어만**) |
| `IA.md` · `FLOWS.md` | 사이트맵·URL / 유저 플로우·실패 경로 |
| `COPY.md` | 화면 문자열 전수 + 보이스 규칙 — **없는 문자열을 지어내지 않는다** |
| `CONTRACT.md` | 프론트↔백엔드 계약의 **이유와 원칙**. 필드·어휘 목록의 정본은 `galmal-backend/contract/v1/` |
| `TAGS.md` | 태그 어휘·배정의 **원칙**. 배정 정본은 `galmal-backend/collector/dests.py` |
| `DECISIONS.md` | **왜 그렇게 정했나 + 기각된 대안** — 폐기된 길을 다시 파기 전에 확인 |
| `PLAN.md` | 기획 작업 방식 · 챕터 · **함정 기록** |
| `SESSIONS.md` | 세 세션 공통 규칙(한 벌) |

다른 두 세션의 작업 방식 문서는 **각자 저장소에** 있다: `galmal-backend/BACKEND.md` · `galmal-frontend/FRONTEND.md`·`BACKLOG.md`.

## 기획의 역할
- 제품 방향·UX·디자인 확정, 목업 제작, 결정을 문서에 기록. **계약 변경을 결정한다**(적는 곳은 백엔드 `contract/`).
- **PH는 프론트 CH보다 하나 앞선다** — 프론트가 스펙 부재로 멈추면 기획의 실패다.
- 결정을 내리면 `SPEC.md`/`DESIGN.md`(무엇을)와 `DECISIONS.md`(왜·무엇을 버렸나) **둘 다** 갱신한다.
- 화면에 새 문자열이 생기면 `COPY.md`에 넣는다.

## `design/` 목업
- 빌더(`design/build_*.py`)는 제품에 **공개 URL로** 기댄다 — `design/_data.py`가 `api.galmal.kr/v1` · `galmal.kr`에서 받는다.
  **저장소 안 파일 경로로 제품 데이터를 읽지 않는다**(M6 T1pre).
- 이미 만든 목업 HTML은 **그날 결정의 스냅숏**이다. 본문에 그날 수치가 적혀 있어 새 데이터로 다시 굽지 않는다.
  빌더를 검증하려고 돌렸으면 산출물은 되돌린다.
- `build_tags.py`는 동결 — 참조 데이터가 v1으로 발행되면 URL로 옮겨 푼다.

## git
- 크론이 없어 충돌이 드물다. 작업 전 `git fetch && git merge origin/main`, 작게 자주 커밋해 main에 반영한다.
- 문서 편집은 파이썬 `newline=""`. 표 여러 줄을 한 스크립트로 고칠 땐 **고친 뒤 위치를 다시 잰다**(2026-09-19 표가 뭉개진 사고).
