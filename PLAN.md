# PLAN.md — 기획·디자인 세션 작업 방식

> **소유: 기획 세션.** 다른 세션은 읽기만 한다.
> 세 세션 공통 규칙(스코프 잠금·세션 간 소통·계약 변경 절차)은 **`SESSIONS.md`** 가 한 벌이다 — 여기 옮겨 적지 않는다.
> 이 저장소만의 규칙은 `CLAUDE.md`, 프론트·백엔드 작업 방식은 각자 저장소의 `FRONTEND.md`·`BACKEND.md`.
> **이 문서는 "어떻게 일하는가"만 다룬다.** 무엇을 정했는지는 `SPEC.md`·`DESIGN.md`, 왜는 `DECISIONS.md`.

## 왜 이 문서가 있나

2026-08-22 점검에서 드러난 문제는 "프로세스가 없다"가 아니라 **기획이 구현에 끌려다닌다**였다.

`BACKLOG.md`에 "**기획 세션 결정 필요**"가 쌓인 채(B3 라벨 밀집, B10 모바일 탭) 프론트의 다음 챕터가
그 항목을 포함하고 있었다. 즉 프론트가 세션을 시작하는 순간 막히는 상태였다.
"백엔드 만들다가 기획하고"가 반복된 원인은 순서를 몰라서가 아니라 **선후행이 깨져서**다.

여기서 이 문서의 존재 이유가 나온다: **기획은 구현보다 앞서 있어야 하고, 그건 규칙으로 강제해야 한다.**

---

## 1. 핵심 규칙 — 한 챕터 앞서기

> **기획 챕터(PH)는 프론트 챕터(CH)보다 정확히 하나 앞선다.**

| 시점 | 기획 세션 | 프론트 세션 |
|---|---|---|
| ~~2026-08-22 ~ 09-03~~ | ~~PH0 · PH1 · PH2 · PH3 · PH3b · PH4 · PH5 · PH6 · PH6b~~ ✅ | ~~CH1 ~ CH6~~ ✅ |
| ~~09-08 ~ 09-19~~ | ~~레포 분리(M1~M6)~~ ✅ · 계약/태그 정본 이관 | ~~CH7 공유 링크~~ ✅ |
| **지금 (2026-09-21)** | **건별 결정으로 돌고 있다** — 챕터가 아니라 프론트·백엔드가 올린 발견에 답하는 중(B53·B54·B61·B62-③·B64·B66·BE8) | **결정 반영 챕터**(「30일」 문구 · 팁 억제 · 팁 `n≥3`) → **B65** 홈→노선 링크 |
| **확정 → 프론트 대기** | **PH5c 데스크톱 자유 줌·팬**(§CH1, 2026-09-22 사용자 확정) + **B70 필터 잠금 버그**(먼저) · **PH5b 모바일 — 피드 우선 + 전체 화면 지도**(`SPEC.md` §CH5b, 2026-09-21 사용자 확정). 프론트가 끌기 성능을 먼저 잰다 · 원래 범위:(B62 ①②④ + 도크→시트 + B16) — **착수 조건(실기기 관찰 3건)은 2026-09-21 찼다.** 열 때 「③ 고친 뒤에도 남는 답답함」부터 묻는다 · 그다음 **PH7 시즌 이벤트**(10월) · **사이트 전체 다크** | B62 반영 |

- 이건 워터폴이 아니라 **파이프라인**이다. 기획이 PH(n+1)을 하는 동안 프론트가 CH(n)을 구현한다. 아무도 놀지 않는다.
- **프론트가 "스펙이 없다"고 멈추면 그건 기획 세션의 실패다.** 앞서 있으면 그 일이 안 생긴다.
- 두 챕터 이상 앞서가지도 않는다. 구현되지 않은 스펙이 쌓이면 현실과 어긋나기 시작한다(2026-08-22에 실제로 발생).

---

## 2. 작업 단위 — `SESSIONS.md` 를 따른다

챕터 → 태스크(커밋 1개) → 곁가지, 그리고 **스코프 잠금**은 `SESSIONS.md` §작업 방식이 정본이다. 기획에만 해당하는 것:

- **기획 챕터(PH)** = 프론트 챕터 하나를 해금하는 스펙 묶음. 태스크 = 문서 하나의 한 덩어리, 또는 목업 하나.
- **곁가지**(작업 중 떠오른 다른 결정)는 **지금 정하지 않는다.** 담당 PH를 배정해 `SPEC.md` §3 미결에 한 줄 남기고,
  보고에 「**발견했고 안 정했습니다**」를 적는다.
- **챕터가 아닌 건별 결정**(프론트·백엔드가 올린 발견에 답하는 것)도 같은 규칙이다 — 결정마다 `DECISIONS.md` 에 쓰고,
  **메시지로만 보내지 않는다**(2026-09-21: 결정 기록이 없어 백엔드가 계약을 못 고치고 멈췄다).

---

## 3. 챕터 진행 5단계

1. **현황 확인** — 이 챕터가 다루는 화면의 **실제 구현을 먼저 읽는다.**
   프론트 저장소의 `public/assets/discover.js`·`discover.css`·`site/home.py`·`site/route.py`를 **읽기 전용**으로 연다(형제 폴더 `../galmal-frontend`).
   → 문서가 현실보다 뒤처지는 사고(2026-08-22 `DESIGN.md` 50m geojson 오기)를 막는 유일한 방법이다.
2. **미결 수집** — 프론트 `BACKLOG.md`의 해당 챕터 항목 + `SPEC.md` 미결 목록에서 이번에 정할 것을 모은다.
3. **근거 만들기** — 가능하면 **실데이터로 검증한다.** 데이터는 **공개 URL**(`api.galmal.kr/v1` · `galmal.kr`)에서 받는다 — 이 저장소엔 제품 데이터가 없다(`design/_data.py`). DB를 봐야 하는 측정은 **백엔드에 청한다.**
   (예: 신선도 배지 등급 경계는 실측 분포 143건에서 뽑았다 — `DECISIONS.md` 2026-08-22)

   > ⚠️ **이 환경 콘솔은 cp949라 파이썬이 찍는 한글이 깨진다.**
   > 숫자는 안 깨지므로 **"수치는 맞는데 라벨만 틀린" 결과**가 나오고, 그럴듯해서 잘 안 걸린다.
   > (2026-08-22 프론트 세션이 실제로 이 함정에 빠져 태그 3개를 잘못 보고했다.)
   > **깨진 출력을 보고 라벨을 추측해 채우지 않는다.** 파일에 UTF-8로 쓰고 `Read`로 읽거나
   > `sys.stdout.reconfigure(encoding='utf-8')`로 다시 돌린다. 한글 값을 문서·계약에 박기 전에는 특히.
4. **결정 기록** — 아래 §4 체크리스트 전 항목.
5. **핸드오프** — main 병합·push → 핸드오프 메모(§7) → **`SendMessage`로 프론트·백엔드에 직접 전달**
   (`SESSIONS.md` §세션 간 소통 — 사용자를 전달책으로 쓰지 않는다).

---

## 4. 결정 품질 체크리스트 (챕터 끝, 전 항목)

- [ ] **무엇을** 정했나 → `SPEC.md`(인터랙션) 또는 `DESIGN.md`(디자인 시스템)에 기록
- [ ] **왜 + 무엇을 버렸나** → `DECISIONS.md`에 기록. 기각안 없는 결정은 검토가 부족했다는 뜻이다.
      🔒 **한 항목 15줄 안**(2026-09-22). 넘치면 현행 문서로 갈 내용이 섞인 것이다 — 현행은 `SPEC.md`/`COPY.md`에 덮어쓰고 여기엔 「왜·무엇을 버렸나」만.
- [ ] **현행 문서에 경위를 남기지 않았나** — `SPEC.md`·`COPY.md`·`DESIGN.md`·`CONTRACT.md`에 취소선·✅·「정정」 블록을 두지 않는다. 뒤집히면 그 줄을 덮어쓴다(`DECISIONS.md` 2026-09-22 (5)).
- [ ] **모호한 형용사가 없나** — "적당히 / 자연스럽게 / 부드럽게 / 깔끔하게"는 스펙이 아니다.
      숫자·조건·상태 이름으로 바꾼다. (사용자가 프론트 코드를 못 보므로, 모호하면 아무도 못 잡는다)
- [ ] **모든 상태를 정의했나** — 정상 / 빈 / 0건 / 로딩 / 에러 / JS 없음 / 모바일. `SPEC.md`의 상태표를 채운다.
- [ ] **화면에 나올 문자열을 `COPY.md`에 넣었나** — 프론트가 문자열을 지어내면 톤이 무너진다.
- [ ] **계약 변경이 있나** → `CONTRACT.md`(이유)와 `DECISIONS.md`(결정)에 **먼저 쓰고** 백엔드에 알린다.
      필드·어휘 목록의 정본은 백엔드 `contract/v1/` 이라 **거기는 백엔드가 고친다** — 커밋 메시지에 우리 결정을 인용한다(`SESSIONS.md`).
- [ ] **`CONTRACT.md` 의 절 제목을 바꿨다면 — 남의 코드 주석이 그 제목을 가리킨다**
      백엔드·프론트 주석에 「`CONTRACT.md` §공통 규칙」 같은 참조가 20곳쯤 있다(2026-09-21 실측). 제목을 바꾸면 **바꾼 목록을 두 세션에 알린다.**
      (경위: 2026-09-19 전에는 백엔드 검증기가 이 문서의 **표를 파싱**해서, 상위 태그에 `**굵게**`를 넣었다가 main CI 가 빨개진 적이 있다.
      표를 백엔드 `contract/` 로 옮긴 뒤로 **이 문서를 읽는 기계는 없다** — 2026-09-21 확인.)
- [ ] **프론트 `BACKLOG.md` 의 해당 항목에 답했나** — 프론트가 그걸 보고 막혀 있다. 단 **「해소」는 화면에서 본 뒤에만** 적는다(함정 13)
- [ ] **목업이 필요한가** — 글로 합의가 안 되는 레이아웃·밀도 문제면 `design/`에 만든다
- [ ] **문서를 옮겼다면 그 문서를 가리키던 문장도 고쳤나** — 절을 이동하면
      `"○○.md의 △△ 참조"`와 `"○○.md가 확정한/적고 있는"` 같은 **현재형 서술이 전부 거짓이 된다.**
      옮긴 절 제목으로 저장소 전체를 `grep`하고, 이력은 이력 표현("2026-08-01 확정")으로 바꾼다.
      **옮긴 내용이 실제로 도착했는지도 대조한다** — PH0에서 태그라인 1건과 정책 근거 1건이 조용히 유실됐다.
- [ ] **저장소의 날짜·시각 라벨을 KST로 단정하지 않았나** — 이 프로젝트는 한국 전용인데
      **내부 시각은 UTC인 곳이 많다.** 크론 러너가 UTC라 `fetched_date`·`collect:` 커밋 날짜가
      KST보다 하루 이르고, API의 `found_at`도 UTC다. 같은 함정에 **두 번 걸렸다** —
      신선도 근거(2026-08-22)와 "크론 2일 미실행" 오보(2026-08-22, 실제로는 20시간 전 정상 수집).
      날짜로 무언가를 주장하기 전에 **그 값의 타임존을 확인한다.**
- [ ] **결론이 맞아도 근거가 맞는지 다시 본다** — PH0에서 신선도 타임존 결론(UTC)은 맞았으나
      근거로 든 "80h = 72h + 9h"가 틀렸다(실제 컷이 96h). **상한값의 우연한 일치는 약한 증거고,
      하한값이 0에 붙는지가 강한 신호다.** 숫자가 딱 맞아떨어질 때일수록 의심한다.

---

## 5. 기획 세션이 하지 않는 것

- **앱 코드를 고치지 않는다.** `galmal-frontend`·`galmal-backend`는 **읽기만** 한다 — 고칠 게 보이면 그 세션에 알린다(`SESSIONS.md`).
- **구현 방법을 지시하지 않는다.** 스펙은 "**무엇을 · 왜**"까지다. "어떻게"(자료구조·함수 분리·CSS 기법)는
  프론트 세션의 자율 영역이다. 볼 수도 없는 코드를 지시하면 스펙만 썩는다.
- **없는 데이터를 전제하지 않는다.** 스펙에 필드가 필요하면 `CONTRACT.md` 변경 절차를 밟는다.
  "백엔드가 주겠지"로 스펙을 쓰지 않는다.
- **시즌 한정 표현을 스펙에 넣지 않는다.** ("광복절 연휴" 같은 것 — 지금만 유효해서 코드가 썩는다. `DECISIONS.md`)

---

## 6. 챕터 로드맵

| # | 챕터 | 정할 것 | 해금 | 상태 |
|---|---|---|---|---|
| **PH0** | **기반 정리** | 문서 현실 동기화 · 역할 재편 · IA · 유저 플로우 · 상태 인벤토리 · 카피 덱 | — | ✅ 완료 (2026-08-22) |
| **PH1** | 지도 무대 | B14 단계 구성 · B3/B15 라벨 겹침 · B4 LOD · B2 트윈 | CH1 | ✅ 완료 (2026-08-22) |
| **PH2** | 필터·정렬 | C-10 칩 어휘 · B5/C-9 예산 · B6 필터 시 지도 · B7 도크 표기 · B16(부분) | CH2 | ✅ 완료 (2026-08-22) |
| **PH3** | 피드·큐레이션 | F18 훅 폐기 · F17 도장 임계값 · F19 단어 분리 · F16 hero · F1/F2 빈·희소 · C-1/C-2/C-5/C-11/C-12 | CH3 | ✅ 완료 (2026-08-22) |
| **PH3b** | 카드 표현 | 도장 3티어 · 직항 배지 · **태그 어휘 12→24종 재배정(C-13)** | CH3 | ✅ 완료 (2026-08-22) |
| **PH4** | 상세·전환 | IA-2 딥링크 · F5 사라진 딜 · F4 links 0건 · 확장 카드 위치 · B9 보류 판단 | CH4 | ✅ 완료 (2026-08-22) |
| **PH5** | 모바일 | B10 탭 동작 · 하단 시트 · 가로 스크롤 피드 | CH5 | ✅ 완료 (2026-09-02) |
| PH6 | 접근성·마감 | B11 키보드·aria · **IA-1 홈→노선 진입로** · IA-3/C-3/C-4 용어·말투 · B12 사진 · B13/B17 성능 | CH6 | ✅ 완료 (2026-09-02) |
| **PH6b** | 대륙 선택 · 표시명 정본 | 「아주 멀리」 대륙 호버·칩 · 예산 눈금 재확정(로그→선형) · **`COPY.md` §2b 지역 표시명 표** · OG 이미지 | CH1(추가) · CH2 | ✅ 완료 (2026-09-03) |

| PH7 | 시즌 이벤트 | 벚꽃·단풍·눈축제 — **도시별 공식 축제 일정** 기반. 팝업/카드 스타일/별도 페이지 중 택 | 신규 CH | **후순위 (10월 착수 목표)** |

- **PH7은 시점이 중요하다.** 지금(8월) 만들면 단풍 시즌(10~11월)까지 아무도 못 보고 코드만 썩는다.
  **만들자마자 켜서 검증할 수 있는 시점**에 만든다.
- 챕터 번호는 **프론트 CH와 1:1로 맞춘다.** 어긋나면 "한 챕터 앞서기"를 셀 수 없게 된다.
- 로드맵에 없는 신규 안건(예: PH0에서 발견한 URL·공유 구조)은 **어느 PH/CH에 넣을지 먼저 배정**하고 시작한다.

---

## 6b. 목업을 눈으로 확인하는 법 (2026-09-03 추가)

브라우저 확장이 안 붙어도 **헤드리스 크롬으로 PNG를 굽고 그걸 직접 읽으면** 화면을 볼 수 있다.
`design/build_og.py`가 이 방식으로 만들어졌고, 렌더 버그를 네 번 잡았다.

```
"C:/Program Files/Google/Chrome/Application/chrome.exe"   --headless=new --disable-gpu --hide-scrollbars   --force-device-scale-factor=1   --screenshot=OUT.png --window-size=1440,900   --virtual-time-budget=8000 "file:///.../design/xxx.html"
```

- `--window-size`로 반응형 분기를 각각 찍는다(1440×900 · 900×800 · 390×844).
- **작은 글씨는 `--force-device-scale-factor=2`** 로 찍으면 읽힌다. Pillow가 없어 크롭은 못 한다.
- **빌드 의존이 아니다.** 사람이 확인용으로 한 번 돌리는 것이라 "Node 금지 / CDN 의존 0"과 무관하다.
- ⚠️ **스크래치패드는 세션과 함께 사라진다.** 그래서 명령을 여기 적어 둔다.

### 🔴 조용히 틀리는 것 넷 — 전부 「그럴듯하게」 나온다

깨지면 알아채는데 **그럴듯하면 못 알아챈다.** 프론트 세션이 정리한 목록이고,
1·4는 기획 목업(`region.html` 대륙 확대 · `open.html` 상세 열기)에도 그대로 걸린다.

| # | 함정 | 증상 |
|---|---|---|
| 1 | `--virtual-time-budget`이 **CSS 전환을 중간에 얼린다** | 멀쩡한 카드가 반투명·부분 렌더로 찍힌다 |
| 2 | 주입 스텝을 개행으로 이으면 **배열이 깨져 통째로 안 돈다** | 에러도 안 보인다 |
| 3 | `node --check`는 **정의 없는 호출을 못 잡는다** | 핀 0개·카드 0장인데 문법은 OK |
| 4 | `*{transition:none}`은 **rAF 트윈을 안 끈다** | 트윈 중간 상태를 「최종 상태」로 읽는다 |
| **5** | 🔴 **헤드리스 크롬 창에 최소 폭(~500px)이 있다** | `--window-size=390`을 줘도 **500px로 그린 뒤 390px만 잘라 저장**한다 |

- 1은 `*{transition:none!important;animation:none!important}` 주입으로 막는다(`build_og.py`에 넣었다).
- **4는 그걸로 안 막힌다.** JS 트윈이 있는 목업은 **트윈이 끝난 뒤**에 찍거나, 값을 읽을 때
  **언제 읽었는지**를 같이 적어야 한다.

### 내가 이걸로 세 번 오진했다

`og.png`에 가로 흰 줄이 나왔을 때 **① 헤드리스 타일 이음매 ② 국가 폴리곤 이음매**로 두 번 짚고
둘 다 틀렸다. 실제 원인은 **지구를 한 바퀴 감는 링**(남극)이었다.
**픽셀을 직접 디코드해서 세로로 훑고 나서야** 찾았다.

> **렌더러를 의심하기 전에 데이터를 재라.** 추측 두 번이 실측 한 번보다 오래 걸렸다.
> 반대 방향도 있다 — 프론트는 **멀쩡한 화면을 버그로 오진할 뻔했다**(위 1번).

### 그리고 5번에 내가 실제로 걸렸다 (2026-09-04)

배포본을 `--window-size=390`·`430`으로 찍고 **"폰에서 가로로 넘친다 · 헤더 알약이 잘린다"**고
사용자와 프론트 양쪽에 보고했다. **틀렸다.**

프론트가 CDP `Emulation.setDeviceMetricsOverride`로 진짜 390px를 만들어 재보니 **넘침이 0**이었다.
내가 검증했다 — **390px 이미지가 500px 이미지의 왼쪽 390px과 픽셀 100% 일치**한다.
**500px로 그려놓고 390px만 잘라 본 것**이라, 잘린 자리가 「넘침」으로 보였다.

**PNG 크기가 390이라 뷰포트도 390인 줄 알았다.** 산출물의 크기와 렌더 조건은 다른 층이다.
`--window-size`는 **창** 크기지 뷰포트 보장이 아니다.

> **모바일 화면은 `--window-size`로 못 잰다.** CDP로 뷰포트를 직접 지정해야 한다.
> 데스크톱 폭(≥500px)만 이 방식으로 믿을 수 있다.

### 6번 — 확인 명령을 문서에 넣기 전에 돌려보지 않았다 (2026-09-08~09, 이틀 연속)

`CLAUDE.md`의 재빌드 확인 명령을 **두 번 연속 틀리게 적었다.** 둘 다 「확인이 확인 구실을
못 하는」 유형이고, 둘 다 **남이 잡아줬다**(백엔드·프론트가 각각).

| 언제 | 적은 것 | 왜 틀렸나 |
|---|---|---|
| 09-08 | `grep -c '"ad":true' … ` → 125 | `deals.json`이 **개행 없는 한 줄**이라 `grep -c`는 **항상 `1`**. 전량 손실만 걸리고 **부분 손실은 통과** |
| 09-09 | `grep -o … \| wc -l` → **125** | 명령은 맞는데 **기대값이 하루 만에 낡았다**(133). 정상인 사람이 고장으로 오인할 상태 |

**두 실패는 방향이 반대다**(프론트 정리):

```
grep -c   너무 느슨했다  →  부분 손실을 통과시킨다 (125건 중 3건만 남아도 통과)
기대값 125 너무 빡빡했다  →  정상을 이상으로 판정한다 (133이 정상인데 고장으로 읽힘)
```

**한 축의 양 끝이다.** 그래서 「더 엄격하게」도 「더 느슨하게」도 답이 아니었고,
**재는 대상 자체를 바꾸는 것**이 답이었다 — 변하는 값(건수)에서 변하지 않는 값(둘의 관계)으로.

> **좋은 확인은 「변하지 않는 것」을 잰다.**

처방은 하나다:

> 🔴 **확인 명령은 문서에 넣기 전에 돌려본다.** 그리고 **정상만 보지 말고 망가뜨려 본다.**

세 번째로 고칠 때 실제로 돌려 `133 133`을 봤고, 백엔드가 **일부러 망가뜨려**
`133 3`이 나오는 것까지 확인했다. `grep -c`는 같은 상황에서 **둘 다 `1`**이었다.

#### 여기서 나온 일반 규칙

> **좋은 검사는 절대값을 기대하지 않는다. 같은 데이터 안의 두 값을 비교한다.**

「제휴 링크가 125건인가」는 매일 낡는다. 「**딜 수와 제휴 링크 수가 같은가**」는 안 낡는다 —
**비교 대상이 같은 파일 안에 있어서 매일 바뀌어도 같이 움직이기 때문이다**(백엔드 정리).

같은 유형을 이 저장소에서 이미 여러 번 만났다:
`test_site_url.py`가 `BASE_URL`과 `CNAME`을 **대조**하는 것, M3 T6이
`subscribe.address`와 `MAIL_ADDRESS`를 **대조**하는 것, M2 T5가
**같은 커밋의** 입력과 출력을 대조하는 것 — 전부 「두 값을 맞춰 본다」이지
「값이 무엇인지 외운다」가 아니다.

### 7번 — 동등성을 작업 트리와 비교했다 (2026-09-09, M2 검증)

프론트가 「기준선 대비 39/39 바이트 동일」이라고 냈다. **내가 독립 검증했더니 39/39 전부 달랐다.**
경보를 울리기 전에 **내 설정을 먼저 의심**했고, 그게 맞았다.

```
git 객체(blob)      31,252 바이트   LF
작업 트리 파일       31,603 바이트   CRLF   ← 351줄 × 1바이트
내 빌드 산출물       31,252 바이트   LF     ← blob 과 정확히 같다
```

원인은 `core.autocrlf=true` + **`.gitattributes` 없음**이다. 체크아웃할 때 CRLF로 바뀌고
커밋할 때 LF로 돌아간다. **왕복은 맞아떨어지므로 저장소는 멀쩡하다** —
깨지는 건 **로컬 비교**뿐이고, 하필 그게 이 이전의 관문이다.

내가 `git archive`로 기준선을 꺼냈는데 그게 autocrlf 변환을 적용했다.

> 🔴 **동등성 검증은 작업 트리가 아니라 blob과 비교한다.**
> `git cat-file -p <sha>:<path>` 를 쓴다. `git archive`·체크아웃된 파일은 **변환이 낀다.**
> 윈도우에서 이 함정은 **항상 켜져 있다.**

#### 같은 병의 두 번째 발현 — 이번엔 **파이프**를 blob으로 착각했다 (2026-09-16)

`.gitattributes`를 언제 넣을지 정하려고 커밋된 줄바꿈을 쟀다. 이렇게 셌다:

```bash
git cat-file -p "origin/main:$f" | grep -c $'$'     # ← 전부 CRLF 로 나왔다
```

그래서 「문서 7개 4,604줄이 전부 CRLF」라고 적고, 그 위에 T4a의 근거를 세우고, **두 세션에
전달까지 했다.** 프론트가 「제 구역 셋은 LF입니다」라고 반례를 냈고, 바이트로 다시 재니
**blob은 전부 LF**였다.

**Git Bash(MSYS)가 `git`의 stdout을 파이프로 넘기며 LF를 CRLF로 바꾼다.** 나는 blob을 본 게
아니라 **파이프가 만들어 준 것**을 봤다. 7번과 정확히 같은 병이다 — 그때는 작업 트리를,
이번엔 파이프를 blob으로 착각했다.

> **줄바꿈은 셸을 거치지 않고 바이트로 잰다.**
> ```python
> b = subprocess.run(["git","cat-file","-p",ref], capture_output=True).stdout
> crlf = b.count(b"
");  lf = b.count(b"
") - crlf
> ```
> 셸을 거치면 **셸의 답**을 듣는다. 그리고 이게 7번의 교정("작업 트리 말고 blob을 봐라")을
> **지킨 뒤에도** 걸렸다는 게 요점이다 — 「blob을 봐라」로는 부족하고
> **「blob을 어떻게 읽었나」**까지 물어야 했다.

**손해**: 오측이 문서에 들어갔고 결정 하나(T4a의 근거)가 그 위에 섰다. 결론은 살아남았지만
**이유가 통째로 틀렸다.** 프론트가 자기 구역을 실측해 반례를 내지 않았으면 그대로 굳었다.

**곁가지로 확인한 것**: 크론(리눅스)은 LF로 쓰고 LF로 커밋한다. 윈도우 세션이 재빌드하면
디스크엔 CRLF로 쓰고 git이 LF로 되돌린다 — **양쪽이 같은 blob을 만든다.** M3 스위치에서
줄바꿈 때문에 대량 diff가 날 걱정은 없다. (확인 안 했으면 M3에서 놀랐을 자리다.)

### 8번 — 조건은 맞게 적었는데 빈도를 안 쟀다 (2026-09-11, `dateModified`)

「크론이 UTC 자정 근처로 가면 갈린다」를 **잠복 변경**으로 적었다(프론트 정보를 받아 그대로).
**조건은 맞았다. 틀린 건 「언젠가」라고 생각한 것**이다 — `gh run list` 한 번 보니
**이미 10회 중 4회가 그 조건**이었다. 미래의 위험이 아니라 **내일 절반 확률로 터지는 버그**였다.

> 🔴 **조건을 적을 때 그 조건이 지금 얼마나 자주 켜지는지를 같이 잰다.**
> 「~일 때 갈린다」만 적으면 읽는 사람은 「드물게」로 읽는다. 빈도가 없으면 등급을 못 매긴다.

그리고 **우연히 통과한 검증을 조심한다** — T1 첫 대조가 39/39였던 건 그날 크론이 자정 후에 돌았기 때문이다.
**한 번 통과는 조건이 안 켜진 날의 통과일 수 있다.** 백엔드가 T1을 「매일 대조」로 짠 덕에 이게 드러났다.

> **하루치 데이터로 된 증명은 그날의 시각만 증명한다**(백엔드 정리). 백엔드의 로컬 대조도 같은 이유로
> 눈멀었다 — KST 오후에 돌려서 KST 날짜 == UTC 날짜였다. **동등성 검사는 입력이 경계(자정·월말·빈 목록)를
> 밟는 경우를 일부러 넣어 본다.** 매일 돌리는 것보다 경계를 한 번 밟아 보는 게 싸다.

### 9번 — 남의 구역 숫자를 낡은 상태에서 셌다 (2026-09-15, 백엔드 정리)

백엔드가 「`design/` 33개가 T3으로 죽는다」고 알려왔는데, 실제로는 **`.py` 0개**였다 —
기획이 그 직전 커밋에서 이미 옮겨둔 뒤였다. `git fetch`는 했지만 **그 시점 이후 들어온 커밋**이었다.

> **남의 구역 숫자는 그쪽 최신을 받고 센다.** `fetch`만으로는 작업 트리가 안 바뀐다.
> 그리고 셀 때 **무엇을 세는지 말한다** — 「33개 파일」이 아니라 「`.py` 몇 · `.html` 몇」이면
> 받는 쪽이 「생성물 푸터 문구는 안 깨진다」를 바로 판단할 수 있다.

이번엔 손해가 없었다(기획이 다시 세서 0개임을 확인했다). 다만 **반대 방향이었으면 위험하다** —
「0개다」라고 낡은 상태에서 세어 전달하면 받는 쪽이 안심하고 지운다.

#### 같은 함정의 두 번째 얼굴 — **아예 안 세고 추정했다** (2026-09-16)

`site/shell.py`의 구독 주소 literal을 프론트에 넘기며 이렇게 적었다:

> *「`shell.py`는 모듈 상수 구조라 호출부 3곳(`home`·`route`·`seo`)이 얽힙니다.
> 이 급이면 CH 하나 잡을 만합니다.」*

**`page(`를 grep한 적이 없다.** 실제 호출부는 `site/route.py:272` **하나**였고
(`home.py`는 `page()`를 안 쓰고 `seo.py`는 HTML을 안 만든다), 프론트는 **한 커밋**으로 끝냈다.

낡은 숫자보다 나쁘다 — 낡은 숫자는 한때 참이었지만 **이건 한 번도 참인 적이 없다.**
그리고 손해가 날 뻔했다: **한 커밋짜리를 별도 챕터로 미루라고 권했다.**
프론트가 그대로 따랐으면 1분짜리 고침이 며칠 밀렸다.

> **남의 구역 작업량을 말할 거면 세고 말한다. 못 세겠으면 「모른다」고 한다.**
> 「~곳이 얽힌다」는 측정값의 문법인데 추정에 썼다 — 받는 쪽은 잰 줄로 읽는다.
> 규모 추정은 **일정을 바꾸므로** 사실 진술과 똑같이 취급한다.

### 10번 — 「낡은 참조를 훑었다」면서 손으로 골랐다 (2026-09-16, T7 보완)

M3 T3에서 `build_site.py`가 사라졌다. T7(2026-09-15)은 「옛 경로를 가리키는 문서를 고친다」는
태스크였고, `CLAUDE.md` 5곳 · `design/*.py` 20개 · `CONTRACT.md` 한 절을 고쳤다고 보고했다.

**`PROJECT.md`를 빠뜨렸다.** 기획 소유 문서이고, 「빌드 진입점은 `build_site.py` 하나」(:157)와
충돌 해결 절차(:150)가 그대로 남아 있었다. **둘 다 따라 하면 실패하는 지시**다.

원인은 훑은 **방법**이다. 고칠 파일을 머리로 떠올려 골랐다 — `CLAUDE.md`는 방금 봤으니 떠올랐고,
`design/`은 내 구역이라 떠올랐고, `PROJECT.md`는 **그날 안 열어봐서 안 떠올랐다.**

> **「전수」라고 적을 거면 grep으로 세고, 그 명령을 보고에 같이 적는다.**
> 손으로 고른 목록은 전수가 아니라 표본이다. 함정 6번(확인 명령을 안 돌려봄)과 같은 뿌리다 —
> **확인 가능한 형태로 만들지 않으면 틀려도 모른다.**

**그리고 grep 결과를 둘로 가른다.** 2026-09-16 전수에서 `build_site` 히트가 39곳 나왔는데
고칠 건 6곳뿐이었다:

| 종류 | 예 | 처리 |
|---|---|---|
| **역사 기록** | `DECISIONS.md` 「`build_site.py`가 sqlite를 물고 HTML을 짜고 있었고」 · `site/route.py:4` 「~에서 인수했다」 | **그대로 둔다.** 고치면 기록이 거짓이 된다 |
| **현행 서술·지시** | `PROJECT.md:157` 「진입점은 `build_site.py` 하나」 · `IA.md:113` 「(`build_site.py:274`)」 | **고친다.** 따라 하면 실패한다 |

가르는 질문 하나: **「이 문장은 과거형인가, 현재형인가.」** 과거형이면 유물이고 현재형이면 지시다.

### 11번 — **예외가 붙은 규칙은 예외가 먼저 죽는다** (2026-09-16, 프론트 발견)

`SPLIT.md` M4 T4를 이렇게 썼다: 「`tests/`는 백엔드로, **단 `test_charts.py` 제외** —
프론트 코드를 테스트한다」.

백엔드는 규칙대로 제외했다. 프론트는 정리하며 `tests/`를 **통째로** 지웠다.
→ **두 저장소 어디에도 없는 상태**가 됐다. 「제외」는 **한쪽이 챙기기로 한 약속**인데
문장이 백엔드 행에만 있었고, **프론트 행에는 「받는다」가 없었다.**

프론트가 R8을 확인하다 「저장소에 테스트가 0개」인 걸 보고 되살렸다. 스스로 M2에서
「이건 프론트로 와야 한다」고 짚었던 파일을 자기 손으로 지운 것이다.

> **한쪽의 「제외」는 반대쪽의 「포함」으로도 적는다.** 한 곳에만 적힌 예외는
> 그 문장을 읽는 사람만 지킨다. 지우는 사람과 받는 사람이 다르면 반드시 샌다.
> 그리고 **이동이 끝나면 「무엇이 사라졌나」를 센다** — 옮긴 것을 세는 걸로는 안 잡힌다.

같은 창에서 백엔드도 같은 종류를 겪었다 — `.github/`를 「워크플로만 있겠지」로 짐작했다가
`skills/` 144개를 발견했고, 프론트는 거기에 `.claude/` 143개를 더 찾았다.
**「어디에 있나」로 짐작하지 말고 「무엇이 있나」를 센다.**

#### 🔴 왜 강한 검증이 이걸 못 잡았나 (프론트 정리, 2026-09-16)

프론트는 **남긴 60개가 옛 저장소 blob과 바이트 동일**한 것까지 확인하고 지웠다.
이 프로젝트에서 가장 강한 형태의 검증이다. **그런데도 못 잡았다.**

> **「남은 것」만 보는 검증은 「없는 것」을 영원히 못 본다. 대조할 대상이 없기 때문이다.**

이게 바이트 대조·계약 검증·불변식 대조가 **공통으로 가진 맹점**이다. 전부 **존재하는 값
둘을 비교**한다. 누락은 값이 하나도 없어서 비교 자체가 성립하지 않는다.

**→ 누락에는 다른 모양의 검사가 필요하다: 여집합을 센다.**
```
사라진 것 = 옛 목록 − 지금 목록      (rename 은 먼저 되돌려 매핑한다)
```
프론트 실측: 옛 526 · 지금 66 · 사라진 것 462 → 소유 패턴으로 걸러 12개 → 전부 오탐 확인.

**rename 되돌리기가 필수다**(프론트 지적). 안 하면 정상적으로 옮긴 `public/assets/*` 5개와
`world.geojson`이 「사라짐」으로 잡힌다. **노이즈가 섞이면 진짜 하나를 못 본다** —
검사를 무디게 만드는 게 아니라 **못 쓰게** 만든다.

#### 그리고 순서가 근본이다 (프론트 자기 분석)

> *"지울 것을 세는 대신 **남길 것을 먼저 확정하고 나머지를 지웠으면** 이 누락이 안 났을 것이다."*

맞다. 「지울 목록」으로 작업하면 **목록에 없는 것이 조용히 남거나 사라진다.**
「남길 목록」으로 작업하면 **남길 것이 전부 있는지**가 작업 자체의 조건이 된다.

#### 곁다리 — 이름이 비슷하다고 같은 구역이 아니다 (두 번째)

프론트가 `discover` 패턴으로 거르자 `collector/discover_data.py`·`tests/test_discover_data.py`가
걸렸다. **백엔드 파일이고 `CLAUDE.md` 프론트 금지 목록에 명시돼 있다.**
`discover.js`·`discover_home.py`와 이름만 겹친다. 이 프로젝트에서 **두 번째**다
(첫 번째가 `discover_home.py` 소유권 혼선). **패턴 매칭의 결과는 후보지 판정이 아니다.**

### 12번 — 측정이 구별할 수 있는지부터 본다 / 검사는 전환 순서를 본다 (2026-09-19, vocab 챕터)

**① 구별 못 하는 측정은 「차이 없음」을 낸다** (프론트 발견). 칩 14개를 옛/새 화면에서 눌러 카드 수를 비교했더니
**전부 79로 같았다.** 필터가 카드를 숨기지 않고 **흐리게** 해서 카드 수가 원래 안 변했던 것이다 — 무엇을 바꿔도 같은 값이
나오는 측정이었다. 흐려지지 않은 카드·핀 수로 다시 재니 칩마다 12가지 결과가 갈렸고 옛/새 전부 같았다.
그리고 **대조군**: 칩 표식을 일부러 지우면 「그 이후」가 24 → 79로 변하는 것까지 본 뒤에 「같다」를 믿었다.
> **「같다」를 믿기 전에, 일부러 깨뜨렸을 때 그 측정이 「다르다」를 내는지 본다.** 함정 11(남은 것만 보는 검증은
> 없는 것을 못 본다)과 짝이다 — 그건 **대상**이 빠지는 병이고, 이건 **자**가 눈금이 없는 병이다.

**② 대칭으로 걸고 싶은 검사는 교착을 만들 수 있다** (기획 실수, 프론트가 잡음). 어휘 → 색표 방향(빠지면 폴백)을 빌드 실패로
걸고 나서, 「대칭이 깔끔하다」며 색표 → 어휘 방향(죽은 키)도 실패로 걸자고 했다. 그런데 새 태그는 **프론트가 색을 먼저 넣고
→ 백엔드가 어휘를 바꾸는** 순서로 들어온다(백엔드 `1154d0d`). 그 사이 「색표에만 있는 키」는 **정상 상태**다.
두 방향을 다 실패로 걸면 **어느 쪽도 먼저 움직일 수 없다.**
> **검사를 걸기 전에, 그 검사가 막는 상태가 정상적인 전환 중에도 생기는지 본다.** 생기면 그 방향은 실패가 아니라 알림이다.
> 사용자에게 보이는 사고를 막는 방향(어휘 ⊆ 매핑)만 실패로 둔다.

#### 규칙을 쓸 때의 진단 문장 (백엔드 정리, 2026-09-09)

검사를 어디 둘지 막혔을 때, **어떻게 진단하느냐가 거기서 멈출지 계속 갈지를 가른다.**

| 진단 | 다음 행동 |
|---|---|
| ❌ 「**테스트로는 못 잡는다**」 | 여기서 멈춘다. 방법이 없다는 말로 들린다 |
| ✅ 「**테스트 자리에서는 한쪽 값에 못 닿는다**」 | **「그럼 닿는 자리는 어디지」**로 이어진다 |

BB32가 실제로 그랬다. 백엔드가 앞엣말로 적고 멈췄고, 뒷말로 바꾸니
**시크릿이 있는 유일한 자리 = 크론 상태 점검**이 바로 나왔다(M3 T6).

> **검사는 「비교할 두 값에 동시에 닿을 수 있는 곳」에 놓는다.**
> 못 닿으면 검사가 없는 게 아니라 **자리가 틀린 것**이다.

**그리고 왜 안 되는지를 명령 옆에 적는다.** 안 적으면 다음 사람이
「간단하게 숫자 하나로 하자」로 되돌린다. `grep -c` 때 이유를 같이 적어둔 덕에
**하루 만에 그 처방이 필요한 상황이 실제로 왔을 때** 되돌아가지 않았다.

---

### 13번 — **「스펙 확정」을 「해소」로 닫았다** (2026-09-21, 프론트 발견 둘째)

`SPEC.md` §CH4 「막대가 없을 때 이유를 말한다」는 2026-09-01 에 확정됐고 `COPY.md` C-16 은 **✅ 해소**로 닫혀 있다.
**화면에는 그 문구가 0건이다.** 상세 **둘 중 하나**(라이브 138건 중 `median <= price` 65건 + `null` 5건 = **51%**)에서
근거 자리가 **말없이 사라진다.** 같은 모양이 이미 하나 있었다 — B65(`이 노선 시세 자세히 →`, 2026-09-01 확정, 구현 0곳).

**둘 다 「기획이 정했다」에서 멈췄고, 아무도 화면을 다시 안 봤다.** 기획은 확정을 적고 닫았고, 프론트는 그 챕터에서 다른 것을 했고,
백엔드는 `route` 설명에 **그 링크가 있는 것처럼** 적어 두기까지 했다(2026-09-21 계약 정리에서 드러남).

🔒 **규칙: 「✅ 해소」는 화면에서 본 뒤에만 적는다.** 스펙이 확정되면 그건 **「확정」**이지 **「해소」**가 아니다.
표기를 나눈다 — **확정(스펙이 있다)** / **반영(화면에 있다)**. 미결 목록에서 지우는 건 **반영**일 때뿐이다.

> **전수 대조가 못 보는 것 셋**(2026-09-22, 프론트): ① **동작으로만 정한 것**(「다시 누르면 닫힌다」 — B70) ② **스펙과 문구가 서로 갈린 것**
> (어느 쪽을 찾아도 「있다」로 세어진다 — 신기록 문구) ③ 두 문서가 같은 값을 다르게 적은 것(신기록 임계 7/14). ②③은 **현행 문서에서 경위를 빼고
> 한 사실을 한 곳에만 두면** 사라진다 — 문서 정리(2026-09-22 결정)의 이유. ①은 아직 방법이 없다.
>
> **왜 이게 비싼가**: 「해소」로 닫힌 항목은 **아무도 다시 안 본다.** 미결이면 언젠가 걸리지만, 해소는 검색에서도 빠진다.
> 조용히 통과하는 형태의 문서판이다.

## 7. 사용자에게 보고하는 말투

**사용자에게는 친구 같은 반말체로 말한다** — `~할게` · `~어때?` · `~야`. (2026-08-22 사용자 지시)

**왜**: 세 세션이 각각 보고하는데 말투가 같으면 **사용자가 누가 말하는지 구분할 수 없다.**
세션마다 다른 말투를 배정하는 중이고, 기획 세션이 대화량이 가장 많을 것 같아 가장 편한 말투를 받았다.

**적용 범위 — 사용자와의 대화에만.**
| 대상 | 말투 |
|---|---|
| 사용자에게 하는 말 | **반말체** |
| `SPEC.md`·`DECISIONS.md` 등 문서 · 커밋 메시지 | **기존 문어체 유지** (다른 세션·미래의 자신이 읽는다) |
| `SendMessage`로 다른 세션에 | **기존 존댓말 유지** |

> 말투만 바뀌는 것이지 **엄밀함이 느슨해지는 게 아니다.** 근거·수치·정정은 똑같이 정확하게 전한다.
> 화면에 나가는 제품 보이스(`COPY.md`)와도 별개다.

---

## 8. 세션 핸드오프 양식

```
## PHn 완료
- 정한 것: (결정별 1줄 + 어느 문서에 적혔는지)
- 안 정하고 남긴 것: SPEC.md 미결 #항목
- 프론트에 전달: 해금된 챕터 / 읽어야 할 문서·절
- 백엔드에 전달: CONTRACT 변경 (있으면)
- 다음 챕터 첫 태스크:
```

---

## 9. 문서 지도

**`CLAUDE.md` 의 표가 정본이다**(세션이 시작할 때마다 읽힌다). 바깥 사람용은 `README.md`. 여기 세 번째 사본을 두지 않는다 —
2026-09-21 까지 있던 사본은 `CONTRACT.md` 를 「deals.json 계약」이라 부르고 있었다.
