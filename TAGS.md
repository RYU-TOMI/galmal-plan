# TAGS.md — 목적지 태그 배정안 (기획 → 백엔드)

> **소유: 기획 세션.** 어휘와 배정은 제품 목소리에 속하므로 기획이 정하고,
> **백엔드가 `collector/dests.py`에 반영**한다. 어휘 정의는 `CONTRACT.md`가 계약으로 들고 있다.
> 재생성·검증: `python design/build_tags.py` (불변식을 어기면 실패한다)

## 왜 다시 배정하나

어휘 12종으로 목적지 84곳을 구별할 수 없었다. 실측(C-13):

- `해변` 하나만 붙은 도시 **10곳** — 괌·나디·**몰디브**·사이판·세부·**싼야**·팔라우·푸꾸옥·푸켓·호놀룰루
- `도시` 하나만 붙은 도시 **8곳** — 나고야·**두바이**·상하이·선전·자카르타·쿠알라룸푸르·프랑크푸르트·도하
- 태그 1개짜리가 **38%**

**몰디브와 싼야가 같은 태그였고, 두바이와 나고야가 같은 태그였다.**
표시 규칙만 바꿔서는 안 됐다 — 하위 태그 우선 표시를 시뮬레이션해도
제주·다낭·오키나와·푸꾸옥·싼야가 전부 `휴양 · 해변`으로 똑같았다.

## 어휘 — 상위 6종(필터) + 하위 18종(표시)

**하위 태그는 반드시 자기 상위를 함께 단다.** 그래야 `야시장`을 보고 `미식` 필터를 눌렀을 때 잡힌다
(`COPY.md`의 "화면에 보이는 태그는 고를 수 있어야 한다" 원칙). 백엔드 계약 검증기가 매번 검사한다.

어휘 목록(상위 6·하위 18과 소속)의 정본은 [`contract/v1/vocab.json`](https://github.com/RYU-TOMI/galmal-backend/blob/main/contract/v1/vocab.json)의 `tags`다 — 여기에 옮겨 적지 않는다.

- **시즌 표현은 넣지 않았다** — `벚꽃`·`단풍`·`설경`은 계절을 타서 문구가 썩는다(`COPY.md` 보이스 규칙).
- **`사찰` 대신 `사원`**을 쓴다. 이스탄불 모스크·델리 힌두사원까지 담아야 한다.
- **표시 태그는 희소할수록 좋다.** 필터 어휘가 희소하면 빈 결과가 뜨지만(그래서 상위는 6종),
  표시 태그는 드물수록 그 도시를 잘 구별한다 — `폭포`가 토론토 한 곳뿐인 건 결함이 아니라 목적이다.

## 효과

- 카드에 보이는 태그 조합: **26가지 → 83가지**
- 어휘 종수: **24종 → 24종**

| 예시 | 전 | 후 |
|---|---|---|
| 몰디브 | `해변` | **`섬 · 리조트 · 스노클링 · 해변`** |
| 싼야 | `해변` | **`리조트 · 해변`** |
| 두바이 | `도시 · 자연` | **`사막 · 마천루 · 쇼핑 · 도시`** |
| 나고야 | `도시 · 문화` | **`쇼핑 · 고성 · 도시`** |
| 세부 | `해변` | **`스노클링 · 리조트 · 섬 · 해변`** |
| 푸켓 | `해변 · 미식` | **`리조트 · 야시장 · 해변`** |
| 토론토 | `도시 · 자연` | **`마천루 · 폭포 · 도시`** |
| 카트만두 | `자연 · 문화` | **`트레킹 · 사원 · 자연`** |

## 배정 — 정본은 `dests.DEST`

`카드`는 **하위 전부 + 상위 1개**(하위 없으면 상위 2개), 최대 4개다. **개수가 곧 신호다** — 즐길 게 많은 도시는 태그도 많다.

> 🔑 **도시별 배정의 정본은 [`collector/dests.py`](https://github.com/RYU-TOMI/galmal-backend/blob/main/collector/dests.py)의 `DEST`다** (2026-09-19, 백엔드 BE10). 도시별 표는 여기서 지웠다 —
> 레포가 나뉜 뒤 백엔드 테스트가 이 표를 사본으로 읽었고, 사본은 첫날 갈렸다(`DECISIONS.md` 2026-09-17).
> 배정을 바꾸려면 기획이 결정(이유)을 백엔드에 보내고, 백엔드가 `DEST`를 고치며 커밋에 그 결정을 인용한다.

### 🔴 배열 순서는 편집 판단이다 — 정렬하지 않는다 (2026-09-19)

화면은 `tags`를 **하위 전부(앞에서부터) + 상위 중 첫째 1개**, 최대 4개(모바일 2개)로 보인다(`discover.js` `cardTags`).
그래서 순서가 두 군데서 화면을 바꾼다:
- **상위 태그들 사이의 순서** — **첫째가 그 도시의 대표 상위 태그**다. 오키나와 `해변·…·문화·자연`에서 카드에 붙는 건 `해변`이다.
  누가 가나다로 정렬하면 `문화`가 붙는다.
- **하위 태그들 사이의 순서** — 앞일수록 좁은 자리(모바일 2개)에서도 살아남는다.
**하위와 상위가 섞인 순서는 화면에 영향이 없다**(화면이 다시 나눈다). 그래서 「하위 먼저, 상위 마지막」 같은
구조 규칙은 **지킬 가치가 없다** — 실측 89곳 중 35곳만 그 모양이고, 나머지를 고쳐도 화면은 그대로다.

**기계로 잠그지 않는다.** 순서는 도시마다 「무엇이 대표인가」라는 편집 판단이라 규칙으로 도출되지 않는다.
사고 모양은 「누가 `sorted()`로 정리한다」인데, 그러면 89줄이 한꺼번에 바뀌어 **리뷰에서 바로 보인다.**
대신 `DEST` 위에 「정렬 금지 — 상위 중 첫째가 카드의 대표 태그」 주석을 둔다(백엔드).
